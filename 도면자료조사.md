# 건축 도면 자료 조사 결과 (2026-08-12)

> 클라우드 세션은 프록시가 한국 사이트를 막아 **원문을 직접 열지 못했다**(`blcm.go.kr`,
> `aihub.or.kr` 둘 다 `EGRESS_BLOCKED`). 아래는 검색으로 확인한 것이고, **로컬에서
> 원문을 열어 확정해야 하는 것**을 그렇게 표시해 두었다.

## 결론 먼저 — **목적이 "3D 배치 시뮬레이션"이면 우선순위가 달라진다**

목적을 확인했다: 도면을 모으는 이유는 **고객 집을 3D로 세워 가전을 미리 놓아 보는 것**이다.
그러면 **정말 필요한 것은 도면 이미지가 아니라 "치수가 맞는 벽 좌표"** 다.
도면은 벽 좌표를 얻는 **수단**일 뿐이고, 3D 에 들어가는 순간 이미지는 버려진다
(우리 `Place3D` 도 벽 다각형과 확정된 mm 만 먹는다).

이 사실이 세 가지를 바꾼다.

1. **인식기를 고치는 것이 도면을 모으는 것보다 값이 크다.**
   도면 1,000장을 더 모아도 그 단지에서만 쓸 수 있지만, 인식이 좋아지면 **고객이 그
   자리에서 올리는 모든 도면**에 적용된다. 매장에서 상담은 "우리가 가진 단지"가 아니라
   "지금 온 고객의 집"으로 들어온다.
2. **저작권 문제가 사라지는 길이 이미 열려 있다.** 벽 좌표는 도면의 **파생 측정값**이라
   저장소에 담을 수 있다(`plan-library.json` 주석이 그렇게 적어 두었고
   `npm run merge:library` 가 그 길이다). **공용 목록은 아직 0건이다** — 여기가 비어 있다.
3. **같은 주택형이면 벽이 같다.** 단지 하나당 주택형 3~5개의 벽선만 있으면 그 단지
   전체가 커버된다. 도면 장수를 쫓는 것보다 **주택형 단위로 벽선을 쌓는 것**이 빠르다.

| 후보 | 3D 배치에 주는 것 | 판정 |
|---|---|---|
| **AI Hub 「건축 도면 데이터」** | **벽·문·창 정답 라벨** — 인식기를 실제로 고칠 수 있다 | **최우선** |
| **벽선 모으기**(`merge:library`) | 상담사가 잡은 벽을 그대로 자산화 · 저작권 무관 | **최우선(동급). 지금 0건** |
| **건축HUB / BLCM 도면정보 API** | 실제 단지 도면 — 질의 3,756건 준비됨 | 확인 30분. **단위세대 평면도를 주는 경우에만** |
| CubiCasa5K | 벽/문/창 벡터 정답 5,000장(핀란드) | 보조 검증용 |
| CAD·BIM MCP (Revit·IFC·ODA) | 없음 — 이미 가진 CAD 파일을 다루는 도구 | **쓰지 마라** |
| RPLAN | 원본 이미지 비공개 | 제외 |

**한 줄로**: 밤샘의 무게는 **①인식 정확도 ②벽선 자산화**에 두고, 도면 수집(BLCM)은
**30분 확인 후 되면 하고 안 되면 버린다.**

---

## 1. AI Hub 「건축 도면 데이터」 — 이번 조사에서 가장 큰 것

- 주소: `https://www.aihub.or.kr/aihubdata/data/view.do?dataSetSn=71465`
- 구성(검색으로 확인): **도면 48,033장** · 객체 2,653,998개 · 텍스트 304,462개
- **아파트 · 연립/다세대 · 단독주택**을 주택 유형별로, **평면도 · 입면도 · 단면도 ·
  구조도**를 도면 종류별로 분류
- **라벨이 우리가 필요한 바로 그것이다** — 시맨틱 세그멘테이션으로
  **문·창·벽**(구조 인식)과 **거실·침실·주방**(공간 인식)이 라벨링돼 있고, 도면 속
  글자에 OCR 라벨까지 붙어 있다

**왜 큰가** — 지금 우리 벽 인식에는 **정답이 붙은 도면이 10장뿐**이다
(`scripts/fixtures/plans-real/`, 치수가 인쇄된 도면이 8%뿐이라 손으로 잰 것). 그래서
`test-real.mjs` 가 "정답 대조"가 아니라 **"품질 지표가 나빠지지 않았는가"** 만 본다.
이 데이터셋이 있으면 **진짜 정답 대조**가 가능해진다.

**로컬에서 확인할 것 (순서대로):**
1. 회원가입 후 **이용 신청** — AI Hub 는 신청·승인 절차가 있다
2. **라이선스를 반드시 읽어라.** 연구·학습용 조건이 붙는 경우가 많다.
   - **상업적 이용 가능한가** · **재배포 가능한가** · **가공물(우리 색인)을 배포해도 되는가**
   - 조건이 안 맞으면 **도면을 앱에 싣지 말고 "인식 정확도 검증용"으로만 쓴다.**
     그것만으로도 값어치가 충분하다.
3. 아파트 평면도만 골라 **표본 100장으로 먼저 우리 인식기를 돌려 정답과 대조**해라.
   전량을 받기 전에 이게 먼저다.

---

## 2. 건축HUB / BLCM 도면정보 API — 절반은 이미 되어 있다

- API 레퍼런스: `http://blcm.go.kr/api/ser/selectApiRefer.do?referGb=floor` (도면정보)
  · `?referGb=plot` (도면정보제공) · `?referGb=basic` (생애이력)
- 예제: `https://blcm.go.kr/api/ser/selectOpenApiExample.do?exampleGb=floor`
- 샘플: `http://www.blcm.go.kr/api/ser/selectOpenApiSample.do?sampleGb=floor`
- 공공데이터포털: `건축HUB_건축물대장정보`(15134735) · `건축HUB_건축인허가정보`(15136267)
- 건축데이터 민간개방: `https://open.eais.go.kr/` (4개 분야 53종) · 문의 070-7866-0907

**우리는 이미 질의를 갖고 있다.** CLAUDE.md 기록대로 K-apt 단지목록에서
`bjdCode`+`bun1`-`bun2` 를 뽑아 **3,829곳 중 3,756곳(98.1%)** 에 대해
`법정동코드-본번-부번`(예: `4121010100-717-3`) 을 만들어 두었고, `addr` 로 검산해
**어긋난 것 0건**이다. 1990년대 919곳 · 2000년대 1,167곳으로 **정작 도면이 없던
옛 단지가 표본에 충분하다.**

**남은 결정적 미확인 — 이것부터 확인해라(30분이면 끝난다):**

1. **API 가 도면 "이미지"를 주는가, 메타데이터만 주는가.**
   검색으로는 확정이 안 됐다. 표본 3건만 실제로 호출해 응답을 통째로 찍어 봐라.
   (`.scratch/blcm-probe.mjs` 가 이미 있다 — 그 결과가 CLAUDE.md 에 안 적혀 있으니
   다시 돌리고 **이번엔 결과를 CLAUDE.md 에 남겨라.**)
2. **주는 도면이 무엇인가.** 건축HUB 안내에 **`도면정보(배치도)`** 라는 항목이 따로 있다
   (`https://www.hub.go.kr/portal/opn/tyb/idx-batch-flrpln.do`).
   **배치도(단지 배치)와 단위세대 평면도는 완전히 다른 물건이다.**
   우리가 필요한 것은 **단위세대 평면도**다. 배치도만 나오면 이 경로는 값이 낮다.
3. **열람 제한.** 건축물현황도(평면도 포함)는 정부24·세움터에서 **공동인증서**로
   발급받는 민원이고, **소유자·이해관계인만** 되는 항목이 있을 수 있다.
   API 가 인증 없이 주는 범위가 어디까지인지 약관에서 확인해라.
4. **이용약관.** 받은 도면을 **우리 앱에 실어 매장에 배포해도 되는지.**
   안 되면 CLAUDE.md 의 기존 결론("벽선만 저장한다")과 같은 길로 간다 —
   **도면 이미지는 커밋하지 않고 벽 좌표만 뽑아 `plan-library.json` 에 넣는다.**
   그 길은 이미 `npm run merge:library` 로 열려 있다.

**이 넷 중 하나라도 막히면 즉시 보고하고 다음 트랙으로 넘어가라.** 밤새 붙잡지 마라.

---

## 3. CubiCasa5K — 보조 검증용

- `https://github.com/CubiCasa/CubiCasa5k`
- 5,000장 · 핀란드 부동산 매물 도면 · **SVG 벡터 주석**으로 벽·난간·문·창·방 80여 종
- 2D→3D 재구성 연구의 사실상 표준 벤치마크

**한국 아파트가 아니므로 단지 도면으로는 못 쓴다.** 다만 **벽/문/창 정답이 벡터로**
붙어 있어, 우리 `keepBands`·`classifyEdges` 가 **표기 관례가 다른 도면에서도 버티는지**
재는 데 쓸 수 있다. 라이선스 확인 필요.

---

## 4. 쓰지 말 것 — CAD·BIM MCP 서버들

조사에서 여러 개가 나왔지만 **전부 우리 문제와 무관하다.**

- `helenkwok/openbim-mcp` (IFC), `LuDattilo/RevitCortex`(Revit 173툴),
  `Demolinator/revit-mcp-server`, `daobataotie/CAD-MCP`, ODA MCP Servers

이들은 **이미 손에 있는 CAD/BIM 파일**(DWG·IFC·RVT)을 다루는 도구다. 우리 문제는
**한국 아파트 평면도 이미지를 구하는 것**이지 CAD 파일을 다루는 것이 아니다.
게다가 Revit 계열은 유료 라이선스가 필요하다. **여기에 밤을 쓰지 마라.**

MCP 레지스트리(claude.ai 커넥터)도 검색했는데 **건축 도면 관련은 하나도 없다.**

---

## 5. 벽선 모으기 — **지금 0건이고, 목적에 가장 정확히 맞는 길**

`saveToLibrary()` 는 이미 **이미지가 아니라 방 경계(mm 좌표)만** 저장한다.
`plan-library.json` 주석이 이유를 적어 뒀다 — *"분양 도면은 저작권이 있어 저장소에 둘 수
없지만 **파생 측정값인 이 좌표는 담을 수 있다**"*. 합치는 도구(`npm run merge:library`)도
있고 불량 5종을 거른다(이미지 섞임 · 벽 3개 미만 · 크기 이상 · 이름 없음 · 중복).

**그런데 공용 목록이 아직 0건이다.** 상담사가 잡아 둔 벽선이 각자 기기 localStorage 에만
남고 아무도 모으지 않았다. **3D 배치가 목적이라면 여기가 제일 아깝다.**

밤샘에 할 것:
1. 우리가 **이미 가진 도면 697장**(색인 등재분)으로 **벽선을 일괄 생성**해라 —
   축척이 색인에 실린 12장은 자동으로, 나머지는 자동 인식 결과를 그대로 저장하되
   **축척 미확정 표시**를 달아 둔다(치수를 단정하면 안 된다).
2. `merge:library` 로 공용 목록에 넣고 **`npm run test:library` 로 불량이 걸러지는지** 확인.
3. 앱에서 단지 → 주택형을 고르면 **벽선이 바로 서고 3D 로 넘어가는지** 3회 시연.
4. 상담사가 매장에서 잡은 벽선을 **내보내기 → 합치기**로 모으는 흐름을 문서 한 장으로 정리.
   (매장 몇 곳만 참여해도 관할 단지가 빠르게 찬다 — 도면을 구걸하는 것보다 빠르다.)

**주의** — 축척이 확정되지 않은 벽선을 "치수가 맞는 것"처럼 목록에 올리지 마라.
그건 이 앱이 가장 경계하는 종류의 거짓이다. 목록에 **축척 확정 여부를 함께 싣고**,
미확정이면 불러왔을 때 화면이 그 사실을 밝히게 해라.

---

## 6. 우선순위 제안 (밤샘 순서)

| 순서 | 할 일 | 시간 | 왜 |
|---|---|---|---|
| 1 | **AI Hub 이용 신청만 걸어 둔다** | 30분 | 승인에 시간이 걸린다. 걸어 두고 다른 걸 한다 |
| 2 | **BLCM API 표본 3건 호출** → 4가지 확인 | 30분 | 되면 큰 것, 안 되면 즉시 버린다 |
| 3 | **벽선 일괄 생성 + 공용 목록 채우기** | 길게 | 목적에 가장 정확히 맞고 저작권 문제가 없다 |
| 4 | AI Hub 승인되면 **표본 100장으로 인식 정확도 대조** | 승인 후 | 인식이 좋아지면 모든 고객 도면에 적용된다 |

**2번이 막히면 미련 두지 말고 3번에 밤을 써라.** 도면 수집은 이미 여러 경로가 소진됐고
(청약홈·건설사 지면·포털), 3D 배치라는 목적에는 **벽선이 도면보다 직접적이다.**
