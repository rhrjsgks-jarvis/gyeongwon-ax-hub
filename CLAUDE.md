# 세일즈 코파일럿 (gyeongwon-ax-hub)

경원영업팀(삼성 가전 영업)을 위한 세일즈 지원 도구. Next.js 14 App Router.

**이름은 '세일즈 코파일럿'이다**(2026-08-10 변경, 옛 이름 '경원 AX 허브').
상담사를 대체하는 것이 아니라 **옆자리에서 같이 나는 부조종사**라는 뜻이고, 이 도구가 실제로
하는 일이 그렇다 — 답을 대신 말하지 않고 근거를 꺼내 준다. 저장소 이름(`gyeongwon-ax-hub`)과
Vercel 프로젝트명은 그대로다(주소를 바꾸면 시범운영 중인 매장 링크가 끊긴다).
화면에 이름이 나오는 곳: `app/layout.tsx`(title·og) · `app/page.tsx`(h1) ·
`components/Navigation.tsx` · `app/admin/page.tsx` · `components/FeedbackButton.tsx`.

**앱 아이콘은 삼성 블루 바탕에 흰 'SC'다**(`public/icons/`, `.scratch/icon-gen.mjs` 로 생성).
`S` 는 **공식 워드마크의 첫 글자를 그대로 잘라 쓴 것**이고, `C` 는 워드마크에 없는 글자라
**S 를 픽셀로 재서 거기에 맞춰 작도했다**(`.scratch/measure-S.mjs`).

**눈대중으로 맞추다 두 번 틀렸다. 반드시 재고 시작할 것:**
| 실측 | 값 |
|---|---|
| S 글리프 폭 | **729px** (892 로 잘랐더니 오른쪽 163px 이 빈 공간이라 S 가 왼쪽으로 밀려 보였다) |
| S 높이 | 1080px (cap height 를 꽉 채운다) |
| S 상단 획 | **171px = 15.8%** (C 를 34% 로 그렸더니 두 배 넘게 두꺼웠다) |
| S 좌측 획 | 277px = 25.6% (S 는 부분마다 두께가 다른 modulated stroke 다) |

그래서 C 는 **획 19%**(상단·좌측 사이) · **가로 0.88 타원**(정원이면 지름 1080 이라 S 729 보다
1.5배 넓어 S 가 작아 보인다) · 개구부 반각 38°.
**두 글자는 살짝 겹친다**(`GAP = -60`, 사용자 요청) — 띄우면 가운데가 비어 한 덩어리로 안 읽힌다.
`purpose:"any maskable"` 이라 글자 폭을 캔버스의 70% 로 두어 원형 마스크에서도 잘리지 않는다.
**아이콘·이름을 바꾸면 `sw.js` 의 `CACHE_VERSION` 을 함께 올릴 것** — manifest 가 캐시에
남아 있으면 옛 이름으로 계속 설치된다. 이미 설치된 기기는 **삭제 후 재설치**해야 확실히 바뀐다.

**삼성 워드마크는 글꼴이 아니라 SVG 패스다**(`components/SamsungWordmark.tsx`).
`font-black` 으로 친 "SAMSUNG" 은 로고가 아니다 — 공식 워드마크는 전용 레터폼이라
A 에 가로획이 없고 U 가 아래로 열려 있다. 색은 `currentColor` 로 두어 남색 바 위에서는
흰색, 흰 바탕에서는 삼성 블루(#1428A0, 원본 색과 같다)로 쓴다.
GitHub → Vercel 자동배포 파이프라인으로 운영 중이며, 이 파일은 Claude Code가 세션마다 자동으로 읽는 프로젝트 메모리다.

## 배포 파이프라인

- 저장소: `https://github.com/rhrjsgks-jarvis/gyeongwon-ax-hub` (main 브랜치, public repo)
- 배포: main에 push하면 Vercel이 자동 빌드/배포 (별도 CLI 배포 불필요)
- **push 는 큰 덩어리가 묶였을 때 한 번만 한다 — 배포 횟수에 한도가 있다.**
  main push 가 곧 배포라, 작업 하나 끝날 때마다 올리면 한도에 걸려 **그날 작업을 끝까지 못 한다**
  (2026-08-10 사용자 지적: *"매번 작업할 때마다 배포를 한다면 배포 횟수 제한에 걸려 작업을 다
  할 수가 없다"*). 한도는 빌드 자원의 문제이지 변경의 위험도와 무관하므로, 작게 자주 올리는
  습관이 여기서는 그대로 손해가 된다.
  - **커밋과 배포는 다른 결정이다.** 커밋은 작업이 끝날 때마다 자유롭게 쌓는다.
    **"커밋해 주세요"는 push 요청이 아니다** — push 하지 않은 커밋은 다음 덩어리에 합쳐진다.
  - **미푸시 10커밋이 넘으면 묻지 않고 올린다**(2026-08-15 사용자 위임: *"배포안된자료가
    일정량 이상이되면 배포진행해주세요"*). 그 아래인데 사용자가 화면에서 확인해야 하는
    변경이면 그때는 물어본다.
  - **개수는 `git fetch` 뒤 `git rev-list --count origin/main..HEAD` 로 센다.** 내가 만든
    커밋을 세면 안 된다 — 13개라고 보고했는데 실제로는 2개였다.
  - **원격에 내가 만들지 않은 커밋이 있는 것은 정상이다.** 사용자가 **로컬에서 직접
    작업해 올린다**(2026-08-17 확인: *"앞으로 계속 로컬로 작업해서 업데이트 할 예정"*).
    실제로 `/dev/telecom` 이 그렇게 들어왔다. **텔레그램 봇이 한 것으로 넘겨짚지 말 것** —
    한 번 그렇게 잘못 짚었다. 모르는 커밋이 보이면 `git show` 로 내용을 먼저 읽고,
    **이미 있는 것 위에 짓는다**(같은 것을 두 번 만들지 않는다).
  - 올리기 전에 테스트를 돌리고(화면 흐름을 건드렸으면 `npm run test:all`),
    올린 뒤에는 프로덕션 URL 로 실물을 확인한다.
  - 텔레그램 봇·스크립트처럼 **Vercel 배포와 무관한 변경**도 같은 main push 를 타므로 함께 묶는다.
- 프로덕션 URL: **`https://salescopilot-store.vercel.app`**
  - **`salescopilot.vercel.app` 은 쓸 수 없다** — 이미 다른 사람의 앱이 그 이름을 쓰고 있다
    ("Sales Copilot - Video Meeting"). 그래서 `-store` 를 붙였다.
    나중에 "왜 -store 가 붙었지" 하고 되돌리지 말 것.
  - 한때 `slaescopilot`(sales → slaes 오타)으로 잡혀 있었다. 지금은 없어진 주소다.
  - 옛 주소 `gyeongwon-ax-hub.vercel.app` 도 아직 열리지만, **팀에 공유하는 링크는 현재
    주소를 쓴다** — `app/page.tsx` 의 `HUB_URL` 하나만 고치면 QR·표시 주소·복사 링크가
    모두 따라간다(표시 문구가 상수와 어긋나 있던 적이 있다).
- Vercel projectId: `prj_JskGnqqArCARl3mNBm6tp1dmdu46` / teamId: `team_o104cKtnAQcEaKospwGIAt9N`
- git push 인증은 로컬에 SSH 키 또는 `gh auth login`으로 한 번만 설정해두면 이후 세션에서 재설정 불필요 (Cowork 샌드박스 환경과 달리 로컬 환경은 인증정보가 세션 간 유지됨).

## 현재상황 상시 표시 (`.claude/statusline.mjs`, 2026-08-16 사용자 요청)

*"현재상황 상시 띄워주세요"* → **터미널 하단 상태줄 + 응답 맨 위** 두 겹으로 붙였다.
역할이 다르다 — 상태줄은 **사용자가** 보는 것이고, 훅은 **내가 빼먹지 못하게** 하는 것이다.

```
 main  ·  ⏳미배포 2/10  ·  v70  ·  🔒local  ·  4️⃣ 도면 수집 — robots 확인 단계
```

띄우는 것은 **틀리면 비싼 것 넷**이다 — 미배포 커밋 수(배포 한도와 직결) · 버전(매장에서
"내 폰이 최신인가"를 이 숫자로 판단한다) · 작업 자물쇠(폰↔PC 동시 작업 사고) · 지금 하는 일.

- **한 곳에서만 센다.** `statusline.mjs` 가 `status()` 를 내보내고 `status-context.mjs`
  (UserPromptSubmit 훅)가 그것을 import 한다. 두 곳에서 따로 세면 화면이 서로 다른 말을
  한다 — 이 저장소가 반복해서 데인 종류다(허브 카드 개수·앱 버전·비교표 값).
- **0 과 '모름'을 구분한다.** `origin/main` 이 없으면 `미배포 ?` 다. 0 으로 적으면
  *"올릴 것이 없다"* 는 거짓말이 된다(비교표에서 '없음'과 '미공개'를 가르는 것과 같은 규칙).
- **화면 버전 ≠ 캐시 버전이면 상태줄이 경고한다.** `test-consistency` 가 잡는 그 짝인데,
  검사를 돌리기 전에 눈에 보이는 편이 낫다.
- **훅은 무슨 일이 있어도 `exit 0`.** 같은 UserPromptSubmit 에 **작업 자물쇠 훅이 함께**
  걸려 있어, 여기서 죽으면 그쪽까지 막힌다. git 저장소가 아닌 곳에서도 정상 종료하는 것을
  확인했다. 훅을 추가할 때 **기존 자물쇠 훅을 덮어쓰지 말 것** — 같은 배열에 덧붙인다.
- **`.claude/now.txt` 는 손으로 적는 한 줄**이라 굳으면 거짓이 된다. 30분이 넘으면
  `(n분 전)` 을 함께 띄운다. **작업을 바꿀 때마다 이 파일을 고칠 것.** gitignore 대상이다.
- 상태줄은 렌더마다 도므로 git 호출은 둘뿐이고 1.5초에서 끊는다(실측 200~300ms).

## 백업 (`npm run backup`, 2026-08-15)

GitHub 이 이미 코드 933개 파일을 들고 있다. **GitHub 이 못 막아 주는 것**을 챙긴다 —
`.scratch/` 의 도구 500여 개(커밋되지 않는다) · 저작권 때문에 못 올리는 실도면 16장 ·
받은 원본. 그리고 **계정이 탈취돼 force push 로 히스토리가 지워지는 경우**를 대비해
저장소 전체를 `git bundle` 파일 하나로 뜬다. 두는 곳은 `BACKUP_DIR`, 없으면 OneDrive.
날짜별 3회차 보관.

- **회차 폴더 이름은 현지 시각이다**(2026-08-16 정정). 예전에는 `toISOString()` 이라
  **UTC** 였고, 한국이 +9 라 10:36 백업이 `…-0136` 으로 적혔다. 더 나쁜 것은
  **00~09시에 뜬 백업이 전날 날짜 폴더로 들어간다**는 점이다(03:00 KST → `20260815-1800`).
  복구할 때 엉뚱한 회차를 고르게 만드는 종류라 고쳤다. 정렬은 그대로 안전하다 —
  한국은 서머타임이 없고 오래된 회차 정리는 `YYYYMMDD-HHMM` 이름 정렬에 기댄다.
  (그 전에 만들어진 UTC 이름 폴더가 섞여 있어도 시간순 정렬은 맞다.)

- **복구는 `git clone` 이 아니라 `git init` + `git fetch <번들> "refs/*:refs/*"` 다.**
  clone 의 기본 refspec 은 `refs/heads/*` 만 옮겨서 **원격에만 있는 브랜치와 stash 가
  이름을 잃는다**(객체는 있는데 가리키는 ref 가 없어 언젠가 gc 로 사라진다). 정작
  GitHub 탈취를 대비하는 백업인데 그것이 날아가면 뜻이 없다. 왕복으로 520커밋 ·
  ref 13개 · 도면 617장 복구를 확인했고, 설명서를 백업 폴더에 함께 넣는다.
- **비밀값은 기본으로 안 담는다.** 텔레그램 토큰은 BotFather 에서 재발급되는데 백업은
  클라우드로 동기화되므로 담아 두는 쪽 손해가 크다. 키 **이름만** 남긴다(`--with-secrets`).
- `.scratch/` 는 1.6GB 중 **스크립트 9.7MB 만**. 나머지는 다시 받을 수 있고, 못 되찾는
  것은 우리가 쓴 도구다.
- **쓴 직후 되읽어 검증한다**(`git bundle verify` + ref 수 대조). 깨진 것을 복구할 때
  알게 되면 이미 늦다.

## 용어 — 앱 안의 정식 명칭이 기준이다

**같은 도구를 허브 카드·사이드바·앱 헤더가 다르게 부르고 있었다**(2026-08-10 정리).
사용자 지적: *"삼성닷컴과 카탈로그 안에 있는 용어들과 일맥상통하게 일치시켜야 한다.
AI Care 검색기라는 명칭은 사실 사용하지 않는 용어다."*
**기준은 앱 안의 정식 명칭**이다 — 그쪽이 카탈로그·브로슈어를 보고 지은 이름이기 때문이다.

| 옛 표기 | 지금 | 근거 |
|---|---|---|
| AI Care 검색기 · 케어십 · 구독케어 | **AI구독 케어 안내** | 앱이 출처로 밝히는 문서가 `AI구독클럽 브로슈어 2026.06` 이다. '케어십'은 쓰지 않는 말 |
| 레벨업테스트 | **레벨업 챌린지** | 앱 헤더가 `SAMSUNG STORE 레벨업 챌린지` |
| 타사비교 | **타사비교 세일즈가이드** | 앱 헤더 |
| 설치환경 가이드 | **제품별 설치환경 가이드** | 앱 헤더 |
| URL 퀴즈 생성 | **URL 퀴즈 생성기** | 앱 헤더 |
| AX 현황 대시보드 | **사용현황 대시보드** | 'AX'는 내부 사업명이라 상담사에게 안 통한다 |
| AS기간 확인 | **AS 관련 정보** | 보증기간만이 아니라 **연락처·이전설치**까지 다룬다 — 헤더가 범위보다 좁으면 상담사가 못 찾는다(care 앱과 같은 이유, 2026-08-11 사용자 요청) |
| 모델파인더 | **통합검색** | 제품만이 아니라 **앱 안의 모든 자료**를 찾는다. 예산·치수 필터가 있는 기존 `/finder` 는 **제품 상세검색**으로 남겼다 |

- **care 앱 헤더를 '방문케어' → '케어 안내'로 넓혔다.** 그 앱은 방문케어만이 아니라
  **자가관리·무상수리**도 함께 다룬다 — 헤더가 범위보다 좁으면 상담사가 못 찾는다.
- 화면 문구를 바꾸면 **`npm run build:index` 를 다시 돌려야 한다**(통합검색 색인이 명칭을 담는다).
  `test-admin.mjs` 는 대시보드 명칭을 문자열로 검사하므로 함께 고칠 것.
- **낡은 수치가 화면에 남지 않게 할 것.** 배치 시뮬레이터 설명이 "카탈로그 실측 103개 사이즈"
  였는데 라인업 통합으로 **70개**가 된 지 오래였다. 데이터를 줄이거나 늘리면 허브 카드 설명도 함께 본다.
  - **다만 "그때 재 보니 617장" 같은 과거 실측은 고치지 않는다.** 고치면 그 관찰이
    언제 무엇을 보고 나온 것인지가 사라져 근거가 무너진다. 고쳐야 하는 것은
    **현재 상태를 서술하는 문장**뿐이다("도면이 있는 단지는 N곳뿐이다").
    이 파일이 실측에 날짜를 붙여 적는 이유가 그래서다 — 둘을 구분할 수 있어야 한다.
    (2026-08-16 색인이 139단지 617장 → 144단지 671장이 되며 실제로 이 판단을 했다.)

## 구조 패턴

각 기능 모듈은 "Next.js 라우트가 정적 HTML 미니앱을 iframe으로 감싸는" 동일한 패턴을 따른다:

| 라우트 (`app/*/page.tsx`) | 감싸는 정적 파일 (`public/*.html`) |
|---|---|
| `/install` | `install-app.html` (설치환경 가이드) |
| `/compare` | `compare-app.html` (타사비교) |
| `/finder` | `finder-app.html` |
| `/care` | `care-app.html` |
| `/quiz` | `quiz-app.html` |
| `/place` | `place-app.html` (가전 배치 시뮬레이터) |
| `/poster` | `poster-app.html` (컨시어지 접수 포스터 — 인쇄용) |

새 모듈을 추가하거나 기존 모듈을 수정할 때:
- 실제 로직/데이터는 대부분 `public/*.html` 안의 인라인 `<script>`에 있다 (React 컴포넌트가 아님). 이 정적 HTML을 직접 편집하는 것이 일반적인 작업 방식이다.
- `app/page.tsx`의 `MODULE_GROUPS` 배열(섹션별 그룹핑, 각 그룹 안에 `modules[]`)이 허브 메인 카드 노출 순서·섹션 구성을 결정한다. 컨시어지·쿠폰 배포프로그램처럼 외부 링크 묶음은 `CONCIERGE_LINKS`/`COUPON_LINKS` 같은 별도 섹션으로 관리한다.
- 사이드바/하단 내비게이션 `NAV_ITEMS`(+ 데스크탑 사이드바 전용 `QUICK_LINKS`)도 함께 확인.
- `lib/logEvent.ts`의 `LogModule` 유니온 타입에 새 모듈 키를 추가해야 로깅이 정상 동작한다.

## 설치환경 가이드 모듈 (`public/install-app.html`)

- 데이터: `INSTALL_DB` 객체, 한글 카테고리명을 키로 사용. 각 엔트리 구조:
  `emoji, subtitle, types[], space[][2], utility[][2], checklist[], cautions[], source, sourceUrl, images?[{src,alt,cap}]`
- 이미지 카드 표시 여부는 JS에서 `images.length` 기준으로 토글 (`imageCard.style.display`).
- 현재 21개 카테고리 (드롭다운 순서 = `INSTALL_DB` 객체 순서, 둘 다 항상 동기화 유지):
  냉장고 4도어 프리스탠딩 → 4도어 키친핏 → 4도어 키친핏 Max → 2도어 → 1도어 → 양문형 → 일반형 → 페어(2대 이상) 설치 → 김치냉장고 → 세탁기·콤보 → 건조기 → 에어컨 → TV → 청소기(무선청소기) → 로봇청소기 → 식기세척기 → 에어드레서 → 인덕션 → 정수기 → 전자레인지 → 공기청정기
- 스마트폰/노트북은 설치환경이 적용되지 않아 카테고리에서 완전히 제거됨 (드롭다운·DB 모두에 없음). 되살리지 말 것.

### 핵심 원칙 — 절대 타협 금지

**삼성닷컴 등 공식 출처에서 실제로 확인·검증된 이미지만 사용한다. 인접 제품이나 유사 모델의 이미지를 재활용/추측으로 넣지 않는다.** 치수가 명시된 설치가이드 이미지를 찾지 못하면 해당 카테고리는 텍스트+원본 링크만 제공하고 정직하게 "이미지 없음" 상태로 둔다 (예: 현재 청소기·전자레인지·공기청정기가 이 상태). 이는 사용자가 반복적으로 강조한 최우선 원칙이다.

### 이미지 리서치 방법론

- 삼성닷컴 페이지는 종종 이미지가 lazy-load(`lozad.js`)되거나 라디오버튼 탭 위젯(`new-set-guide`, 패널 ID `guidePanel0X-0X-...`)·JS 모달 안에 있어 단순 fetch로는 `<>`(빈 src) 또는 아예 누락된다. **브라우저 도구(Playwright MCP, Chrome DevTools 등)로 렌더링된 DOM을 직접 조회**해 `data-src`나 모달 내부 `<img>`의 실제 src를 확인해야 한다. (단순 fetch/curl만으로 "이미지 없음"이라 단정한 뒤 나중에 브라우저로 재조사해 실제로는 있었던 사례가 로봇청소기·인덕션에서 있었음.)
- 캡션(`cap`)에 들어가는 치수·모델명은 반드시 해당 페이지에서 직접 확인한 값만 기재하고 출처(`samsung.com`)를 명시한다.

### 규격도는 우리 저장소에 둔다 (`public/install-img/`, 2026-08-12)

**받는 것은 `npm run fetch:install-img` 하나로 끝난다. 반드시 로컬 세션에서 돌릴 것** —
클라우드 세션은 프록시가 `images.samsung.com` 을 막는다(`connect_rejected`, 실측 확인).
도면 수집이 로컬 전용인 것과 같은 제약이다. 옮긴 뒤 `npm run build:index && npm test`.

옮기는 이유가 셋이다:
- **공유가 화면을 그대로 찍는데**(html2canvas) 다른 도메인 이미지는 그 서버가 CORS 를
  허용해야 캔버스에 들어간다. 안 되면 **고객에게 나가는 그림에서 규격도 자리가 빈다** —
  설치 상담에서 가장 중요한 그림이 빠지는 셈이다.
- **매장 전파가 약해도 열려야 한다.** 서비스워커는 우리 도메인 것만 캐시할 수 있다
  (three.js·html2canvas 를 `public/vendor/` 에 둔 것과 같은 이유). `sw.js` 가
  `/install-img/` 를 캐시 우선으로 잡는다 — 파일 이름에 개정번호(`_v3`·`_v5`)가 박혀
  있어 내용이 바뀌면 이름이 바뀌므로 굳을 걱정이 없다.
- 삼성닷컴이 경로를 바꾸면 규격도가 통째로 사라진다.

**받지 못한 것은 손대지 않는다.** 실패한 항목은 삼성닷컴 주소 그대로 두고 보고한다 —
없는 파일을 가리키게 만들면 화면에서 규격도가 **조용히** 사라진다(이 모듈의 "확인된
이미지만 쓴다" 원칙과 같은 이유). 원본 주소는 `orig` 로 남겨 출처를 되짚을 수 있게 한다.

- `test-install.mjs` 가 **상대경로로 바뀐 것은 파일이 실제로 있고 `orig` 를 달고 있는지**
  검사하고, 아직 안 옮긴 개수를 함께 알린다.
- `test-install-fetch.mjs`(`npm run test:installfetch`)가 **가짜 이미지 서버**를 띄워
  스크립트 전 과정을 사본에 돌려 본다 — 삼성닷컴이 막힌 환경에서도 "정말 제대로
  바꿔치기하는가"를 확인할 수 있는 유일한 방법이다. 특히 **실패한 것을 건드리지
  않는지**를 지킨다.
  - **그 테스트에서 자식 프로세스를 `execFileSync` 로 부르면 영원히 멈춘다** — 가짜
    서버가 같은 프로세스에서 돌아 이벤트 루프가 막힌다(실제로 멈췄다). 비동기로 부를 것.

## 제품 상세검색 — **AI 추천 버튼은 감춰 둔 상태다** (2026-08-14)

사용자 결정 — *"AI추천 버튼은 가려두고 지속적으로 업데이트한뒤 완벽해지면 오픈"*.
`#mAi` 에 `hidden` 하나만 붙였다. **지우지 않는다** — CAT_FEATURES(17개 카테고리 기능표)·
예산 가중치·스코어링은 다시 만들기 아까운 자료다. 완성되면 `hidden` 만 떼면 열린다.
`test-finder` 가 **감춰져 있는지**를 검사하므로(hidden 을 떼면 실패) 실수로 열리지 않는다.
엔진 자체는 같은 파일 8절이 계속 검사해 코드가 썩지 않는다.

### 2026-08-16 — 열한 건을 고쳤다. **여는 것은 여전히 사용자 결정이다**

재현 하네스가 셋이다 — `.scratch/ai-eval.mjs`(5가지 상황, `ONE="문장"` 으로 한 건만) ·
`ai-eval-wide.mjs`(상담 문장 12개) · `ai-eval-wide2.mjs`(안 돌려 본 카테고리 8개).
**넓힐 때마다 새 결함이 나왔다** — 5개로는 안 보이던 것이 12개에서, 그것도 20개에서 나왔다.

| # | 무엇이 틀렸나 | 실측 (전 → 후) |
|---|---|---|
| 1 | 문장에 쓴 예산을 무시(버튼을 눌러야 분배) | 400만 상담 프리미엄 **584만 → 369만** |
| 2 | 문장 해석 과잉(용도 규칙이 늘 더해짐) | 부모님 TV **3품목(TV 50만) → 1품목 150만** |
| 3 | 티어가 가격 3등분(같은 값이 갈림) | 349만 색상 변형이 프리미엄/스탠다드 → 같은 티어 |
| 4 | 후보가 40%인데 말 안 함 | "냉장고 24/32종" 이라고 밝힘 |
| 5 | **티어 역전**(3번 고치며 생김) | 스탠다드 59만 < **가성비 89만** → 하한 미달은 어느 티어에도 안 넣음 |
| 6 | 예산 초과를 조용히 넘김 | TV 예산 60만에 **890만** → 경고 + 싼 쪽에서 선택 |
| 7 | 예산 초과 시 최저가가 안 보임 | 건조기 50만에 199/129/119 → **129/119/89** |
| 8 | 로봇청소기를 달라는데 무선 | **VR 3종**만 나옴 |
| 9 | 김치냉장고가 냉장고를 딸고 옴 | 예산이 둘로 쪼개지던 것 → 김치냉장고만 |
| 10 | 에어컨 스탠드에 벽걸이 | **AF 3종**만 나옴 |
| 11 | 인덕션 3구에 1구 플레이트 | **3구 2종**만 나옴 |

- **8·10·11 은 같은 병이다** — *"상담사가 말한 형태를 무시한다"*. 제품 상세검색이 이미
  `SUBCAT` 으로 푼 문제인데 AI 추천에 옮겨 오지 않아 남아 있었다. 접두가 가른다
  (청소기 VR/VS/VC · 에어컨 AF/AR/AW), 인덕션만 전부 NZ 라 **제품군 문자열 + `fx` 화구수**를 본다.
  **키워드로 가르지 말 것** — 제품군 이름이 형태를 안 밝히는 것이 많다.
- **5 번은 내가 3 번을 고치다 만든 것**이다. 고친 뒤 **넓게 다시 돌려 보지 않으면** 이런 것이
  그대로 남는다.
- `test-finder` 가 전부 회귀 검사한다 — 티어 가격 순서 · 예산 초과 경고와 최저가 노출 ·
  형태 지킴 4종 · 품목 포함관계.

**열지 여부 — 2026-08-16 사용자 재확인: 계속 감춰 둔다.**
*"AI추천 버튼과 전기요금은 당분간 숨겨둡니다"*. 열한 건을 고친 **뒤에 내린 결정**이므로
**"이제 고쳤으니 열자"고 다시 제안하지 말 것.** 여는 것은 사용자가 말할 때만 한다.
(근거도 그쪽이 맞다 — 아래 열한 건은 전부 "돌려 보니 나온 것"이라 더 넓히면 더 나올
가능성이 높다. 언젠가 열더라도 **매장 몇 곳에서 먼저 써 보는 편**이 낫다.)

**아직 남은 것 — 열기로 할 때 함께 정해야 하는 것**

1. **가격 없는 제품을 티어에 어떻게 넣을지.** 티어·예산이 모두 가격 위에 서 있어 규칙을
   새로 세워야 한다. 지금은 빼되 **뺐다고 말한다**(냉장고 24/32종).
2. **가성비 하한 40%** 는 판단값이다. 실제 상담으로 재보고 정할 것.

## 제품 상세검색 — 복합 조건 (`/finder`)

- **면적(평·㎡)으로 거를 수 있다**(`P.areaMin`·`prodArea`). *"무풍 공기청정 19평"* 처럼 묻는데,
  예전에는 그 말이 그냥 토큰이 되어 **조용히 무시됐다** — "19평 에어컨"이 "에어컨"과 결과가
  똑같았다(61종). 지금은 3종으로 좁혀진다.
  - 뜻이 **"그 공간을 감당하는가"** 라 `면적 >= 요청` 으로 거른다. 치수(`이하`)와 **방향이 반대**다.
  - 1평 = 3.3058㎡. 라벨이 품목마다 달라(`냉방면적(㎡)`·`적용면적`·`청정면적`)
    **라벨에 '면적'이 들어간 항목**에서 첫 숫자를 읽는다(값에 단위가 붙은 것도 있다).
  - 면적 표기가 없는 제품은 **후보에서 뺀다** — 감당 여부를 모르는 것을 "됩니다"로 내밀 수 없다.

### 조건은 AND, 조건 안의 표기는 OR (2026-08-11)

예전에는 **카테고리만 맞으면 나머지 조건이 하나도 안 맞아도 남겼다**(`sc=1` 폴백).
조건을 겹쳐 좁히라고 만든 도구에서 조건이 아무 일도 안 하면 없느니만 못하다.

- 토큰 하나가 조건 그룹 하나다(`P.groups`, 그룹 안은 SYN 확장으로 OR).
  **친 말 자체를 반드시 forms 에 넣을 것** — SYN 은 치환표라 `"헤드폰":["버즈"]` 처럼
  원래 말이 목록에 없는 항목이 있고, 그대로 두면 "JBL 헤드폰"이 **0건**이 된다.
- **없는 말은 조건에서 뺀다**(`P.ignored`). "부모님 효도 워치"의 '효도'처럼 사람이 덧붙이는
  말 하나가 문장 전체를 0건으로 만들면 안 된다. 목록으로 관리하지 않고 데이터가 스스로
  판정한다(통합검색과 같은 원칙). **조용히 빼지 않는다** — 해석 바에 적는다.
  - **판정 범위는 DB 전체가 아니라 "지금 보고 있는 선반"(카테고리·형태로 걸러낸 후보)이다.**
    전체로 판정했더니 *"비스포크 냉장고"* 가 **0종**이 됐다 — 냉장고 kw 에는 '비스포크'가
    한 종도 없는데 에어드레서·청소기에 14종이 있어 조건이 살아남아 AND 를 깨뜨렸다.
- **사람이 치는 표기를 그대로 받는다.** `parseQuery` 가 토큰 **앞뒤** 문장부호를 뗀다 —
  *"냉장고?"* 가 0종이었다. 가운데 기호는 두어야 `2026.03.19`·`w×h×d` 가 안 쪼개진다.
- **숫자 안의 쉼표는 지우고 나머지 쉼표만 구분자로 본다.** 통째로 공백으로 바꾸던 탓에
  *"1,200만원 이하 TV"* 가 예산을 **200만원**으로 읽었다 — 0건도 아니고 **조용히 틀린 답**을
  내밀었다(1,000만은 0만원). 통합검색 `normalize()` 와 같은 규칙이다.
- 카테고리 토큰은 카테고리 필터가 이미 하므로 조건으로 또 걸지 않는다(`req:false`).

### 카테고리 안에서 형태를 가르는 말 (`SUBCAT`)

DB 카테고리는 `청소기` 하나인데 상담에서는 **로봇/무선/유선**을 따로 찾는다. CATSYN·DECOMP 가
셋을 전부 같은 카테고리로 떨어뜨려 *"로봇청소기 직배수"* 에 **무선청소기가 섞여 나왔다**
(2026-08-11 사용자 지적). **모델 접두가 형태를 정확히 가른다**(DB 전수 확인, 예외 없음):

| 품목 | 접두 |
|---|---|
| 청소기 | **VR** 13종(로봇) · **VS** 9종(무선 스틱) · **VC** 4종(유선 캐니스터) · VW 1종(업소용) |
| 세탁기·콤보 | **WA** 2종(통돌이) · **WF·WW·WH** 17종(드럼) · WD 11종(콤보) · SK 8종(설치키트) |

- **키워드로 가르면 안 된다.** '로봇'이 든 kw 는 13종 중 6종뿐이라(제품군 이름이
  `AI 스팀 울트라 VR90F` 처럼 형태를 안 밝힌다) 진짜 로봇청소기 7종이 사라진다.
- **SUBCAT 은 decompound 보다 먼저 본다.** `["로봇청소기",["로봇","청소기"]]` 로 쪼개지고 나면
  형태를 되살릴 방법이 없다.
- 형태 필터는 **자기 카테고리 안에서만** 건다 — "드럼세탁기 건조기"에서 건조기까지 지우면 안 된다.
- **띄어 쓴 것과 조사 붙은 것까지 함께 본다**(2026-08-11, 배포본 점검에서 발견).
  표를 토큰 하나에 그대로 대조하던 탓에 **붙여 쓴 말만 걸렸다** — 상담사는 띄어 쓴다:

  | 질의 | 고치기 전 | 지금 |
  |---|---|---|
  | 드럼세탁기 | 17종 (드럼) | 17종 |
  | **드럼 세탁기** | **38종** — 콤보 11 · 설치키트 8 · 통돌이 2 가 섞였다 | **17종** |
  | **무선 청소기** | **10종** — 로봇청소기가 섞였다 | **9종** |
  | **통돌이 세탁기** | **38종** (통돌이는 2종뿐인데) | **2종** |

  - '통돌이'는 **조사 제거가 '이'를 떼어 `통돌`** 이 되는 바람에 표에 없는 말이 됐고,
    그대로 "없는 말이라 뺐습니다"로 조건에서 빠졌다. **SUBCAT 은 조사를 떼기 전에 본다.**
  - 띄어쓰기는 **다음 토큰까지 이어 붙여**(bigram) 대조하고, 맞으면 두 토큰을 함께 소비한다.
    후보 순서는 `t+다음` → `조사뗀t+다음` → `t` → `조사뗀t`.
  - 오탐이 나면 안 된다 — "무선 이어폰"이 청소기로 끌려가지 않는지 함께 검사한다.
    비대상 질의 8개(청소기·세탁기·냉장고·TV·에어컨 등) 건수가 그대로인 것도 확인했다.

### 인치와 형은 같은 말이다

TV 데이터는 `65형`, 모니터·노트북·태블릿은 `27인치` 로 적혀 있다. 상담사는 둘 다 쓴다.
`SYN` 에 `"65인치":["65","tv"]` 로 넣어 뒀던 탓에 **"65인치 TV"가 63종**(TV 전체)이었다 —
①확장에 `tv` 가 들어가 모든 TV 가 조건을 통과했고 ②맨숫자 `65` 는 `165hz`·`1,658.8` 에 걸린다.
지금은 `(\d+)(인치|형)` 을 파서가 **표기 짝**으로 잇는다(통합검색 `UNIT_PAIRS` 와 같은 규칙).
**맨숫자로 넓히지 말 것** — 65인치 11종 · 83인치 3종 · 27인치 모니터 8종.

### 브랜드가 앞에 붙은 카테고리 (하만)

하만 54종은 카테고리 이름이 `JBL 사운드바`·`JBL 헤드폰`처럼 브랜드가 앞에 붙어 있다.
CATSYN 에 `사운드바` 하나만 매핑해 둔 탓에 **"JBL 사운드바"를 쳐도 삼성 사운드바 9종이
나왔다**(JBL 제품은 한 종도 없이). 품목으로 물으면 그 품목 전부가 나와야 하고 브랜드는
조건이 걸러 준다 — `사운드바`·`스피커`·`이어폰`·`헤드폰`에 하만 카테고리를 함께 매핑했다.

- **'공기청정'(기 없이)은 카테고리가 아니라 기능이다.** 무풍 에어컨 26종이 이 말을 달고 있어,
  카테고리로 잡으면 *"무풍 공기청정 19평"* 이 공기청정기로 끌려가 0종이 된다(정답은 에어컨 3종).
- **`SYN` 에 상위 개념을 섞지 말 것.** `"직배수":["직배수","로봇"]` 이던 탓에 직배수가 아닌
  로봇청소기까지 걸렸다(13종 중 실제 직배수는 2종). 형태는 SUBCAT 이 가른다.

## 모델파인더 — 삼성스토어 카탈로그 기준 정합성

모델파인더 DB는 **삼성스토어 카탈로그(`samsungstore.com/event/catalog.sesc`)를 기준**으로 맞춘다.
사용자가 구글드라이브로 카탈로그 PDF(mobile/it/md/dp)를 공유해주면 대조·갱신한다.

- 카탈로그에는 **가격이 없다**(4개 PDF 전수 확인). 모델코드·치수·스펙만 있으므로, 카탈로그에서
  새로 추가한 제품은 `price: null`로 두고 기존 제품의 가격은 유지한다.
- `price: null` 처리 규칙(파인더 로직에 반영됨): JS에서 `null <= 300`이 참이라 그대로 두면
  가격 미상 제품이 최저가처럼 예산검색 상단에 뜨는 왜곡이 생긴다. 따라서 ①예산 조건이 걸린
  검색에서 제외 ②가격 정렬 시 항상 뒤로 ③패키지/AI 예산배분 후보에서 제외 ④카드에는
  "가격 문의"로 표시한다. 새 제품을 가격 없이 추가할 때 이 규칙이 유지되는지 확인할 것.
- PRODUCTS 배열(CE+MX)과 별도로 **HARMAN 배열(54종)이 런타임에 합쳐진다**. 화면 문구의
  "OOO종"은 둘의 합이므로 제품을 추가하면 `finder-app.html`의 표기 2곳과 `app/page.tsx`
  모델파인더 카드 설명도 함께 갱신해야 한다(과거 실제 수량과 표기가 어긋나 있었음).
- MD 카탈로그(제휴·리빙 상품)는 삼성 자체 제품이 아니므로 `src:'LIVING'` + 카테고리명 앞에
  "리빙"을 붙여 구분한다(리빙 안마의자·비데·욕실케어·도어락·도어벨·전동커튼·선풍기·밥솥 등 9개 카테고리).
- IT 카탈로그 스펙표는 "모델명 → 모델코드 N개 → (라벨 + 값 N개) 반복" 구조라 파싱 가능하지만,
  지면 뒤쪽(프린터 섹션) 값이 라벨 정렬 붕괴로 섞여 들어오는 구간이 있다. 실제로 모니터 해상도에
  프린터 DPI 값이 들어온 사례가 있었으므로, 파싱한 값은 반드시 형식 검증 후 반영할 것.
- 2026-07-31 카탈로그 대조로 발견·정정한 오류: **S26+의 모델번호가 SM-S936N(실제로는 S25+의
  코드)으로 잘못 들어가 있었다 → SM-S947N**. S26/S26+ 치수도 카탈로그 실측값으로 정정.
- 2026-08-02 대조(태블릿·웨어러블·데스크탑·SSD). 사용자가 "카탈로그 내용이 정확하니 불일치하면
  카탈로그로 교체"라고 확정했으므로, 이후에도 **DB값과 카탈로그값이 다르면 카탈로그를 채택**한다.
  이때 발견한 오류 유형이 반복되므로 신규 대조 시 특히 확인할 것:
  - **모델코드가 한 세대 어긋나 있는 패턴**이 다수였다 — Tab S11 `SM-X710`(실제 S10 코드)→`SM-X736/X730`,
    Tab S10 FE `SM-X510`(실제 S9 FE 코드)→`SM-X526/X520`, Buds4 Pro `SM-R630N`(Buds3 Pro)→`SM-R640N`,
    Buds4 `SM-R660N`→`SM-R540N`, Buds3 FE `SM-R400N`→`SM-R420`. 스펙 행 자체가 이웃 세대 것인
    경우도 있었으므로(구 S10 FE/FE+ 항목의 치수·무게가 S9 FE/FE+ 값) 코드만 고치지 말고 행 전체를 대조할 것.
  - 워치 라인업이 통째로 한 세대 뒤처져 있었다(DB "워치8 시리즈" → 카탈로그는
    **Watch9 / Watch Ultra2 / Watch8 Classic / Fit3**). 카탈로그 섹션 첫 페이지의 목차(제품명+쪽번호)와
    각 스펙 지면의 쪽번호 마커를 대조하면 어떤 코드가 어느 제품인지 확정할 수 있다.
  - 워치·링·핏·데스크탑·SSD는 애초에 모델코드가 하나도 없었다. 신규 카테고리를 추가할 때
    모델코드 필드를 빠뜨리지 않는지 확인할 것(통합검색 상세보기의 핵심 항목).
- 2026-08-02 dp(가전) 카탈로그 반영. **174MB PDF는 Google Drive MCP `read_file_content`가 빈 문자열을
  반환한다 — 실측 한계는 50MB(읽힘)와 53MB(빈 응답) 사이**다. 사용자에게 분할 업로드를 요청할 때 이 값을
  기준으로 안내할 것. 6분할로 1~141쪽 전체를 확보했고, 스펙표는 16~25쪽(TV)·36~141쪽(가전)에만 있다.
- dp 카탈로그 스펙표는 IT 카탈로그와 같은 "모델코드 N개 → (라벨 + 값 N개) 반복" 구조라 파싱되지만,
  **한 지면에 표가 둘 나란히 놓인 구간에서 두 표가 번갈아 추출된다.** 파서를 쓸 때 반드시:
  ① 같은 라벨은 **최초 1회만 채택**할 것(뒤쪽 표의 동일 라벨이 먼저 읽은 올바른 값을 덮어써 컬럼이 밀린다.
     실제로 냉장고 RS80F64J3F 치수가 912×1,786×716 대신 옆 표의 640×1,830×800으로 들어갔었다)
  ② 화이트리스트 밖 라벨은 **라벨과 그 값 줄까지 함께** 건너뛸 것(라벨만 건너뛰면 값이 다음 라벨에 붙는다)
  ③ 지면 구분선(`-----`)·페이지 마커에서 표를 끊고, 코드 블록에서 일정 줄 이상 멀어지면 중단할 것
  ④ 반영 전 **원문에서 눈으로 확인한 정답값 몇 개로 회귀 검증**할 것(위 3개를 고친 뒤 7/7 통과했다)
- 색상 변형 코드가 한 컬럼을 공유해 값 개수 < 코드 개수인 표(세탁기 등)가 있다. 이때는 스펙을 넣지 말고
  **모델코드만** 반영한다(`note`에 사유 기재). 치수를 틀리게 넣는 것보다 비워두는 편이 낫다 —
  설치 상담에서 치수 오류는 바로 사고다.
- **끝까지 확정하지 못한 값은 넣지 않는다.** 2026-08-02 대조에서 microSD(PRO Plus/EVO Plus)는
  IT 카탈로그 표의 헤더가 앞 표(외장 SSD T9/T7) 열 이름으로 오염돼 `MB-MH*`/`MB-MB*` 코드와
  제품명의 대응을 카탈로그만으로 확정할 수 없어 **기존 값을 그대로 두었다**. 노트북
  Book6 360(15)·Book6(15)·Book5 Edge(16)도 IT 카탈로그에서 대응 코드를 특정하지 못해 구형
  `NP` 코드가 남아 있다. 추정으로 채우지 말 것 — 설치환경 이미지 원칙과 동일하다.

### 2026-08-02 dp 카탈로그 전량 반영 (386 → 524종, 43개 카테고리)

- 신규 카테고리 6개: **업소용 냉장고 / 냉동고 / 정수기 / 슈드레서 / 시스템에어컨 / 제습기**.
  카테고리를 추가하면 `finder-app.html`의 `CATSYN`(검색어→카테고리 매핑)에 넣고
  `UNSUPPORTED`("DB 미수록" 안내)에서 빼야 한다. 정수기·제습기·냉동고가 여기 해당했다.
  `scripts/test-finder.mjs`의 `CAT_QUERIES`와 카테고리 개수 검사값도 함께 갱신할 것.
- **표의 "코드 개수 ≠ 값 개수"는 열 병합 때문이며, 열 묶음은 컬러 칩 개수로 확정된다.**
  세탁기 표(코드 7 ↔ 값 5)를 이전에 `[WF80H24ADW][ADS][ADT][ADY][WW13]`로 잘못 읽어
  WF25CB8895 2종을 통째로 누락하고 AI 맞춤세탁 등급·디스플레이가 어긋나 있었다. 실제로는
  컬러 칩이 정확히 7개(W / S·T·Y / BE / BV / B)라 `[WF80H24ADW][WF90F25AD*][BE][BV][WW13]`이다.
  컬러 칩은 병합되지 않고 코드 순서대로 나열되므로 **열 대응을 확정하는 가장 신뢰할 수 있는 단서**다.
- **형제 표의 규칙을 교차검증에 쓸 것.** 같은 지면 콤보(WD) 표에서 "80 시리즈=다이얼 스크린(71.1㎜),
  90 시리즈=스마트 스크린(177.8㎜)"이 확인되며, 세탁기·건조기 표의 디스플레이 값 순서가 이 규칙과
  일치해 열 순서가 코드 순서임이 증명된다.
- **치수가 이웃 표에서 넘어오는 사고가 또 있었다.** 세탁기 686 × 1,110 × 875는 콤보(WD) 값이고
  세탁기는 686 × 984 × 875다. 세탁기 표에 없는 '직수 파워 오토 클린' 행도 콤보 표에서 넘어와 있었다.
- **합계가 맞는지로 열 대응을 검증할 수 있다.** 업소용 냉장고는 코드 셀이 묶여 6열처럼 보였지만
  총내용적·냉동실·냉장실 값이 각각 11개이고 11행 전부 냉동+냉장=총내용적이 성립해 11열로 확정했다.
  모델명 자릿수(0620·0622→640㎜, 114x→1,260㎜, 1762→1,900㎜)가 치수 병합 구간과 일치한 것도 근거.
- 모델코드에 스펙이 인코딩된 카테고리가 많아 그대로 검산에 쓸 수 있다 — 홈멀티 `4P36`→4실,
  접미 `SSY/CSY`→무풍/무풍공기청정, `A/B`→단배관/다배관, `U/X`→무풍 O/X, 무풍 1Way `AC023`→2.3㎾,
  제습기 `23100`→23L, 벽걸이 `AR60F09`→29.3㎡, 에어컨 컬러 접미 `W/B/L/R`→화이트/베이지/블루/로즈골드.
- **각주에만 나오는 와일드카드 코드(`RM90F91B1*` 등)는 제품이 아니다.** 과거 이를 SKU로 오인해
  28건을 넣었다가 지우는 과정에서 실모델(`DW90F79F1UAP` 등)까지 함께 지운 적이 있다.
  지울 때는 스펙표 헤더에 있는 코드인지 반드시 확인할 것.
- 표 병합으로 코드별 대응을 끝내 확정할 수 없는 값은 비워둔다 — 상업용 천장형 에너지소비효율,
  DF90H24R4/R5(에어드레서)·제트 Lite·Q930H 스펙표는 추출본에서 코드 행/표가 누락돼 미반영이다.
  Q930H·Q800H는 본문에 채널 구성만 나와 그 항목만 넣었다.
- 구글드라이브 분할본 페이지 대응: `(5)(2)(1)`=1~7쪽, `(5)(2)(2)`=8~15쪽, `(5)(1)`=16~25쪽(TV 스펙표),
  `(4)`=26~35쪽(Audio·하만 스펙표), `(1)`=36~75쪽, `(2)`=76~114쪽, `(3)`=115~141쪽(청소기·데이코).
- USP는 카탈로그 소개 지면의 셀링포인트 헤드라인을 옮긴 것이다. 문구를 확인할 수 없는 카테고리
  (리빙 제휴상품·전자레인지/오븐·XR·SSD·비카탈로그 사운드바)는 지어내지 않고 출처 표기만 남겼다.

### 데이코 전용 카탈로그 (2026-08-11 반영 완료 — 7 → 15종)

카탈로그 페이지에는 5종이 있다 — `samsungstore.com/event/catalog.sesc?menu=w110` →
AV/생활가전(`Dir=268`) · IT종합(`264`) · 모바일(`265`) · MD 소형가전(`266`) · **데이코(`200`)**.
앞 4종은 전부 "2026년 07-08월호"이고, 데이코 전용 카탈로그(88쪽)만 오래 안 보고 있었다.

- **PDF 없이 읽는다.** 뷰어(aiibook)가 페이지를 낱장 이미지로 서빙한다:
  `https://samsung.aiibook.net/b2c/catImage/200/s001.jpg` ~ `s088.jpg` (낱장 20~130KB).
  목차는 같은 폴더의 `mokcha.txt`("쪽번호:제목", 하위 항목은 `-`로 시작).
  `Dir` 만 바꾸면 다른 카탈로그도 같은 방식으로 열린다.
- **`Product Line-up` 지면(50~52쪽)에 모델코드와 치수가 전부 정리돼 있다.** 제품 소개 지면을
  다 읽을 필요가 없다 — 라인업 지면이 15종을 한 곳에 모아 준다. 셀링포인트만 각 소개
  지면(14·18·21·32·34쪽)의 헤드라인에서 옮겼다.
- **CLAUDE.md 의 옛 기록이 틀려 있었다.** "빠진 5개 = T-TYPE 냉장고·컬럼 김치냉장고·와인
  셀러·오븐·컴팩트 오븐"이라고 적혀 있었는데, **컬럼 김치냉장고(`DRQ24982RAP*`)와 와인
  셀러(`DRW24980RAP*`)는 이미 DB 에 있었다.** 실제로 빠진 것은 **T-TYPE 냉장고 · 컬럼
  냉장고 · 컬럼 냉동고 · 오븐(75L) · 컴팩트 오븐(50L)** 이다. 기록을 믿지 말고 DB 를 먼저 셀 것.
- 추가한 8종(색상 변형 포함) — `DRF427520APB/S`(T-TYPE, 1,048×2,118×605) ·
  `DRR24981RAPB/S`(컬럼 냉장고, 604×2,118×605) · `DRZ24981LAPB/S`(컬럼 냉동고, 같은 치수) ·
  `DOB24M977SD`(오븐 75L, 595×595×570) · `DOC24M977SD`(컴팩트 오븐 50L, 594×453×570).
  냉장고 계열은 **Door Panel 제외/포함 두 치수**가 모두 적혀 있어 둘 다 담았다.
- 제품 수 표기가 세 곳이다 — `finder-app.html` 2곳(헤더·안내문) · `app/page.tsx` 카드 설명.
  **524 → 532종(+Harman 54 = 586종)**. 고친 뒤 `build:index`·`build:sizereps` 를 **둘 다**
  다시 돌릴 것(순서도 중요하다 — size-reps 를 나중에 만들면 색인이 뒤처져 test:search 가 깨진다).

**USP 를 카테고리 단위로 붙이면 다른 제품 문구가 섞인다** (2026-08-12 발견·정정).
데이코 15종이 `데이코 빌트인` 한 카테고리라, 소개 지면 헤드라인을 카테고리에 통째로 붙인 탓에
**컬럼 김치냉장고(`DRQ24982RAP*`)와 와인 셀러(`DRW24980RAP*`)에 T-TYPE 냉장고 문구**
("국내 최초 빌트인 T-Type" · "듀얼 오토 아이스메이커" · "히든 워터 디스펜서")가 들어가 있었다.
**상담사가 화면을 그대로 읽으면 틀린 설명이 된다** — 김치냉장고에 아이스메이커도 디스펜서도 없다.

- 고칠 때는 **그 제품 `fx`(스펙표 전 항목 대조값)에서 검증되는 문구로만** 채운다.
  카탈로그 소개 지면은 클라우드 세션에서 재확인할 수 없다(`samsung.aiibook.net` 이 프록시에
  막힌다 — 도면 수집이 로컬 전용인 것과 같다). **원문을 못 보면 지어내지 말고 스펙표로 간다.**
- **`kw` 꼬리에 usp 가 소문자로 그대로 붙어 있다.** usp 만 고치고 kw 를 두면 검색이 옛 문구로
  걸린다 — `kw.endsWith(usp.join(' ').toLowerCase())` 로 검산하며 함께 고칠 것.
- 제품 객체는 `JSON.stringify` 로 직렬화돼 있어 **파싱 → 재직렬화가 원본과 바이트 단위로 같다.**
  객체를 통째로 바꿔치기해도 diff 가 그 항목에만 남는다(고치기 전에 일치부터 확인할 것).
- 나머지 11종은 제품 타입과 일치해 손대지 않았다. **T-Type 문구는 T-TYPE 냉장고 2종에만 남는다.**

### 삼성닷컴 전수 — **모델명 1,896종을 받았다** (2026-08-17)

사용자 지시: *"제품 상세검색에 없는, 삼성 홈페이지엔 있는 제품들을 수록. **모든 모델명을
먼저 기록**하고, 모델명이 모두 수집되면 그 후 순차적으로 스펙표를 만든다."*

**목록은 `cxhr/pf/goodsList` 로 받는다 — DOM 을 긁지 말 것.**
목록 지면의 카드 링크를 긁었더니 **카테고리당 11종에서 멈췄다**(냉장고 실제 192종).
'더 보기' 버튼은 **필터 패널 것**이라 숨겨져 있고 스크롤로도 안 늘어난다. 네트워크를
관찰하니 지면이 스스로 이 API 를 부른다 — **AI구독 케어 절에 이미 적혀 있던 그 경로다.**

```
GET /sec/cxhr/pf/goodsList?dispClsfNo={번호}&sortType=10&page=1&rows=500
    → { count, products:[{ mdlCode, goodsNm, mdlNm, curPrice, imgPath1~8 }] }
```

- **`dispClsfNo` 를 손으로 적지 않는다.** 목록 지면을 열면 그 요청에서 얻는다.
- `rows=500` 이면 한 번에 전부 온다(23개 카테고리 전부 `count` 와 일치, 미달 0).
- 도구는 `.scratch/sec-catalog.mjs`, 결과는 **`scripts/fixtures/sec-catalog.json`**(커밋됨).
  **가격은 담지 않는다** — 가격 표기는 타사비교에서 출고가로만 한다(2026-08-17 사용자).

**robots — `Disallow: /sec/` 에 속지 말 것.** 그 줄은 **`User-agent: Yandex` 그룹**(75행부터)
것이다. 우리에게 적용되는 `*` 그룹이 막는 것은 **`/sec/search/…` 검색 결과 지면뿐**이라
목록·제품·지원 지면은 허용이다. **`/sec/robots.txt` 와 `/sec/sitemap.xml` 은 Akamai 가
403 을 준다**(브라우저로도) — 글로벌 `www.samsung.com/robots.txt` 를 봐야 한다.

**우리 DB 코드가 삼성닷컴 코드와 다르다 — 이것이 500 의 정체였다.**
파일럿 60종에서 지원 지면이 **33건 500** 이었고 **MX 전체**(스마트폰·태블릿·노트북·모니터·
데스크탑·SSD·버즈·링·XR)와 리빙 제휴상품이 전부 그랬다. 접미 변형 재시도는 **0건**
살렸다 — 변형이 아니라 **다른 코드**이기 때문이다. 삼성닷컴이 쓰는 코드로 다시 두드리니
**8종 전부 200** 이 됐다(`SM-F976NZVIKOO` 사양 18항목 등).

| 실측 (2026-08-17) | |
|---|---|
| 삼성닷컴 현행 모델 | **1,896종** (23개 카테고리, 실패 0) |
| 우리 DB 532종 중 삼성닷컴에서 확인되는 것 | **148종 (28%)** — 나머지는 단종·카탈로그 전용·리빙 제휴상품 |
| 삼성닷컴에만 있는 SKU | 1,714 → **제품명으로 묶으면 1,217종** |
| 가장 많이 빠진 곳 | TV 268 · 모니터 108 · 에어컨 95 · 시스템에어컨 89 · 스마트싱스 88 |

**SKU 와 제품을 구분할 것.** 삼성닷컴 코드는 색상·유통까지 가르는 SKU 다 —
`SM-F976NZVIKOO`(자급제) · `…DGIKOO`(전용컬러) · `…ZVPKOD`(통신사폰)가 전부 같은
*Z 폴드8 울트라* 다. "1,789개가 빠졌다"고 세면 부풀려진다.

**다음 단계(스펙표)에서 이미 드러난 것** — 사양 추출률이 카테고리마다 다르다.
MX 는 16~18항목이 잘 나오는데 TV·모니터·에어컨·청소기는 3~4항목만 잡힌다(지연 로드이거나
사양 탭을 열어야 하는 것으로 보인다). **전수를 돌리기 전에 이 추출부터 고칠 것.**
그리고 **라벨이 카탈로그와 다르다** — 파일럿에서 라벨 72종 중 카탈로그와 같은 이름은
21종(29%)뿐이었다. 정규화 표 없이 합치면 같은 항목이 두 이름으로 쌓인다.

**기준 충돌을 먼저 정할 것.** 이 파일의 기존 원칙은 *"카탈로그가 기준"* 이다.
삼성닷컴을 섞으면 한 항목에 출처가 둘이 된다 — **카탈로그 값이 있으면 그대로 두고
빈 항목만 채우며 출처를 남기는** 방식을 제안해 두었다(사용자 확인 전).

### 삼성닷컴 사양 전수 — **586 → 2,012종** (2026-08-18)

**사양이 어디 있는지가 갈래마다 다르다. 한쪽만 보면 절반이 빈다.**

| 갈래 | 사양이 있는 곳 | 다른 쪽은 |
|---|---|---|
| CE·TV·모니터·프린터 | **제품 상세 지면(PDP)** | 지원 지면에는 없다 |
| MX(폰·태블릿·북·버즈·워치) | **지원 지면** | PDP 에 `spec` 이 붙은 id·클래스가 **하나도 없다** |

PDP 로 1,896종을 받아 113종이 0쌍이었고(갤럭시북 69% · 태블릿 60%), **기다림을 늘려 봤자
0/6** 이었다 — 경합이 아니라 블록 자체가 없다. 지원 지면으로 다시 받아 **89종을 회수**했다.

**PDP 에서 사양을 꺼내려면 세 겹을 지나야 한다:**
1. **「스펙」 길잡이를 누른다** — 안 누르면 `#compGoodsSpec` 이 아예 안 생겨 0쌍이다
2. **`#compGoodsSpec` 안만 읽는다** — 지면 전체를 긁으면 맨 위 **결제 블록**이 잡힌다
   (최대 혜택가·무이자 할부·멤버십 포인트 …)
3. **한 겹 더 들어간다** — `dt` 는 묶음 이름(디스플레이·사운드)이고 실제 항목은 `dd` 안의
   `<li><strong class="spec-title">화면크기</strong><p class="spec-desc">214 cm</p></li>` 다.
   묶음을 값으로 담으면 `fx` 한 줄에 열 항목이 뭉쳐 검색도 비교도 안 된다

**탭을 합치지 말 것 — 여기가 가장 위험하다.** 사양 탭은 **패키지 구성품**이다(TV = 본체 +
베젤, 에어컨 = 실내기·벽걸이·실외기·배관). 다 눌러 한 Map 에 담으면 **다른 제품 사양이
섞인다.** 본체는 **모델코드 최장 공통 접두**로 가린다 — 포함 관계로 보면 못 가른다
(`KQ85LSH03EFXKR` vs `KQ85LSH03E-WT` 는 서로의 접두가 아니다).
**탭은 `role="tab"` 으로 찾는다.** 클래스에 `tab` 이 들어갔는지로 보면 냉장고의
**툴팁 버튼 `button.spec-title` 14개**가 탭으로 잡혀 같은 52쌍을 14번 세어 728쌍이 된다.

**받은 것의 26%는 사양이 아니라 법정 표시다** — `제조자/수입자`·`제조국가`·`KC 인증 필
유무`·`동일모델의 출시년월`·`A/S 책임자와 전화번호`·`품질보증기준`. 모든 제품에 100% 붙어
1,896종이면 1만 줄이 넘고 `kw` 에 섞여 검색까지 흐려진다. **정확히 일치할 때만** 뺀다 —
부분일치로 빼면 `인증기관` 같은 진짜 사양이 날아간다.

**카테고리 옮기기 — 접두는 애매한 칸을 가를 때만.** 삼성닷컴 23칸 ↔ 우리 43칸이다.
모델 접두를 카테고리보다 **먼저** 보게 했더니 김치냉장고 131종 중 **22종만** 우리
김치냉장고로 가고 나머지가 냉장고로 흘렀다(김치냉장고 코드도 `RS…`·`RM…` 로 시작한다).
삼성닷컴이 이미 "김치냉장고"라고 말하는데 우리 규칙이 덮어쓴 셈이다. 지금은 **한 칸이
여럿으로 갈리는 5개**(냉장고·세탁/건조·의류관리·스마트폰·워치)만 접두로 가른다 →
1,784/1,896(94%). 못 가른 것은 짐작해 넣지 않는다.

**인라인에 넣지 않는다 — `public/finder-extra.json` 으로 받아 붙인다.**
`finder-app.html` 은 이미 1.1MB 이고 해석·실행에만 1,242ms 가 걸린다(프로파일의 58%가
파싱). 1,200종을 인라인에 더하면 2.65MB 라 매장 폰에서 3초 넘게 파싱만 한다.
**`kw` 는 파일에 담지 않고 앱이 만든다** — `fx` 를 소문자로 베낀 글이라 함께 넣으면
2.7MB 가 그대로 중복된다(6.50 → **3.53MB**). `sw.js` 가 SWR 로 잡는다.

**기존 값은 덮지 않는다.** "카탈로그가 기준"이라는 이 파일의 원칙 그대로, 카탈로그 대조로
넣은 값은 두고 **그 제품에 없던 항목만** 채운다(`fill` 200종 21,255줄) · 우리 DB 에 없던
제품만 새로 담는다(`add` 1,426종). **가격은 담지 않는다.**

도구(전부 `.scratch/`, 로컬 전용) — `spec-collect.mjs`(전수 · JSONL 로 이어 씀 ·
끊겨도 재개) · `spec-retry.mjs`(빈 것을 지원 지면으로) · `spec-extract.mjs`(PDP 추출) ·
`sec-cat-map.mjs` · `sec-normalize.mjs` · `sec-merge.mjs`.

### 미착수 — 삼성닷컴 전수 (2026-08-09 조사)

**삼성닷컴(`www.samsung.com/sec`) 전수 반영은 성격이 다른 작업이다.**
- 목록 페이지는 SSR 이고 `waitUntil:'networkidle'` 로는 **타임아웃 난다**(추적 스크립트가 계속
  돈다). `domcontentloaded` + 고정 대기로 받을 것.
- 제품 목록 JSON API 는 안 보이고, 모델코드는 카드 링크 URL 에서 뽑힌다.
  **치수는 목록에 없어 제품 상세를 건마다 방문해야 한다.**
- **모집단이 카탈로그와 다르다.** 삼성닷컴이 훨씬 많고 단종·온라인전용이 섞인다. 그래서
  "카탈로그를 삼성닷컴으로 교체"가 아니라 **어느 쪽을 기준으로 삼을지부터 정해야 한다** —
  CLAUDE.md 의 기존 원칙은 "카탈로그가 기준"이므로 이걸 바꾸는 것은 별도 결정이다.

## LG 베스트샵 카탈로그 (타사비교 전용)

`20260708_best_catalogue`(188쪽)는 **LG전자** 카탈로그다. 사용자가 명확히 못박은 사용 범위:
**"타사비교 목적과 경쟁우위 파악"이 전부이며 앱에 LG 제품을 노출할 목적이 아니다.**
따라서 LG 모델은 `public/compare-app.html`(타사비교)에만 넣고 **모델파인더(`finder-app.html`)·
통합검색 인덱스에는 절대 넣지 않는다.** 반영 후 `grep -c <LG모델코드> public/finder-app.html
public/search-index.json`이 둘 다 0인지 확인할 것.

- 구글드라이브 `read_file_content`는 188쪽 원본을 78쪽에서 잘라 반환한다(파일 문제가 아니라 도구 한계).
  4분할 업로드본을 각각 읽어 이어 붙여야 전체가 확보된다. 응답이 토큰 한도를 넘으면 결과가
  `tool-results/*.txt`에 `{"fileContent": "..."}` JSON으로 저장되므로 python으로 꺼내 쓴다.
- **표 형식이 두 가지**다. ①모델코드가 줄마다 하나씩 오는 형태(`scratchpad/lgparse.mjs`가 파싱)와
  ②`모델명 A B C 라벨 값 값 값 …`처럼 한 줄로 뭉개진 형태. 세탁기·건조기·스타일러·청소기·에어컨은
  대부분 ②라서 `grep -n '^모델명 '`으로 표 위치를 먼저 찾아야 한다.
- LG 표는 "라벨 → 값 N개"가 코드 수와 정확히 일치해 삼성 dp 카탈로그 같은 열 병합 보정이 필요 없다.
- **카탈로그에 가격이 없다.** 스펙만 확인된 모델은 `price`를 넣지 않고 비워 둔다 —
  `priceLabel()`이 "가격 문의"로 표기하고 `specValue()`가 undefined를 반환해 가격 행이 렌더되지 않는다.
  추정 가격을 채우지 말 것(모델파인더 `price: null` 규칙과 같은 원칙).
- **브랜드마다 흡입력 표기 기준이 다르다.** 삼성 로봇청소기는 "흡입력 W + 진공도 Pa"를 따로 적고
  LG 홈봇 RONi는 "최대 흡입력 30W"만, 로보킹은 "진공도 10,000 Pa"만 적는다. 단위가 같은 값끼리만
  비교 행에 넣을 것(그래서 로봇청소기는 `pa`만 넣고 W는 넣지 않았다).

### 대조로 드러난 "현장에서 반박당하는 문구" — 되살리지 말 것

타사 카탈로그를 보는 진짜 이유가 이것이다. 우리 셀링포인트가 사실인지 확인하는 것.
`scripts/test-compare.mjs`의 `[10-c]` 구간이 아래를 회귀 검사한다.

- **"국내 일체형 최대용량(건조 20kg)"** — 2026년형 LG 워시콤보 `FC2521*X6C`는 **세탁 25 / 건조 21kg**이라
  성립하지 않는다. 세탁기·콤보와 건조기 카테고리 삼성 모델의 `on`에서 삭제했다.
- **"Micro RGB는 삼성만의 기술"** — LG도 2026년형에 Micro RGB(`100MRGB96BK`/`86MRGB96BKA`/
  `75MRGB96BKA`)를 냈다. TV 셀링포인트에서 "삼성 독자 기술" 문구를 뺐다.
- **"LG 그램은 IPS"** — 그램에도 OLED 트림(`16Z90U-KU7HK`, WQXGA+ 2880×1800 OLED VRR)이 있다.
  AMOLED 우위 문구는 "고객이 보는 모델을 먼저 확인" 전제로 바꿨다.
- **에어컨 에너지등급** — LG 2026 `FQ**GN9`는 전 모델 1등급, 삼성 `AF90H`는 19평(62.6㎡)이 2등급 /
  17평(56.9㎡)이 1등급이다(dp 카탈로그). 등급 차이를 부정하지 말고 평형을 맞춰 제안하도록 대응전략에 넣었다.
- LG 에어컨은 **모델코드 숫자가 그대로 평형**이고 냉방면적과 일치한다(FQ25→81.8㎡, FQ18→58.5㎡).

### 삼성 쪽 빈 칸 — **찾아봤지만 출처가 없어 비워 둔 것** (2026-08-15)

비교표는 양쪽에 값이 있어야 행을 그리므로 빈 칸 하나가 그 행을 통째로 지운다.
그래서 어디가 비었는지 재고(`.scratch/cmp-thin.mjs`·`cmp-gap.mjs`) 전부 두드려 봤다.
**아래는 다시 파지 말 것** — 같은 곳을 또 뒤지게 된다.

| 빈 칸 | 두드려 본 곳 | 결과 |
|---|---|---|
| 로봇청소기 `pa` 3종 | 지원 페이지 · 제품 지면 | **삼성은 Pa 를 어디에도 안 적는다.** 흡입력을 **W** 로만 적고 시험 근거를 밝힌다(KTL · KS B 7303:2025). 울트라의 22,000 Pa 는 **dp 카탈로그**에서 온 값이라 그 값을 옆 모델에 옮겨 적지 않는다 |
| 에어컨 `eGrade` AF90H19D27SRS | 모델파인더 | 파인더는 1등급인데 **근거 표기가 없고**, 이 파일은 dp 카탈로그 근거로 19평을 2등급이라 적는다 — **충돌**이라 확정 전까지 비워 둔다(그 모델은 19+6평 2in1 이라 또 다른 물건일 수 있다) |
| 김치냉장고 `bins`·`eGrade` | dp 카탈로그 | 항목 자체가 없다 |
| 공기청정기 `pm`·`eGrade` | dp 카탈로그 | 감지 최소입자가 없다 |
| 식기세척기 `noise` | dp 카탈로그 · 제품 지면 | 소음 수치가 없다 |

**채운 것** — 식기세척기 `install`(빌트인) 3종. 근거는 samsung.com 제품 지면 주소가
`built-in-…` 이고 제품명이 '빌트인'이라는 것. LG 는 넷 다 적어 두어 삼성만 비면
*"빌트인이냐 프리스탠딩이냐"* 라는 상담 첫 질문이 화면에서 사라진다. 평균 4.9 → **5.9행**.

**식기세척기 모델코드 2건 — 2026-08-16 실재 확인으로 해소.** 짐작해 고치지 않고
`test-consistency` PART C 에 등재해 두었던 것이 옳았고, 그 사이 **판정 방법이 틀렸던 것**이
드러났다:

- 옛 판단 *"samsung.com 현행 11종에 없다"* 는 **목록 지면만 본 것**이었다.
  **지원 페이지는 목록에 없는 모델도 해결해 준다** — `support/model/DW80F71Y1UEWS/` 가
  HTTP 200 으로 *"Bespoke AI 식기세척기 빌트인 12인용 `DW80F71Y1UEW`"* 를 준다.
  `DW80F73Y1UEWS` 도 *"14인용 `DW80F73Y1UEW`"* 로 실재한다.
- 반대로 **카탈로그가 적은 `DW80F73Y1UWW` 가 HTTP 500 에 제품명도 없다** — 그쪽이
  틀린 코드였다. `DW80F73X1UEW`(X 변형)도 실재하므로 Y·X 는 함께 둔다.
- **모델코드를 "없다"고 판정할 때 목록 지면만 보지 말 것.** 지원 페이지로 한 번 더 두드린다.
  (지원 페이지는 코드 끝의 유통 접미 `S` 가 있어도 해결되고, 화면에는 접미 없는 형태로 적는다.)

남은 것은 **파인더 미수록**이라는 다른 성격의 문제다 — 파인더는 카탈로그가 기준이라
지원 페이지만 보고 스펙 없이 제품을 넣지 않는다. 다음 카탈로그 대조 때 함께 넣을 것.

### 스마트폰·노트북 긱벤치 — **S26 은 공식 차트에 없다** (2026-08-15)

사용자 요청으로 긱벤치를 근거로 삼았는데, **차트가 S25 까지**다(재확인함).
SD8 Elite Gen 5 기기 자체는 올라와 있다(vivo iQOO 15 · OnePlus 15 · Honor Magic8 Pro) —
삼성 S26 만 아직 없다. iPhone 17e 도 없다.

- **robots 확인**: `User-agent: *` 는 `Allow: /` 이지만 **`Disallow: /search`** 다.
  차트 지면(`/android-benchmarks`·`/ios-benchmarks`)만 쓴다. 검색 지면으로 개별
  제출본을 긁지 말 것 — 규칙 위반이고, 개별 제출본은 중앙값이 아니라 편차도 크다.
- **같은 칩을 쓴 다른 기기 점수를 S26 행에 옮겨 적지 않는다.** 그러면 우리 제품에
  대해 측정하지 않은 수치를 말하게 된다.
- 그 결과 **S26 대 아이폰 비교에는 성능 수치 행이 없다.** AP 이름 행은 뜨지만
  등급이 안 붙는다(이름 항목은 원래 등급을 매기지 않는다). 차트에 오르면 채울 것.

### 원문이 "없음"이라고 한 것은 값으로 넣는다 (2026-08-14 사용자 결정)

비교표는 **양쪽에 값이 있어야** 그 행을 그린다. 그래서 빈 칸으로 두면 **우리가 없는
항목이 화면에서 통째로 사라진다** — 삼성 `AX060CG500GBD` 는 원문이 *"UV살균 LED = 없음"*
인데 비워 두니 LG 의 UV팬살균과 견주는 행이 **12개 조합 중 10개에서 안 그려졌다.**
상담사가 자기 약점을 못 보는 셈이라, 타사 카탈로그를 보는 목적 자체에 반한다.

**경계 둘을 반드시 지킬 것** (`test-compare [12]` 가 검사한다):
- **문자열 항목에만.** `uv`·`voice`·`install`·`disp` 처럼 우열을 수치로 재지 않는 항목이다.
  우열 판정은 **양쪽이 숫자일 때만** 도므로 "없음"이 들어가도 비교가 깨지지 않는다.
  **`eGrade`·`noise` 같은 수치 항목에는 절대 넣지 말 것** — 넣으면 그 항목이 영영
  무승부가 된다(수치 항목에 넣어 물리는 것을 확인했다).
- **원문이 명시한 것만.** "확인 못 했다"와 "실제로 없다"는 다른 말이고, 이 앱은 전자를
  **비워 두는 것**으로 표현한다. 섞으면 그 구분이 사라진다.

### 우열은 S~E 등급으로 말한다 (2026-08-15 사용자 요청)

*"엑시노스2600이랑 A19 pro 칩셋이 동급이라고 표기되어있는데 어떤근거로 동급이라는건지
알수가없습니다. 확실히 우위를 가려서 등급표기를 해주세요"* → *"S급 A급 B급 C급 D급 E급"*.

**옛 배지는 재 보지도 않고 '동급'이라 말하고 있었다.** 우열 판정은 양쪽이 숫자일 때만
도는데 배지는 그 밖의 경우를 전부 '🟡 동급'으로 찍었다 — AP(칩셋)뿐 아니라 TV 패널
방식(`Micro RGB` vs `OLED evo`) 등 **399개 조합**이 그랬다. 지금은 등급으로 바꿨다.

**등급은 지금 견주는 두 모델 사이에서 낸다**(`pairGrades`). 사용자 지시:
*"등급은 상대방과 비교했을때 기준으로하면됩니다. 모든 수치를 등급으로 만들어두고
그냥 불러오는게아닙니다"*. 이긴 쪽 **S**, 진 쪽은 뒤진 만큼
(A 90%↑ · B 80%↑ · C 65%↑ · D 45%↑ · E 45%↓, 97% 이상이면 둘 다 S).

**두 번 다른 방식으로 만들었다가 버렸다. 되돌리지 말 것:**
- **카테고리 전체 모집단** — 표에 200MP 플래그십이 하나 있다는 이유로 **50MP 가 E급**이
  됐다. 상담사가 보는 것은 그 두 모델뿐인데 화면에 없는 제품이 등급을 좌우하면 안 된다.
- **순위** — "몇 등인가"만 보고 "얼마나 차이 나는가"를 버려서 두 방향으로 다 틀렸다:
  Geekbench **842점과 3,023점이 이웃 등급**(3.6배 차이인데), 카메라 **50MP 와 48MP 가
  두 칸**(거의 같은데).

예외 셋 — 전부 이유가 있다:
- **에너지소비효율등급은 절대 등급 그대로**(1등급→S … 5등급→D). 상대 순위로 돌리면
  표에 1·2등급밖에 없을 때 **2등급이 E** 가 된다. 2등급은 E급이 아니다.
- **크기·용량·화소(`CHOICE_KEYS`)는 같은 급일 때만**(작은 값/큰 값 ≥ 0.90). 사용자 지시:
  *"크기는 고객의 선호도 문제입니다. 비교군에서는 등급을 매길순있습니다. 예를들면
  77인치 vs 75인치 일 경우는 등급을 매겨도됩니다"*. 97인치가 표에 있다는 이유로
  65인치를 E급이라 하면 안 된다.
- **이름(칩셋·패널 방식)은 등급을 매기지 않는다** — 잴 수치가 없다. 우열을 말하려면
  Geekbench 같은 **수치 항목을 따로 넣어야 한다.**

**범례를 반드시 함께 띄운다**(`.grade-legend`). 기준을 밝히지 않으면 "E급"이 근거 없는
말로 읽힌다. 모집단은 **이 표에 담긴 같은 카테고리 모델 전체**다.
`test-compare [17]` 이 네 규칙을 검사한다.

### Geekbench 는 공식 차트에서만 (2026-08-15)

사용자 요청: *"휴대폰이나 노트북은 긱벤치 자료같이 공식력있는자료를 활용해주세요"*.
출처는 **Primate Labs 공식 차트**뿐이다 — 기술 블로그의 수치는 쓰지 않는다
(실제로 블로그가 Exynos 2600 을 3,105 로 적었는데 공식 차트 기준과 맞지 않는다).

| | 주소 | 단위 |
|---|---|---|
| 폰 | `browser.geekbench.com/ios-benchmarks` · `/android-benchmarks` | **기기별 중앙값** |
| 노트북 | `browser.geekbench.com/processor-benchmarks` | **CPU별 중앙값** |

- **잦은 요청은 403 이 난다.** 한 번에 한 지면씩, 사이를 두고 받을 것.
- **차트에 없는 것은 비워 둔다.** 갤럭시 S26 시리즈·Exynos 2600(S25 까지만 있다) ·
  iPhone 17e · 퀀텀6 · Ultra X7 358H · Ultra5 125H · Ultra7 155H.
- **이름이 비슷한 다른 칩을 집지 말 것** — `Ultra 9 275H` 를 찾으면 차트의
  **`275HX`(24코어)** 가 걸린다. 다른 칩이다.
- **다른 기기 점수를 우리 모델에 옮기지 말 것** — vivo iQOO 15 의 8 Elite Gen5 점수가
  있지만 S26 Ultra 의 값이 아니다(같은 칩이라도 기기마다 다르다).

### 잴 수 없는 값 — `미공개`·`비대상` (2026-08-15)

위의 `없음`과 **뜻이 다른 말이 둘 더 있다.** 셋을 섞어 쓰면 화면이 거짓말을 한다:

| 표기 | 뜻 | 어디에 |
|---|---|---|
| `없음` | 제품에 그 기능이 **실제로 없다** | **문자열 항목에만** 값으로(위 절) |
| `미공개` | **제조사가 값을 밝히지 않았다** — 애플은 아이폰 RAM 을 사양 지면에 아예 안 적는다 | `UNKNOWN_SPEC` |
| `비대상` | **그 제도의 적용 대상이 아니다** — 에너지소비효율 '비대상' 공기청정기. 등급이 나쁜 것이 아니라 매기지 않는 것이라 숫자로 적으면 거짓이 된다 | `UNKNOWN_SPEC` |

뒤 둘은 `UNKNOWN_SPEC` 이 알아보고 **배지도 단위도 붙이지 않고 '참고'로** 둔다. 안 그러면:
- 우열 판정이 숫자에서만 도는 탓에 `12` vs `미공개` 에 **"🟡 동급"** 이 붙는다
- 단위가 무조건 붙어 **"미공개GB"** 가 화면에 나간다

**단위 규칙은 "문자열이면 안 붙인다"가 아니다.** 그렇게 잡으면 치수(`"912 × 1,853 × 916"` +
`mm`) 67종이 깨진다 — 전수로 세어 확인했다. 가르는 것은 자료형이 아니라 **값에 숫자가
있는가**이고 `unitOf()` 가 그것을 본다. 화면 표와 프롬프트 생성기 두 곳이 같은 규칙을
타야 복사한 글과 화면이 어긋나지 않는다.

- **`UNKNOWN_SPEC` 은 `var` 다.** 고전 스크립트 최상위의 `const`·`let` 은 window 에 붙지
  않아 테스트가 읽어 갈 수 없다. `test-compare [13]` 은 이 목록을 **앱에서 읽어 온다** —
  따로 적었더니 '비대상'을 넣자마자 어긋나 검사가 그 항목을 안 보고 통과했다.
  (jsdom 안의 정규식은 다른 realm 이라 `instanceof RegExp` 로 확인하면 안 된다.)

### 같은 사실을 두 번 적는 곳 — `on` 과 `specs` (2026-08-15)

화면에 같은 값이 두 번 나온다: 위쪽 **비교표**(`specs`)와 아래쪽 **기능 목록**(`on`).
로봇청소기 3종이 `on` 에 *"6,000Pa 흡입력"* 을 적어 놓고 `specs.pa` 는 비어 있었다 —
비교표는 양쪽에 값이 있어야 행을 그리므로 **같은 사실이 목록에는 보이는데 비교 행만
통째로 사라졌다.** 상담사에게는 *"왜 이건 비교가 안 되지"* 가 된다.
`test-compare [14]` 가 `on` 의 Pa 표기를 비교표와 대조한다(빠뜨림·불일치 양쪽).

### 전기요금 환산 — **감춰 둔 상태다** (2026-08-15 사용자 결정)

*"전기요금은 매번 케이스가 다를수있음으로 전기요금작업은 하되 일단 노출하지않고
가지고만있는걸로 합니다"*. `#elecCost` 에 `hidden` 하나만 붙어 있다 — 열려면 그것만
떼면 된다. **지우지 말 것**, 한전 요금표를 확인해 넣은 자료다.
제품 상세검색의 AI 추천 버튼과 같은 방식이고, `test-compare [15]` 가 감춰져 있는지
검사하면서 **엔진은 계속 검사**해 코드가 썩지 않게 한다.

**2026-08-16 사용자 재확인: 계속 감춰 둔다** — *"AI추천 버튼과 전기요금은 당분간
숨겨둡니다"*. 그날 비어 있던 값(기후환경요금·부가세·기반기금)을 **다 채운 뒤에 내린
결정**이므로 **"이제 값이 다 있으니 열자"고 다시 제안하지 말 것.** 여는 것은 사용자가
말할 때만 한다. 남은 미반영 둘(연료비조정단가 분기 확인·슈퍼유저요금)은 위에 적어 뒀다.

사용자 판단이 맞았다는 것이 계산에서 그대로 나온다 — 냉장고 **같은 17kWh 차이**가
가정의 총사용량에 따라 **연 30,753원 ~ 73,739원(2.4배)** 으로 갈린다. 누진제라
한계 단가가 구간마다 다르기 때문이다. 그래서 화면은 금액 하나를 못 박지 않고
세 가지 사용량 가정을 나란히 보여주게 만들어 뒀다.

- **요금표 출처는 `online.kepco.co.kr/PRM005D00`**(주택용 저압, 2024-01-01 시행,
  3단계 누진). **`home.kepco.co.kr` 것을 쓰지 말 것** — 폐지된 6단계 누진
  (59.1~690.8원)이 아직 떠 있고 실제로 그것을 먼저 읽었다.
- **연료비조정단가 +5.0원/kWh**('26년 3분기 한전 공지). **분기마다 바뀌므로 열기 전에
  다시 볼 것.** 웹검색 요약은 −3원이라고 했다 — 공지와 달라 공지를 따랐다.
- **기후환경요금 단가 9.0원/kWh** (2026-08-16 확보). RPS 7.7 + ETS 1.1 + 재정지원 0.2,
  *"'23. 1월부터 적용"*. 출처는 **요금표 지면이 아니라 제도 설명 지면**이다 —
  `cyber.kepco.co.kr/ckepco/front/jsp/CY/H/C/CYHCHP00211.jsp`. 요금표에는 부과 방식만 있다.
  **웹검색 요약은 5.3원이라고 했다** — 원문과 달라 원문을 따랐다(연료비조정단가 때와 똑같다.
  이 절에서 검색 요약이 틀린 것이 이번이 두 번째다).
- **부가가치세 10% · 전력산업기반기금 2.7%** 도 넣었다. 출처는 한전 **한글 전기요금표**
  `online.kepco.co.kr/PRM004D00` 원문 — *"청구금액 = 전기요금 + 부가가치세 + 전력산업기반기금"* ·
  *"부가가치세(원미만 4사 5입) : 전기요금 X 10%"* · *"전력사업기반기금(10원미만 절사) :
  전기요금 X 2.7%"*. **기반기금은 2.7% 다** — 흔히 3.7% 로 알려져 있어 그대로 넣을 뻔했다.
  원단위 처리(반올림·절사)도 원문대로 구현했고, **한계 단가에도 세금을 얹는다**(고객이
  실제로 더 내는 돈은 청구금액 기준이다).
- **아직 안 넣은 것 — 슈퍼유저요금.** 요금표 지면에 *"하계(7~8월) 1,000kWh 초과 전력량요금은
  736.2원/kWh"* 가 있는데 우리 표는 3단계에서 끝난다. 상담에서 쓰는 가정(200·350·500kWh)이
  전부 그 아래라 지금은 문제가 없지만, 사용량 가정을 올리면 그때 넣어야 한다.
- **연료비조정단가는 분기마다 다시 볼 것.** 이번에는 현재 분기 공지 원문을 찾지 못해
  기존 +5.0 을 그대로 뒀다(요금표 지면이 밝히는 **상하한이 ±5원/kWh** 라 상한값이다).

### 타사 사양을 어디서 받는가 (2026-08-15 확정)

**제품 지면 DOM 을 긁지 말 것.** 사양표가 지연 로드거나 아예 이미지다 — LG 12종을
그렇게 헛돌았다(잡히는 3행이 예약 위젯이었다). 네트워크를 관찰해 원본 API 를 찾는 것이
빠르고 결정적이다.

| 브랜드 | 경로 |
|---|---|
| **LG** | ① `apiv2.lge.co.kr/searchsvc/ajax/v1/search/product?keyword={코드}` → `modelId`(MD…)·`modelUrlPath`·`productAttrKeywds` ② `apiv2.lge.co.kr/pdpsvc/ajax/v1/models/{modelId}/spec` → 44~68행. Origin 을 보므로 **브라우저 안에서 fetch** 해야 한다 |
| **드리미** | 한국 공식 지면(`kr.dreametech.com/products?no=`)이 **네이버 브랜드스토어로 넘어간다**. `brand.naver.com/n/v2/channels/{ch}/products/{id}?withWindow=false` 의 `attributeName`/`attributeRealValue`. 다만 **판매자 입력값**이라 제조사 지면과 대조할 것 |
| **에코백스** | API 없음. **모델 전용 지면 본문**에 있다. 주소를 추측하면 전부 404 라 목록 지면에서 실주소를 얻는다 |
| **로보락** | Shopify 마케팅 지면뿐, 사양 API 없다 |
| **삼성** | 지원 페이지 `samsung.com/sec/support/model/{전체코드}/`. 접미 없는 코드는 500 |

도구: `.scratch/lg-spec-api.mjs` · `rc-hunt.mjs` · `rc-verify.mjs`.
**`.scratch/` 는 커밋되지 않으므로**(`npm run backup` 이 챙긴다) 경로는 여기 적어 둔다.

**마케팅 지면의 숫자를 그대로 집으면 안 된다 — 실제로 셋을 걸렀다:**
- 드리미 지면의 *"35,000Pa 수준의 미친 흡입력"* 은 **고객 후기** 문장이었다
- 로보락 홈페이지의 36,000Pa 는 **S10 MaxV** 것이다
- S8 MaxV 지면의 8,000Pa 는 **다른 모델과 비교하는 블록**의 값이다

조사 스크립트가 값이 아니라 **앞뒤 문맥 70자를 함께 찍게** 해 둘 것. 그래야 눈으로 걸러진다.
시리즈 지면은 특히 위험하다 — `kr.roborock.com` 의 'S8 Pro 시리즈' 10,000Pa 는
**S8 Pro/Pro+ 값이고 S8 Pro Ultra 는 그 지면에 없다**(한국 라인업에서 빠졌다).

**넣지 못한 것과 그 이유** — 다시 파지 말 것:
- LG 에어컨 `FQ25/22/18GN9BE1` **소음** — 사양표에 소음 항목 자체가 없다(라벨 전수 확인)
- LG 정수기 `WD722RK`·`WD524VC` **필터 단계** — 필터 이름 둘만 있고 '단계' 수가 없다.
  이름을 세어 2단계로 적으면 삼성 4단계와 견주는 셈이라 현장에서 반박당한다
- LG 청소기 **배터리** — 삼성 40분이 어느 조건인지 카탈로그에 없다. LG 는 조건별로
  30/20/13/10/6분을 따로 적어 기준을 못 맞추면 아무 숫자나 고르는 셈이 된다.
  **`dust` 는 먼지통이 아니라 '필터 µm' 이다** — 먼지봉투 2.5L 를 넣을 뻔했다
- **다이슨 흡입력** — AW(air watts)라 삼성 W 와 단위가 다르다
- LG `AS305DWWA` **UV** — 사양표에 줄이 없다. LG 는 없는 기능을 'X' 로 적는데(펫 모드=X)
  UV 는 줄 자체가 없어 "확인 못 함"이지 "없음"이 아니다

**에너지등급 충돌은 해소됐다 — 애초에 충돌이 아니었다** (2026-08-17).
`AF90H19D27SRS` 가 **파인더 1등급 vs dp 카탈로그 19평 2등급**으로 어긋나 오래 비워
두었는데, 에너지공단 신고 DB 에서 `AF90H19` 를 **전수(6건)로 보니 둘 다 있다.**

| 실내기 | 실외기 | 정격 | 월 kWh | 등급 |
|---|---|---|---|---|
| `AF90H19D27SN` · `AF90H19D38WN` | **D01BX** | 7,700W | 210.5 | **1등급** |
| `D35EN` · `D35WN` · `D24GN` · `D24SN` | **D0PBX** | 7,700W | 223 | 2등급 |

- **가르는 것은 평형이 아니라 실외기다.** 같은 19평·같은 냉방능력인데 실외기 조합으로
  갈린다(17평도 똑같다 — `D01BX` 1등급 · `D0QBX` 2등급).
- 우리 모델은 `D27S` 계열이라 **1등급**이고 카탈로그가 말한 2등급은 다른 조합이다.
  **두 출처가 서로 틀린 것이 아니라 다른 물건을 가리키고 있었다.**
- **"충돌한다"로 판정하기 전에 그 계열 전수를 볼 것.** 한 건씩 찾으면 어느 쪽이 맞는지
  영영 못 가른다.

옛 기록 — 그때 두드려 보고 실패한 곳들(2026-08-16):
- **samsung.com D2C 지면** — *"전 모델 에너지 소비효율 1, 2등급"* 이라는 **라인업 문구**만
  있다. SKU 하나를 확정해 주지 않는다.
- **samsung.com 지원 페이지** — `AF90H19D27SRS` 는 **HTTP 500**(접미 문제로 보인다).
- **한국에너지공단 효율관리제도(`eep.energy.or.kr`) — 2026-08-17 검색 DB 를 찾았다.**
  상단 메뉴의 '제품검색'은 `certi_intro_01.aspx`(안내 지면)로 되돌아온다. **본문 가운데
  바로가기의 '제품검색'이 `certi_intro.aspx`** 이고 거기서 품목을 누르면 실제 DB 가 열린다.
  - 품목마다 주소가 다르다 — **TV `certi_list_143.aspx`**(21,332건) ·
    **전기냉방기 `certi_list_260.aspx`**(5,703건). 표에 **효율등급 · 화면대각선 ·
    소비전력 · 신고업체 · 완료일**이 있다.
  - **직접 주소로 들어가면 WAF 가 막는 지면이 있다**(`/more/api_info.aspx` →
    *"사이트를 공격할 수 있는 문자열"*). 그때는 사이트 안에서 눌러 들어간다.
  - **모델명 검색은 돋보기 버튼을 눌러야 돈다.** 입력만 하고 Enter 를 쳐도 옛 결과가
    그대로 남아 **찾은 줄 알고 엉뚱한 값을 읽게 된다.** 건수(`총 N 건`)가 바뀌었는지 볼 것.
  - **전기냉방기 지면은 검색이 응답하지 않았다**(2026-08-17). TV 지면은 정상이다.
    그래서 `AF90H19D27SRS` 충돌은 **아직 미결**이다 — 다음에 이 지면부터 다시 볼 것.
그래서 타사비교의 그 칸은 **계속 비워 둔다** — 충돌하는 두 값 중 하나를 고르는 것은
근거 없이 정하는 일이다. (부수 확인: 파인더는 그 모델을 **19+6평 2in1** 로 적는데
samsung.com 의 `SRT` 지면이 62.6/18.7㎡ 라 그 표기 자체는 맞다. 19평 단품과 2in1 은
서로 다른 물건이라 등급이 달라도 모순이 아닐 수 있다.)

**TV 등급은 이 DB 에서 채웠다** (2026-08-17). 신고가 의무라 **국내 판매 여부까지 함께 드러난다**:

| 모델 | 등급 | 화면대각선 | 신고 |
|---|---|---|---|
| TCL `85C855` | **5등급** | 215cm · 199.3W | 143240579 · 2024-03-19 |
| 하이센스 `85U8Q` | **4등급** | 215cm · 161.6W | 143250485 · 2025-06-11 |

- **`U8QG`·`U7QG`(G 붙은 코드)는 신고 0건이다.** 등록된 것은 `65U8Q`(4등급 164cm) ·
  `75U7Q`(3등급 189cm)이고 신고업체는 **쿠팡**이다. TV 는 국내 판매 시 신고가 의무이므로
  **G 는 국내에 없는 글로벌 코드일 가능성이 크다** — 우리 비교표가 그 코드로 적고 있어
  **제품 식별 자체를 다시 봐야 한다**(값을 못 채운 것과는 다른 문제다).
- **`U8Q` 등급을 `U8QG` 칸에 옮겨 적지 않았다.** 식기세척기 `U01`/`USW` 가 색상 변형이라
  둘 다 실존했던 것과 같은 함정이다 — 뒷자리가 다르면 다른 제품일 수 있다.

### "잠정 준용"은 근거 없는 값의 신호어다 (2026-08-16)

하루에 **같은 사고를 두 번** 잡았고, 둘 다 소스 주석이 스스로 밝히고 있었다:

| 곳 | 주석이 적어 둔 말 | 실제 |
|---|---|---|
| 세탁기·건조기 `noise` | *"표시 안 된 noise는 콤보 확인값(51.7dB)을 **잠정 준용** — 재검증 필요"* | 13칸으로 번짐(LG 6칸 포함). LG 는 사양표에 소음 항목 자체가 없다 |
| 로봇청소기 `noise` | *"noise는 신규 모델 다수 확인 못해 인접 티어 기존값을 **잠정 준용** — 재검증 필요"* | 브랜드마다 값 하나씩(삼성 55·LG 58·에코백스 60·드리미 58). **삼성 값은 아예 틀렸다** |

- **삼성 로봇청소기 실제 소음은 63dB 다** — 지원 페이지 원문 *"(물걸레+먼지흡입 마루)
  저소음 63dB / (자동먼지비움) 87dB"*, 4종 동일. 우리는 **55** 로 적고 LG 58 보다
  조용하다고 **등급까지** 붙이고 있었다 — 근거가 없는 정도가 아니라 **우리에게 유리한
  방향으로** 틀렸다. 이런 값이 가장 위험하다.
- 타사 로봇청소기는 전부 지웠다 — LG 는 사양 API 라벨 전수(52·73·61행)에 **소음 항목이
  없고**, 로보락·에코백스·드리미는 한국 제품 지면 전수(3,761~21,717자)에 **dB 0건**이다.
- **찾는 법** — `.scratch/cmp-echo.mjs` 가 **브랜드를 가로질러 반복되는 수치**를 찍는다.
  다만 **후보 29건 중 진짜는 2건뿐**이었다(소음·배터리). 나머지는 정상이다:
  화면 크기·저장공간·주사율(120Hz)·세탁 용량처럼 **업계 표준이거나 모델명에 이미 적힌 값**.
  자동으로 고치지 말고 원문으로 판단할 것.
- **진짜를 가르는 신호는 "반복"이 아니라 셋이다:**
  ① 값이 **브랜드마다 하나씩** 균일하다(모델마다 다를 값인데 시리즈를 가로질러 같다)
  ② **원문에 그 항목이 아예 없다**(LG 는 소음·배터리를 안 적는다)
  ③ 소스 주석에 **"잠정 준용"·"재검증 필요"** 가 적혀 있다
- **TV 밝기는 재 보고 정상으로 판단했다**(2026-08-16). 반복이 **브랜드 안의 같은 시리즈**에서
  일어나고(삼성 QNH80 4종 1500 · QNH70 3종 1200 · 하이센스 U8 2종 3000), 밝기는 시리즈
  단위로 발표하는 값이다. 브랜드를 넘는 일치(1500·1200)는 밝기 수치가 원래 반올림 값에
  몰려서다. **다시 파지 말 것** — 위 신호 ①②③ 중 어느 것도 해당하지 않는다.
- `test-compare [18]` 이 세탁기·건조기·로봇청소기 세 카테고리에서 **근거가 등재된
  모델만 소음을 가진다**를 지킨다(되돌려 넣어 무는 것을 확인했다).

**2026-08-16 에 추가로 두드려 보고 못 넣은 것** — 같은 곳을 또 파지 말 것:
- **LG 세탁기·건조기 `소음`** — 에어컨과 똑같다. **사양표에 소음 항목 자체가 없다**
  (`FC2521SX6C` 44행 · `FX24ENER` 63행 · `FH25ESEFX` 66행 라벨 전수 확인).
  삼성에서 확인한 51.7dB 을 여기에 옮겨 적었던 것을 지웠다(아래 '소음 값' 절).
- **삼성 `AF90H17D38EN` 소음** — 지원 페이지 357줄에 '저소음 모드'라는 **기능만** 있고
  수치가 없다. 다른 에어컨 5종의 값은 dp 카탈로그에서 온 것이다.
- **로봇청소기 타사 `충전시간` 6칸 전부 미공개** — 로보락 S8 Pro/MaxV Ultra ·
  드리미 X60 Ultra/Master 지면에서 잡히는 "충전" 문장은 **전부 고객 후기·문의**다
  (*"충전속도가 1시간에 10% 정도밖에"*). 에코백스 T90 Pro Omni 는 공식 지면 12,532자
  전수에 표기가 없고, **T30S 프로는 현행 KR 목록에서 빠졌다**(제품 링크 9개 중 없음).
  `.scratch/rc-hunt.mjs`(문맥 70자를 함께 찍는다) · `.scratch/ecovacs-spec.mjs`.
- **LG `FQ16DCBSE2U`·`FQ18HDBBE2S`·`WTK14VG2BGA`** — LG 검색 API 에 **그 코드가 없다**
  (단종이거나 코드 변형). 사양을 받을 출발점이 없다.
- **LG 홈봇 RONi `pa`** — 재확인. 사양표가 **`최대흡입력(W) = 30`** 만 적는다(Pa 없음).
- **LG `FH25ESEFX`(워시콤보+미니워시) `size`** — **계산해서 채우지 말 것.** 사양 API 는
  본체 700×990×830 과 미니워시 700×360×770 을 따로 준다. 더하면 높이 1,350 인데,
  같은 구성인 `FC2521SX6CX` 의 카탈로그값이 **1,360** 이다 — **적층 높이는 단순 합이
  아니다**(결합부 여유가 있다). 10mm 틀린 값이 설치 상담에 나가느니 비워 두는 것이 낫다.
  구세대라 2026 카탈로그에도 없다. 이 칸 때문에 건조기 카테고리가 평균 3.8행으로
  가장 얇지만, 그렇다고 지어내지 않는다.

**채운 것** — LG `R585WKA1` 충전시간 300분(사양 API `최소충전시간`. 같은 표의
`최대사용시간(약) 140분` 이 DB 의 `bat:140` 과 일치해 모델 대응이 검산된다) ·
삼성 `RWP54421BF*M` 음성 제어 Bixby(D2C 지면 원문. **지원 페이지에는 음성 라벨이 없다** —
품목마다 어느 지면에 있는지가 다르니 한쪽만 보고 "없다"고 하지 말 것).

### 노트북은 CPU·RAM·저장공간을 단일값으로 적을 수 없다 (2026-08-15)

**기본 모델코드 하나가 17~26개 SKU 를 아우른다**(`NT960XJG` 21개 · `NT760VJG` 26개).
CPU·RAM·저장공간이 정확히 SKU 마다 갈리는 값이라 하나를 골라 적으면 21개 중 20개에게
거짓말이 된다. 이 DB 가 배터리(78.64Wh)·무게(1.89kg)·화면(16인치)처럼 **모든 SKU 가
공유하는 사양만** 담고 있는 것도 같은 이유다. 스마트폰은 이름에 `(256GB)` 가 박힌
단일 구성이라 넣을 수 있었다. LG 5종 값은 수집해 뒀다(`.scratch/nb-lg.mjs`, 무게가
DB 값과 일치해 SKU 대응이 검산된다).

### 비교표 렌더링에서 주의할 것

- `updateCompetitors()`의 등급 자동매칭은 `specItems[0]`을 기준으로 삼았는데, 전 카테고리에 가격 행을
  추가하면서 `specItems[0]`이 `'price'`가 됐다. 가격은 `specs`가 아니라 모델 최상위에 있어
  **이 블록이 통째로 죽어 있었다**(늘 0번끼리만 비교됨). 지금은 가격을 제외한 첫 항목을 기준으로 쓰고
  `specValue()`로 읽으며, 등급이 같으면 **가격이 확인된 모델**을 기본 선택한다.
- 비교표는 **양쪽 모두 값이 있어야** 그 행을 렌더한다. 한쪽에만 스펙을 넣으면 그 행은 조용히 사라지므로,
  새 항목을 추가할 때는 삼성 쪽 대응 값이 있는지 먼저 확인할 것.

## 가전 배치 시뮬레이터 (`/place`, `public/place-app.html`)

구매할 가전을 고르면 도면에 맞춰 배치를 추천하고 이격거리·간섭·방 이탈을 판정한다.
상담 중에 "이 자리에 들어갑니까"에 답하는 것이 목적이다.

### 절대 원칙 — 근거 없는 숫자를 화면에 띄우지 않는다

- **벽은 자동으로 인식하되, 도면 선을 추측해 그리지는 않는다.** 방 안쪽을 flood fill로 채우고
  그 경계를 벽 안쪽 면으로 삼는다(`detectRoomAt`). "어떤 선이 벽인가"를 판단하지 않으므로
  벽 위치를 몇 cm 틀리게 잡는 종류의 오차가 없다. 결과는 확정이 아니라 초안으로 보여주고
  사용자가 확인한다. 벽이 끊긴 도면에서는 바깥으로 새어 나가는 것을 감지해 거부한다(`reg.border`).
- **경계는 픽셀 격자를 한 칸씩 따라 추적한다**(`traceContour`). 예전에는 줄마다 좌·우 끝점만 읽어
  계단으로 압축했는데, 그러면 **안쪽으로 파인 부분을 직선으로 가로지른다** — ㄱ자·ㄷ자 방이나
  방 안으로 튀어나온 기둥·붙박이장 위를 벽이 지나가 실제와 다른 자리에 그려졌다. 격자 추적은
  진행 방향의 오른쪽이 항상 방 안쪽이 되도록 돌아 항상 닫힌 직교 다각형이 나온다.
  - 뒤이어 `simplifyRect`가 **300mm보다 작은 요철만** 흡수한다(치수 글씨·해칭). 계단을 지울 때는
    양옆 벽을 **길이로 가중평균한 자리**로 모아 긴 벽이 이기게 한다. 이보다 큰 굴곡은 실제
    벽감·기둥이므로 살린다 — 지우면 그 자리에 가전을 놓게 된다.
  - `mergeCollinear`는 **닫힌 고리로** 봐야 한다. 앞에서 뒤로만 훑으면 시작점과 끝점 사이에
    1px짜리 가짜 변이 남고, 그걸 요철로 오인해 지우다 다각형이 무너진다(실제로 무너졌다).
  - **벽의 정의는 "두께보다 긴, 직교하는 곧은 띠"다**(`keepBands`). 도면 인식 문헌이 공통으로
    쓰는 기준이다(Heras et al., *Unsupervised Wall Detector in Architectural Floor Plans* /
    Lv et al., CVPR 2021 — "parallel lines arranged in orthogonal directions, usually longer
    than thicker"). 표기법은 도면마다 다르지만 이 성질은 공통이라, **가로·세로 방향의 선형
    열기**로 뽑는다.
    - 길이 기준 **500mm**. 문 옆 벽 토막·600mm 기둥은 남고, 실명 글씨 한 덩이(400~460mm)·
      해칭 빗금·문 열림 호(굽어 있어 곧은 구간이 짧다)는 걸러진다. 이보다 올리면 작은
      기둥·벽감이 사라져 **그 자리에 가전을 놓게 된다.**
    - 두께 기준 **450mm**. 가로·세로 모두 그보다 두꺼운 덩어리는 아무리 길어도 벽이 아니다
      (변기·욕조·침대·600mm 깊이 싱크대). 아파트 내력벽은 150~250mm라 걸리지 않는다.
    - **단, 벽 그물망에 붙어 있는 두꺼운 것은 남긴다.** 기둥·벽감처럼 방을 실제로 둘러싸는
      구조물은 두꺼워도 벽이다(ㄷ자 방의 기둥 900×1,400mm). 떨어져 있는 것만 가구로 본다.
    - 대각선 벽은 이 기준에서 빠지므로, **이 연산으로 절반 넘게 사라지면 쓰지 않는다.**
  - **떨어져 있는 작은 조각은 지운다**(`dropSpecks`). 굵게 인쇄된 실명 글씨("거실"·"침실2")·
    범례·치수 숫자는 열기로도 안 지워져 그대로 벽이 되고, 방 한가운데에 남으면 닫기가 그것들을
    서로·벽과 이어 붙여 **방을 가로지르는 벽**을 만든다. 기준은 **길이 800mm** — 가로·세로
    어느 쪽도 그에 못 미치는 조각만 지운다. 픽셀 수 비율로 지웠더니 짧은 벽 토막까지 사라져
    방이 붙어 버렸다. 벽에 닿아 있는 가구(싱크대·붙박이장)는 큰 덩어리의 일부라 지우지 않는다 —
    실제로 그 자리를 차지하고 있어 가전을 놓을 수 없는 곳이 맞다.
  - 인식 해상도 상한은 `DETECT_MAX`. **벽 위치 오차가 그대로 배치 판정 오차**라 900 → 1,200으로
    올렸다(900에서는 원본 2,000px 도면의 5px 벽이 2.2px가 돼 열기 연산에 지워질 수 있었다).
    열기 반경은 `dim / DETECT_MAX`로 잡아 실제 반경 1px은 유지하고 해상도만 얻는다.
- **도면 범위는 "어두운 픽셀 전부"가 아니라 건물이다**(`structureBox`). 도면 바깥에는 치수선이
  둘러 있고 그 **끝 마커는 굵어서 열기에도 살아남는다.** 그걸 건물로 보면 범위가 통째로 커져
  **건물 밖으로 새어 나가도 감지하지 못한다** — 실제로 거실이 세대 전체 92㎡로 잡히고 인식
  영역이 치수선 구역까지 뻗어 나갔다. 벽은 전부 이어져 하나의 큰 덩어리이고 치수 마커·글씨는
  작은 조각이므로, 가장 큰 덩어리와 그 1할이 넘는 것만 남겨 범위로 쓴다.
- **건물 안쪽은 "정리하지 않은 원본"으로 잡는다**(`envelopeMask`). 창은 벽에 뚫린 구멍이 아니라
  벽 사이에 유리선을 가늘게 그은 것인데, 그 가는 선이 정리(열기)에서 지워져 구멍이 된다.
  원본에는 유리선이 남아 외벽 링이 이어지므로 도면 가장자리에서 배경을 채워 닿지 않는 곳이
  건물 안이 된다 — 사용자 표현 그대로 **"창문까지가 벽"** 이다. 방 채우기는 이 범위를 못 벗어난다.
  외벽이 실제로 뚫린 도면(방 하나만 그린 그림)에서는 건물 안이 거의 남지 않으므로 쓰지 않는다.
- **인식 전에 도면을 정리한다**(`cleanWalls`). 실제 분양 도면은 가구·글씨가 섞여 있고 **문 자리가
  뚫려 있어**, 그대로 채우면 방 → 복도 → 도면 바깥으로 새어 나가 인식이 실패한다(실제로 실패했다).
  형태학 연산 두 단계로 처리한다 — **열기**(얇은 선 제거)와 **닫기**(문 자리 메우기).
  닫기는 팽창 후 침식이라 **벽 두께와 방 크기가 보존된다**. 한쪽만 하면 방이 통째로 작아진다.
  - **열기 반경은 화면 픽셀 기준**이다. 도면의 벽은 실제 두께가 아니라 선 굵기로 그려지므로
    mm로 잡으면 안 된다 — 60mm(8px)로 잡았다가 벽(7px)이 통째로 지워져 인식이 실패했다.
  - **이진화 임계는 고정하지 않는다.** 컬러 렌더링 도면의 어두운 바닥이 통째로 벽이 되어
    "누른 자리가 벽 위입니다"가 뜬다. 어두운 것의 비율이 도면답지 않으면(4할 초과 / 0.4% 미만)
    임계를 옮긴다. 도면에서 선이 차지하는 넓이는 아무리 빽빽해도 4할을 넘지 않는다.
  - **벽을 칠하지 않고 윤곽선만 그린 도면이 있다.** 그런 도면에서 열기는 벽을 통째로 지워
    인식이 전부 실패한다. 열기로 그림의 **절반 넘게 사라지면** 선으로 그린 도면이라 보고
    열기를 건너뛴다. 가구·글씨·문 열림 호가 남아 복도가 끊길 수 있지만, 벽이 없는 것보다 낫다.
  - **닫기 반경은 하나로 정하지 않고 0부터 단계적으로 올린다**(`closeSteps`, 최대 1,800mm).
    500mm 하나로 고정했더니 창문 표시와 가까운 선들이 뭉쳐 검은 덩어리가 되고 문이 통째로
    벽이 됐다. 어느 단계를 쓸지는 아래 세 규칙으로 고른다.
  - **메워서 만든 구간은 벽이 아니라 개구부(문·창)로 표시한다**(`classifyEdges`). 창문이 "사라졌다"가
    아니라 "여기가 창문이다"로 보여야 한다. 개구부는 주황 점선으로 그리고, 그 앞을 막는 가전에는
    "문·창 앞을 막습니다" 주의를 붙인다(하드 판정 아님).
    - 판정 근거는 "바깥에 벽이 안 보인다"가 아니라 **"닫기가 여기를 메웠다"** 다. 경계에서
      바깥으로 7px만 훑던 예전 방식은 ①경계선이 몇 px만 밀려도 뒤집히고 ②복도 건너편 벽이
      가까우면 문이 벽이 되고 ③해칭이 성긴 벽은 개구부가 됐다.
    - **닫기는 실제 벽 옆에도 1~2px짜리 얇은 테두리를 남긴다.** 그 한 겹만 보고 판정하면
      멀쩡한 벽이 통째로 개구부가 된다(ㄱ자 도면에서 벽 1,380mm가 문으로 떴다). 그래서
      **메운 자리를 지나 빈 곳이 나올 때까지** 나가 본다 — 도중에 그려진 벽에 닿으면 벽,
      메운 자리를 뚫고 빈 곳이 나오면 개구부. 메운 두께가 300mm를 넘으면 벽으로 안 본다.
    - 길이 기준(개구부 최소 500mm·탐침 400mm·메움 300mm)은 **축척을 알면 mm로 잡는다.**
      도면 크기에 따라 기준이 달라지면 같은 문이 도면마다 다르게 판정된다.
    - **그런데 자동 인식 시점에는 축척이 없다 — 폴백이 사실상 늘 쓰인다.** 예전 폴백은
      도면 긴변의 1.2%였는데, 축척이 실린 도면 15장으로 재 보니 **실제 500mm의
      1.6~3.7배(중앙값 2.1배) 작았다.** 그래서 짧은 벽 토막까지 개구부로 판정됐고
      화면에서는 *"어디는 벽으로 인식하고 어디는 안 된다"* 로 보였다(2026-08-10 사용자 보고).
      지금은 **1mm ≈ 긴변의 0.0054%** 로 환산한다(실측 보정). 도면마다 실제 폭이 11~27m라
      어림이지만 **틀리는 방향이 안전하다** — 기준이 커지면 개구부로 덜 보고 벽으로 더 본다.
    - **부분 누락은 `개구부 길이비율`로 잰다.** 벽이 마스크에서 사라지면 닫기가 그 자리를
      메우고 그 구간이 개구부로 보고되므로, 이 비율이 곧 누락 지표다. 문 900mm + 창 1,800mm면
      둘레 14m 기준 19% 안팎이 정상이다. `test-real.mjs`가 지킨다 — 폴백을 되돌리면
      이 검사가 먼저 깨진다(확인함, 아래 짝비교 항목).
    - 근거를 못 찾으면 **벽으로 둔다**. 없는 개구부를 만들어 내지 않는다.
- **색인의 "평면도"가 다 평면도는 아니다**(2026-08-12 실측). 617장을 기울기 방향
  히스토그램으로 재 보니 축(0°·90°)에 몰린 비율의 중앙값은 0.72 인데, 낮은 쪽 46장은
  **아이소메트릭 렌더링 · 인테리어 사진 · 품목표 · 제목 띠**였다. 눈으로 확인한 값 —
  0.05·0.10 입체 / 0.31 품목표 / 0.35 제목 띠 / **0.42 는 위가 입체 아래가 평면이라
  쓸 수 있는 도면**. 벽 인식 전체가 "벽은 가로·세로 띠"라는 전제 위에 서 있어 그런
  이미지에서는 글씨·범례가 벽이 되고 **얇은 띠 같은 방**이 나온다.
  - **단지 불러오기 목록에서 뺀다**(2026-08-12 사용자 요청). 색인이 도면마다 `axis` 를
    싣고 `openLibrary` 가 0.35 미만을 거른다 — **617 → 583장**.
    - **단, 그 단지의 도면이 전부 걸리면 하나도 빼지 않는다**(4곳: 트리우스 광명 ·
      두산위브더제니스 센트럴 용인 · 이천 중리지구 예미지 · 고덕 A-55 대광로제비앙).
      도면 한 장을 빼는 것은 *"이건 평면도가 아니다"* 이지만, 단지가 목록에서 사라지면
      ***"우리는 그 단지 도면이 없다"*** 는 다른 말이 되고 그건 사실이 아니다.
      그런 단지는 목록에 `벽 인식이 어려운 도면만 있습니다` 라고 적는다.
      실제로 두산위브더제니스는 큰 입체도 옆에 **쓸 수 있는 작은 평면도**가 함께 있었다.
    - 값은 세 곳에서 같은 계산이어야 한다 — `place-app.html` 의 `axisConcentration()` ·
      `build-plan-index.mjs`(새 수집분) · `scripts/patch-plan-axis.mjs`(기존 색인 따라잡기).
      셋 다 **배포본 이미지를 420px 로 줄여** 잰다. 원본을 재면 목록에서 숨긴 기준과
      열었을 때 뜨는 경고가 어긋난다.
  - **이미지를 거부하지 않는다.** 목록에 남은 것을 열었을 때도 `axisConcentration()` 이
    0.35 미만이거나 `keepBands` 가 스스로 포기하면(`state.baseInfo.tilted`) 화면이 그
    사실과 고칠 길을 밝힌다. 둘은 **서로 다른 실패**를 잡는다 — 기울기는 입체·글자 지면,
    띠 포기는 벽이 아예 없는 사진 몽타주. 좁은 화면에서는 하단 요약 띠에도 한 줄 나간다.
  - `test-plans.mjs` 가 10장(입체 5 · 평면 5)으로 지키며, **멀쩡한 도면에 경고가 붙는 것**을
    더 무거운 실패로 따로 센다.
  - 재려면 `.scratch/detect-isometric.mjs`(코퍼스 전수) · `.scratch/shot-3d-real.mjs`(3D 실물).
- **전체 인식은 같은 공간을 두 번 등록할 수 있다**(2026-08-12). 중복 걷어내기가
  "중심 20px 안 + 넓이 8% 안"만 봐서, 트인 거실·주방을 다른 닫기 반경으로 찌르면 다각형이
  달라져 둘 다 남았다 — 디에트르 84 에서 **거실 35.2㎡ 와 주방 30.3㎡ 가 87% 겹친 채**
  공간 7곳 합 104.7㎡(전용 84㎡ 의 125%)가 됐다. 지금은 넓이 겹침을 실제로 재서
  **겹침 70% 이상 + 넓이 0.6배 이상**이면 큰 쪽만 남긴다(6곳 74.3㎡ = 88%).
  - **넓이 비 조건을 빼지 말 것.** 겹침만 보고 지웠더니 합성 84타입에서 트인 거실(31㎡)
    안에 든 **진짜 주방(16.8㎡)이 지워졌다.** 같은 공간을 두 번 잡은 것은 넓이도 비슷하고
    (0.86배), 큰 공간 안의 작은 방은 훨씬 작다(0.54배).
  - **45% 규칙이 다 끝난 뒤에 한다.** 먼저 지우면 합계가 줄어 멀쩡한 트인 거실이 45% 를
    넘게 되고 그 규칙이 거실을 쪼개러 든다.
  - 정리본은 **기본으로 보여주지 않는다.** 도구막대 ◧ 를 누를 때만 원본 **위에 반투명하게
    겹쳐** 알고리즘이 무엇을 보고 있는지 확인하는 용도다. 예전에는 인식 단계에서 원본을
    정리본으로 통째로 갈아 끼웠는데, 그러면 도면의 가구·바닥·색이 사라져 **현장감이 무너진다**
    (2026-08-09 사용자 지적: *"벽 인식을 한다고 해서 굳이 기존 도면 색상을 없애고 벽만 남길
    필요는 없다 … 업데이트된 도면에 내가 구매하고 싶은 제품이 설치되면 어떤 느낌인지, 이게
    포인트인데 너무 벽에 치중된 것 같다"*). **원본을 대체하는 방식으로 되돌리지 말 것.**
- **닫기 반경 고르기 — "새지 않는 가장 작은 값"은 틀렸다.** 실제 세대 평면도는 도면 안에서
  완전히 닫혀 있고(창은 벽을 뚫지 않는다 — 벽은 이어져 있고 유리선만 가늘게 긋는다) 문이
  전부 열린 채 그려져 있다. 그래서 **반경 0에서 세대 전체가 방 하나로 잡혔다** — 어느 방을
  눌러도 90㎡가 나온다. 세 단계로 고른다:
  1. **경계가 도면 가장자리에 닿지 않는 후보를 먼저 쓴다.** 닿아 있으면 그건 방의 경계가
     아니라 도면이 잘린 자리다.
  2. **처음으로 크게(1.25배) 줄어든 지점**을 쓴다. 반경을 키우면 문이 막히며 옆방이 통째로
     떨어져 나가 면적이 확 준다. '마지막' 지점을 쓰면 문보다 넓은 개구부(주방 2m·현관 1.85m)
     까지 막혀 트인 공간이 잘린다.
     **절대 픽셀·mm 기준으로 상한을 두지 말 것** — 같은 900mm 문을 막는 데 필요한 반경이
     도면 해상도에 따라 0.5~0.9배로 달라져 저해상도 도면에서 어긋났다.
  3. 그렇게 고른 후보가 **문 폭(`MAX_SEAL_MM` 1,500mm)보다 넓은 자리를 막았으면** 한 단계
     물러선다. 막은 자리는 개구부로 보고되므로 결과로 판정할 수 있다.
- **자동 판정은 도면 관례를 전제로 한 추정이다.** 전제를 벗어나는 도면이 반드시 나오므로
  **"◂ 더 넓게 / 더 좁게 ▸"** 로 사용자가 닫기 단계를 직접 옮길 수 있게 했다.
  - 이 조절은 **모달이 아니라 도면 위 막대**(`#draftbar`, `showDraftBar`)에서 한다. 모달로
    물었더니 도면이 가려져 "무엇이 바뀌는지 모르겠다"는 지적을 받았다 — 바뀌는 것은 도면 위
    파란 영역인데 그게 안 보였다. 이름 붙이기(`nameRoomSheet`)만 확정할 때 모달로 묻는다.
  - 막대에는 **넓이(㎡) · 직전 값 → 넓어짐/좁아짐 · 범위 n/N**을 함께 띄운다. 숫자가 같이
    움직여야 사용자가 눈으로 검산할 수 있다.
- **마지막 안전망은 사람이다 — 경계를 끌어서 고칠 수 있다**(`hitBoundary`/`moveVertex`/
  `moveEdge`/`toggleEdgeOpen`, 도구막대 '경계 수정'). 도면 표기가 관례를 벗어나면 어떤 규칙으로도
  정확히 맞출 수 없다. 모서리를 끌면 그 점이 움직이고(이웃과 축이 120mm 이내면 맞춰 준다),
  벽을 끌면 **법선 방향으로만** 밀리며 양옆 벽이 따라 늘어난다. 벽을 그냥 누르면 벽 ↔ 개구부가
  바뀐다. 자동 인식을 고치려다 규칙을 복잡하게 만들기 전에 이 손잡이로 해결되는지 먼저 볼 것.
- **`state.mmPerPx` 는 "도면 이미지 1px 이 몇 mm 인가"다.** "월드 단위가 mm 인가"가 아니다.
  그 사실은 `state.scaled` 가 따로 들고 있다. 둘을 섞으면 화면이 무너진다:
  - `loadFromLibrary` 가 `mmPerPx = 1` 을 넣던 시절, **1,000px 도면이 가로 1m 로 그려져**
    좌상단 50px 썸네일이 되고 방 다각형만 크게 남았다(사용자 지적).
  - 저장된 방만 복원할 때는 **`mmPerPx = null`, `scaled = true`** 다. 도면 이미지가 없으니
    이미지 축척도 없는 것이 맞다.
  - 도면을 함께 띄우려면 **그 이미지의 mm/px 를 알아야 한다.** `saveToLibrary` 가 저장 시점의
    값을 함께 남기고(`entry.mmPerPx`), 없으면 색인의 자동 판독값을 쓴다.
    **둘 다 없으면 이미지를 띄우지 않는다** — 틀린 크기로 겹쳐 보이는 것이 안 보이는 것보다 나쁘다.
  - `draw()` 가 마지막 안전망이다: `mmPerPx` 를 알면 그것으로, **축척 미확정이면** 이미지 픽셀이
    곧 월드 단위. 그 둘 다 아니면(mm 월드 + 이미지 축척 모름) **아예 그리지 않는다.**
- **한 공간이 전용면적의 90% 이상이면 경고한다.** 문이 전부 열린 채 그려진 도면에서 닫기 반경이
  모자라면 세대 전체가 방 하나로 잡히는데, 그대로 이름 붙여 저장하면 나중에 "거실 51.9㎡"처럼
  보인다 — 52㎡ 세대의 거실이 52㎡일 수는 없다. `loadFromLibrary` 는 방을 1:1 로 복원할 뿐
  **합치지 않으므로**(왕복 테스트가 보증), 그런 항목은 **저장 시점에 이미 그렇게 잡혀 있던 것**이다.
- **축척(mm)은 반드시 사용자가 확정한 값에서 온다.** 자동 인식은 모양만 잡고, 실제 치수는
  치수선 두 점 보정(`askScale`) 또는 가장 긴 벽 길이 입력(`askWallLength`)에서 온다.
  **자동 인식이 치수까지 만들어 내게 하지 말 것** — 판정 근거가 사용자가 눈으로 확인한 값에 묶여 있어야 한다.
- **축척 확정 전에는 벽 길이에 mm를 붙이지 않는다**(`state.scaled`). 초기 구현에서 축척 없이
  `2,227mm` 같은 숫자가 표시됐는데, 상담 중에 그걸 실제 치수로 읽으면 사고다.
- **축척 없이는 가전을 아예 올리지 않는다**(`needScale`). 가전 치수는 언제나 mm 인데
  **축척 전 도면의 월드 단위는 이미지 픽셀**이라(`drawPlan` 이 그렇게 그린다) 둘을 같은
  화면에 올리면 크기 관계가 통째로 거짓이 된다. 실제로 냉장고가 도면 폭의 7배 자리에
  놓이면서 화면 맞춤이 줌아웃해 **도면이 854px → 118px 로 줄었다**(사용자가 잡아냈다).
  `stageOutside`·`autoPlace`·`startManual` 셋 다 막고, 시트를 열기 전에도 막는다 —
  체크를 다 한 뒤에 막으면 헛수고가 된다.
  - `planBox()`도 같은 이유로 고쳤다. 도면이 있는데 축척이 없으면 벽 검사로 떨어지고,
    벽마저 없으면 **6000×4000 폴백**을 쓰고 있었다. 이제 이미지 크기를 그대로 쓴다.
- **축척을 맞추는 주 경로는 "거실 한 변의 길이 입력"이다**(`askWallLength`). 벽 하나의 실제
  길이를 넣으면 나머지 벽과 도면 전체가 같은 비율로 환산된다.
  치수선 두 점 보정(`askScale`)이 더 정확하지만 **분양 도면 중 치수가 인쇄된 것은 8%뿐**이라
  그걸 먼저 권하면 92%에서 상담이 막힌다. `needScale` 시트의 기본 버튼이 거실 길이 쪽인 이유다.
  - **축척을 잴 방은 "가장 넓은 방"으로 시작하되 사용자가 넘겨 고를 수 있어야 한다.**
    예전에는 이미지 한가운데 한 점만 찔러 봤는데, 도면 가운데가 늘 거실은 아니라
    **주방이 잡히면 "가장 긴 벽"도 주방 벽이 되고 축척이 그 한 값에서 나오므로 도면 전체
    치수가 어긋난다**(사용자 지적). 지금은 4×4 격자로 찔러 중복을 걷어내고 넓은 순으로
    후보를 세운다(`state.roomCands`). 캐시 덕에 16회 탐침이 100ms 안쪽이다.
  - **축척 기준은 "안방의 가로 벽"이다**(2026-08-09 사용자 결정). 고객이 아는 치수는 거실보다
    안방인 경우가 많고, 안방 치수는 상담에서 가로로 이야기한다.
    - 방: 가장 넓은 방(거의 언제나 거실+주방)을 **빼고** 나머지에서 고른다. 그중에서도
      **네모난 방을 앞세운다**(가로:세로 < 2). 닫힌 주방은 길쭉해서 넓이만 보면 안방보다
      클 수 있는데, 그걸 안방으로 잡으면 축척이 통째로 틀어진다.
      시험 도면 실측 — 거실 292×512(비 1.75) · **안방 312×292(비 1.07)** · 침실 312×212.
    - 벽: **가로 벽면 전체**(`longestHRun`). 세로 벽을 재면 고객이 아는 숫자와 어긋난다.
      가로 벽면이 하나도 없는 도면에서만 전체에서 가장 긴 벽으로 물러선다.
      - **조각 하나를 재면 안 된다.** `classifyEdges` 가 한 벽면을 [벽토막 | 개구부 | 벽토막]
        으로 쪼개 놓아, "가장 긴 가로 **조각**"을 고르면 **창 하나를 재게 된다.**
        실제 75㎡ 도면에서 침실1 아래 창 구간만 잡혔고 거기에 3,600을 넣으면 축척이 통째로
        틀어진다(사용자 지적). 벽·개구부를 구분하지 말고 이어 붙일 것 — 창이 끼어 있어도
        그 벽면의 안목치수는 양 끝 사이다.
      - **반드시 닫힌 고리로 돌 것.** 앞에서 뒤로만 훑으면 배열 시작·끝에 걸친 런이 두 토막으로
        남는다 — `mergeCollinear` 를 순환으로 고쳤을 때와 같은 함정이다. 런이 끊기는 자리를
        먼저 찾아 거기서 한 바퀴 돈다.
      - 같은 y 허용오차는 이 시점에 축척이 없어 mm 로 못 잡는다. **방 짧은 변의 2%(최소 2px)** —
        도면에서 벽 선 굵기가 대개 3~9px 다.
      - 실측(도면 23장): 기준 벽이 방 가로를 덮는 비율 **중앙값 1.0**, 90% 미만이 4장.
        그 4장은 버그가 아니라 **기둥·벽감으로 벽면이 실제로 끊긴 방**이다(경계 38구간 등).
        거기를 이어 재면 벽이 아닌 허공을 재게 되므로 **끊긴 채로 두는 것이 맞다** —
        화면이 그 벽을 주황으로 보여주므로 사용자는 그 벽 길이를 넣으면 된다.
    - **조각을 축척 기준으로 삼지 않는다**(`roomLike`). 가장 넓은 방을 빼고 고르다 보니 후보가
      둘뿐인 도면에서 남은 하나가 창틀·틈새여도 집었다(조각도 네모나서 "네모난 방" 조건마저
      통과한다). 실측으로 **39장 중 8장(21%)** 이 이랬고 최악은 32×21px(도면의 0.11%)였다.
      하한: 넓이가 도면의 3% 이상 + 짧은 변이 도면 짧은 변의 8% 이상(실측 중앙값 9.2%).
      **가장 넓은 것마저 이를 못 넘으면 자동 확정하지 않고** 사용자에게 넘긴다 —
      그런 도면은 인식 자체가 약한 것이라 조각으로 축척을 물으면 안 된다. (8장 → 0장)
    - **어디까지나 추정이다.** 초안 막대의 **'다른 방 ▸ n/N'** 으로 넓은 순으로 넘겨 고칠 수 있다.
    - 자동이 그 방을 골랐으면 **이름 제안도 그 방 이름**이어야 한다(`nextRoomName`). 목록 순서대로
      '거실'을 내밀면 화면이 "거실 가로가 몇 mm 입니까?"라고 되묻는데 실제로는 다른 방을 가리킨다.
    - **화면에서 부르는 이름은 '침실1'이다.** 실제 수집 도면 120장을 OCR 로 세어 보니
      **'침실1' 58장(48%) vs '안방' 35장(29%)** 으로 침실1 표기가 1.66배 흔했다(2026-08-09 실측).
      화면 말이 도면 말과 같아야 상담이 매끄럽다. `ROOM_NAME_POOL` 순서도 이에 맞춰 뒀다.
      다시 세려면 `plan-index.json` 의 도면을 tesseract(kor, PSM 11)로 훑으면 된다.
  - **질문 문구는 상담사가 고객에게 그대로 읽을 수 있어야 한다** —
    *"안방 가로가 몇 mm 입니까?"* + *"주황색으로 표시된 벽입니다 — 넣으시면 도면 전체가
    그 비율로 맞춰집니다. 안방 가로는 보통 3,000~3,600mm."* 방 이름은 **사용자가 붙인 이름**을
    쓴다. '안방'으로 못 박으면 다른 방을 골랐을 때 화면이 거짓말을 한다.
  - **도면을 올리면 곧장 두 점 클릭 모드로 보내지 말 것.** 예전 `useImage` 가 `setMode('scale')`
    이었는데, 두 점 보정은 벽과 벽을 정밀하게 눌러야 하고 **손가락으로는 몇 px 씩 어긋나
    축척이 통째로 틀어진다**(사용자 지적). 지금은 `autoDetectCenter()` 로 방을 자동 인식해
    **가장 긴 벽을 찾아 두고 사용자는 길이만 입력**한다 — 정밀 클릭이 한 번도 없다.
  - **길이를 확정할 때 `state.mmPerPx` 를 같이 세워야 한다.** 벽 좌표만 mm 로 환산하고
    `mmPerPx` 를 null 로 두면 벽은 mm 축에, 도면 이미지는 "이미지 픽셀 = 월드 단위" 축에
    그려져 **같은 화면에 자가 둘**이 된다. 실제로 방이 도면보다 4.27배 크게 나와
    **도면 위에 제품을 올릴 수가 없었다**(사용자 지적: *"거실 공간만 별도로 크게 나오고
    이미지는 작게 줄어듭니다"*). 벽이 이미지 픽셀에서 왔으므로 환산 계수가 곧 mm/px 다.
    증상(도면이 작아짐)과 원인(단위 불일치)이 멀어 조용히 재발하므로 `test-plans` 가
    **길이 입력 후 벽이 `planBox()` 안에 들어가는지**를 검사한다.
  - **어느 벽을 재는지 도면 위에 표시한다**(`state.measureSeg`, 굵은 주황 + "이 벽의 길이").
    가장 긴 벽을 자동으로 고르므로 클릭은 필요 없지만, 어느 벽인지 모르면 엉뚱한 길이를
    넣게 되고 그러면 축척이 통째로 틀어진다. 화면에 보이면 눈으로 검산된다.
  - **길이도 모달이 아니라 도면 위 막대로 묻는다**(`#draftbar` 재사용). 정작 봐야 할 것이
    주황으로 표시된 그 벽인데 모달이 그 위를 덮었다 — 초안 조절을 막대로 옮긴 것과 같은
    이유다. 문구도 직설적으로 둔다: **"지금 이 거리는 몇 mm 입니까?"** ·
    **"입력하신 값에 맞춰 도면 비율을 수정하겠습니다"**. 이름 붙이기 모달이 열린 채로 올 수
    있으므로 막대를 띄우기 전에 `closeSheet()` 를 부른다(`sheet()` 를 안 쓰면 저절로 안 닫힌다).
- **입력칸에 글자를 치는 중에는 단축키를 받지 않는다.** 가전이 선택된 상태에서 길이를 고치다
  `Backspace` 를 누르면 그 가전이 지워졌다. `keydown` 핸들러가 `INPUT`/`TEXTAREA` 를 걸러 낸다.
- **자리를 못 찾으면 억지로 놓지 않고 "자리 없음"으로 보고한다.** 밀어 넣고 "들어갑니다"라고
  말하는 것이 이 도구에서 가장 위험한 실패다. `test-place.mjs`가 1×1m 방으로 회귀 검사한다.
- **겹친 채로 놓이는 상태를 만들지 않는다.** 드래그 중 `collisionAt()`으로 후보 위치를 검사해
  본체 겹침·이격 침범·방 이탈이면 이전 위치로 되돌린다. 설치할 수 없는 배치를 화면에 그려 두면
  상담 중에 그대로 읽힌다. 단 도어 오픈은 막지 않는다(힌지 방향을 몰라 양쪽에 그리므로).

### 화면은 "도면 위에 제품이 놓인 느낌"이 주인공이다

이 도구가 답하는 것은 **"이 집에 이 제품을 놓으면 어떤 느낌인가"** 이지 "벽이 어디인가"가 아니다.
벽 인식은 판정을 위한 수단이므로 화면에서 도면을 이겨서는 안 된다.

- **원본 도면은 손대지 않고 그대로, 불투명하게 그린다.** 벽·방·가전은 그 위에 얹는 표시다.
- **벽선은 가늘고 반투명(2px·45%)** 하게 겹친다. 도면에 이미 벽이 그려져 있다.
  단 **개구부(문·창)는 또렷하게** 남긴다 — 도면만 봐서는 알 수 없는 판정 결과이고
  그 앞을 막는 가전에 경고가 붙으므로 근거가 보여야 한다.
- **벽 길이(mm)는 기본으로 끈다**(`state.showDims`, 도구막대 ↔). 모든 벽에 숫자가 떠 있으면
  도면이 숫자에 덮인다. 필요할 때만 켠다.
- **가전은 진하게 그린다**(면 68% + 흰 테두리 + 흰 글자에 어두운 외곽선). 예전에는 13%
  투명도라 컬러 렌더링 도면 위에서 제품이 배경에 묻혔다 —
  *"제품 컬러는 더 진하게 해야 도면 컬러와 겹치지 않게 확인이 가능하다"*(사용자).
  흰 테두리가 있어야 어두운 도면 위에서도 윤곽이 끊기지 않는다.

### 기본 흐름 — 도면 밖에 꺼내 두고 끌어다 넣는다

사용자가 정의한 이 도구의 쓰임새: **"선택된 가전을 일단 도면 밖에 배치해 주고, 상담사가 직접
도면 안으로 끌고 들어와서 대략적인 설치 느낌을 보여주는 도구."**

- `stageOutside(picks)` — 고른 가전을 도면 **오른쪽 바깥**에 세로로 세운다(`staged:true`).
  `fitAll()`로 도면과 대기 가전이 모두 보이게 화면을 맞춘다 — 안 보이면 끌 수가 없다.
- **대기 중에는 아무 판정도 하지 않는다.** 아직 놓은 것이 아니므로 "방 밖" 경고를 띄우면
  진짜 문제가 경고 더미에 묻힌다. 화면에서도 회색 점선으로 그려 "놓은 것"과 구분한다.
- 끌어서 **본체 중심이 방 안에 들어오면** `staged`가 풀리고 그때부터 판정이 시작된다
  (`pointermove`에서 매번 다시 계산하므로 밖으로 다시 빼면 대기로 돌아간다).
- **벽을 넘으면 되돌리지 않고 안쪽으로 밀어 넣는다**(`nudgeInside`). 예전에는 직전 위치로
  되돌렸는데, 그러면 벽 근처에서 제품이 손에서 자꾸 빠져 "왜 안 움직이지"가 된다.
  벽에 설치할 수는 없으므로 **옮겨 주고 경고로 알리는** 것이 맞다(사용자가 정한 동작).
  방은 ㄱ자·ㄷ자도 되므로 사각형으로 자를 수 없어, 방 중심 쪽으로 한 걸음씩 옮겨 본다.
  다른 가전과 부딪히는 것은 여전히 막는다(`hitsOther`).

### TV 는 항상 벽걸이 · 사운드바는 TV 아래 중앙 (2026-08-17 사용자 요청)

*"TV는 항상 벽걸이높이로 배치"* · *"사운드바는 TV 하단에 살짝 공간 두고 센터정렬"* ·
*"85인치 기준 600mm — 소파에서 앉아서 시청했을 때의 적정 높이. 더 큰 TV 는 비율에 맞춰
자동 하향조정"*.

- **높이만 바꾸면 안 된다 — 부품 자체를 벽걸이로 골라야 한다**(`mainPart`).
  `size-reps` 는 TV 를 스탠드/벽걸이로 **따로** 들고 있고 55형 기준 `1255×1237×**483**` vs
  `1255×748×**221**` 로 **깊이가 2.2배**다. 스탠드 치수를 그대로 쓰면 3D 에 **받침 달린
  TV 가 벽에 떠 있고**, 평면에서 **바닥 자리를 두 배로 먹어** 그 앞에 다른 가전을 못 놓는다.
  - **고르는 곳이 넷이다** — 목록 정렬(`foot`) · 드롭다운 · 정보줄(`info`) · 담기(`collect`).
    예전에는 넷이 각각 `o.parts[0]` 을 집었다. 갈리면 **화면에 적힌 치수와 실제로 놓이는
    치수가 달라진다.** 한 함수를 지나가게 할 것.
  - **부속 목록을 `slice(1)` 로 만들지 말 것.** 주 부품이 항상 0번이 아니다 — TV 는
    벽걸이가 주 부품이라 그렇게 자르면 **벽걸이가 부속으로 적히고 스탠드가 사라진다.**
  - **TV 카테고리에는 프로젝터도 있다**(The Freestyle `폭 135mm` · The Premiere
    `폭 530~550mm`). 파트가 `본체` 하나뿐이라 폴백이 그것을 골라 **바닥에 놓는 것이 맞다.**
    "모든 TV 는 벽걸이"로 검사하면 여기서 깨진다.
- **높이 기준은 `EYE_MM = 1139`(화면 중심)다.** 사용자 기준 *85인치 하단 600mm* 에
  83~85형 벽걸이 실측 높이 **1,077mm** 를 더해 나온 값이다. 공식은 그대로
  `중심 − 높이/2` 이고, **그 공식이 곧 "클수록 하향조정"** 이다 —
  65형 701 · **83~85형 600** · 98형 514 · 115형 400mm.
  - 층고를 뚫지 않게 상한(`CEIL_MM - H`)을 함께 건다.
- **사운드바는 중심·각도·방을 TV 에 맞춘다**(`alignUnderTV`). 예전에는 **높이만** 따라가고
  평면 좌표는 각자 `findSpot` 이라 **3D 에서 다른 벽에 붙어 있었다.**
  - **각도까지 맞춰야 한다** — TV 만 벽을 보고 사운드바가 90° 틀어져 있으면 벽을 파고든다.
  - 틈은 `SB_GAP_MM = 60`(가정). **높이를 정하는 길이 둘**(`defaultMountH`·`alignUnderTV`)
    이라 **같은 값을 내야 한다** — 갈리면 배치 경로에 따라 높이가 달라진다.
  - **자동배치에서 사운드바를 맨 뒤로 미룬다.** 폭 내림차순만 믿으면 안 된다 —
    사운드바 최대 1,030mm 가 TV 42~43형 986mm 보다 **넓어서** 먼저 놓이는 조합이 있다.
    같은 이유로 **사운드바가 TV 보다 넓으면 안내**를 붙인다(막지는 않는다).
  - **손으로 옮긴 것은 따라가지 않는다**(`posSet`) — 설치 높이의 `mhSet` 과 같은 장치다.
  - **TV 를 지우면 다시 계산한다** — 다른 TV 로 옮겨 가거나 바닥으로 내려온다.
    안 하면 3D 에 **걸 TV 도 없이 벽에 떠 있는 사운드바**가 남는다.
- **벽걸이가 창·문 자리에 오면 하드로 막는다**(`warn`). 바닥 가전이 창 앞에 오는 것은
  주의로 족하지만 **벽걸이는 걸 벽 자체가 없다** — *"여기 걸면 됩니다"* 가 거짓이 된다.
  도어 오픈을 하드로 안 만든 결정과 어긋나지 않는다(그쪽은 힌지 방향을 몰라 양쪽에 그리는
  추정값이고, 이쪽은 "벽이 있는가"라는 단순한 사실이다).
- **벽에 거는 것은 반드시 벽을 등진다**(`snapWallMounted`, 2026-08-17 사용자:
  *"TV는 반드시 벽을 등집니다"*). 자동배치는 원래 벽에 붙이는데 **끌어 옮기거나 돌리면
  그 관계가 풀려** 방 한가운데 떠 있는 벽걸이 TV 가 3D 에 선다. 2D·3D 양쪽 끌기 끝에서
  다시 붙인다.
  - **`snapToWall` 을 그대로 쓰면 안 된다** — 그쪽은 개구부(창·문)도 벽으로 친다.
    벽걸이는 `open` 구간을 뺀 **실제 벽**에만 붙는다. 붙일 벽이 없으면 손대지 않는다.
  - **회전은 막는다.** 각도는 붙은 벽이 정하는 값이라 손으로 돌리면 화면이 벽을 본다.
    다른 벽으로 옮기려면 끌어다 놓으면 되고 그때 다시 맞춰진다. 사운드바도 같이 막는다.
  - **앞면은 로컬 `+y` 다**(`drawGlyph` 가 화면을 `d*0.9` 에 그린다). 각도 `a` 로 돌리면
    **(−sin a, cos a)** 로 간다 — 부호를 뒤집어 쓰면 *"벽을 본다"* 는 반대 결론이 나온다
    (검사에서 실제로 한 번 틀렸다).
- **배치는 3D 에서 한다**(2026-08-17 사용자: *"2D로 가전제품 배치하지 않습니다.
  3D에서만 배치합니다"*). 가전을 확정하면 곧바로 3D 를 연다.
  - **2D 의 끌기·놓기 코드는 지우지 않았다.** 3D 가 판정에 쓰는 함수
    (`nudgeInside`·`hitsOther`·`retagRoom`·`snapWallMounted`)가 전부 그쪽에 있고 검사도
    그 길로 돈다. **2D 는 도면·축척·방을 잡는 화면**으로 남는다.
  - 그래서 *"2D 에서 벽걸이인지 보이게 하자"* 는 하지 않았다 — 2D 는 배치 화면이 아니다.
- `test-plans` 가 일곱을 지킨다 — 파트가 벽걸이인가 · 85인치 하단 600 · 클수록 내려가는가 ·
  중심/각도/틈이 맞는가 · 손으로 옮기면 안 따라가는가 · **벽을 등지는가** · **안 돌아가는가**.

### 축척 폴백 상수 — **재보정할 것이 없다** (2026-08-17 실측)

*"`0.000054` 를 15장이 아니라 697장으로 다시 재면 낫지 않겠나"* 는 **해 볼 필요가 없다.**

- 색인에 축척이 실린 도면은 **12장뿐**이다(697장 중). 나머지는 애초에 참값이 없다.
- 그 12장으로 재면 **현재 상수가 이미 중앙값과 3% 차이**다 —
  현재 `5.40e-5`(실제폭 18.5m 가정) vs 실측 중앙값 `5.25e-5`(19.0m).
- 남은 흩어짐(실제폭 **14.0~25.2m**)은 도면마다 담는 범위가 다른 것이라
  **어떤 상수로도 못 줄인다.**

**더 나아 보이는 길 하나 — 다만 표본이 없다.** 전용면적(`exclusiveM2`)으로 도면마다
따로 추정하면 흩어짐이 **±29% → ±9%** 로 좁아진다(도면 폭이 제각각인 이유가 바로
세대 크기라서 그렇다). **그런데 축척·전용면적이 둘 다 있는 도면이 4장뿐**이라
근거로 삼을 수 없다. 바로 앞에서 **틀린 축척이 어림값보다 나쁘다**는 것을 90장으로
확인했으므로(위 절), 4장으로 핵심 추정기를 바꾸지 않는다.
**축척이 확정된 도면이 30장쯤 모이면 그때 다시 볼 것.**

### 가전 외형 — **데이터가 밝히는 것만 그린다** (2026-08-17)

삼성 공식 3D 모델을 구할 수 없어 *"치수는 정확하게, 외형은 단순하게"* 로 두었는데,
**`size-reps` 의 이름이 이미 말하고 있는데 그림이 안 보고 있던 것**이 여럿이었다.
지어내는 것이 아니라 **있는 데이터를 읽는 것**이라 원칙에 어긋나지 않는다.

| 무엇이 틀렸나 | 전 | 후 |
|---|---|---|
| 양문형 냉장고 | 좌우 도어인데 **가로 분할선이 하나 더** | 2×1 |
| 1도어 키친핏 | 가로로 갈림 | 통짜 1×1 |
| 1도어 세트 2~4벌 | 2×2 | 캐비닛 수만큼 세로 분할 |
| 뚜껑형 김치냉장고 | 앞면에 도어선 | 앞면 없음 · 윗면 뚜껑 |
| 더 플레이트 **1구** | 화구 **4개** | 1개 |
| 시스템에어컨 **판넬**(두께 35mm) | 실내기와 같은 토출 호 | 아래를 보는 흡입구 |
| 시스템에어컨 **실외기** | 실내기와 같음 | 앞면 팬 그릴(=이격 잡는 방향) |
| 사운드바 **우퍼** | 앞면이 통째로 그릴 띠 | 드라이버 원 |
| 벽걸이 TV(2D) | **스탠드 목**이 달려 있음 | 벽에 닿는 면 |

- **가르는 근거는 `size`·`group`·`part` 문자열뿐이다**(`fridgeLayout`·`burnerCount`·`it.part`).
  **이름이 안 밝히는 것은 예전 기준 그대로 둔다** — 업소용 다목적 도어 수, 빌트인 63cm
  화구 수는 카탈로그에서 확인하지 않았으므로 지어내지 않는다.
- **2D 와 3D 가 같은 함수를 본다.** 같은 품목이 앱마다 다르게 생기면 눈이 헤맨다.
- **TV 카테고리에 프로젝터가 있다**(The Freestyle · The Premiere). 파트가 `본체` 하나뿐이라
  폴백이 그것을 골라 **바닥에 놓는 것이 맞다** — "모든 TV 는 벽걸이"로 검사하면 깨진다.
- `test-plans` 가 둘을 지킨다 — **냉장고 도어 구성 8종** · **부품마다 그림이 갈리는가**.
  - **부품 검사를 메시 개수로 하면 안 된다.** 원(`CircleGeometry`)이든 판(`PlaneGeometry`)
    이든 메시는 하나씩이라 셋 다 같은 수가 나온다(실제로 그렇게 한 번 헛돌았다).
    **형상 종류와 자리**를 지문으로 쓴다.

### 2D라서 생기는 겹침 예외 (`STACKABLE`)

실제로는 **높이가 달라 부딪히지 않는데** 위에서 내려다본 그림에서는 같은 자리로 보이는 조합이 있다.
이 예외가 없으면 **실제로 되는 설치를 "겹칩니다"로 막는다.**

| 조합 | 근거 |
|---|---|
| TV ↔ 사운드바 | 사운드바는 TV 아래에 건다 |
| 세탁기 ↔ 건조기 | 건조기를 세탁기 위에 적층한다 |

- **콤보(세탁+건조 일체형)는 제외한다** — 그 자체가 건조까지 하는 물건이라 위에 건조기를
  얹지 않는다. `isCombo()`가 `group`의 '콤보' 또는 모델 접두 `WD`로 가른다.
- 그래서 **`size-reps`에서 세탁기와 콤보를 라인업으로 갈랐다**(`lineupOf`). 안 나누면 대표가
  콤보(`WD90H25AHS`)로 뽑혀 **세탁기+건조기 적층 상담이 아예 불가능해진다.**
  라인업을 나눈 카테고리는 `DEFAULT_LINE`에 기본값을 넣어야 추천에서 빠지지 않는다.
- 겹침이 정상인 조합은 경고(`warn`)가 아니라 **안내(`soft`)** 로 남긴다.
- 겹쳐 놓이면 라벨이 서로 뭉개지므로 중심이 가까운 것 수만큼 **아래로 어긋나게** 적는다.
- **언젠가 3D로 가면 높이축이 생겨 이 예외 자체가 필요 없어진다**(사용자 희망사항 —
  도면을 회전해 가며 제품도 3D로 렌더링. 지금은 계획 없음).

### 놓는 방법 — 자동 배치 · 직접 놓기

- **자동 배치**(`autoPlace`)는 이격·방 경계를 지키는 자리를 찾아 준다. 방이 있어야 한다.
- **직접 놓기**(`startManual` → `nextPending` → `placeAt`)는 고른 가전을 대기열에 넣고
  **한 대씩 도면을 눌러** 놓는다. 상담에서 보고 싶은 것은 대개 고객이 가리키는 자리이고,
  자동이 자리를 못 찾았을 때 "안 됩니다"로 끝나면 거기서 막힌다. 자동 배치 실패 시트에도
  '직접 놓기' 버튼을 둔다. 방이 없어도 된다 — 자리를 사용자가 지정하기 때문이다.
  - 진행 상황은 **도면 위 막대**(`showPlaceBar`, `#draftbar` 재사용)로 알린다. 모달로 물으면
    정작 봐야 할 도면이 가려진다(초안 조절 막대와 같은 이유).
  - `mode==='place'` + `state.pending` 경로는 처음부터 있었는데 **pending 을 설정하는 곳이
    없어 죽은 코드였다.** 되살린 것이므로 진입점을 지울 때 주의할 것.
- **자동 배치가 끝나면 반드시 `setMode('idle')`.** 방을 확정한 뒤에는 방을 더 잡을 수 있게
  `detect` 모드가 유지되는데, 그대로 두면 배치된 가전을 누르는 순간 `pointerdown` 이
  드래그 분기까지 가기 전에 **detect 분기에서 return** 해 그 자리를 새 방으로 인식한다 —
  **가전을 집을 수가 없다.** `test-place.mjs`와 `test-plans.mjs`가 둘 다 검사한다.

### 도면 전체를 잡는다 — 방은 울타리가 아니라 이름표다 (2026-08-11)

*"한 공간만 치수를 입력하면 그 안에만 놓을 수 있는데, **도면 전체에 비치할 수 있어야**
한다. 3D 도 도면 전체가 구현되는 방식이어야 한다. **고객은 어디에 놓을지 정해진 상태가
아니다**"*(사용자). 이 한 문장이 두 가지를 바꿨다.

**① `벽 자동 인식`이 도면 전체를 한 번에 잡는다**(`detectAllRooms`).
**축척을 확정하는 순간에도 자동으로 한 번 돈다**(2026-08-12 사용자 지적:
*"부분선택을 하고 자동으로 축척이 완료되면 전체벽을 3D화 해야합니다. 도면의 한 부분만
3D로 만드는 게 아닙니다"*). 축척은 방 하나의 가로 벽만 있으면 나오지만 배치는 집 전체가
있어야 하는데, 예전 `askWallLength` 의 `commit()` 은 **축척을 잰 그 방 하나만 등록하고
끝났다** — 사용자가 `벽 자동 인식`을 따로 누르지 않으면 3D 에도 그 방 하나만 섰다.
이미 등록된 그 방은 `sameSpace()` 가 알아보고 건너뛴다(겹침 70% + 넓이 0.6배).
도면 없이 벽을 직접 그린 경우에는 인식할 이미지가 없으므로 돌리지 않는다.
`test-plans.mjs` 의 `축척→전체` 가 지킨다(공간 4곳 미만이면 실패).
- 6×6 격자로 찔러(`autoDetectCenter` 와 같은 방식) 살아남은 것을 **전부** 공간으로 등록한다.
  4×4 로는 욕실·현관이 통째로 빠진다. `sealedMask` 캐시 덕에 실측 1.8초다.
- 크기 하한은 축척 기준을 고를 때(3%)보다 낮춘 **넓이 1.2% · 짧은 변 5%** — 여기서는
  욕실·현관도 잡아야 하기 때문이다.
- **여러 방이 한 덩어리로 잡힌 것은 버린다.** 문이 열린 채 그려진 도면에서는 온 집(때로는
  도면 여백까지)이 한 영역이 된다 — 실측으로 전용 85.5㎡ 세대에서 **169.6㎡** 가 잡혔다.
  - 넓이 비율(도면의 50%)로 거르려 했더니 도면마다 여백이 달라 흔들렸다(그 169.6㎡ 가 43%로 통과).
  - **포함 관계로 판정한다** — 다른 후보 2곳 이상의 중심을 담으면 합쳐진 덩어리다.
  - **경계상자로 보면 안 된다.** 상자로 쟀더니 ㄱ자 거실의 상자가 옆 침실 중심을 품어
    **진짜 거실(31㎡)까지 버렸다.** 다각형 자체(`inLoop`)로 재야 한다.
  - **포함 관계만으로도 부족하다.** 합쳐진 영역의 다각형은 속 방들의 벽을 피해 구불구불
    감싸므로 그 방들의 중심이 **기하학적으로는 바깥**에 있다 — 그래서 안 걸린다.
    그래서 **전체 대비 비율**을 함께 본다: 잡힌 것을 다 합한 넓이의 **45%** 를 넘으면 덩어리다.
    - 문턱 45% 는 실측에서 나왔다 — 디에트르 84 의 덩어리가 **49%**(102㎡/208㎡), 대방 86A 의
      **정상적인 거실+주방 트인 공간이 42%**. 40% 로 잡았더니 그 거실까지 쪼개져
      31.0 → 27.7㎡ 가 됐다. 한국 아파트의 트인 거실은 원래 이만큼을 차지한다.
    - 넓이 상한(예: 90㎡)으로는 못 잡는다 — 세대 크기마다 달라진다.
  - **덩어리는 버리기 전에 닫기 반경을 키워 다시 잡아 본다.** 반경을 키우면 문이 막히며
    옆방이 떨어져 나가 면적이 준다 — 화면의 "◂ 더 좁게"를 자동으로 하는 것이다.
    `detectRoomAt(x, y, forceR)` 의 세 번째 인자가 그 손잡이다. 끝내 안 줄면 그때 버린다.
    실측 — 디에트르 84 가 8곳(최대 102㎡) → **7곳(최대 35.7㎡)** 이 됐다.
- **실측치는 도면을 몇 조각으로 보느냐에 따라 통째로 바뀐다.** 여기 적힌 값이 옛
  `sheetBoxes`(한 도면을 여덟 조각으로 갈라 보던 것, 2026-08-13 수정) 시절 값이라
  한동안 어긋나 있었다. **인식을 고치면 이 줄도 함께 다시 잴 것** —
  `.scratch/place-demo.mjs`(프로덕션 빌드에 단지 불러오기부터 눌러 들어간다).
  - 대방 엘리움 86A (전용 85.53㎡) — **11곳 · 합계 104.3㎡(122%)**.
    초과분은 전부 **이중 계산**이다: 주방 16.3 ⊂ 거실 30.1 · 세탁실 ⊂ 안방.
    트인 거실 안의 주방을 따로 잡는 것은 **일부러 그렇게 둔 것**이라(넓이비 0.6 조건)
    합치지 않고, 대신 목록이 `30.1㎡ · 주방 포함` 이라고 적는다.
  - 디에트르 84 (전용 84.37㎡) — 7곳 · 74.7㎡(89%).
  - 자이 헤리티지 60 (전용 59.52㎡) — 6곳 · 32.5㎡. **이 % 는 쓰지 말 것** — 아래 참조.
  - 세 장 모두 **2D 공간 수 = 3D 공간 수**, 3D 포함 9.5~12.1초.
- **색인에 축척이 없는 도면(92%)에서는 "전용면적 대비 덮음 %" 가 인식 품질의 증거가
  아니다.** 넓이는 축척의 **제곱**에 비례하는데 그 축척이 사람이 넣는 값이라서다 —
  시연 스크립트가 일률적으로 3,600mm 를 넣으므로 진짜 벽이 3,000mm 면 넓이가 0.69배,
  4,300mm 면 1.43배가 된다. 자이 헤리티지 60 이 55% 로 나와 "인식이 나쁘다"고 읽었는데,
  **축척을 1mm/px 로 두고 픽셀로 다시 재니 셋 중 가장 높았다**
  (`.scratch/cov-probe.mjs`) — 자이 60 **50.7%** · 디에트르 84 40.8% · 대방 86A 21.3%
  (도면 이미지 넓이 대비. 시트 여백이 도면마다 달라 절대 비교는 아니고, 같은 도면을
  고치기 전후로 견주는 용도다).
  - 인식을 고쳤는지 판정할 때는 **축척이 색인에 실린 도면**으로 보거나 픽셀로 재라.
  - `applyDetected` 는 **축척 확정 전에는 공간을 등록하지 않는다.** 그래서 축척 없이
    `detectAllRooms()` 를 부르면 반환값은 6인데 `state.rooms` 는 0이다 — 조사 스크립트가
    여기 걸려 "인식 실패"로 오독하기 쉽다.

### 인식을 재려면 — **축척을 먼저 상담사가 하는 대로 맞춰라** (2026-08-14 실측 오류)

조사 스크립트에서 `state.mmPerPx = 1` 로 두고 `detectAllRooms()` 를 부르면 **앱의
실제 동작을 재는 것이 아니다.** 실측으로 확인한 오차가 크고 방향이 한쪽이라
**문제를 과대평가한다**:

| 도면 | mmPerPx=1 (틀린 측정) | 실제 경로(축척 확정 후) |
|---|---|---|
| 성남복정 84 | 1곳 · 5.8% | **3곳 · 16.6%** |
| 대방 86A | 9곳 · 21.3% | **11곳 · 27.3%** |
| 자이 헤리티지 60 | 6곳 · 50.7% | **12곳 · 53.7%** |

- **왜 어긋나는가** — `keepBands`·`dropSpecks`·닫기 반경·크기 하한이 전부 mm 기준이고
  `state.mmPerPx` 로 px 로 환산된다. 1 을 넣으면 "500mm = 500px" 가 되어 문턱이
  수십 배로 부푼다(806px 도면에서 띠 길이 문턱 8px → **250px**).
- **함정이 하나 더 있다.** `useImage` 가 `autoDetectCenter` 를 돌리며 기반 마스크를
  **미리 만들어 캐시**한다(그때는 `mmPerPx = null` 이라 폴백 문턱이 맞다). 그래서
  나중에 1 을 넣어도 `structureBox`·`keepBands` 결과는 멀쩡해 보이고, **`detectAllRooms`
  안쪽 mm 기준만 조용히 어긋난다** — 눈으로는 정상으로 보인다.
- **바르게 재는 법**: 색인에 축척이 있으면 `useImage(src, mmPerPx)`, 없으면 화면이
  묻는 대로 **자동으로 고른 벽에 3,600mm 를 넣는다**(`#wl` → `#wl-ok`). 넓이는
  `roomArea / mmPerPx²` 로 이미지 픽셀²에 되돌려 축척 가정과 무관하게 견준다.
  하네스: `.scratch/corpus-detect2.mjs`(옛 `corpus-detect.mjs` 는 위 오류가 있다).

**두 가설을 세웠다가 재 보고 둘 다 버렸다 — 다시 세우지 말 것:**
- ~~해상도가 낮아서~~ → 작은/보통/큰 도면의 공간 수 중앙값이 **7 / 8 / 8** 로 차이 없다
  (`.scratch/size-vs-detect.mjs`). 코퍼스의 16.2%가 긴변 600px 미만이지만 무관하다.
- ~~여백 없이 잘려 `envelopeMask` 가 무력해져서~~ → 성남복정 84 는 **`baseInfo.env === true`**
  (봉투를 정상적으로 찾았다). 게다가 테두리가 어두운 비율은 잘 되는 도면 쪽이
  **더 높다**(22.7% vs 14.7%, `.scratch/border-hyp.mjs`).

**목록에서 "인식이 안 되는 지면"을 거르려던 안은 버렸다.** 성남복정1지구 84 처럼
**멀쩡한 평면도가 낮게 나오는 경우**가 있어(방 이름이 다 적힌 컬러 도면),
숨기면 우리 인식 실패를 도면 탓으로 돌리게 된다. 눈으로 확인한 진짜 비평면도 —
호반써밋 85-2(면적표+KEY MAP) · 철산역자이 60(인테리어 사진+면적표) ·
동탄 포레파크 85-2(옵션 안내, 한 장에 부분 평면 여럿).

**코퍼스 100장 짝비교 (2026-08-14, 두께 폴백 보정 전후)** — `.scratch/corpus-detect2.mjs`
로 같은 100장(해시 순)을 전후로 재고, **덮음이 100% 를 넘는 5장은 뺐다**(건물 넓이 대비인데
100% 를 넘을 수 없다 — 기준 벽이 엉뚱하게 잡힌 비평면도 지면에서 mm/px 가 터진다).

| | 전 | 후 |
|---|---|---|
| 공간 수 중앙값 | 8 | **9** |
| 덮음 중앙값 | 33.5% | **37.0%** |
| 덮음 5% 미만 | 6장 | **3장** |
| 5pt 이상 개선 / 악화 | — | **20장 / 9장** |

**`structureBox` 가 축척 전에는 건물을 좁게 잡는다** — 성남복정 84(806×557)에서
축척 전 **540×531**, 축척 확정 후 **804×554**. 뒤엣것이 맞다. 원인은 위와 같다 —
문턱이 mm 기준이라 축척이 없으면 폴백 비율로 어림하고, 그 어림이 이 도면에서 빗나가
정리 단계가 왼쪽 절반의 벽 덩어리(원본 28,492px)를 잘게 부순다.
**축척 전 건물 상자는 `autoDetectCenter` 가 기준 방·기준 벽을 고르는 데 쓰이므로**,
여기가 어긋나면 화면이 엉뚱한 벽을 주황으로 표시하고 사용자가 거기에 길이를 넣는다.
`.scratch/comp-probe.mjs` 가 원본·정리본의 덩어리를 나란히 보여준다.

**② `it.room` 은 울타리가 아니라 이름표다.**
- `insideFor` 가 **잡힌 공간 어디든** 허용한다. 예전에는 가전에 붙은 방 밖으로 나가면 막았다.
- 다른 방으로 옮기면 소속이 따라간다(`retagRoom`) — 그래야 방별 요약과 경고가 계속 사실이 된다.
- **집 밖으로 나가는 것은 여전히 막는다.** 다만 문구를 고쳤다 — 집 밖인데 "침실2 밖으로
  나갑니다"라고 하던 것을 공간이 여럿이면 **"집 밖으로 나갑니다"** 로 바꿨다. 화면이
  거짓말을 하면 안 된다.
- `nudgeInside` 도 자기 방이 아니라 **가장 가까운 공간**으로 민다. 자기 방으로 끌고 가면
  사용자가 옆방에 놓으려던 것을 도로 뺏는 셈이다.
- 자동 배치(`findSpot`)는 지정한 방을 그대로 우선한다 — 냉장고를 주방에 놓는 기본값은 유용하다.
- **테스트가 옛 규칙("자기 방 밖은 막는다")을 명시적으로 검사하고 있었다.** 규칙이 바뀌었으므로
  `test-place.mjs` 의 그 검사도 함께 바꿨다(왜 바뀌었는지 주석에 남겼다).

### 공간(방) 모델 — 여러 방 · 이름 · 방별 배치

한 세대에는 방이 여럿이고, 가전은 **"어느 방에 놓을 것인가"** 가 정해져 있어야 한다.
냉장고를 침실에 추천하면 안 되고, 침실2가 꽉 찼다고 거실까지 못 쓴다고 말해서도 안 된다.

- `state.rooms[] = {id, name, parts:[{walls}]}`. 가전은 `it.room`으로 자기 방을 가리킨다.
  방 밖 판정(`insideFor`)·자동 배치(`findSpot`)·경고 문구가 전부 그 방 기준으로 돈다
  ("침실2 밖으로 나갑니다").
- **한 방이 여러 조각일 수 있다**(`parts`). 자동 인식이 방을 절반만 잡는 경우가 실제로 있어
  (붙박이장 선·확장 창호선이 방을 가른다) 나머지를 눌러 **같은 방에 이어 붙인다**.
  다각형을 합치지 않고 조각으로 들고 있어 경계가 뭉개지지 않는다 — `inRoom`은 조각 중
  하나에만 들어 있으면 참, `findSpot`은 조각마다 따로 훑는다.
- `state.walls`는 **모든 방의 경계를 합친 것**(`syncWalls`)이고 그리기·벽 스냅·개구부 검사에만 쓴다.
  **방이 하나도 없으면 예전처럼 `state.walls` 하나만 보고 판정한다** — 직접 그리기 경로와
  기존 테스트가 그대로 동작해야 하기 때문이다. 이 규칙을 깨지 말 것.
- **추천은 개수를 먼저 한 문장으로 말한다** — *"84A 타입 추천 가전 4종을 미리 골라 뒀습니다"*.
  체크박스가 스무 줄 늘어서 있으면 무엇이 이미 골라져 있는지 안 보인다. 상담은
  "84A는 보통 이 넷입니다"로 시작해 **빼는 쪽으로** 진행되므로, 개수를 먼저 말하고
  *"필요 없는 것은 체크를 풀고, 대수는 옆 칸에서 조절하세요"* 로 빼는 법을 바로 알려 준다.
  - 부르는 이름은 `planLabel()` — 단지에서 불러왔으면 **주택형**(`state.typeName`, 84A),
    직접 올린 도면이면 **전용면적**, **둘 다 모르면 아무 이름도 붙이지 않는다.**
    "84타입"이라고 단정할 근거가 없는데 그렇게 부르면 상담에서 그대로 읽힌다.
  - 추천할 근거가 없을 때는 개수를 0으로 적지 말고 **무엇을 하면 되는지**를 말한다
    (단지 불러오기 / 축척 맞추고 방 잡기).
- 가전 선택 시트의 방 드롭다운 기본값은 `defaultRoomFor(cat)`이 잡는다 — `ROOM_PLAN`의 용도와
  방 이름을 맞춰 본다(냉장고 → '주방', 에어드레서 → '침실1'/'안방'). 어디까지나 기본값이다.

### 좁은 화면(휴대폰) — 매장 기기가 폰일 수 있다

2026-08-09 사용자가 휴대폰으로 열어 보고 준 지적 셋을 반영했다. **위를 비우고 아래를 채운다.**

- **도구막대는 4개만 밖에 둔다** — 단지 불러오기 · 도면 이미지 · 가전 선택 · 결과 저장.
  나머지 8개(축척·벽 인식·벽 그리기·경계 수정·주방 추천·단지 저장·◧·↔)는 `⋯`(`#bar-more`)
  안으로 접는다. 열두 개가 두 줄로 접히면 도면이 그만큼 밀려 올라간다.
- **안내문(`#status`)은 좁은 화면에서 숨긴다.** 세 줄짜리 문장이 화면 위쪽을 먹었고,
  아래 요약 띠가 그 역할을 대신한다. 도구막대 높이 196px → 101px.
- **하단 요약 띠(`#mini`, `renderMini`)** — 좁은 화면에서 오른쪽 패널이 서랍으로 숨으면
  상담 중 볼 것이 없었다. 늘 봐야 하는 것만 상시로 띄운다: 단지·주택형 / 축척 확정 여부 /
  공간 수·전용면적 / 가전 대수·대기 수 / 문제 건수. 가전을 고르면 그 치수·이격을 한 줄 더.
  '자세히'를 누르면 서랍이 열린다. 넓은 화면에서는 오른쪽 패널이 있으므로 띄우지 않는다.
- **좁은 화면에서 시트 안 목록은 `flex:1` + `min-height` 를 줘야 한다.** `.sheet` 가
  `flex-column` 이라 최소 높이가 없으면 위아래 설명글에 밀려 목록이 **통째로 찌부러진다** —
  실측으로 `#a-list` 가 높이 **14px** 로 눌려 16줄이 있는데도 하나도 안 보였다(사용자 지적).
  단지 목록(`.liblist`)만 고치고 가전 목록(`.picklist`)을 빠뜨린 탓이다. **새 목록을 만들면
  좁은 화면 규칙을 함께 넣을 것.**
  - 항목마다 붙는 정보줄(`.pickinfo`)이 세 줄씩이라 한 줄이 100px 을 먹었다.
    `-webkit-line-clamp:2` 로 잘라 보이는 줄이 3 → 7개가 됐다. 잘린 내용은 고른 뒤
    하단 요약 띠가 전부 보여준다.
- **단지 고르기는 시·도를 먼저 고른다**(`#libchips`). 지역이 17곳이라 폰에서 계속
  쓸어내려야 했다. 지역 이름이 `경기 평택`·`강원 원주` 꼴이라 **앞 토막이 곧 시·도**다 —
  칩으로 걸러 경기 11 / 강원 6이 되고, 목록에는 시·군만 적어 줄을 짧게 만든다.
  좁은 화면에서는 시트를 94% 높이로 열고 남는 자리를 전부 목록에 준다(드래그할 공간).
- **목록이 주인공인 시트는 목록에 `min-height`를 박는다.** `.sheet`가 세로 플렉스라
  위아래 설명글은 글자여서 줄지 않고 **목록만 찌그러진다** — 가전 선택에서 실제로 한 줄만
  남아 *"어디가 가전선택창인지 한참을 찾았다"* 는 지적을 받았다(2026-08-09). `.picklist`는
  좁은 화면 46vh / 넓은 화면 32vh를 최소로 잡고, 목록 위에 **`.pickhead`("여기서 고르세요")**
  로 자리를 밝힌다. 아래 긴 설명글은 `<details class="help">`로 접어 둔다.
- **가전 한 줄은 좁은 화면에서 두 줄로 접는다** — 1줄 체크·이름·수량·방 / 2줄 사이즈 드롭다운.
  한 줄에 다 넣으면 오른쪽(방 드롭다운)이 화면 밖으로 잘렸다. 치수·모델코드·이격을 적는
  `.pickinfo`는 항목마다 세 줄이라 **체크한 것만** 펼친다(`:has`, 미지원 브라우저에서는
  규칙이 무시돼 전부 보인다 — 안전한 방향).
- **`.modal-actions`는 시트 바닥에 붙인다**(sticky). 목록에 자리를 주자 확정 버튼이 스크롤
  밖으로 밀려났다. 시트 안쪽 여백(`--pad`)까지 덮어야 목록이 버튼 옆·아래로 비쳐 보이지 않는다.

### 휴대폰 손가락 조작

- **한 포인터는 "누르기 / 끌기"로 가른다**(지도 앱과 같다). 끌면 **도면 이동**, 눌렀다 떼면
  **모드가 정한 일**(방 인식·축척 점 찍기·벽 꼭짓점·가전 놓기).
  - 그래서 모드 동작은 `pointerdown` 이 아니라 **`pointerup` 에서** 한다(`modeTap`).
    누르는 즉시 해 버리면 끌기를 팬으로 쓸 수 없고, **PC 마우스는 포인터가 하나뿐이라
    도면을 옮길 방법이 아예 없었다**(사용자 지적).
  - 문턱은 6px(`TAP_SLOP`). 손가락은 가만히 있지 못해 0으로 두면 누르기가 안 된다.
  - **테스트에서 도면을 누를 때는 `pointerdown`+`pointerup` 을 함께 쏠 것.** 예전처럼
    `pointerdown` 만 쏘면 끌기로 취급돼 아무 일도 안 일어난다.
- **가전을 누르면 가전이 반응한다 — 모드와 상관없이.** 방을 확정하면 "다른 방도 잡을 수 있게"
  `detect` 모드가 유지되는데, 그 상태에서 가전을 누르면 **가전이 아니라 그 자리를 방으로**
  인식하려 들어 **이동도 회전도 안 됐다**(사용자 지적). 화면 맨 위에 그려진 것이 가전인데
  손은 그 아래 도면을 만지는 셈이다. 빈 곳을 눌렀을 때만 모드가 정한 일을 한다.
- **회전은 버튼으로도 된다**(하단 요약의 `↻ 90°`, `✕ 삭제`). 더블탭만 두면 아무도 모른다 —
  실제로 *"더블클릭으로 알고 있습니다"* 라는 말을 들었다. 더블탭 창은 400 → **500ms**
  (손가락은 마우스보다 느리다).
- **회전 단위는 카테고리가 정한다**(`rotStep`). 기본 90°, **에어컨은 45°** —
  *"에어컨의 경우 회전각이 45도 단위로 해주세요. 대각선으로 설치 많이 합니다."*
  (2026-08-09 사용자). 스탠드 에어컨을 거실 모서리에 비스듬히 놓는 설치가 흔한데
  90° 단위로는 그 자리를 아예 그릴 수 없다. 45° 두 번이 90° 라 예전 조작도 그대로다.
  - 회전은 **버튼·더블탭·`R` 키 세 곳**에서 일어난다. 반드시 한 함수를 지나갈 것 —
    한 곳만 고치면 "버튼은 45°인데 더블탭은 90°"가 되고 화면을 봐야만 알 수 있다.
    `test-place.mjs` 가 `.a += Math.PI/2` 가 남아 있는지까지 검사한다.
  - 버튼 라벨(`rotLabel`)도 같은 함수에서 만든다. 적힌 각도와 도는 각도가 다르면
    화면이 거짓말을 한다.
- **두 손가락은 언제나 확대·축소·이동**이다. 한 포인터 끌기로도 옮길 수 있지만, 확대는
  두 손가락뿐이라 함께 둔다.
  - **두 손가락을 정확히 동시에 대는 사람은 없다.** 첫 손가락이 먼저 닿아 방 인식을 이미
    저질러 놓은 뒤 두 번째가 온다. 그래서 `pinchStart` 가 직전 상태(`preTouch`)로 되돌린다 —
    확대하려던 것뿐인데 엉뚱한 방이 잡히면 안 된다.
  - 확대 배율 상한을 3 → 8 로 넓혔다. 좁은 화면에서 3배는 모자란다.
- **`⤢` 화면 맞추기** — 도면이 화면 밖으로 나갔을 때 한 번에 되돌린다(`fitAll`). 좁은 화면에서는
  이것 말고 방법이 없으므로 `⋯` 안에 넣지 말 것.
- **목록 서랍(`#side`)은 닫는 길이 여럿이어야 한다.** 좁은 화면에서 서랍으로 뜨는데,
  하단 요약의 `자세히` 가 **열기만 하고**(`add`) 서랍 안에 닫기도 없어서 **한 번 열면 다음 작업을
  아예 못 했다**(사용자 지적). 닫는 길이 하나뿐이면 그 하나가 막혔을 때 갇힌다:
  `✕`(서랍 안) · `☰` 재클릭 · **도면 터치** · `←` · 휴대폰 뒤로가기 — 다섯 가지를 모두 둔다.
  여는 곳에서는 `toggleSide()` 로 `pushBack()` 까지 함께 해야 뒤로가기와 어긋나지 않는다.
- **`setPointerCapture` 실패는 삼킨다.** 이미 놓인 포인터면 던지는데, 잡기 실패가 조작을 막으면
  안 된다. 실제로 이 예외 하나가 이후 포인터 처리를 통째로 죽였다.
- **`←` 뒤로가기** — 휴대폰 뒤로가기를 누르면 **앱이 통째로 닫혔다**(사용자 지적).
  모달·도면 위 막대를 열 때 `pushBack()` 으로 히스토리를 한 칸 쌓아 두면 휴대폰 뒤로가기가
  그것부터 소비한다. `stepBack()` 이 한 단계씩 물린다: 모달 → 막대 → 모드 → 선택 해제.
  쌓은 만큼만 소비하므로 앱을 벗어나야 할 때는 정상적으로 벗어난다.

### 화면에 적는 것 — 모델코드는 적지 않는다

- **모델코드를 화면에 띄우지 않는다**(2026-08-09 사용자 결정). 대표모델은 크기를 가늠하려고
  세운 자리끼움이지 "이 제품을 사시라"는 제안이 아닌데, 코드가 박혀 있으면 고객이 그 모델을
  제안받은 것으로 읽는다. 드롭다운 정보줄·배치 목록·결과 이미지 모두에서 뺐다.
  - **데이터에는 그대로 남긴다**(`state.items[].model`, `size-reps.json`). 치수 출처를
    되짚을 수 있어야 하고, `test-sizereps` 가 모델코드 실존을 검사한다.
  - 대신 **제품군**(`group`)은 남긴다 — "Micro RGB", "AI 4도어"는 고객에게 통하는 말이다.
- **사이즈는 상담에서 부르는 말로 적는다**(`sizeText`). TV 는 `115형` → **`115인치`**.
  - **표시 계층에서만 바꾼다.** 데이터 표기를 바꾸면 추천표(`BY_AREA`)와 골든값이 함께 깨진다.
  - 사양 꼬리가 사이즈를 되풀이하면 적지 않는다 — `115인치 … · 115형 (290cm)` 은 같은 말이
    두 번이다. 사이즈가 범위(83~85형)면 그 안의 값도 되풀이로 본다.
  - 배치 목록에서도 이름표에 이미 사이즈가 들어 있으므로(`TV 115인치`) 별도 pill 을 붙이지 않는다.

### 상담을 끝맺는 것들

- **결과를 이미지 한 장으로**(`buildShareCanvas` / `shareResult`, 도구막대 '결과 저장').
  상담이 끝나면 고객 손에 아무것도 안 남는 것이 이 도구의 가장 큰 구멍이었다. 도면 화면
  아래에 단지·주택형·전용면적·날짜와 제품별 모델코드·치수·이격·경고를 붙여 내보낸다.
  - **캔버스를 다시 그리지 않고 화면에 보이는 것을 그대로 복사한다**(`drawImage(cv, …)`).
    화면과 다른 그림이 나가면 "내가 본 것과 다르다"가 된다.
  - `navigator.share`(파일 공유)를 먼저 쓰고, 없거나 취소하면 내려받기로 떨어진다.
    **취소(`AbortError`)는 실패가 아니다** — 조용히 끝낸다.
  - 2배 배율로 그린다. 카톡으로 보내면 재압축되는데 등배로 만들면 모델코드가 뭉개진다.
- **안 들어가면 "무엇이면 들어가는지"를 말한다**(`fitAlternative`). 예전에는 "억지로 놓지
  않았습니다"로 끝나 상담이 거기서 멈췄다. 같은 카테고리·라인업에서 발자국이 작은 순으로
  **실제로 `findSpot` 을 돌려 보고** 되는 것 중 가장 큰 것을 알려 준다.
  **없는 답을 지어내지 않는다** — 진짜 안 되면 안 된다고 한다.
  폭만 보면 안 된다. 폭이 같고 **깊이만 줄어** 들어가는 경우가 있다(845~905L 912×930 →
  602~640L 912×716). 자리를 정하는 것은 발자국 넓이다.
- **삭제는 목록의 ✕ 로도 된다**(`removeItem`). 예전에는 `Delete` 키뿐이었는데
  **매장 기기가 태블릿이면 키보드가 없어 지울 방법이 아예 없었다.**
  키보드와 버튼이 같은 함수를 지나가야 한쪽만 고치는 사고가 안 난다.
- **제품은 위에서 본 실루엣으로 그린다**(`drawGlyph`) — 냉장고 도어 분할선·손잡이,
  세탁기 원형 도어, TV 화면 방향, 인덕션 화구. 라벨 붙은 사각형만으로는 "여기 이 제품이
  놓인다"는 느낌이 안 산다. **위에서 봤을 때 보이는 것만** 그리므로 치수를 왜곡하지 않는다.
  제품 로컬 mm 좌표(x −w/2..w/2, y 0..d)에 캔버스 변환을 걸어 회전·배율을 신경 쓰지 않는다.
  화면에서 26px보다 작아지면 생략한다 — 지저분하기만 하다.
  (사용자가 바라는 3D 렌더링의 저비용 대용이다. 3D로 가면 이 함수는 필요 없어진다.)

### 데이터 출처 두 갈래

- **치수** → `public/size-reps.json` (아래 절 참조). 모델파인더 DB의 카탈로그 검증값만 쓴다.
- **이격거리** → `place-app.html`의 `CLEAR` 표. **설치환경 가이드(`INSTALL_DB`)의 `space` 항목에
  실제로 적힌 값만** 넣는다. 수치가 없는 11개 카테고리(시스템에어컨·업소용냉장고·인덕션·정수기·
  사운드바·제습기·데이코·리빙 5종)는 0으로 두고 화면에 그 사실을 표시한다. 인접 카테고리를
  준용한 3건(슈드레서·에어컨 실내기·냉동고)은 `weak:true`로 표시한다.
- `test-plans.mjs`(`npm run test:plans`)가 **도면 코퍼스**를 브라우저에서 전부 인식시켜 검사한다.
  `scripts/fixtures/plans.mjs`에 실제 표기 관례를 재현한 평면 8벌(84타입 표준·컬러 어두운 바닥·
  회색 스캔본·아웃라인·경량 칸막이벽·저해상도·단일 실·ㄷ자)을 mm 좌표로 두고, "여기를 누르면
  이런 방이 나와야 한다"를 **넓이(㎡)**로 적어 둔다. 넓이로 보는 이유는 벽을 한 칸 틀리게 잡거나
  파인 부분을 가로지르거나 옆방을 삼키면 전부 넓이에 드러나기 때문이다.
  **인식 로직을 건드리면 이 스위트가 먼저 깨진다.** 새 도면 유형에서 문제가 나오면 코퍼스에
  그 유형을 추가하고 고치는 것이 순서다.
- **실제 도면 회귀 검사는 `npm run test:real` 이다**(2026-08-09 신설). 상담에서 터진 문제는
  전부 진짜 도면에서 나왔다 — 축척 기준으로 창틀 조각(32×21px)이 뽑히고, 기준 벽이 창 구간만
  잡히고, 세대 전체가 방 하나로 잡혔다. 사용자가 매장에서 손으로 찾아 준 것들이라 그 확인을
  자동화한 것이다. `public/plans/` 의 697장에서 해시 순으로 30장을 뽑아 쓴다.
  - **한 장 한 장의 정답을 적지 않는다.** 치수가 인쇄된 도면이 8%뿐이라 손으로 잰 기대값을
    채울 수 없고, 앱이 내놓은 값을 적으면 검사가 아무것도 못 지킨다. 대신 **품질 지표가
    나빠지지 않았는지**를 본다 — 자동 인식률 · 기준 방이 조각인가 · 기준 벽이 벽면 전체인가.
  - 기준선은 실측값에서 여유를 둔 **하한**이다(인식률 90%→하한 80%, 덮는 비율 1.00→하한 0.90).
    개선하면 이 숫자를 함께 올려 다시 못 내려가게 할 것.
  - 두 버그를 실제로 잡는지 확인했다 — 하한을 빼면 `조각을 골랐다 6장`, 벽면 병합을 빼면
    `덮는 비율 중앙값 0.81` 로 실패한다.
  - **표본은 파일명 해시 순으로 뽑는다.** 예전에는 목록을 일정 간격으로 훑었는데(`i += step`)
    **도면을 한 장만 넣어도 30장이 통째로 밀린다.** 실제로 홍천 2장을 넣자 개구부 비율
    중앙값이 20.1% → 23.0% 로 뛰었다 — 인식은 한 줄도 안 바뀌었는데 검사가 깨졌다.
    해시 순은 단지가 골고루 섞이면서도 결정적이고 코퍼스가 자라도 표본이 거의 그대로다.
  - **개구부 비율은 코퍼스 중앙값이 아니라 도면별 짝비교로 본다**(2026-08-12).
    옛 "상한 22%"는 옛 순차 표본에서 나온 값이라 표본이 바뀌면 무너졌고, 안정 표본으로
    다시 재니 정상 26.3% vs 버그 28.2% 로 **1.9pt** 차이밖에 안 났다(옛 3.9pt 는 표본 운).
    지금은 커밋된 `scripts/fixtures/plans-real/open-ratio.json`(140장)에 도면별 값을 두고
    **증감의 중앙값이 +1.0pt 를 넘으면 실패**한다. 같은 버그가 **+2.1pt** 로 또렷하게 잡히고
    정상 코드는 0.0pt 다. 기준선 재생성은 `REAL_BASELINE=write REAL_N=150 npm run test:real`.
    **인식을 개선했으면 기준선을 다시 써서 커밋할 것** — 안 그러면 개선분이 그대로 굳는다.
- **손으로 잰 기대값이 필요하면 `scripts/fixtures/plans-real/`에 넣는다.** 합성 평면은 "이런 표기 유형에서
  깨지지 않는다"까지만 보증한다. 실제 분양 도면을 넣고 `index.json`에 "여기를 누르면 몇 ㎡"를
  적으면 같은 하네스가 전수 검사한다. 기대값은 **도면에 적힌 치수로 계산한 값**을 쓴다 —
  앱이 내놓은 값을 적으면 검사가 아무것도 지키지 못한다.
  - 이미지는 **커밋하지 않는다**(public repo·저작권). `.gitignore`가 걸러 내고 측정값만 담긴
    `index.json`만 올라간다. 그래서 CI에서는 자동으로 SKIP되고 파일을 가진 로컬에서만 돈다.
  - **도면 수집은 로컬 세션에서 해야 한다.** 클라우드 세션은 프록시가 외부 호스트를 막는다
    (`CONNECT tunnel failed, response 403`). 설치환경 가이드 이미지를 모을 때(PR #54)와 같다.
    로컬에 그대로 붙여넣을 지시문이 그 폴더 README에 있다.
  - **2026-08-11 현재 10장 25프로브.** 치수가 인쇄된 후보(축척 자동판독이 성공한 15장)를
    전수로 훑어 만들었다. 만들며 알게 된 것 — 자세한 것은 그 폴더 README:
    - **넓이보다 폭이 촘촘한 검사다**(`wMm`). 앱은 안목을 재고 인쇄 치수는 대개 벽심이라
      폭이 6~9% 작게 나오는데, **넓이는 이 오차가 두 번 곱해져 15~19%** 가 된다. 세로 사슬이
      없는 도면(후보 9장 중 3장)도 폭으로는 검사할 수 있으니 버리지 말 것.
    - **탐침 지점을 옮겨야 방 전체가 잡히는 도면이 있다.** 눈금 중점이 늘 좋은 자리는 아니다 —
      침실이 1,755mm 로 잘렸다가 조금 옮기니 2,646mm 로 제대로 잡혔다.
    - **`known` 은 알려진 미해결이다.** 도면 치수로는 맞는데 인식이 못 따라가는 자리 — 보고는
      하되 실패로 세지 않는다(기대값을 앱 출력에 맞추면 검사가 무의미해지고, 그대로 두면 CI 가
      영원히 빨개서 다른 작업을 막는다). 고쳐지면 "이제 통과한다"고 **실패시켜** 알린다.
      **현재 3건이고 셋 다 "어느 닫기 반경에도 답이 없는" 쪽이다**(2026-08-16 전수 확인,
      `.scratch/radius-sweep.mjs`). 이 도구가 판정 기준까지 들고 있다 —
      ⓐ 맞는 반경이 사다리에 있는데 고르는 규칙이 다른 것을 집는다 → **고칠 수 있다**
      ⓑ 어느 반경에도 기대값이 안 나온다 → **벽이 실제로 없다(한계)**.
      안 가르고 규칙부터 만지면 멀쩡한 도면이 깨지므로 **반드시 먼저 훑어 볼 것.**
      - `daebang-85` 침실2 — 8단계 중 4단계 실패, 나머지가 47.1 → 40.1 → **35.9㎡**.
        8.24㎡ 근처에 아예 못 간다(거실로 넓게 트여 있다).
      - `dietre-84` 침실2·침실3 — **8단계 전부 실패.** 가변형 벽체(붉은 점선)라 닫을
        실선 벽이 없다.
    - **저해상도 도면에서 폭 검사에 백분율 자만 쓰면 안 된다**(2026-08-16 해소한 2건).
      자이 헤리티지 23·24 는 553×442 라 **1px 이 45mm** 인데 기대 범위가 ±12% 였다.
      같은 도면의 형제 프로브는 인쇄값의 92~96%(침실4 2,953/3,190 · 거실 4,594/4,800)로
      정상인데 **침실3 만 85~87%** 였다 — 원인은 해상도가 아니라 **침실3·4 를 가르며
      공유 벽에서 1~2px 을 더 먹는 것**이다(2026-08-14 에 두 방이 5,795mm 로 합쳐지던 것을
      고치며 맞바꾼 비용). 덜 재는 방향이라 배치 판정에서는 안전한 쪽이라 그대로 두고,
      **하한에만 2px 어치를 준다**(`test-plans.mjs`). **상한은 건드리지 말 것** — 이 가드가
      실제로 잡아 온 사고가 "두 방이 합쳐져 폭이 2배"라 상한이 이빨이다.
    - **축척 색인의 자동 판독값을 믿지 말 것.** "확실"로 표시된 15장 중 **2장이 치수가 아예
      없는 3D 투시도**였고(본문 문단 숫자를 사슬로 오독한 것으로 보인다), 우미린 더 스카이는
      색인 19.521 이 실측 24.4 와 크게 어긋났다. 이 코퍼스는 전부 직접 잰 값을 쓴다.
    - 도구 — `.scratch/measure-chain.mjs`(사슬 눈금 추출) · `probe-sweep.mjs`(여러 점 찔러 보기) ·
      `contact-sheet.mjs`(여러 장을 한 장에 모아 2D/3D 판별). 한 장씩 여는 것보다 훨씬 빠르다.
- `test-place.mjs`가 **이격 수치의 근거 문구가 `install-app.html` 원문에 있는지 문자열로 대조**한다.
  설치환경 가이드를 고치면 이 검사가 실패하므로, 그때 시뮬레이터 값도 함께 갱신해야 한다
  (실제로 PR #54가 원문을 바꾸면서 main의 `npm test`가 깨졌고 PR #56에서 해소했다).

### 단지 **목록**은 도면과 별개다 (`public/apt/` · **관할 30개 시·군 3,829곳**, 2026-08-15)

도면이 있는 단지는 **150곳**뿐이다. 그것만 띄우면 상담사가 대부분의 고객에게
***"그 단지는 없습니다"*** 로 시작해야 하는데, 이름·동·입주년월만 있어도 고객 집을
특정하고 평형 상담을 시작할 수 있다. **도면은 고객에게 받아서 올리면 된다.**
도면 수집이 사실상 고갈된 뒤(아래) 사용자가 고른 길이다
(*"지역과 아파트를 지정해두고 도면은 천천히 업데이트하는걸로 합니다"* — 2026-08-15).

| 도 | 단지 | 비고 |
|---|---|---|
| 경기 12개 시 | 2,969곳 | 용인 530 · 수원 455 · 화성 428 · 성남 346 · 평택 312 · 안양 224 · 광주 147 · 하남 123 · 이천 114 · 오산 110 · 광명 96 · 안성 84 |
| 강원 18개 시·군 | 860곳 | 원주 232 · 춘천 144 · 강릉 127 · 속초 71 · 동해 60 · 태백 37 · 삼척 33 · 평창 21 · 홍천 20 · 정선 19 · 횡성 19 · 양양 17 · 영월 16 · 철원 15 · 고성 12 · 양구 6 · 화천 6 · 인제 5 |

- **만드는 것은 `npm run build:apt`**(`scripts/build-apt-index.mjs`) 하나다.
  원본은 `.scratch/` 의 K-apt 수집본이고, **없으면 그 도를 건너뛰고 기존 파일을 그대로 둔다** —
  반쪽짜리로 덮으면 배포본에서 단지가 통째로 사라진다.
- **`npm run test:apt` 가 지킨다** — 목차·건수 일치, 관할 30곳 전부 있는지, 파일명이 영문인지,
  안내 문구가 섞이지 않았는지, 앱이 목차의 `sido`·`key` 를 쓰는지, 커밋된 목록이 최신인지.

- 출처는 **K-apt(공동주택관리정보시스템)** — 100세대 이상 **의무 등록**이라 전수이고,
  `occuFirstDate` 가 있어 **1990~2000년대 단지가 그대로 들어온다**(청약홈은 2021-10
  이후만 조회돼 과거 단지가 통째로 비어 있었다). 연대별 1990년대 640 · 2000년대 965곳.
- **API 응답의 한글은 서버가 `?` 로 바꿔 보낸다**(`charset=ISO-8859-1`, 바이트 자체가
  물음표라 어떤 디코딩으로도 못 살린다). 코드로 목록을 받고 **이름은 화면 `<select>`
  에서 읽는다**(경기 `.scratch/kapt-collect.mjs` → `kapt-names.mjs`,
  강원 `kapt-codes-gw.mjs` → `kapt-gw.mjs` — 강원은 한 바퀴에 이름·입주년월을 함께 받는다).
- **강원 시·도 코드는 `51`(강원특별자치도)다** — 옛 `42` 가 아니다. 짐작하면 0건이
  나오고 원인을 못 찾는다. 목록을 먼저 찍어 확인할 것.
- **단지목록 응답에 법정동코드와 지번이 이미 들어 있다**(2026-08-17 확인).
  `POST /kaptinfo/getKaptList.do` 한 건의 필드 —
  `bjdCode`(법정동 10자리) · **`bun1`(지번 본번) · `bun2`(부번)** · `addr` · `x`·`y`.
  예전 수집기가 `code`·`bjd`·`occu` 만 골라 담고 **나머지를 버리고 있었다.**
  - 건축HUB 도면 API 의 `q` 가 **`법정동코드+지번본번-부번`** 이라 이것으로 그대로 만들어진다
    (`4121010100-717-3`). **단지 상세를 3,829번 방문할 필요가 없다** — 목록만 다시 돈다.
  - **`addr` 이 검산해 준다.** 한글은 `???` 로 깨져도 **숫자는 안 깨져서** 꼬리의
    `717-3` 을 `bun1`/`bun2` 와 대조할 수 있다. 어긋나면 담지 않는다 —
    틀린 지번으로 도면을 받으면 **남의 집 도면**이 온다.
  - 앞의 0 은 표기다(`0717`→`717` · `0000`→`0`). 도구 `.scratch/kapt-jibun.mjs`
    (수집) · `kapt-fields.mjs`(응답 필드 전부 찍기) · `blcm-probe.mjs`(연대별 표본 조사).
  - **응답 필드를 버리기 전에 전부 찍어 볼 것.** 이 한 번을 안 해서 "지번을 어디서
    구하나" 를 오래 붙들고 있었다.
- **`getKaptList.do` 는 시군구 5자리(`41210`)를 주면 그 시군구 전체를 한 번에 준다.**
  읍면동을 돌 필요가 없다 — 관할 30곳이 **요청 30번**이면 끝난다.
  - **읍면동 응답의 `code` 는 뒤 3자리뿐이다**(`107`). 그것을 그대로 넘기면 0건이거나
    **전국이 통째로 온다** — 실제로 그렇게 **70,320곳**(예상 3,829)을 담았고 예시 질의가
    서울(`1141…`)이었다. 그래서 수집기가 **돌아온 `bjdCode` 가 물어본 시군구로 시작하는지**
    확인한다.
  - **건수가 곧 검산이다.** 광명 96건 = 배포본 목록의 광명 96곳. 수집기가 `public/apt/index.json`
    과 시별로 대조해 찍는다(22/30개 시 완전 일치).
- **실측 (2026-08-17): 3,829곳 중 3,756곳(98.1%)에 질의를 만들 수 있다.**
  나머지 **73곳은 지번 자체가 없어**(`bun1` 이 0) 담지 않았다 — 시별 부족분 합계가
  정확히 73이라 **차이가 전부 이것으로 설명된다.** 연대별로는 1990년대 919 · 2000년대
  1,167곳이라 **정작 궁금한 옛 단지가 표본에 충분하다.**
  - `addr` 로 지번이 검산된 것 3,756곳 · **어긋나 버린 것 0**.
- **파일명은 영문이다**(`suwon.json`·`chuncheon.json`). 한글 경로는 URL 인코딩·서비스워커
  캐시에서 조용히 깨진다. 목차 `index.json` 을 먼저 받아 도시 목록에 단지 수를 적고,
  시를 누를 때 그 시 파일만 받는다(가장 큰 용인이 45KB).
- **시·도와 파일명 표를 앱에 또 두지 않는다.** `APT_KEY` 를 앱에 적고 지역명을
  `'경기 ' + 시` 로 만들던 때, 강원을 넣는 순간 **춘천·원주까지 전부 '경기'** 가 됐다.
  목차가 `{city, sido, key, n}` 을 함께 실으므로 앱은 그것만 읽는다.
- **K-apt 는 자치구까지 내려가 적는다** — `수원권선구`·`성남분당구`·`용인기흥구`·
  `화성동탄구`·`안양동안구`. 꼬리의 시/군/구만 떼면 `수원권선` 이 되어 표에 없는 말이 되고
  **그 시가 통째로 빠진다**(실제로 경기 2,969 → 986 이 됐다). 표의 시 이름이 **접두로
  붙는지**를 본다.
- **`<select>` 안내 문구가 단지로 섞인다** — 결과가 없는 읍면동에서 `검색 결과가 없습니다`
  가 option 으로 온다(강릉 강동면 등 두 도에서 한 건씩). 목록에서는 단지처럼 보인다.
- **도면이 한 장도 없는 시도 도시 목록에 남긴다.** 예전에는 도면 색인에서만 도시를 뽑아
  **하남이 통째로 사라졌다** — 도면 한 장을 빼는 것은 *"이건 평면도가 아니다"* 지만
  도시가 사라지면 ***"우리는 그 지역을 안 다룬다"*** 는 다른 말이고 그건 사실이 아니다
  (같은 이유로 단지 하나가 사라지는 것도 막아 뒀다).
- 도면 있는 단지와 **이름이 겹치면 넣지 않는다**(공백 무시 부분일치 — 표기가 조금씩 다르다).
- 누르면 **단지명을 기억한 채 파일 선택기가 열린다** — 고객 도면을 그 자리에서 받는다.
- `sw.js` 가 `/apt/` 를 **SWR** 로 잡는다. 상담 시작이 단지 고르기라 이게 안 뜨면 그 자리에서 막힌다.
- **도면 없는 단지에 평면을 지어내지 말 것.** 목록은 "그 단지가 있다"까지만 말한다.

### 벽선은 모을 수 있다 — 도면과 성격이 다르다 (`npm run merge:library`, 2026-08-17)

2026-08-17 사용자 질문 — *"도면을 그대로 가져오는 게 안 되면 **벽선만** 가져와도 됩니다.
우리가 하려는 건 도면을 보는 게 아니라 **벽선을 3D 로 세워 가전을 배치**해 보는 것"*.

**이 앱의 원래 설계가 그것이다.** `saveToLibrary()` 는 이미 이미지가 아니라 방 경계
(mm 좌표)만 저장하고, `plan-library.json` 주석이 이유를 적어 뒀다 — *"분양 도면은
저작권이 있어 저장소에 둘 수 없지만 **파생 측정값인 이 좌표는 담을 수 있다**"*.
그래서 **도면 수집이 막혀도 커버리지를 늘릴 수 있다.**

**비어 있던 것은 "합치는 손"이었다.** 화면에 내보내기(JSON)는 있는데 받아서 공용 목록에
넣는 도구가 없어, 상담사가 잡아 둔 벽선이 각자 기기 localStorage 에만 남고 공용 목록은
**0건**이었다. 그래서 `merge-plan-library.mjs` 를 만들었다.

- **쓰레기가 들어가지 않게 하는 것이 이 도구의 값어치다.** 합치기 자체는 쉽다. 공용 목록은
  배포본에 실려 여러 매장이 함께 쓰므로, 잘못 잡힌 벽이 들어가면 **그걸 불러온 다른
  상담사가 틀린 치수로 상담한다.** 다섯 가지를 검사한다 —
  ①이미지·base64 섞임(저작권) ②벽 3개 미만 ③한 변 30m 초과·방 1~200㎡ 밖
  ④단지·타입 이름 없음 ⑤이미 있는 것.
- **범위는 넉넉히 잡았다.** 목적은 "정확한 값 고르기"가 아니라 **"명백히 잘못된 것 거르기"** 다.
  좁히면 멀쩡한 것을 막는다.
- **덮어쓰지 않는다.** 같은 단지·타입이면 건너뛰고 보고만 한다(`--force` 로만 덮는다) —
  조용히 덮으면 남이 확인해 둔 값이 사라진다.
- `--dry` 로 먼저 볼 것. `npm run test:library` 가 **불량 4종을 실제로 물리는지** 검사하고
  `npm test` 체인에도 넣었다 — 따로 두면 아무도 안 본다(`test:prod` 가 6회 전부 실패한 채
  방치돼 있던 것에서 배웠다).
- 합친 뒤 **커밋해야 매장에 나간다** — 이 파일은 배포본에 그대로 실린다.

### 단지 도면 수집 (`public/plan-index.json` · 현재 150개 단지 697장)

**경원 관할 구역 — 영업 담당 구역이지 매장 소재지가 아니다**(2026-08-07 사용자 확인).

| 도 | 관할 |
|---|---|
| **경기 (12개 시)** | 수원 · 성남 · 용인 · 화성 · 평택 · 오산 · 안성 · 이천 · 광주 · 안양 · 광명 · 하남 |
| **강원 (전역 18개 시·군)** | 춘천 · 원주 · 강릉 · 동해 · 태백 · 속초 · 삼척 · 홍천 · 횡성 · 영월 · 평창 · 정선 · 철원 · 화천 · 양구 · 인제 · 고성 · 양양 |

경기는 경원팀과 **경인팀이 나눠 맡는다** — 김포·구리·고양·부천·안산 등은 경인팀이라 여기 없다.
**강원은 전역이 경원 담당이다.** 예전에 매장이 있는 강릉·원주·춘천·속초 4곳만 적어 뒀는데
그건 매장 소재지이지 담당 구역이 아니다 — 동해·삼척·홍천·평창 단지가 통째로 빠졌고,
청약홈 5년치에서 강원이 6건만 잡힌 원인이 이것이었다.

- 매장 → 도시 대응(상담사는 관할 시보다 자기 매장 이름을 안다):
  수원=스타필드수원·북수원·서수원·권선·영통·롯데수원·AK수원·타임빌라스수원·갤러리아광교·디지털시티 /
  성남=분당·AK분당·판교·현대판교·판교SDS / 용인=수지·구성·기흥·처인·에버랜드·신세계사우스시티 /
  화성=동탄·롯데동탄·화성캠퍼스·남양 / 평택=평택·고덕·세교·AK평택·KGM평택 /
  안양=평촌·롯데평촌·안양본·이마트안양 / 광명=소하·광명기아 / 하남=미사·신세계하남 /
  오산 · 안성 · 이천(증포) · 광주(경기 광주시)
- **동명 지역 함정** — 경기 광주 ↔ 광주광역시, 강원 고성 ↔ 경남 고성. 도를 모르는 채로 찾으면
  엉뚱한 단지를 끌어온다. 별칭 `남양`은 **`남양주`(경원 밖)에 걸리므로 쓰지 않는다** —
  실제로 남양주 단지 3곳이 화성으로 잘못 잡혔다.
- **정의는 이 파일에 둔다.** 구현은 `.scratch/gw.mjs`(`GYEONGWON`·`gyeongwonCity()`)에 있는데
  **`.scratch/`는 `.gitignore` 대상이라 커밋되지 않는다** — 그 PC를 벗어나면 사라지고,
  클라우드 세션·다른 기기는 알 수가 없다(실제로 사용자가 "이미 공유했다"고 다시 알려 준 적이 있다).
  커밋된 `scripts/build-plan-index.mjs` 는 더 느슨한 `경기|강원` 검사만 하므로 둘이 어긋난다.
- 2026-08-12 기준 **관할 30곳 중 12곳에 도면이 0장**이다 — 경기는 **하남**, 강원은 태백·속초·
  삼척·횡성·영월·정선·철원·화천·양구·인제·양양. (홍천은 2026-08-12 재시도로 2장 확보.)

**왜 채우기 어려운지 — 2026-08-12 전수 재시도로 확인한 것**

청약홈이 2021-10 이후만 조회되는 것이 절반이고, **나머지 절반은 분양 사이트가 사라져서**다.

- 0장 지역 중 공고가 **있는** 곳은 8곳뿐이고(하남1·속초4·홍천3·철원2·양양2·삼척1·정선1·인제1),
  **태백·횡성·영월·화천·양구는 공고 자체가 0건**이다 — 그 기간에 아파트 분양이 없었다.
  도구로 해결할 수 있는 문제가 아니다.
- 공고가 있어 예전에 시도했다가 실패한 9곳을 다시 두드려 보니 **5곳은 도메인이 아예
  사라졌다**(DNS 미해석). 분양이 끝나면 사이트를 닫는다 — 앞으로도 복구되지 않는다.
  1곳은 403, 3곳만 살아 있었다.
- 살아 있는 3곳 중 **홍천 대상 웰라움**에만 '타입별평면안내' 페이지가 남아 있어 3장을 받았고
  판별기가 2장을 평면도로 통과시켰다. 나머지 2곳(하남 modoo! · 철원 석미)은 큰 이미지가
  없거나 조감도 썸네일뿐이었다.
- **결론 — 청약홈 경로로 더 얻을 수 있는 것은 사실상 없다.** 남은 12곳을 채우려면 다른
  출처가 필요한데, 부동산 포털은 크롤링을 금지하고 있다(위 '포털 정찰' 참조).

**다른 경로도 전부 막혔다 — 다시 파지 말 것** (2026-08-14~15 전수 확인):

| 경로 | 결과 |
|---|---|
| 청약홈 | 경기 225곳 중 162곳 처리 · **도메인 사망 24곳** — 더 없다 |
| LH 청약플러스 | 평면도 **0장**(임대는 유니트 평면을 안 싣는다) |
| 웨이백 머신 | 77곳 시도 · 전부 이미 처리한 단지 — **새로 얻은 것 0** |
| 건설사 기업 지면 | 준공 단지 평면도를 안 남긴다 — **0** |
| 서희 GO집 | **0** |
| GS 자이 | 적중률 81% 인데 **`robots.txt` 가 `Disallow: /`** — 받은 31장 전부 지웠다 |
| **롯데캐슬** | robots `Allow: /` · 평면도 있음 — **2026-08-16 전수 확인 후 소진**(아래) |
| **힐스테이트** | `hillstate.co.kr` 이 **서비스 점검으로 다운**(robots 요청도 `/error.html` 로 간다). 2026-08-16 하루 두 번(09시·11시) 확인, 둘 다 다운 |
| **펜테리움**(금강주택) | **robots 가 `/upload/` 를 막는데 이미지가 전부 거기 있다** — 수집 불가 |

**2026-08-16 세 곳 전수 확인 — 여기서 더 나올 것은 사실상 없다.**

- **롯데캐슬** — 사이트가 **현재 분양·공사 중인 27곳만** 유지하고 준공 단지는 내린다.
  경원 관할 6곳 중 3곳은 이미 보유, 신규 3곳 중 평면도가 있는 곳은 **오산 위너스포레
  하나**였다(향남·평촌은 분양 전이라 '사업개요'뿐). 그 10타입은 색인에 넣었다.
  → 준공 단지가 목표라면 이 경로로는 안 된다. *"건설사 지면은 준공 단지 평면도를
  안 남긴다"* 가 **브랜드 사이트에서도 그대로**다.
- **펜테리움** — 도메인이 `penterium.kr`(브랜드) · `kkapt.co.kr`(금강주택)이다
  (`penterium.co.kr`·`daewonik.co.kr` 는 DNS 자체가 없다 — 옛 기록이 틀렸다).
  브랜드 사이트에는 분양·공사·입주 단지 지면이 제대로 있지만, **robots 의
  `User-agent: * → Disallow: /site/, /upload/`** 가 결정적이다. 실제로 이미지가
  `/upload/board/`·`/upload/banner/` 에서만 온다(입주단지 상세 실측: 큰 이미지 1장,
  경로 `/upload/board/…`). 게다가 **평면 메뉴 자체가 없다.**
  → **받으면 안 된다.** 자이에서 31장을 받아 놓고 나중에 robots 를 확인해 전부 지운 것과
  같은 상황이다. 순서는 언제나 **robots → 구조 → 검증 → 수집**이다.

- **단지명 검색으로 공식 출처를 찾는 것은 표본으로 재 보고 접었다** —
  시·연대로 고르게 뽑은 **50곳 중 브랜드 공식 출처가 있는 곳은 2곳(4%)**.
  90~2000년대 단지는 **브랜드 자체가 없다**(`.scratch/sample50.mjs`).
- **`robots.txt` 를 먼저 보고 나서 받는다.** 자이 이미지를 먼저 받아 놓고 나중에
  확인해 전부 지웠다. 순서는 **robots → 구조 확인 → 검증 → 수집** 이다.

수집·판별은 전부 로컬 `.scratch/`에서 하고(`npm run build:plans`가 색인과 이미지를 만든다),
저장소에는 줄인 이미지와 색인만 올라간다. 파이프라인 순서와, 밤새 돌려 알아낸 사실들:

1. **청약홈이 출발점이다** (`applyhome-list.mjs` → `applyhome-detail.mjs`).
   지역·기간으로 공고 목록을 주고, 상세 페이지가 **분양 홈페이지 주소**를 들고 있다.
   단지마다 웹검색하던 방식은 수십 곳이 한계였는데 이걸로 병목이 사라졌다.
   - 목록 필터 버튼은 글자가 **'조회'**(`#pbSearchForm .search_btn`)다. `/검색/`으로 찾으면
     사이트 통합검색(`konanSearchform`)이 잡혀 **조건이 한 번도 안 걸린다** — 실제로 그랬고,
     계속 "최근 12개월 1페이지"만 읽고 있었다.
   - 페이저는 `fn_link_page(n)`을 직접 부른다. 링크 글자를 찾아 누르면 10페이지 묶음을
     넘어갈 때 멈춘다.
   - 조회 가능 기간은 **2021-10 ~ 현재**뿐이다. 그 전에 입주한 아파트는 여기 없다.
   - 상세 URL은 `selectAPTLttotPblancDetail.do?houseManageNo=&pblancNo=`
     (목록만 `…View.do`다). 두 값은 목록 행의 `data-hmno`/`data-pbno`에 있다.
2. **홈페이지에서 이미지를 긁는다** (`grab-batch.mjs`). 호스트가 195개로 파편화돼 있어
   (단지마다 별도 도메인, 상당수 한글 IDN) 사이트별 스크래퍼는 불가능하다. 어느 사이트에나
   있는 관례만 쓴다 — 메뉴에 '평면/타입/유니트/세대안내', 그 페이지에 큰 이미지.
   - **"이미지 없음"의 진짜 이유는 대개 핫링크 차단이다**(2026-08-17 실측).
     `grab-state.json` 이 38곳을 *이미지 없음* 으로 적어 두었는데, 열어 보니 이미지가
     멀쩡히 있고 **받기가 403** 이었다. 브라우저에서는 1300×3421 로 디코드되는데
     직접 요청하면 거부된다. **지면 주소를 `Referer` 로 붙이면 200 으로 온다**
     (실측 638KB) — 브라우저가 그 지면을 그릴 때 보내는 것과 같은 요청이다.
     - **원인을 잘못 적으면 다시 시도할 근거를 잃는다.** *"받기가 거부됨"* 과
       *"그 단지에 도면이 없음"* 은 완전히 다른 말인데 둘 다 `이미지 없음` 이었다.
     - 이 버그는 **0장으로 끝난 곳에만 있었을 리가 없다** — 한 지면에서 일부만 403 이면
       그 단지는 '성공'으로 기록되고 **빠진 장수는 아무도 모른다.** 전수로 다시 볼 것.
     - `naturalWidth` 는 **디코드 전에는 0** 이라 크기 필터(`w>=450`)에 통째로 걸러진다.
       `img.decode()` 를 기다린 뒤 잴 것. `page.mouse.wheel` 은 커스텀 스크롤 컨테이너에서
       안 먹으므로 `window.scrollBy` 를 쓴다.
     - **robots 확인이 기존 수집기에는 없었다.** 재수집기(`.scratch/grab-retry2.mjs`)가
       호스트마다 보고 막힌 곳을 건너뛴다. 진단 도구는 `grab-why0.mjs`(왜 0장인가) ·
       `grab-deep1.mjs`(메뉴 한 단계 안쪽).
   - **메뉴 이름이 브랜드마다 다르다.** 실제로 본 것 — `평면정보`(동문) · `세대안내`
     (한신더휴·서한이다음) · `설계특화`·`단지설계`·`동호수배치도`·`특화설계`.
     뒤 셋은 **단지 배치도라 평면도가 아니다**(호반써밋에서 확인, 방 이름 0회).
3. **평면도인지 가린다** (`classify-plans.mjs`). 방 이름(거실·침실·주방·욕실…)이 **3종류
   이상** 읽히면 평면도다. 조감도·인테리어컷·배너에는 세 종류가 함께 안 나온다.
4. **주택형·전용면적을 읽는다** (`read-types.mjs`), **축척을 읽는다** (`scan-plans.mjs`).
5. **청약홈 주택형표로 메운다** (2026-08-09). 도면에서 OCR 로 추측하지 말고 **공식 수치**를 쓴다 —
   공고 상세의 "주택형" 표에 `084.9967A` 처럼 적혀 있고 **그 코드 자체가 전용면적**이다.
   - 도면 색인과 공고는 **주소(`addr`)로 잇는다** — 138곳 중 132곳(96%)이 정확히 붙는다.
     단지명은 표기가 달라 하나도 안 붙는다.
   - 도면 타입 `84A` ↔ 주택형 `084.9967A` 를 숫자+타입문자로 맞춘다. **후보가 여럿이면 손대지 않는다.**
   - 성과: 전용면적 결측 **289 → 184장**, 어긋나 있던 4장 정정, 주택형 미판독 165 → 154장.
   - **남은 154장은 도면에 주택형 글자가 아예 없다.** 후보를 3~8개로 좁힌 뒤 다시 OCR 해도
     7%(11/161)밖에 못 읽었다. 자유 판독이 아니라 후보 선택인데도 이 정도라면 글자가 없는 것이다 —
     더 파지 말 것. (`.scratch/fetch-types.mjs` → `ocr-types.mjs` → `match-types.mjs`)

### 주택형 이름은 **글자가 붙은 쪽**을 쓴다 (2026-08-16)

`read-types` 의 `type` 은 **전용면적을 반올림한 값**이라(84.98 → `85`) 84A·84B·84C 가
전부 `85` 로 뭉개지고 `-2`·`-3` 이 붙어 **`85 · 85-2 · 85-3`** 이 된다. 그러면 상담사가
화면에서 주택형을 고를 수가 없다 — 매장에서 부르는 이름은 `84B` 이지 `85-2` 가 아니다.
`read-types.mjs` 주석이 *"T1·T2 만 뜨면 고를 수가 없다"* 고 경고한 그 상태가 **숫자 형태로
이미 22개 단지에 퍼져 있었다**(고성 퍼스트뷰는 84·117·137 세 계열이 전부 그랬다).

그래서 `build-plan-index.mjs` 가 **OCR 이 숫자만 읽었고 파일명에 주택형 글자가 있으면
파일명을 쓴다.** 다만 파일명을 무조건 믿지 않는다 — **두 출처가 서로 검산한다**:
파일명이 말하는 숫자가 OCR 이 읽은 전용면적과 **2㎡ 안쪽**일 때만 채택한다.
그래야 `01_84a.jpg` 인데 실제로는 59㎡ 인 도면에서 엉뚱한 이름을 붙이지 않는다.

- **이름이 바뀌면 파일 경로가 바뀌고, 경로를 키로 쓰는 것이 함께 깨진다.**
  `public/plan-names.json`(방 이름 좌표)이 그렇다 — 86장이 옛 경로로 남아 **그 도면들이
  방 이름을 잃었다.** `node .scratch/ocr-room-names.mjs 4` 로 빠진 것만 다시 읽고
  (이미 있는 것은 건너뛴다) 쓰이지 않는 옛 키는 정리할 것.
- **경로가 그대로여도 `내용이 바뀌면` 좌표가 어긋난다**(2026-08-17). 방 이름 좌표는
  **이미지 대비 0~1** 이라 자르기가 달라지면 같은 파일명이라도 **이름표가 엉뚱한 방에 붙는다.**
  단지에 도면이 늘면 같은 주택형에서 **다른 크롭이 대표로 뽑혀** 기존 파일 내용이 바뀐다 —
  실제로 재수집 후 **44장이 바뀌었고 그중 40장이 옛 좌표를 들고 있었다.**
  `test-plans` 의 *"가장 넓은 곳이 거실"* 검사가 이걸 잡는다(실제로 `침실1` 로 뒤집혔다).
  - **고치는 순서**: `git status public/plans` 로 `M` 인 파일의 키를 **지우고** 나서
    `ocr-room-names.mjs` 를 돌린다. 그 도구는 **이미 있는 키를 건너뛰므로** 안 지우면
    옛 좌표가 그대로 남는다. 색인에서 사라진 키도 함께 정리한다.
  - 즉 **`build:plans` 를 돌렸으면 방 이름도 함께 봐야 한다.** 색인만 고치고 끝내면
    화면이 조용히 틀린 이름을 부른다.
- **빌더가 "예전보다 나빠진 크롭"을 막는다**(`KEEP_RATIO`, 2026-08-17). 같은 시트를 다시
  처리하면 `recrop` 이 다른 블록을 골라 **도면이 썸네일로 바뀌는 일**이 있다 —
  광명 호반써밋 74B **640×1180 → 200×125**, 홍천 대상 웰라움 84B 1032×1200 → 1173×270
  (가로로 긴 띠라 평면이 아니다). 짧은 변이 200px 면 인식 상한 1,200px 을 쓰지도 못하고
  **벽 두께가 1px 도 안 돼** 인식이 통째로 실패한다.
  - **크기 하한으로 자르면 안 된다** — 색인에는 `1197×142` 처럼 원래 가늘고 긴 정상 항목이
    많다. 가르는 것은 절대 크기가 아니라 **"이전 색인보다 넓이가 절반 미만이 됐는가"** 다.
  - **가드를 넣었다고 도는 것은 아니다.** 빌더가 맨 앞에서 `public/plans` 를 통째로 지워
    되살릴 예전 파일이 이미 없었고, 가드가 **한 번 헛돌았다**. 지우지 말고
    `public/.plans-prev` 로 **옮겨 두었다가** 다 만든 뒤 지운다.
- **자이 robots 는 깨져 있다 — Allow 목록을 읽어 주기로 했다**(2026-08-17 사용자 결정).
  `xi.co.kr` 의 robots 는 `Disallow: /` 다음의 **`Allow:` 가 값이 비어 있고** 허용 경로가
  **맨 줄로 나열**돼 있다. 문법대로면 전면 금지지만 우리가 쓰는 `/csh/`·`/PTG/` 가 그
  목록에 실제로 있다 — 사용자 결정은 **"허용해서 사용한다"** 이고, 구현은 **뜻대로 읽되
  근거 없는 곳은 그대로 막는** 쪽이다.
  - **무작정 무시하지 않는다.** 26곳 중 **자이 6곳만 열렸고** 나머지는 계속 막힌다 —
    Allow 목록에 없는 경로이거나, `Allow: /$`(첫 지면만) 뿐인 사이트다.
  - **지면마다 다시 봐야 한다.** 첫 지면만 검사했더니 `Allow: /$` 인 사이트에서
    **평면 지면까지 통째로 받았다**(데시앙·수자인). 지금은 페이지마다 판정한다.
  - **끝의 `/` 를 너그럽게 볼 것.** 규칙이 `/csh/` 인데 실제 주소는 `/csh` 로 온다 —
    이것 때문에 허용 목록에 있는 23곳이 그대로 막혔다.
  - 실익은 **거의 없었다** — 자이 이미지는 2026-08-08 수집분이 `.scratch/plans/` 에 이미
    있어 새로 받은 4장은 평면도가 아니었다. **이 결정의 값어치는 이미 갖고 있던
    140장(20단지)에 근거를 준 것**이다.
  - 되돌리려면 색인만이 아니라 `.scratch/plans/` 원본도 함께 지울 것 — 색인만 지우면
    다음 빌드에 되살아난다.
- **`scripts/test-plans.mjs` 가 이름으로 도면을 지목한다**(`plans/c39/84B.jpg` 등 12장).
  단지 id 는 **자리 순번이라 단지를 하나 추가하면 뒤가 통째로 밀린다.** 실제로 이번에
  밀려서 스위트가 `page.evaluate: undefined` 라는 **알 수 없는 오류로 죽었다** —
  지금은 맨 앞에서 12장이 제자리인지 먼저 확인하고, 없으면 **같은 주택형을 가진 단지를
  후보로 알려 준다.** 어느 파일이 그 도면인지는 **md5 로 대조**해 확정할 것(순서로 짐작하면
  틀린다 — 옛 `c39/84.jpg` 는 새 `c39/84B.jpg` 였다).
- **이 파일이 실측치를 인용할 때 쓰는 이름도 함께 바뀐다.** 아래 '디에트르 84' 는 지금
  색인에서 **`북수원이목지구 디에트르 더리체2차 84B`** 다(같은 이미지, md5 확인).

**치수가 인쇄된 도면은 8%뿐이다.** 세 가지로 확인했다 — ①수집 1,185장 중 치수 사슬이
잡힌 것 96장 ②입주자모집공고 PDF 12건에 유니트 평면도 0건 ③잘 그려진 평면도(오산세교
한양수자인 74A)에도 치수 없음. 분양 마케팅 사이트는 예쁘게 보이려고 치수를 지운다.
**그래서 색인 관문을 "치수가 있는가"가 아니라 "평면도인가"로 잡는다** — 축척은 어차피
사용자가 확정하게 돼 있어 치수가 없어도 쓸 수 있고, 치수로 거르면 900장을 버리게 된다.

축척 자동 판독(`scale-lib.mjs`)은 치수선이 **연속된 사슬**이라는 성질을 쓴다 — 이웃한 두
치수 글씨의 중심 거리는 두 치수의 평균(mm)에 비례한다. 주의할 점:
- **쌍마다 따로 내고 중앙값을 쓴다.** 전부 합쳐 Σmm/Σpx로 쓰면 전개했을 때 양 끝 두 점만
  쓰는 것과 같아져, 중간 숫자를 하나 놓치면 축척이 통째로 어긋난다.
- **PSM 12**(sparse text + 방향감지)여야 한다. 세로 치수는 90° 회전돼 인쇄되는데 PSM 3은
  못 읽는다. 못 읽으면 "가로·세로 교차검증"이 절반만 돌아 검증이 성립하지 않는다.
- 가로/세로는 **글자 상자 모양**(h > w)으로 가른다. 각도 추정이 필요 없다.
- **확신도가 '확실'·'보통'인 것만 색인에 싣는다.** '낮음'은 10~40% 틀릴 수 있는데, 20%
  틀리면 폭 700mm 냉장고를 840mm로 재는 셈이라 "들어갑니다"가 거짓이 된다.
  같은 단지의 도면은 축척이 비슷하게 나오므로(같은 시트 서식) 중앙값에서 15% 넘게 벗어난
  것도 뺀다 — 서로 검산해 주는 공짜 표본이다.
- 주택형 글자는 **A~H·P·T·S만** 받는다. 아무 대문자나 받으면 "75.7115㎡"가 "75715m"으로
  읽혀 76M이 되고 "159세대"가 "159M|CH"로 읽혀 100M이 된다.

**오류를 판정 결과로 저장하지 말 것.** OCR 탭이 죽으면(`Target crashed`) 그 워커가 이어서
맡은 이미지가 전부 같은 오류로 끝나는데, 예전 판은 그걸 그대로 기록했고 `plan` 필드가 없으니
**"평면도 아님"으로 읽혔다.** 수원 224장이 통째로 이렇게 사라졌고, 파이프라인은 "평면도
568장"이라고 자신 있게 보고했다. 지금은 페이지를 다시 띄우고 그 작업을 큐로 되돌린다.

### 알아둘 함정

- **같은 카테고리인데 모델 세대에 따라 이격이 다른 경우가 있다.** 에어드레서는 현행 라인업
  (`DF*0H24R**`·`DF24CB`·`DF18CB`) 좌우 2.5mm, 구형 Bespoke(`DF10A9500CG`·`DF60A8500CG`) 14mm로
  **5.5배** 차이라 나란히 설치하면 판정이 뒤집힌다. `CLEAR_BY_MODEL`로 모델코드에 따라 갈라진다.
- **냉장고 키친핏은 벽 이격이 아니라 냉장고장 내측 이격**이고, **키친핏 안에서도 라인업에 따라
  갈린다**(`CLEAR_BY_LINE` 2단): 키친핏 Max·1도어 좌우 각 **4mm** / Infinite·구형 Bespoke
  4도어 각 **12mm**. 기본값은 프리스탠딩 후면 50mm.
  - **한 값(12mm)으로 뭉뚱그리지 말 것.** 그러면 키친핏 Max의 4mm가 3배로 부풀려진다 —
    "이렇게 좁은 자리에도 들어갑니다"가 이 라인업을 파는 이유인데 정반대로 재는 셈이다.
  - 여기서 **이격을 크게 잡는 것은 안전한 방향이 아니다.** 프리스탠딩 후면 50mm는 방열 공간이라
    모자라면 사고지만, 키친핏 이격은 냉장고장 안에 들어가는 **끼움 여유**다. 크게 잡으면
    들어갈 자리를 "안 들어갑니다"라고 말하게 되고 그 손실이 더 크다. `test-place.mjs`가 검사한다.
- **벽 스냅의 법선 부호는 "사용자가 클릭한 쪽"** 으로 정한다(`snapToWall`). 벽 각도만 보고 +90°로
  정하면 벽 선분의 점 순서에 따라 가전이 방 밖에 놓인다. 자동 배치는 방 중심 기준(`inwardNormal`).
- **실외기는 실내기와 이격 기준이 다르다**(`OUTDOOR`: 전면 500 · 후면 150 · 좌우 100).
- 겹침 판정은 SAT. **변끼리 맞닿는 것은 겹침이 아니다** — 그렇지 않으면 나란히 붙여 놓을 수 없다.

### 도어 오픈 공간 (`DOOR_ZONE`) — 하드 판정이 아니라 주의 경고

문을 열었을 때 **옆으로** 튀어나오는 공간. 설치환경 가이드에 수치가 적힌 유형만 넣는다.

- **양문형** 좌우 각 407mm — "도어 오픈 시 전체 폭 1,726mm" − 본체 912mm (RS63R557EB4·
  RS84B508115 설치 규격도). 814~841L급(RH81R9151S8)은 1,670mm라 각 379mm.
- **4도어 프리스탠딩** 좌우 각 295mm — "도어 측면 최소 여유 246~295mm(모델별 상이)" 중 큰 값.
- 유형 판별은 `size-reps`의 `group` 문자열로 한다(`양문형 2도어`, `4도어 (2026 카탈로그)` 등).

**하드 판정으로 만들지 말 것.** ①힌지 방향(좌/우)을 알 수 없어 양쪽에 그린다 ②4도어는 모델별
편차가 있다 ③이격거리처럼 걸면 실제 주방 대부분이 "배치 불가"가 되어 도구가 쓸모없어진다.
`it.soft[]`에 담아 주황 점선으로 표시하고 "실측 확인"으로 안내한다.

전면으로 열리는 깊이(콤보 도어 오픈 시 1,430mm 등)는 `CLEAR`의 `front`에 이미 들어 있다.
뚜껑형 김치냉장고·통버블 세탁기는 **위로** 열려 평면에 영향이 없어 `TOP_OPEN` 안내만 붙인다 —
평면 구역을 만들면 안 된다(`test-place.mjs` [11]이 검사).

### 3D 보기 (`Place3D`, 2026-08-11 신설)

**목적은 "고객에게 우리 집 느낌을 보여주는 것"이다**(2026-08-11 사용자 확정). 높이 방향
간섭 판정이 아니다 — 그쪽을 노렸다면 상부장·천장·배관을 넣어야 하지만 그러지 않았다.

- 코드는 `place-app.html` 맨 아래 **별도 `<script type="module">`** 에 있다. three.js 가 ESM 이라
  위 고전 스크립트에서 import 할 수 없다. 모듈은 defer 라 `window.__place` 가 이미 있다.
- **three.js 는 `public/vendor/` 에 받아 두고 상대경로로 부른다. CDN 으로 되돌리지 말 것** —
  매장 전파가 약해도 열리게 하려고 서비스워커까지 두는 앱이다(QR 을 직접 그리는 것과 같은 이유).
  `sw.js` 가 `/vendor/` 를 캐시 우선으로 잡는다.
- **바닥에 원본 도면 이미지를 그대로 텍스처로 입힌다.** 도면에 이미 그려진 마루색·타일·가구가
  살아나 자산을 만들지 않고도 "우리 집"이 된다. 2D 에서 *"원본을 정리본으로 갈아 끼우면
  현장감이 무너진다"* 는 지적을 받은 것과 같은 원리다.
- **좌표계 — 앱 월드 (x, y)mm 를 (x, 0, y)m 로 보낸다.** y 는 화면 아래쪽이고 3D 는 Y 가 위다.
  흔한 방법인 "XY 평면에 만들고 X축 −90° 회전"을 쓰면 **z 부호가 뒤집혀 평면도가 좌우 반전된다.**
  방이 대칭이면 화면으로는 똑같아 보여 눈으로 못 잡으므로, 회전에 기대지 않고
  `ShapeUtils.triangulateShape` 결과를 XZ 에 직접 얹고 법선만 검사해 감기를 뒤집는다.
  `test-plans.mjs` 가 바닥 정점을 꺼내 부호까지 대조한다(뒤집어서 실제로 물리는 것을 확인함).
- **앞을 막는 벽은 숨긴다**(`cullWalls`, 돌하우스 처리). 벽을 다 세우면 어느 각도에서도 앞벽이
  방을 가린다. 어느 쪽이 바깥인지는 **방 경계상자 중심**으로 알고, 카메라가 그 바깥쪽에 있으면
  감춘다 — 매 프레임 내적 하나다.
- **개구부(문·창)는 세우지 않고 위에 인방(2,100mm)만 남긴다.** 도면만으로는 문인지 창인지,
  창이면 아래턱이 몇 mm 인지 알 수 없다. 유리를 그리면 문을 창으로 단정하게 되고, 통째로
  비우면 방이 안 닫혀 보인다. **지어내지 않는 쪽**을 골랐다.
- **층고 2,300mm·벽 두께 60mm 는 가정이다** — 평면도에 없는 값이라 화면(`#d3bar`)이 층고를
  밝힌다. 도면을 아무리 모아도 이 값들은 나오지 않는다.
- **가전은 실측 W×H×D 상자 + 최소 디테일.** 2D `drawGlyph` 와 같은 철학 —
  **치수는 정확하게, 외형은 단순하게.** 삼성 공식 3D 모델을 구할 수 없으므로 그럴듯한 곡면을
  지어내지 않는다. 지어낸 외형은 고객이 실물로 오해한다.
  - **디테일 어휘를 2D 와 똑같이 나눈다** — 같은 품목이 앱마다 다른 그림이면 눈이 헤맨다:
    냉장고·김치·냉동·업소용 도어선+손잡이(폭 800㎜ 또는 제품군으로 통짜/양문) ·
    세탁기·콤보·건조기·에어드레서·슈드레서 원형 도어(**통버블은 위로 열려 사각**) ·
    TV·모니터 화면 · 사운드바 그릴 띠 · 인덕션 윗면 화구 4개 ·
    식기세척기·전자레인지/오븐·데이코 앞면 도어패널+손잡이 ·
    공기청정기·청소기·정수기·제습기 **윗면** 원(2D 가 위에서 본 원을 그리므로) · 에어컨 토출구 띠.
  - **2D 에 실루엣이 없는 품목(리빙 비데·선풍기·안마기·욕실케어)은 3D 에도 만들지 않는다.**
    2D 에 추가하게 되면 그때 3D 도 함께 넣을 것.
  - `test-plans.mjs` 가 **양방향으로** 검사한다 — 2D 가 그리는 품목인데 3D 가 흰 상자면 실패,
    2D 에 없는 품목에 3D 디테일이 붙어도 실패(지어낸 외형). 현재 **24개 전부**에 디테일이 있다.
- **접지 그림자는 바닥에 놓는 것에만 붙인다**(2026-08-17). 그림자가 없으면 가전이 바닥에
  얹혀 있지 않고 **떠 보인다** — "우리 집 느낌"이 가장 크게 깨지는 지점이다. 그렇다고
  **그림자 맵을 켜지 않는다**: 바닥이 원본 도면 텍스처라 전체가 어두워지면 마루색·가구가
  죽는다(조명을 부드럽게 둔 것과 같은 이유). 발밑에만 부드러운 원을 깐다.
  - **벽에 건 TV·천장 카세트에는 붙이지 않는다** — 발자국 자리에 그림자가 생기면 없는
    물건이 거기 있는 것처럼 보인다.
  - 텍스처·재질은 **한 장을 만들어 모두가 나눠 쓴다**(제품마다 만들면 GPU 자원이 샌다).
  - `userData.isShadow` 로 표시해 **검사가 '디테일' 로 세지 않게** 한다 — 안 그러면
    모든 품목이 디테일을 가진 것으로 잡혀 "2D 에 없는데 3D 에 붙었다"가 헛되이 뜬다.
- **대기(staged) 중인 가전은 세우지 않는다.** 아직 놓은 것이 아니다(2D 가 회색 점선으로
  구분하는 것과 같다). `test-plans.mjs` 가 검사한다.
- 시점 프리셋 셋 — **둘러보기 / 눈높이 / 위에서**. '위에서'는 2D 평면도와 같은 구도라
  2D ↔ 3D 를 눈으로 잇게 해 준다.
- **가전 이름표는 기본으로 끈다**(`이름` 버튼). 고객에게 보여주는 화면이라 글자가 떠 있으면
  "우리 집 느낌"이 흐려진다(2D 에서 벽 길이를 기본으로 끈 것과 같은 이유). 그래도 흰 상자가
  여럿이면 상담사가 헷갈리므로 켜는 손잡이를 뒀다.
  - **모델코드는 적지 않는다** — 카테고리와 사이즈까지만(2D 라벨과 같은 규칙).
  - 켜짐은 **글자가 아니라 색**으로 알린다("이름 표시/이름 끄기"로 쓰면 라벨이 길어져
    폰에서 버튼이 두 줄로 접힌다). 좁은 화면(≤520px)에서는 버튼 크기도 줄인다 —
    안내띠가 화면의 20%를 먹으면 정작 보여줄 3D 가 그만큼 밀린다. 실측 140 → **94px**.
- 한 손가락 **회전**, 두 손가락 확대. 2D 는 한 포인터가 '이동'이라 관례가 다른데, 3D 에서
  가장 하고 싶은 일이 "돌려 보기"라 이렇게 뒀고 안내띠가 그 사실을 적는다.
- **가전을 누르면 가전이 움직이고, 빈 곳을 끌면 카메라가 돈다**(2026-08-12). 보기 전용이면
  옮기려고 2D 로 돌아가야 하는데 그러면 고객 앞에서 화면이 왔다 갔다 한다. 2D 에서
  "가전을 누르면 모드와 상관없이 가전이 반응한다"고 정한 것과 같은 규칙이다.
  - 바닥면(y=0)에 광선을 쏘아 교점을 새 자리로 삼고, **판정은 2D 와 같은 함수**를 쓴다
    (`nudgeInside`·`hitsOther`·`retagRoom`) — 3D 에서만 되는 배치가 생기면 안 된다.
  - 겹침은 **놓는 순간에만** 본다. 끄는 내내 막으면 손에서 자꾸 빠진다(2D 와 같은 이유).
  - **`build()` 는 요청할 때만 화면을 다시 맞춘다**(`build(refit)`). 예전에는 끝에서 늘
    `view()` 를 불렀는데, 끄는 동안 매 프레임 build 가 돌기 때문에 **카메라가 계속 제자리로
    튕겨** 나갔다. 문 열기·이름표를 켜도 보던 각도를 잃었다. `test-plans.mjs` 가 지킨다.

#### 도어 열기 (`DOOR_OPEN`·`doorLeaves`·`doorHits`, 2026-08-11)

*"냉장고는 문을 열었을 때와 닫았을 때 규격이 다르다"*(사용자). 3D 에서 문을 열어 보고
부딪히면 **빨갛게** 칠하고 "이 배치로는 안 됩니다"라고 말한다.

- **값은 설치가이드 원문 실측에서만 온다.** 원문이 두 방식으로 준다:
  `side` = "도어 오픈 시 **전체 폭**"(냉장고, 옆으로) → (전체폭 − 본체폭)/2 ·
  `front` = "**깊이**(도어 오픈 시)"(세탁기, 앞으로) → 오픈깊이 − 본체깊이 · `top` = 위로(평면 무관).
  계산 결과 — **양문형 좌우 각 407mm**(1,726−912) · **4도어 각 291mm**(1,498−916) ·
  **콤보 앞 555mm**(1,430−875) · 워시타워 앞 513mm. 각도도 원문에 있다(양문형 170° ·
  4도어 125° · 키친핏 105~108° · 김치 스탠드 130° · 뚜껑형 92°).
- **건조기는 단독 실측이 원문에 없어 같은 규격대 콤보 값을 준용했다**(`weak:true`).
  실측이 나오면 바꿀 것.
- **열리는 도중을 봐야 한다**(`doorHits` 가 12단계로 훑는다). 다 열린 자세만 보면 놓친다 —
  4도어 문은 **90° 를 지날 때 앞으로 가장 많이 나오고(511mm) 다 열리면 오히려 덜 나온다(419mm)**.
  그 사이에 있는 물건은 스윕을 봐야만 잡힌다. `test-plans.mjs` 가 **다 열린 자세로는 안 걸리는
  배치**로 이걸 검증한다(그 검사가 무의미해지면 스스로 실패한다).
- **힌지 방향은 카탈로그·도면 어디에도 없다.** 문짝이 둘인 제품(양문형·4도어)은 좌우 대칭이라
  문제가 없고, 하나인 제품은 기본 오른쪽에 `it.hinge` 로 뒤집을 수 있게 뒀다. 좌우를 단정하면
  절반은 틀린 그림이 된다.
- **판정 강도를 두 단계로 나눈다** — 이게 CLAUDE.md 의 기존 결정("도어 오픈을 하드 판정으로
  만들지 말 것")과 부딪히지 않는 이유다:
  - **배치할 때**는 지금처럼 `DOOR_ZONE` → `it.soft` 주황 주의. 힌지를 몰라 양쪽에 표시하는
    값이라 그걸로 막으면 실제 주방 대부분이 "배치 불가"가 되어 도구가 쓸모없어진다.
  - **3D 에서 문을 여는 것은 상담사가 직접 한 행동**이므로 그 자리에서 하드하게 말한다.
    고객에게 "문 열면 안 됩니다"를 보여주는 것이 이 기능의 목적이다.
- 3D 그림과 판정은 **같은 다각형**(`doorLeaves`)을 쓴다. 다르면 화면에서는 부딪히는데 경고가
  없거나 그 반대가 된다.
- **나란히 벽에 붙은 두 제품은 냉장고 문이 서로 안 부딪힌다** — 문이 앞으로 젖혀지기 때문이다.
  처음에 그런 배치로 시험해 놓고 "판정이 안 된다"고 오해했다. 실제로 막히는 것은
  **구석(옆 벽)** 과 **앞에 놓인 물건**이다.

#### 3D 결과 공유

'결과 저장'은 **화면에 보이는 것을 그대로** 내보낸다 — 3D 를 보고 있으면 3D 가 나간다.
고객에게 보여준 그림과 손에 남는 그림이 다르면 "내가 본 것과 다르다"가 된다.

- **내보내기 전에 반드시 `Place3D.render()` 를 부른다.** `preserveDrawingBuffer` 는 **마지막으로
  그린 프레임**을 보관할 뿐이라, 장면을 바꾼 직후 캔버스를 복사하면 바뀌기 전 그림이 나간다 —
  실제로 문을 연 직후 공유했더니 **문이 닫힌 냉장고**가 찍혔다.
  `test-plans.mjs` 가 문 열기 전후 카드가 서로 다른지로 이걸 지킨다.
- 카드에 **층고가 가정값임**과 **문 열림 여부**를 함께 적고, 막히는 제품이 있으면
  빨간 줄로 이름까지 적는다. 화면에서 보여준 경고가 카드에서 사라지면 안 된다.

**도면 수집은 3D 의 전제가 아니다.** 3D 가 먹는 것은 벽 다각형과 확정된 치수인데 그건
앱이 이미 만든다(도면 인식 / 벽 그리기 / 안방 가로 입력 — 세 갈래 중 아무거나). 도면
라이브러리는 "상담 시작을 빠르게" 하는 커버리지 기능이다. 2026-08-11 사용자와 이 점을
정리하고 **3D 를 도면 수집보다 먼저** 하기로 했다.

### 미구현

도어 스윙을 호(arc)로 그리지 않고 직사각형 구역으로만 본다. 힌지 위치와 개폐 각도를 함께
확정할 수 있는 자료가 나오면 호로 바꿀 수 있다(김치냉장고 스탠드형 130°, 키친핏 Max 108°처럼
각도만 있는 경우가 있으나 힌지 위치가 없어 아직 못 그린다).

## 사이즈별 대표모델 치수 (`public/size-reps.json`)

배치 시뮬레이터가 쓰는 근거 데이터. `scripts/build-size-reps.mjs`가 모델파인더 DB에서 추출한다
(`npm run build:sizereps`). 현재 **68개 사이즈 / 24개 카테고리**(기본 노출 46줄).

- **치수 라벨은 화이트리스트(`DIM_LABELS`)로만 읽는다.** 카탈로그에 `W×H×D`와 `H×W×D` 두 표기가
  섞여 있어 순서대로 읽으면 **폭과 높이가 뒤바뀐다**. 화이트리스트에 없는 라벨은 넣지 않는다.
  `W×H×D(실외기포함)`은 두 물체를 합친 값이라 배치 단위로 쓸 수 없어 제외했다.
- 한 제품이 여러 덩어리로 설치되는 경우(에어컨 실내기/실외기, TV 스탠드/벽걸이, 청소기 본체/
  청정스테이션, 데이코 본체/설치 개구부)는 `parts[]`로 나눠 담는다.
- **2단 구조** — 1단은 사람이 고르는 단위(`TV 65형`, `냉장고 602~640L`), 2단(`options[]`)은 그 안에서
  발자국이 다른 실제 모델. **화면에는 1단만 한 줄씩 보여준다**(`options[0]` = 발자국이 가장 큰 것).
  2단은 정보줄에서 편차(`spreadW`/`spreadD`)를 밝히는 데 쓴다.
  - 여기까지 세 번 바뀌었다. ①옵션을 전부 펼쳐 TV 44줄 → 고를 수 없다 ②발자국이 비슷한 것끼리
    묶어 73줄 → 여전히 길고 한 사이즈가 여러 줄로 갈라진다 ③**사이즈당 한 줄**(현재).
    ③으로 되돌린 근거는 사용자 판단이다 — *"대표모델의 목적은 도면에 넣었을 때 대략적인 느낌을
    보는 것이지 정확히 이렇다고 표시하려는 게 아니다."* 즉 사이즈 안의 깊이 편차(65형 222~499mm)는
    이 도구가 감당할 오차로 본다. **다시 쪼개지 말 것.**
- 1단 기준은 카테고리별로 다르다 — TV는 인치, 공기청정기는 면적, **김치냉장고는 용량(L)**,
  **냉장고는 도어 구성**, **에어컨은 설치 형태**, **콤보는 세탁/건조 용량**, 나머지는 폭(W).
  - 냉장고를 폭으로 묶으면 "폭 910mm" 한 줄에 602~905L이 들어가 **용량 표시가 무의미해진다**
    (사용자가 잡아낸 오류다). 상담도 "34평이면 900리터급"으로 진행되지 폭으로 진행되지 않는다.
  - 용량이 10% 안에 들면 한 줄로 묶고 **규격이 가장 큰 것을 대표로** 쓴다("비슷하면 큰 사이즈로").
  - 통합 축은 `MERGE_AXIS`가 정한다. 용량이 DB에 없어 폭 키로 떨어진 항목이 섞이므로
    **640(L)과 700(mm)을 같은 수직선에 놓지 않도록** 종류가 다르면 묶지 않는다.
- **냉장고는 도어 구성으로 고른다**(2026-08-09 사용자 결정) — *"4도어 프리스탠딩, 4도어 키친핏,
  양문형, 1도어세트(냉장+냉동), 1도어세트(냉장+냉동+김치), 이런 식으로만 되도 됩니다."*
  용량으로 고르던 때는 한 줄에 형태가 섞였다(845~905L = 912×930 **4도어** + 912×892 **양문형**).
  매장에서 "몇 리터"보다 먼저 정해지는 것이 형태이고, 형태가 곧 발자국이라 배치 판정과도 맞는다.
  - 형태는 **제품군 문자열과 모델 접두 둘 다**로 가른다. 12개 조합 전수 확인 결과 둘이 완전히
    일치한다(RR=1도어 · RS=양문형 · RB/RT=일반형 · RM=4도어). 제품군이 '카탈로그 수록 모델
    (2026)' 처럼 형태를 안 밝히는 항목도 접두로 갈린다.
  - **접두만으로는 부족하다.** 같은 RM 시리즈에 4도어와 상냉장하냉동이 함께 있다 —
    `RM70F91R1AP`(700×1853×685)는 제품군도 형태를 안 밝힌다. 이대로 두면 4도어 줄의 옵션이
    되어 자리가 모자랄 때 **"같은 4도어 중 700×685 규격이면 들어갑니다"라는 없는 제품을
    안내한다.** DB 안에서 검증되는 경계로 자른다 — 4도어로 명시된 것은 전부 912㎜, '일반형'은
    700·595㎜ 라 **800㎜** 로 가른다.
  - 설치 방식은 **키친핏일 때, 그리고 짝이 있는 4도어일 때만** 이름에 넣는다. 양문형·일반형은
    키친핏 짝이 없어 '양문형 프리스탠딩' 이 군더더기다.
  - **한 형태에 줄이 둘 이상이면 용량을 붙여 가른다**(정렬이 끝난 뒤에 — 이름이 바뀌면
    `NAME_ORDER` 가 못 찾는다). 형태가 하나뿐이면 붙이지 않는다.
- **한 줄 안의 깊이 편차는 `fitAlternative` 가 감당한다.** 4도어 프리스탠딩 한 줄에 깊이
  683~930㎜(247㎜ 차이)가 함께 들어왔다. 예전 `fitAlternative` 는 줄 대표(`options[0]`,
  가장 큰 것)만 봐서 **"같은 4도어인데 얕은 모델이면 들어갑니다"** 를 말할 수 없었는데,
  상담에서 가장 쓸모 있는 답이 그것이다. 지금은 **옵션 단위로** 후보를 만든다.
  - 같은 줄 안의 대안은 **10%(`MERGE_SPAN`) 이상 작을 때만** 말한다. 그냥 "더 작은 것"으로
    두면 912×930 대신 **912×922** 를 내미는데, 8㎜ 차이는 사실상 같은 제품이다.
  - 이름이 같으므로 `same` 플래그를 함께 돌려주고, 화면이 *"같은 4도어 프리스탠딩 중
    912×716mm 규격이면 들어갑니다"* 로 말한다. 다른 줄이면 이름만 말한다.
- **`BY_AREA` 의 냉장고는 평형과 무관하게 '4도어 프리스탠딩'이다.** 1단이 도어 구성으로
  바뀌며 4도어가 한 줄이 됐기 때문인데, 그래도 상담이 어긋나지 않는다 — 작은 주방에
  912×930 이 안 들어가면 위의 대안 제시가 이어 준다. 평형별로 용량을 단정하는 것보다
  근거가 분명하다. **여기 적힌 이름이 `size-reps.json` 에 없으면 그 카테고리가 조용히
  추천에서 빠진다** — `test-plans.mjs` 가 잡는다.
- **공기청정기는 스탠드형만 쓴다**(2026-08-09 사용자 결정: *"공기청정기는 벽걸이형을
  제외하고 스탠드형으로만 업데이트해주세요"*).
  `AX99N4020WWD`(블루스카이 4000 **벽걸이형**)는 1050×600×130 이라 위에서 내려다보면
  사운드바 같은 긴 막대가 된다 — 치수가 틀린 것이 아니라 벽에 거는 물건이라서 그렇다.
  청정면적 85㎡ 로 가장 커서 대표로 뽑히는 바람에 그것이 도면에 올라갔다.
  - `hidden:true` 로 표시하고 **`loadReps` 에서 한 번에 거른다.** 드롭다운에서만 숨기면
    "안 들어갑니다" 뒤에 그것이 **대안으로 튀어나온다** — 목록·추천·대안이 같은 목록을
    봐야 한다. **데이터에서 지우지는 않는다**(치수와 출처를 되짚을 수 있어야 한다).
  - 에어컨 벽걸이는 사용자가 "한 줄로 합쳐 남긴다"고 따로 정했으므로 숨기지 않는다.
  - **벽에 거는·창에 끼우는 줄은 목록 맨 뒤로 보내고, 거실 자동 추천에서도 뺀다**
    (`/벽걸이|창문형/`). 발자국 폭으로 줄세우면 벽걸이가 맨 위에 서고, 면적만 보면
    미리 체크까지 된다.
- **에어컨은 냉방면적이 아니라 설치 형태로 고른다**(2026-08-09 사용자 결정) —
  *"에어컨 실내기 크기는 거의 동일하니 슬림형인지 와이드형인지, 클래식인지, Q9000인지
  4가지로만 구분하면 될 것 같습니다."* 실제로 무풍 클래식(AF70F)과 Q9000 클래식(AF60F)은
  363×1,883으로 발자국이 **완전히 같아** 냉방면적으로는 애초에 구분이 안 된다.
  6줄이 된다 — 슬림형·와이드형·클래식·Q9000 스탠드 + **벽걸이 + 창문형**.
  - 설치 형태는 **모델 접두**로 가른다(AF=스탠드 · AR=벽걸이 · AW=창문형). 제품군 문자열로
    가르면 `갤러리 스탠드 구형 (2024) / 창문형` 처럼 둘이 한 칸에 적힌 항목에서 뒤집힌다.
  - 슬림/와이드는 이름으로 못 가른다 — AF80F(492㎜)와 AF90H(360㎜)가 **둘 다 '무풍콤보 갤러리'** 다.
    실제로 가르는 것은 폭이므로 **폭 450㎜**로 가른다.
  - **벽걸이는 한 줄로 합치고 대표 치수는 가장 큰 것**(1099×292×215)을 쓴다. 폭이 799~1099㎜로
    벌어지지만 작게 재서 "들어갑니다"를 거짓으로 말하는 쪽이 더 위험하다(사용자 선택).
- **콤보는 세탁/건조 용량으로 부른다** — *"콤보는 25/21kg 이런 식으로 대략적으로 맥락만
  확인하는 걸로."* 세탁기는 그대로 폭이다(위에 건조기를 적층하는 상담이 폭으로 진행된다).
- **사이즈 기준이 이름인 카테고리는 통합에서 뺀다**(`NAMED_SIZE`, `named:true`).
  에어컨은 슬림(360)·클래식(363)·Q9000(363)이 폭 10% 안에 들어와 한 무리로 묶이는데,
  그러면 라벨이 `슬림형 스탠드~Q9000 스탠드` 가 되어 구분이 통째로 사라진다.
  **화면 정렬도 폭 순이 아니라 데이터 순서**를 그대로 써야 한다(`named` 플래그) —
  폭으로 줄세우면 벽걸이(1099) → 와이드형(492) → 창문형(402) 순이 되어 스탠드 사이에
  창문형이 낀다. 데이터 순서는 `NAME_ORDER` 가 정한다.
- **이름 사이즈로 바꾸면 "숫자를 이름에서 읽던 곳"이 전부 조용히 깨진다.** 예외도 경고도
  없이 값만 틀려 상담에 그대로 나가므로, 바꿀 때 아래 세 곳을 반드시 함께 볼 것:
  1. 통합 라벨(`family`/`numOf`) — `25/20kg` 을 2520 으로 읽어 `685~2520` 이 나왔다.
     한 값 안에 숫자가 둘인 표기는 **자리별로** 범위를 잡는다(`25/18~20kg`).
  2. 추천(`recommendPicks`) — 냉방면적이 전부 0 이 되어 폴백이 엉뚱한 줄을 가리킨다.
     **㎡ 가 붙은 `specs` 값에서만** 읽을 것. 아무 숫자나 읽으면 **'Q9000 스탠드'가 9,000㎡** 다.
     폴백도 `list[list.length-1]` 이 아니라 **가장 큰 것**이어야 한다 — 정렬이 바뀌면
     가장 큰 거실에 창문형이 붙는다.
  3. 사양 꼬리 단위(`specText`) — 에어컨 냉방면적은 DB 에 단위 없는 숫자다. 예전에는 사이즈
     키가 `냉방 62.6㎡` 라 단위가 보였다. 사이즈·옵션 두 곳에 쌓이므로 한 함수로 묶는다.
- **에어컨은 냉방면적을 화면에 적지 않는다**(2026-08-09 사용자: *"에어컨의 경우 냉방면적은
  없어도 됩니다"*). 고르는 기준이 설치 형태로 바뀌었으니 면적은 상담을 헷갈리게만 한다.
  **데이터에는 남긴다** — 추천이 그 값으로 평형을 맞춘다.
- **거실 에어컨 추천은 스탠드만 본다.** 설치 형태로 줄이 갈리면서 냉방면적만 보면 작은 거실에
  **창문형**이 추천된다 — 창에 끼우는 물건이라 거실 상담에 올릴 것이 아니다. 벽걸이는 침실
  몫인데 방별 추천이 아직 없다. `test-place.mjs` 가 회귀 검사한다.
- **키친핏 1도어는 세트 구성 3줄을 따로 만든다**(`SETS`, `set:true`). 1도어는 한 대만 놓는 물건이
  아니라 같은 캐비닛을 나란히 붙여 냉장고장 한 벌을 만드는 상품이라, 자리를 재는 단위가 한 벌이다.
  냉장+냉동(**1,196㎜**) / 냉장+냉동+김치(**1,797㎜**) / Infinite 냉장+냉동+와인+김치(**2,398㎜**).
  냉장고장 내경으로는 좌우 각 4㎜를 더해 1,204 / 1,805 / **2,406㎜**.
  - 1도어 키친핏은 **냉장·냉동·김치·와인이 전부 595 × 1,855 × 688㎜ 같은 캐비닛**이다.
    설치환경 가이드('냉장고 1도어') 소제목과 규격도 캡션이 그렇게 적고 있고, DB의
    냉장 `RR40C8995APG`와 김치 `RQ34C8945APG`의 치수가 완전히 같아 이를 뒷받침한다.
    빌드가 두 모듈 규격이 어긋나면 예외를 던지니 카탈로그 갱신 시 바로 드러난다.
  - 폭 = **모듈 폭 × N + 제품 간 6㎜ × (N−1)**. 외곽 이격(좌우 각 4㎜)은 여기 더하지 않고
    `CLEAR_BY_LINE`이 붙이며, 합친 냉장고장 내경만 `note`에 함께 적는다.
  - **간격 값은 반드시 `냉장고 페어(2대 이상) 설치` 엔트리의 라인업별 표에서 가져올 것.**
    그 표가 유일하게 라인업을 갈라 적고 있다(개별 카테고리 엔트리는 한쪽 라인업 값만 적혀 있어
    그것만 보면 틀린다 — 실제로 두 번 틀렸다):

    | 라인업 | 제품 간 | 냉장고장 내측 좌우 |
    |---|---|---|
    | 신형 Bespoke 키친핏 Max · 1도어 | **6㎜** | **각 4㎜** |
    | Infinite Line · 구형 Bespoke 4도어 | 10㎜ | 각 12㎜ |

  - **세 번 틀렸고 전부 사용자가 잡아냈다.** ①김치 모듈을 695㎜(키친핏 313L `RQ33*`)로 잡음 —
    그 제품은 1도어 조합 모듈이 아니다 ②모듈 사이에 5·12㎜(바깥 이격)를 넣어 이중 계산
    ③제품 간을 구형 10㎜로 계산 — 좁은 간격이 이 상품의 강점인데 그걸 스스로 지웠다.
  - 세트는 **폭 > 높이**가 정상이라 `test-sizereps.mjs`의 축 뒤바뀜 검사에서 뺀다(업소용 냉장고와 같다).
    4세트 폭 2,410㎜는 골든값으로 고정돼 있다 — 계산값이라 틀어져도 눈에 안 띄기 때문이다.
- **김치냉장고 4도어 키친핏은 카탈로그에 없고 설치가이드에만 있다**(`guideOnly:true`).
  모델파인더의 김치냉장고 중 키친핏은 313L(`RQ33*`, 695㎜)뿐이고, 폭이 비슷한
  `RK80F49E1A`(799 × 1,853 × 794)·`RK70F49F1DD`(799 × 1,825 × 787)는 **스펙표에
  `설치 유형 = 프리스탠딩`으로 명시**돼 있다(이 필드를 안 보고 "폭만으로는 못 가른다"고
  판단했다가 정정했다). 그래서 카탈로그만 보면 이 제품이 통째로 없다.
  - 치수는 설치가이드 **세 곳**에 있고 서로 검산된다 — 규격도 캡션 2건이 **폭 795㎜ ·
    제품 높이 1,853㎜**, '김치냉장고' 엔트리가 **냉장고장 가로 803㎜ 이상 · 좌우 이격 4㎜**.
    803 = 795 + 4 + 4 로 정확히 맞는다.
  - **깊이 700㎜는 제품 깊이가 아니라 냉장고장 요건**이다(가이드에 제품 깊이가 없다).
    화면이 그 사실을 밝힌다 — 제품 깊이인 척하지 말 것.
  - 모델코드는 **비워 둔다.** 가이드가 와일드카드 `RK**F42*`로만 적고 있고, "각주에만 나오는
    와일드카드 코드는 제품이 아니다"라는 규칙이 있다. `test-sizereps.mjs`가 `guideOnly` 항목의
    모델코드는 건너뛰되 **근거(note)에 출처가 있는지**를 대신 검사한다.
    (빈 모델코드를 그냥 통과시키면 `finder.includes('""')`가 늘 참이라 검사 자체가 무력해진다.)
- **추천 표(`BY_AREA`)의 사이즈 이름은 여기서 만든 이름과 정확히 같아야 한다.** 어긋나면 그
  카테고리가 조용히 추천에서 빠진다. `recommendPicks()`가 가리키는 옵션 인덱스도 드롭다운이
  쓰는 것과 같아야 한다(둘 다 `k=0`) — 다르면 미리 체크가 안 된다.
- `test-sizereps.mjs`가 재생성 대조·치수 범위·**축 뒤바뀜(세워 쓰는 가전은 높이 > 폭)**·
  모델코드 실존·골든 치수 3건을 검사한다. 업소용 냉장고는 가로로 넓은 형태가 정상이라
  (`CRFF-1762` = 폭 1,900 × 높이 1,830) 축 검사에서 제외돼 있다.
- **모델파인더 DB를 고치면 `npm run build:sizereps`를 다시 돌려 커밋해야 한다** — 통합검색
  인덱스와 같은 규칙이다.

## 아이콘 (`components/Icon.tsx`)

허브 카드·사이드바·하단탭 아이콘은 **이모지가 아니라 SVG** 다(2026-08-10 사용자 요청:
*"삼성블루 컬러를 활용하여 삼성스러운 이미지로"*).

- **이모지를 쓰지 않는 이유** — OS 가 그리는 컬러 비트맵이라 ①삼성 블루와 무관한 색이 튀고
  ②윈도우·안드로이드·iOS 에서 모양이 제각각이며 ③키우면 뭉갠다.
- **그리는 규칙은 삼성 워드마크에서 가져왔다** — 완전한 기하학(정원·직선), 일정한 획,
  넓은 카운터. 24×24 그리드 · stroke 1.6 · `currentColor` · 사선은 45°만.
- **모듈마다 다른 색을 쓰지 않는다.** 전부 `#1428A0` — 색이 흩어지면 브랜드가 흩어진다.
  구분은 색이 아니라 **아이콘 모양**이 한다.
- 16px 에서 읽히는지 반드시 확인할 것(`.scratch/icon-sheet.mjs` 가 30/20/16/반전을 한 장에 그린다).
  실제로 저울(타사비교)·톱니 쿠폰이 16px 에서 뭉개져 다시 그렸다.
- 돋보기는 **통합검색 전용**이다. 모델파인더는 깔때기(조건으로 좁힌다) — 둘이 같으면 헷갈린다.

### 이모지를 지울 때의 함정 — 두 번 데었다

1. **문자 클래스로 지우면 반쪽만 지워진다.** `/[🔍📚🏬]/` 는 서로게이트 페어(2코드 유닛)를
   반쪽만 매치해 `U+FFFD` 를 남긴다. 화면에 ◈ 로 보인다. `String.prototype.split` 이나
   유니코드 플래그(`/u`)를 쓸 것.
2. **제목에서 지웠으면 그 제목을 비교하는 코드도 함께 고칠 것.**
   `title: '🏬 매장운영 도구'` 만 고치고 `group.title === '🏬 매장운영 도구'` 를 두었더니
   **쿠폰·컨시어지 카드가 통째로 사라졌다.** 문자열로 섹션을 찾는 코드가 세 군데 있었다
   (`ANCHOR_SECTION` 2곳 + 렌더 조건 1곳).

## 개발중인 서비스 (`/dev`, 사이드바 최하단 · 잠금)

아직 다듬는 중인 도구를 그대로 열어 두면 **상담사가 반쯤 만든 화면을 고객에게 보여준다.**
그래서 사이드바 최하단에 **자물쇠 아이콘과 함께** 두고 비밀번호로 잠근다(2026-08-17 사용자 요청).
관리자 대시보드 바로 위 — 상담사가 쓸 일이 있는 쪽이 위에 온다.

- **목록은 `lib/devModules.ts` 한 곳에서만 적는다.** 허브 메인의 「개발중」 그룹과 `/dev`
  가 같은 것을 보여줘야 한다 — 두 곳에 적으면 어긋나고, 어긋난 쪽을 본 상담사가 없는
  도구를 찾는다(허브 카드 개수·앱 버전·비교표 값에서 반복해서 데인 종류다).
- **잠금은 관리자와 같은 장치**(`/api/admin-auth` + `scope:'dev'`) — `sessionStorage` +
  만료 2시간, 검증은 서버에서만.
- **비밀번호를 하나 더 만들지 않는다.** `DEV_PW`(또는 `DEV_PW_HASH`)가 있으면 그것을 쓰고
  **없으면 관리자 비번을 그대로** 쓴다. 매장에 알려 줄 비밀번호가 늘수록 새는 곳도 는다.
  나중에 갈라야 하면 환경변수만 넣으면 되고 코드는 그대로다.
  `test-admin` 이 넷을 지킨다 — DEV_PW 우선 · 폴백 · 해시 방식 · **관리자가 DEV_PW 로
  열리지 않는가**(대시보드가 조용히 열리는 종류의 사고다).
- **통신 요금제 상담 도구는 이 저장소가 아니라 별도 배포다.** `next.config.js` 의 rewrites 가
  요청을 넘기고 로그인도 저쪽이 한다(`SALES_APP_PASSCODE`) — 즉 **잠금이 두 겹**이다.
  주소 끝에 슬래시를 붙이지 말 것(308 이 무한히 오간다).

## 개발자 문의 버튼 (`components/FeedbackButton.tsx`)

**헤더 우측에 고정된 아이콘이다**(2026-08-17 사용자 결정). `Navigation.tsx` 헤더 안에서
렌더하고 `app/layout.tsx` 에서는 렌더하지 않는다.

- **예전에는 오른쪽 아래에 떠 있었고 끌어서 옮길 수 있었다.** 배치 시뮬레이터처럼 화면을
  꽉 쓰는 도구에서 아래 버튼들을 가려서 그렇게 했던 것인데, 그건 고친 것이 아니라
  **문제를 상담사에게 떠넘긴 것**이었다 — 옮긴 자리(`ax_feedback_pos`)는 그 기기에만
  남아 매장 태블릿을 새로 깔면 또 가린다. **되돌리지 말 것.**
- **같은 결정을 이미 한 적이 있다** — 공유 버튼이 미니앱 안에 떠 있다가 AS 안내문 첫 줄을
  가려 2026-08-11 에 헤더로 옮겼다. 같은 병이라 같은 해법을 썼다.
- **3D 로 배치하기로 한 뒤 더 분명해졌다** — 3D 는 화면을 꽉 쓰고 하단에 조작 띠가 있어,
  떠 있는 버튼이 가장 방해되는 화면이 앞으로 주 화면이 된다.
- **공유 아이콘과 같은 모양을 쓴다**(30px · 흰색 18% 배경). 둘이 다르게 생기면 같은 줄에서
  하나만 버튼처럼 보인다.
- **좁은 화면에서는 `경원영업팀` 을 숨긴다.** 아이콘이 하나 늘어 우측이 빽빽해졌다 —
  버전 표시가 이미 같은 방식이다. 실측: 320·360·390·430·768·1024·1440px 전부
  **가로 넘침 0건**(공유 아이콘이 함께 뜨는 미니앱 지면 포함). 재려면
  `node .scratch/header-fit.mjs [경로]` — 경로는 **앞 슬래시 없이** 넘긴다
  (MSYS 가 `/as` 를 윈도우 경로로 바꿔 버린다).
- 옛 `ax_feedback_pos` 는 마운트 때 지운다 — 남겨 두면 기기에 쓰레기로 굳는다.

## AS 관련 정보 (`/as`, `public/as-app.html`)

품목별 **무상보증 · 핵심부품 · 부품보유기간(내용연수) · 멤버십 로열블루 연장**을 한 화면에 보여준다.
*"많은 상담사들이 헷갈려하는 부분"*(2026-08-10 사용자)이라 만들었다.

- **자료는 삼성전자서비스 `보증기간 산정기준` 원문 표 3개에서만 만든다**
  (`samsungsvc.co.kr/info/fee?tab=assure` — 품목별 보증기간 / 핵심부품 무상기간 / 부품보유기간).
  **원문에 없는 품목은 넣지 않는다.** AS 기간은 틀리면 그대로 고객 분쟁이다.
- 상담에서 실제로 헷갈리는 지점을 화면이 함께 적는다:
  - **에어컨 2년은 '냉방 전용'일 때다.** 냉난방 겸용은 일반 제품이라 1년.
  - **인버터 컴프레서 10년에는 조건이 붙는다** — 에어컨은 2015년 1월 이후 제조분, 시스템에어컨 제외.
    건조기는 **정속형 10년 / 인버터형 12년**으로 또 다르다.
  - 드럼세탁기 DD 모터 10년은 **2008년 1월 제조분부터**.
- **확인 못 한 것은 비워 두고 그렇게 적는다.**
  - 부품보유기간이 원문 표에 없는 **7품목**(공기청정기·식기세척기·인덕션·냉동고·업소용 냉장고·
    데이코·사운드바)은 "고객센터 1588-3366 확인 필요". `test-as.mjs` 가 검사한다 —
    나중에 누가 숫자를 채워 넣으면 실패한다.

### 멤버십 무상수리 서비스 기간 연장 (2026-08-11 원문 확인)

오래 "확인 필요"로 비어 있던 값이다. 출처는 **`samsung.com/sec/membership/membershipBluecare/`** —
등급별 혜택 페이지가 "무상수리 서비스 기간 연장 참조"로 가리키던 그 별도 페이지다.

**연장 기간은 "총 3년"이고, 이것은 기존 무상보증기간을 포함한 값이다.**
원문: *"등급 유지 기간 내 구매하신 4대 품목의 무상수리 서비스 기간을 총 3년으로 연장"* ·
*"고장성 불량에 한해, **기존 무상보증기간을 포함한** 총 3년간의 무상수리 서비스 제공"*.

- **"3년 연장"으로 고쳐 적지 말 것.** 기본 보증이 1년인 냉장고는 실제로 +2년이지만,
  냉방전용 에어컨은 기본이 2년이라 +1년뿐이다. "3년 연장"이라고 말하면 그 고객에게
  1년을 더 약속하는 셈이다. `test-as.mjs` 가 `ext === '총 3년'` 과 "포함" 문구,
  그리고 `+3년`·`3년 추가` 표현이 새어 들어왔는지를 검사한다(값을 틀리게 넣어 물리는 것을 확인함).
- **품목별로 계산해서 적지 않는다**(2026-08-11 사용자 결정). 에어컨은 냉방전용 2년 /
  냉난방겸용 1년으로 **한 품목 안에서 기본 보증이 갈려** 계산값이 어느 쪽이든 절반은 틀린다.
  원문 문구를 그대로 싣고 계산은 상담사에게 맡긴다.
- **대상 등급은 로열블루와 프레스티지 둘 다다.** 예전에는 로열블루만 적고 있었다.
- **대상 여부는 `RB` 표가 가른다** — 원문의 <대상 제품 / 제외 제품> 표 그대로이고 상태가 셋이다:
  `ok`(대상) · `no`(원문이 제외로 명시) · `na`(4대 품목이 아니라 원문이 다루지 않음).
  대상이 아닌 품목에서도 **카드를 감추지 않고 "대상 아님"으로 적는다**(사용자 결정) —
  상담사가 "혹시 되나?"를 그 자리에서 확인할 수 있어야 한다.
- 헷갈리는 세 가지를 원문 그대로 옮겼다. 임의로 뒤집지 말 것:
  - **냉동고는 대상이다** — 냉장고 칸에 `Bespoke(냉장고, 냉동고)` · `일반(냉장고(300L이상), 냉동고)`.
  - **김치냉장고·업소용 냉장고는 제외다** — 같은 칸의 제외 목록에 명시.
  - **건조기는 제외지만 "(원바디 제외)"가 붙는다** — 원바디 건조기는 세탁기 칸의 대상 제품이다.
- 그 밖의 조건: 삼성전자 멤버십 **지정매장** 구매분 · 고장성 불량에 한함(파손·긁힘·찍힘 제외) ·
  **무상보증(교환/환불)의 연장이 아니다**(수리 서비스만).

### 연락처·협력사 탭 — **품목·업무 단위 접이식 묶음 9개**

탭은 둘이다(AS기간 · 연락처·협력사). 연락처 탭 안이 접이식 묶음이다
(`GROUPS`, `<details class="grp">`):

| id | 묶음 | 담기는 것 | 건수 |
|---|---|---|---|
| `g-as` | AS · 수리 모음 | `CONTACTS` | 6 |
| `g-lg` | 물류 모음 | `CENTERS` 21곳(+`OPS` 상황실·운영·VOC) + `VOC_HQ` | 21 |
| `g-mv` | 빌트인 가전 이전설치 관할 | 접수 순서 4단계 + `B2B` | 20 |
| `g-sink` | 식기세척기 싱크장 리폼 | `SINK` | 4 |
| `g-ind` | 인덕션 이전설치 | 중앙에너지 + `MID` 19지사 | 1 |
| `g-wat` | 정수기 이전설치 | 중앙에너지 + `MID` 19지사 | 1 |
| `g-hood` | 후드 이전설치 | 중앙에너지(전용 회선) + `MID` 19지사 | 1 |
| `g-sac` | 시스템에어컨 천장형 이전설치 | 티지이엔지 | 1 |
| `g-it` | B2B IT 부문 관할 | `B2B_IT` | 12 |

**다 펼쳐 두면 백 줄이 넘어 상담 중에 못 읽는다**(2026-08-11 사용자 지적:
*"이렇게 다 넣으면 너무 길어집니다. AS연락처안에 검색기능이 있기때문에 버튼눌르면
열리게만들어주세요"*). **찾는 일은 위 검색창이 한다** — 묶음은 어디에 무엇이 있는지만 보인다.

- **묶음은 품목·업무 단위로 쪼갠다**(2026-08-11 사용자 요청). 한때 이전설치 관련을
  'B2B·협력사 모음' 하나에 몰아 뒀는데, 상담사는 *"정수기 이전설치 어디죠"* 로 찾지
  *"B2B 협력사가 뭐죠"* 로 찾지 않는다(사용자도 *"B2B IT 부분관할? 이게 뭔지 모르겠습니다"*
  라고 했다). **담당이 갈리는 단위가 곧 찾는 단위다.** 그래서 묶음 부제에 *무엇인지*를 적는다.
- 인덕션·정수기·후드는 담당이 같지만(중앙에너지) **번호가 후드만 다르고**, 무엇보다 품목으로
  찾으므로 셋을 갈라 둔다. 세 묶음이 `MID` 표를 함께 쓰지만 **접혀 있어 길이 부담이 없다.**
- **'아직 등록되지 않은 연락처' 카드는 없앴다**(2026-08-11 사용자 확인). 비워 뒀던 둘이
  실제로 채워졌다 — 설치 협력사는 이전설치·리폼·중앙에너지·IT 로, 지점 AS 담당은 물류센터
  상황실·운영·VOC 로. **다시 만들지 말 것** — 채운 자료 옆에 "미등록"이 남아 있으면 그
  자료까지 미등록으로 읽힌다.

- **기본은 닫힘이고, 제목에 건수를 함께 적는다** — 열어 보지 않고도 무엇이 얼마나 있는지
  알아야 한다. `test-as.mjs` 가 기본 접힘·묶음 3개·건수 표기를 검사한다.
- **상단 검색 결과를 누르면 묶음까지 펼친다**(`openGroup`). 접힌 묶음 안으로 데려가면
  **아무 일도 안 일어난 것처럼 보여** 찾아 준 뜻이 없어진다. 탭 전환 + 묶음 펼침 +
  목록 필터가 함께 걸린다.
- **접수 순서는 B2B 묶음 맨 위에 둔다.** 이전설치는 번호만 알면 사고가 난다 — 싱크대 리폼이
  먼저고 가구장이 먼저다. 원문이 상담 대화 예시까지 적어 둔 이유가 그것이고, **번호와 절차가
  떨어지면 안 된다.**

### 센터별 상황실·운영·VOC (`OPS` · `VOC_HQ`)

원문의 **운영업무 협조(B2C)** · **VOC 연락처(B2C)** 두 표다(2026-08-11 사용자 요청).

- **별도 표로 만들지 않고 `CENTERS` 에 코드(`L1xx`)로 붙였다.** 두 표 모두 행 단위가
  물류센터라, 따로 실으면 **21줄짜리 목록이 셋이 되어 같은 센터가 세 번 나온다** —
  방금 "너무 길어진다"고 지적받은 그 문제다.
- **상황실 번호가 물류센터 대표번호와 21곳 중 19곳이 정확히 같다.** 판이 다른 두 자료
  ('센터별 배송 서비스지역' 26.1.2 · 'B2B 권역현황' 25.02)가 서로를 검산해 준 셈이다.
  어긋난 **울산**(배송 1577-3967 ↔ 상황실 054-254-0725 동국)과 **창원**(1577-3966 ↔
  070-4694-4540 삼영물류)만 `sitN` 으로 양쪽 다 싣는다. `test-as.mjs [9-b]` 가
  **예외가 그 둘뿐인지**를 지킨다 — 예외가 늘면 어느 한쪽 표기가 틀린 것이다.
- **인덕션·정수기·후드 번호는 지역이 아니라 품목으로 갈린다**(2026-08-11 사용자가 "지역별로
  다른 걸로 안다"며 확인 요청 → 원문 재확인). 중앙에너지 **한 곳이 전국을 맡고 접수는 본사콜
  하나**(1899-4850)이며 **후드만 전용 회선**(1577-5488)이다. 지역별로 갈리는 것은 **설치를
  나가는 지사 19곳(`MID`)** 이고 **그 지사들의 개별 전화번호는 원문에 없다** — 지어내지 말 것.
- **화성·원주의 VOC 연락처는 비워 뒀다** — 원문에 `010` 번호밖에 없다. 그 센터는 운영
  회선으로 안내한다. 채워 넣으면 `[9-d]` 가 실패한다.
- 사람 이름 검사는 **직급이 이름 끝에 붙은 것만** 본다(`NAME_TITLE`). 그냥 알파벳 나열로
  검사했더니 **'프로서비스'(순천 협력사)가 '프로'로 잡혔다** — 회사명 안의 글자를 걸러야 한다.

### 이전설치·빌트인 자료 (`B2B` · `B2B_IT` · `SINK` · `B2B_NATION`)

2026-08-11 사용자가 준 사내 배포본 `B2B+이전설치센터권역현황,연락처`(2025.02)로 만들었다.
- **번호는 원문에 적힌 대로 전부 옮긴다**(2026-08-11 사용자 지시: *"010번호를 제외한
  나머지번호는 적혀있는대로 적어주세요"*). 한동안 '이전설치 문의전화' 칸만 옮겼는데,
  그러면 **물류전화처럼 따로 적힌 번호가 통째로 사라진다** — 경성티에스는 물류
  (032-675-5100)와 이전설치(032-672-3624)가 다른 번호다. `x:[[라벨, 번호]]` 로 담고
  화면에 라벨과 함께 적는다(번호만 나열하면 어디로 걸어야 하는지 모른다).
  `test-as.mjs` 가 되찾은 5건(물류·후드·추가 회선)을 골든으로 고정한다.
- **막는 것은 `010`(휴대폰) 하나다.** 나머지 국번은 업무 회선이라는 것이 자료 소유자의
  판단이다. **이름·이메일·임원 명단은 계속 싣지 않는다** — 지시는 번호에 대한 것이었고,
  이 앱은 로그인 없는 공개 주소이며 저장소도 public 이다. `[7-d]` 가 이메일(`@`)·직급
  붙은 이름·`010` 번호·임원 명단을 검사한다.
- **`n`(이전설치 문의전화)은 협력사 단위다.** 원문에서 그 칸이 병합돼 있어 한 협력사가 맡은
  센터 전체에 같은 번호가 걸린다. `[7-c]` 가 "협력사 하나당 번호 하나"를 검사한다.
- **B2B IT 부문(`B2B_IT`, 12행)은 가전과 담당 협력사·권역이 다르다** — 같은 안동이라도
  가전은 성광티시엠, IT 는 다존텍이다. **표를 합치지 말 것.** 검색칸은 하나로 두고 두 목록을
  함께 거른다 — 상담사는 지역을 칠 뿐, 그 지역을 어느 부문이 맡는지 미리 알 이유가 없다.
  - 이 표는 '이전설치 문의전화' 칸이 없고 담당자 이름 옆 전화만 있어 **대표번호인지 직통인지
    확정할 수 없어 한동안 비워 뒀다.** 2026-08-11 사용자가 **대표번호가 맞다**고 확인해 줘서
    실었다. 원주(033-731-3432)·제주(064-756-3260)가 가전 표의 협력사 대표번호와 **정확히
    같은 것**이 교차검증이고, `test-as.mjs` 가 그 일치를 검사한다.
  - 번호는 협력사 단위가 아니라 **묶음 단위**다 — 원문에서 담당자·전화가 새로 적힌 행부터
    아래 행들이 물려받는다(아이시티가 세종·아산 / 익산·광주 둘로 갈리는 이유).
  - 동탄은 원문에 회선이 셋이라(`031-377-9135~7`) 나머지 둘도 함께 적는다.
- 싱크장 리폼은 **이전설치와 담당이 다르다** — 전국을 4곳이 나눠 맡고, 아비스만 대표번호
  (1811-7958)와 이전설치 문의번호(1644-0385)가 갈린다.

### 화면에 보이는 번호는 전부 눌린다 (2026-08-11)

*"연락처로 되어 있는 것들은 모두 클릭하면 전화로 바로 연결되거나 복사할 수 있게"*(사용자).
예전에는 **줄의 대표번호 하나만** 눌렸다 — 서서울TC 는 대표 + 운영 2회선인데 나머지 둘은
눈으로 읽고 받아적어야 했다.

- 번호 하나가 조각 하나다(`telx`). 줄 전체를 `<button>` 으로 두면 **안쪽 번호를 버튼으로
  만들 수 없다**(중첩 button 은 HTML 위반이라 브라우저에 따라 클릭이 먹지 않는다) —
  번호가 여럿인 줄은 `<div class="ct">` 로 두고 번호마다 버튼을 넣는다.
- **거는 번호와 적는 번호를 구분한다.** `031-922-8546~7` 을 그대로 넘기면 숫자만 남겨
  `03192285467` 로 잘못 걸린다. 화면에는 원문대로 적고 걸기는 앞 번호로 한다.
- **한 칸에 둘인 값은 쪼갠다**(`telsplit`). `031-377-9136 · 9137` 처럼 뒷자리만 적힌
  번호는 앞 번호의 국번을 물려받는다(원문이 `031-377-9135~7` 이라 실제 번호다).
- **안내문에 박힌 번호도 눌린다**(`telify`). 접수 순서 3단계의 아비스 번호가 글자로만
  있었다 — "보이면 눌린다"는 목록이든 안내문이든 같다.
- **라벨 칸으로 세워 적는다**(`opsRow`). ` · ` 로 이어 붙이면 좁은 화면에서 아무 데서나
  줄이 바뀌어 어느 번호가 무엇인지 알 수 없다(사용자 지적).
- `wireContacts` 선택자는 `.ct` 가 아니라 **`[data-tel]`** 이다 — 줄만 잡으면 안쪽
  번호가 안 걸린다. `test-as.mjs` 가 "보이는 번호 = 눌리는 번호"를 전수 대조한다.

### 상단 통합 검색 (`searchDocs` · `runSearch`)

**허브 통합검색과 같은 규칙을 쓴다**(2026-08-11 사용자 지적: *"AS검색에서 검색해도 다른
내용도 검색이 됩니다"*). 예전에는 친 글자를 그대로 `kw.includes` 로 대조해서:

| 증상 | 실측 |
|---|---|
| **한 글자에 아무거나 걸렸다** | `장` 23건 · `기` 58건 · `이` 52건 · `대` 40건 |
| **화면에 있는데 안 찾아졌다** | `로열블루` 0건 (멤버십 카드가 색인에 없었다) |
| **상담사가 쓰는 말이 0건** | `냉장고 보증기간` · `에어컨 몇년` · `부품보유기간` |

- **한 글자는 조건으로 쓰지 않는다**(`asTokens`). 허브가 같은 이유로 이미 막아 둔 규칙이다.
- **조사·꼬리말을 뗀 형태를 함께 본다**(`AS_STOP_TAIL`) — 원형을 버리지 않고 더하기만 한다.
- **0건이면 안 걸린 조건 하나를 빼고 다시 찾고, 뺀 말을 화면에 적는다**(`.sr-note`).
  조용히 빼면 왜 이 결과인지 알 수 없다.
- 표기 짝(`AS_SYN`)은 **화면 말 ↔ 자료 말이 갈리는 것만** 넣는다(`보증기간`→`무상보증`,
  `dd모터`→`dd motor`). 상위·하위 개념을 섞으면 조건이 무의미해진다.
- 품목 kw 에 **화면에 적히는 말**(`부품보유`·`내용연수`)을 넣었다 — 화면에 적힌 말로
  못 찾는 것은 검색이 아니다(허브 색인에서 배운 것과 같다).
- `test-as.mjs` 가 한 글자 6자 전부 0건인지, 위 6개 질의가 찾아지는지 회귀 검사한다.


탭보다 **위**에 둔다. 상담 중에 나오는 말("냉장고 컴프레서 몇 년이죠" · "수원은 어느 센터죠")이
어느 탭에 있는지는 상담사가 알 이유가 없다.

- **찾은 뒤 그 탭까지 데려간다** — 고르면 탭 전환 + 품목 선택 + 목록 필터가 함께 걸린다.
  찾기만 하고 두면 결국 탭을 다시 뒤진다.
- 판정은 공백으로 쪼갠 **모든 토큰을 포함해야 하는 AND** 다(허브 통합검색과 같은 생각).
- 결과 상자는 공유 버튼(share-kit)에 가려졌었다 — 열려 있는 동안 `body.sres-open` 으로 버튼을 숨긴다.

### 연락처 탭 — 물류센터 배송·설치 관할 (`CENTERS`)

*"어디로 전화하죠"* 는 품목과 무관하게 아무 때나 나오므로 **별도 탭**이다.
AS·구매 대표번호(공식 페이지 확인분)에 더해 **물류센터 21곳**을 싣는다 —
2026-08-11 사용자가 사내 배포본 `센터별 배송 서비스지역`(2026.1.02 시행)을 준 것으로 채웠다.
지사 구성은 **서울 3 · 중부 4 · 남부 7 · 서부 7**.

- **관할 원문(`a`)은 손대지 않고 그대로 옮긴다.** 관할이 틀리면 배송이 통째로 어긋나므로
  요약하거나 다듬지 않는다. 검색용 `kw` 만 파생값이다.
- **원문에서 지명을 기계적으로 뽑으면 관할이 뒤바뀐다.** 표에는 "다른 센터로 넘어갔다"는
  주석이 **넘겨준 쪽 칸에** 적혀 있어, 그것까지 긁으면 한 지역이 두 센터에서 잡힌다.
  실제로 셋이 그랬고 전부 넘겨받은 센터에만 남겼다:

  | 지역 | 원문 표기 (넘겨준 칸) | 실제 관할 |
  |---|---|---|
  | 안산시 | 평택TC 칸의 `*인천TC로 변경(안산시)` | **인천TC** ('26.1월~) |
  | 보령시 | 세종TC 칸의 `(아산TC로 변경 : 보령시)` | **아산TC** ('25.1월~) |
  | 영천시 | 대구TC 칸의 `/// 포항TC(영천시)` | **포항TC** |

- **반대로 한 지역이 진짜 두 센터에 걸린 것은 둘 다 남긴다** — 평창군·홍천군(원주↔강릉)과
  함평군(광주↔목포)은 읍·면 단위로 갈린다. 하나로 정리하면 나머지 반쪽이 사라진다.
- **시·도 이름은 키워드에서 뺀다.** 표의 머리글이라 그대로 두면 '서울'이 네 센터를 다 잡아
  검색이 무의미해진다. 대신 `부산광역시 전지역`처럼 시·도 단위 관할은 `전지역`으로 싣는다.
- **번호 체계를 단정하지 말 것.** 대부분 `1577-39xx` 지만 **남서울TC 만 `1899-9300`** 이다.
  (테스트 골든값을 원문에서 확인하지 않고 적었다가 제주RDC·익산TC 를 틀렸다 —
  값은 원본 엑셀에서만 가져올 것.)
- `test-as.mjs` `[6]` 구간이 센터 수·지사 구성·골든 5건·**이관 3건·분할 3건**·시·도 제외·
  검색 동작·`data-tel` 존재를 검사한다. 표를 갱신하면 여기가 먼저 깨진다.

## 공유 (`public/share-kit.js`) — **지금 화면을 처음부터 끝까지 그대로 캡처한다**

**공유는 앱 헤더 우측 상단이 맡는다**(2026-08-11 사용자 요청). 미니앱 안에 떠 있던 버튼은
본문을 덮었고(AS 안내문 첫 줄이 실제로 가려져 `padding-right` 로 자리를 비워 두고 있었다)
화면마다 자리가 달라 눈이 헤맸다. `경원영업팀` 글자를 왼쪽으로 밀고 그 자리에 아이콘을 둔다.

### 카드를 다시 그리지 않고 화면을 읽는다 (2026-08-12 전환)

예전에는 내용을 **캔버스에 다시 그려** 카드를 만들었다. 그 방식의 고장이 전부 같은 모양이었다 —
**"지금 보고 있는 것"과 "나가는 것"이 갈린다.** care 앱이 탭과 무관하게 `curCat` 회차표를
만들어 블루패스를 보면서 공유하면 '에어컨 회차표'가 나갔고, 제품 상세검색은 `openId` 가 남아
**직전에 보던 에어컨**이 담겨 나갔다. 고칠 때마다 다른 앱에서 같은 종류가 또 났다.

사용자 요구도 처음부터 그것이었다 — *"지금 보여지는 화면이 그대로 공유되었으면 좋겠습니다.
공유버튼을 누르면 **전체페이지를 캡쳐**해서 보내는 개념"* · *"상단만 캡쳐가 되는 것 같은데
**처음부터 끝까지** 전체 캡쳐가 되야 합니다"*(2026-08-12).

- **html2canvas 로 `document.body` 를 통째로 그린다**(`captureScreen`). 화면을 읽어 그리므로
  갈릴 여지가 애초에 없다. 실측 — `/as` 780×4,930px(640KB·759ms) · `/finder` 780×7,210px
  (919KB·977ms).
- **보이는 부분만 찍히지 않게 크기를 직접 준다.** 기본값은 뷰포트라 스크롤 아래가 잘린다 —
  `height` 를 `scrollHeight`(body·documentElement 중 큰 값)로, `windowHeight` 도 같이 넘기고
  `scrollX/Y = 0`. `test-e2e.mjs` 가 **나온 이미지 높이 ≥ `scrollHeight` 의 95%** 인지 검사한다.
- **배율은 두 한도를 함께 지킨다** — ①**넓이** 1,200만 픽셀(`sqrt(12e6/(w*h))`, 폰 메모리)
  ②**한 변 16,000px**(브라우저 캔버스 한도 16,384px). 2배 고정이면 긴 화면이 통째로 실패하고,
  1배 고정이면 카톡 재압축에서 글자가 뭉갠다.
  - **넓이만 보면 한 변 한도를 그냥 넘는다.** `MAX_SIDE` 를 넣기 전 실측 — **AS 연락처의
    묶음을 전부 펼치면 폰 폭에서 15,211px** 이고 넓이 규칙이 고른 배율 1.42 가 캔버스를
    **21,634px** 로 만들었다. 그 그림은 빈 채로 나간다.
  - 그래서 **1배 밑으로도 내려간다**(위 경우 1.05배 → 410×16,000px). 글자가 작아지는 것보다
    빈 그림이 고객에게 나가는 쪽이 훨씬 나쁘다. `test-e2e.mjs` 가 이 최악의 화면으로 검사한다.
- 공유 버튼·토스트·시트는 **찍히지 않게 뺀다**(`ignoreElements`). 화면에는 있지만 고객에게
  보낼 그림에 우리 UI 가 들어갈 이유가 없다.
- **html2canvas 는 `public/vendor/` 에 받아 두고 처음 공유할 때 받는다**(199KB·gzip 45KB).
  three.js 를 vendor 에 둔 것과 같은 이유 — 매장 전파가 약해도 열려야 한다. `sw.js` 의
  `/vendor/` 규칙이 캐시 우선으로 잡는다. **CDN 으로 되돌리지 말 것.**
- **한계 — 다른 도메인 이미지는 빈칸으로 찍힐 수 있다.** samsung.com 이미지처럼 CORS 헤더가
  없으면 캔버스가 읽지 못한다(`useCORS` 를 켜 두었지만 서버가 허락해야 동작한다). 우리
  저장소 안의 이미지는 문제없다.
- `build()` 는 이제 **파일 이름을 짓는 데만** 쓴다. 앱별 `bluepassShareBuild` 같은 함수는
  남아 있지만 그림을 만들지는 않는다.

### 헤더 ↔ 미니앱

- 미니앱은 **혼자서도 열리는 파일**이라, 안쪽 버튼을 감추는 것은 **iframe 안일 때뿐**이다.
- 상태는 미니앱이 알린다(`share-state`). **화면을 찍는 방식이 된 뒤로는 늘 `on:true`** 다 —
  화면이 있으면 공유할 것이 있다. 예전에는 "만들 것이 없으면 감춘다"였는데, 그 판단이
  앱마다 흩어져 있어 **AS 에서는 아이콘이 끝내 안 떴다.**
- **알림만 기다리면 유실된다.** 미니앱이 먼저 뜨고 헤더가 나중에 듣기 시작하면 그 한 번이
  사라진다. 헤더가 **되물어 본다**(`share-ping`, 250ms 간격·6초 상한).
- 누르면 헤더가 미니앱에 `share-click` 을 보내고 **캡처는 미니앱이 한다** — 찍을 DOM 이
  그 안에 있다(헤더에서 iframe 속을 캔버스로 읽을 수 없다).

## 화면 크기 대응 — 분기점은 **1024px**(`lg`)이다

2026-08-11 사용자 요청으로 모바일·태블릿·PC 를 각각 재고 고쳤다.

- **하단 바로가기 순서는 사용자가 정한다**(2026-08-11): **허브 · 통합검색 · 제품 상담 도구 ·
  교육 · 배치 시뮬레이터 · AS 관련 정보**. '제품 상담 도구'·'교육'은 페이지가 아니라
  **분류**라 허브의 그 섹션(`/#tools`·`/#edu`)으로 데려간다.
  - **분류 탭은 Next `<Link>` 가 아니라 평범한 `<a>` 로 둔다.** `<Link>` 는 해시만 다른
    이동에서 `hashchange` 를 일으키지 않아, **허브에 있는 상태에서 누르면 주소만 바뀌고
    섹션이 접힌 채 그대로**였다(실측). 브라우저에 맡기면 같은 화면에서는 hashchange 가,
    다른 화면에서는 새로 뜨면서 마운트가 그 일을 한다.
  - `app/page.tsx` 의 앵커 처리도 **마운트 한 번이 아니라 `hashchange` 를 함께 듣는다.**
    같은 이유다 — 탭이 하는 일이 그것이라 안 되면 탭이 아무 일도 안 하는 셈이다.
  - `test-e2e.mjs` 가 순서와 "눌렀을 때 섹션이 펼쳐지는가"를 회귀 검사한다.
- **하단 바로가기는 사이드바에서 파생한다 — 손으로 따로 적지 말 것**(`NAV_ITEMS = [...NAV_ENTRY,
  ...NAV_GROUPS[0].items]`). 둘을 각각 손으로 적었더니 어긋나서, 하단에만 '레벨업 챌린지'
  (교육 그룹)가 있고 AS 관련 정보·배치 시뮬레이터·제품 상세검색은 빠져 있었다
  (2026-08-11 사용자 지적: *"사이드바 메뉴가 동일하게 하단 바로가기로 나와 있어야 합니다.
  하단 바로가기에 엉뚱한 것들이 있습니다"*). 담는 것은 **진입점 둘(허브·통합검색) +
  '제품 상담 도구' 그룹** — 한 줄이라 전부는 못 담고, 상담 중에 손이 가는 것이 그 그룹이다.
  8칸이라 글자를 9px·두 줄로 접는다(실측 360/390/430px 전부 넘침 0, 바 높이 57px).
- **내비게이션 껍데기(사이드바·하단탭·본문 좌패딩)만 `lg`(1024px)에서 갈린다**
  (`components/Navigation.tsx` · `app/layout.tsx`). 예전에는 `md`(768px)라
  **태블릿 세로에서 사이드바가 224px 를 먹어 카드가 242px 로 눌렸다**(실측).
  지금은 태블릿 세로가 하단탭 + 카드 362px 다.
- **`app/page.tsx` 의 분기는 전부 `md`(768px) 그대로 둔다.** 그쪽은 "섹션을 펼치느냐"라
  성격이 다르다. 한때 여기까지 `lg` 로 올렸다가 **태블릿 세로에서 모든 섹션이 접힌 채
  화면 아래 400px 가 비었다** — 되돌리지 말 것.
- **본문 폭은 `max-w-3xl xl:max-w-5xl 2xl:max-w-6xl`.** 예전에는 768px 고정이라
  1920px 화면에서 좌우가 통째로 비었다. 카드는 `grid-cols-2 xl:grid-cols-3`.
  더 넓히지 않는 이유는 한 줄이 길어지면 읽기 어려워지기 때문이다.
- 실측(허브 기준): 모바일 390 → 본문 358 · 태블릿 세로 768 → 카드 362 ·
  태블릿 가로 1024 → 카드 370(사이드바) · 노트북 1366 → 3열 333 · 데스크탑 1920 → 3열 376.
  **가로 스크롤 넘침은 전 해상도 0건**이어야 한다.

- **미니앱을 감싸는 음수 마진은 본문 패딩과 같은 분기점을 써야 한다.** 본문이
  `p-4 lg:p-6` 인데 아홉 페이지가 `-m-4 md:-m-6` 로 남아 있어 **768~1023px 구간에서만
  가로가 8px 넘쳤다**(태블릿 세로). 내비게이션을 md → lg 로 옮길 때 딸려 갔어야 할 것이
  빠진 것이다 — 지금은 전부 `-m-4 lg:-m-6`.

### 미니앱(`public/*-app.html`)도 PC 폭을 따로 준다

허브만 고치면 **정작 도구를 여는 순간 폰 화면이 가운데 떠 있다.** 미니앱은 각자
자기 CSS 를 갖고 있어 본문 폭이 460~840px 로 묶여 있었다(2026-08-11 사용자 지적:
*"일부 인터페이스가 PC에서 접속했는데도 모바일 인터페이스로 나온다"*).

- 좁은 화면 값은 **건드리지 않는다** — 매장 기기가 폰인 경우가 많다.
  넓은 화면에서만 늘린다: **≥1024px → 960px · ≥1440px → 1140px**.
- 늘릴 대상은 앱마다 다르다 — `as`·`finder` 는 `.wrap`+`.topbar-in`(머리와 본문이
  같이 가야 한다), `compare`·`install` 은 `main`, `test` 는 `#quizContent`·`#submitBtn`·
  `#resultScreen`. **`care` 는 `.wrap` 과 함께 하단 고정바 `.btm` 을 반드시 같이 늘릴 것** —
  `.btm` 이 `max-width:460px` 로 본문 폭에 묶여 있어 혼자 두면 바만 좁게 남는다.
- `place` 는 전체 화면 도구라 폭 제한이 없고, `quiz`·`poster` 도 해당 없다.
- 실측(2026-08-11): 여섯 앱 모두 390 → 390 · 1440 → 1140 · 1920 → 1140,
  **가로 넘침 0건**.

### 화면에 적는 개수는 데이터에서 세어 넣는다

설치환경 가이드가 `(20종)`으로 박혀 있었는데 `INSTALL_DB` 는 **21개**였다.
`test-install.mjs` 가 **화면 표기 = 드롭다운 항목 수**를 대조한다(둘 다 DB 에서 나온다).
같은 함정이 모델파인더 "OOO종"·배치 시뮬레이터 "N개 사이즈"에도 있으니 숫자를 박지 말 것.

## AI구독 케어 — 화면 순서

**혜택이 회차표보다 먼저다**(`buildBenefits`, 2026-08-10 사용자 요청).
상담은 *"이 제품 케어가 몇 회차냐"* 가 아니라 *"구독하면 뭘 받느냐"* 로 시작한다.
그래서 제품을 고르면 **무상수리 최대 N년 · 방문케어 N회 · 공임비 0원 · 재설치 1회 · 일정 변경**이
남색 카드로 먼저 뜨고, 그 아래에 계약기간 타일과 회차별 내용이 온다.

- **문구를 지어내지 않는다.** 타일은 그 제품의 `includes[]` 에서 뽑고, 연수는 `contract` 에서
  계산한다(72개월 → "최대 6년"). 없는 항목은 타일 자체를 만들지 않는다.
- 제외·주의는 같은 카드 아래 한 줄로 붙인다 — 혜택만 보여주고 조건을 숨기면 상담에서 뒤집힌다.

## AI구독 케어 — 삼성닷컴 수집 경로

`care-app.html` 의 `DATA` 는 손으로 다듬은 회차별 케어 내용이라 **자동 갱신으로 덮지 말 것.**
사진만 `CARE_IMGS` 로 따로 둔다(삼성닷컴이 바꾸면 다시 받으면 그만이다).

- **제품 목록은 DOM 에 11개만 렌더링된다.** 스크롤·더보기로 안 늘어난다. API 를 쓸 것:
  `GET /sec/cxhr/pf/goodsList?dispClsfNo={카테고리번호}&page=1&rows=100&sortType=10`
  카테고리 번호는 목록 페이지를 열 때 나가는 요청에서 캡처한다.
  응답 필드: `mdlCode` · `goodsNm` · `goodsDetailUrl` · `imgPath1~8`.
- **모델코드 접미가 다르다** — 우리 `DV80H20DDSN` ↔ 삼성닷컴 `DV80H20DDS`,
  `VR90F01SAG` ↔ `VR90F01SAG98CS`. **접두로 매칭할 것.**
- **냉장고·김치냉장고는 일반 경로 페이지에 케어 섹션이 없다.**
  `ai-subs-kitchen/{모델}-subscribe2-d2c/` 같은 **구독 전용 경로** 모델을 써야 나온다.
- 케어 정보는 제품 페이지 **본문**에 있다(모달을 열 필요 없음) —
  `구독케어` 아래 `항목명 → 설명`, 주기가 갈리면 `12개월`/`36개월` 로 나뉜다.
- 방문케어 현장 사진은 `images.samsung.com/kdp/static/pd/subscription_service/` 에 있고
  **라벨과 쌍으로** 가져와야 한다 — 사진만 있고 무슨 장면인지 모르면 상담에 못 쓴다.

## QR 코드 (`lib/qr.ts` · `components/QRCode.tsx`)

**QR 은 우리가 직접 그린다. 외부 API 로 되돌리지 말 것.** 예전에는 허브 QR 이
`api.qrserver.com` 에 이미지를 요청했는데 ①그 서버가 죽으면 QR 이 통째로 빈칸이 되고
②매장 와이파이가 불안하면 안 나오며 ③공유하려는 주소가 남의 서버 로그에 남는다.
한 번 본 화면은 열리게 하려고 서비스워커까지 두는 앱에서 QR 만 네트워크에 묶여 있을 이유가 없다.

- 범위는 용도에 필요한 만큼 — 바이트 모드 · 오류정정 M · 버전 1~10(270자까지). 넘으면 **던진다**
  (조용히 틀린 QR 을 내놓지 않는다).
- **기능 모듈(파인더·타이밍·정렬·형식정보)에는 마스크를 적용하지 않는다.** 여기까지 덮으면
  파인더가 뭉개져 **어떤 리더기로도 안 읽힌다.** 형식정보의 EC 레벨도 함정이다 —
  **M 은 `0b00`** 이고 이름 순서(L·M·Q·H)가 아니다. 둘 다 그림은 멀쩡해서 눈으로는 못 잡는다.

**그래서 눈이 아니라 되읽어서 검사한다**(`scripts/test-qr.mjs`, `npm run test:qr`).
`qrMatrix` 는 DOM 을 안 쓰는 순수 함수라 브라우저 없이 돌아간다(`test-admin.mjs` 가
`lib/logEvent.ts` 를 직접 import 하는 것과 같은 방식). 네 겹이다 — ①구조(파인더·타이밍·고정
검정 모듈) ②형식정보 BCH 유효성과 EC 레벨 ③블록별 RS 신드롬 0 ④마스크를 풀고 지그재그를
되짚어 원래 문자열 복원. **여러 블록으로 갈리는 버전(8 등)을 반드시 케이스에 넣을 것** —
블록이 하나인 버전만 보면 인터리브 관련 버그가 통과한다(실제로 그렇게 놓칠 뻔했다).

- **포스터는 같은 생성기를 인라인으로 복사해 갖고 있다.** `public/*.html` 미니앱이 자립형인
  것은 이 저장소의 구조라 복사 자체는 맞다. 다만 조용히 갈라지면 **인쇄물 QR 만 안 읽히고
  인쇄물은 회수가 안 되므로**, `test-qr.mjs` 가 두 구현이 같은 격자를 내놓는지 대조한다.
  포스터의 `/* ── GF(256)` 표식을 기준으로 떼어 오므로 **그 주석을 지우면 이 검사가 실패한다.**
- 화면에 쓸 때는 `components/QRCode.tsx` 를 쓴다. 캔버스가 아니라 `<img>` 로 내놓아
  기존 스타일이 그대로 먹고 휴대폰에서 길게 눌러 저장·공유가 된다.

## 화면이 뜨는 속도 — 병목은 회선이 아니라 CPU 다

폰·4G·CPU 4배 느리게(매장 조건) 재 본 값이다. **받는 시간보다 푸는 시간이 훨씬 길다** —
제품 상세검색은 120KB 를 받는 데 74ms, 그것을 해석·실행하는 데 **1,242ms** 다.
프로파일의 58%가 `(program)`, 즉 **JS 소스를 파싱·컴파일하는 시간**이다(1MB 인라인 데이터).
그래서 gzip·이미지 최적화 같은 처방은 여기서 효과가 없다.

| 화면 | 전체 | 받기 | 해석·실행 |
|---|---|---|---|
| 제품 상세검색 | 1,830ms | 74ms | **1,242ms** |
| 배치 시뮬레이터 | 1,713ms | 30ms | **1,222ms** |
| 타사비교 | 1,198ms | 82ms | 699ms |

- **three.js 는 3D 를 처음 켤 때 받는다**(2026-08-11). 정적 import 이던 시절에는 배치
  시뮬레이터를 열기만 해도 **687KB(gzip 166KB)** 를 받았다 — /place 가 받는 양이
  **759KB → 88KB**. CPU 는 30ms 밖에 안 준다(V8 이 안 쓰는 코드를 게으르게 컴파일하기
  때문). **모듈 본문은 한 줄도 옮기지 않고** 통째로 `window.load3D()` 안에 감쌌다 —
  3D 는 테스트가 가장 촘촘히 지키는 부분이라 옮겨 적다 생기는 사고가 더 비싸다.
- **폰트가 화면을 막지 못하게 한다.** `@import` 와 렌더 차단 `<link>` 를 쓰던 탓에 CDN 이
  느리면 그동안 화면이 비어 있었다 — 실측으로 포스터가 폰트 응답을 기다리다 **13초**
  걸렸다(CPU 는 97% 놀고 있었다). `media="print"` 로 받아 다 받은 뒤 `all` 로 바꾼다.
  - **자체 호스팅이 정답이다** — three.js 를 `vendor/` 에 받아 둔 것과 같은 이유.
    다만 폰트 파일을 받으려면 CDN 에 닿는 환경이 필요해 **로컬 세션에서 할 일로 남겼다**
    (도면 수집이 로컬 전용인 것과 같은 제약).
- **재 보고 판단할 것.** 인라인 리터럴을 `JSON.parse` 로 바꾸는 흔한 처방은 9회씩 재 보니
  876 → 842ms 로, 편차(731~1046ms)가 이득보다 커서 **넣지 않았다.**
- 더 줄이려면 제품 데이터를 쪼개야 한다(검색용 경량본 + 상세는 펼칠 때 — 통합검색이 쓰는
  수법). `fx`(172KB·33%)가 검색 로직에서도 쓰여 빌드·테스트까지 함께 손대는 별도 작업이다.

## 오프라인 캐시 (`public/sw.js`)

매장에서 전파가 약하거나 끊겨도 한 번 본 화면은 열리게 하는 것이 목적이다. `components/ServiceWorker.tsx`가
**프로덕션에서만** 등록한다(개발 중에는 캐시가 수정 내용을 가려 디버깅을 방해한다).

- 네비게이션(HTML 문서)은 **네트워크 우선** — 배포 직후에도 Next 페이지가 최신이어야 한다.
- 모듈 미니앱(`*-app.html`)·검색 인덱스는 **stale-while-revalidate** — 캐시를 즉시 보여주고 뒤에서 갱신.
  데이터가 하루 늦는 것보다 "안 열리는" 쪽이 영업 현장에서 훨씬 치명적이다.
- `_next/static/`은 캐시 우선(파일명에 해시가 있어 내용이 바뀌면 URL이 바뀐다).
- `/api/*`는 **캐시하지 않는다** — 로그·인증이 굳는다.
- 배포 시 캐시 무효화가 필요하면 `CACHE_VERSION`을 올린다. `activate`에서 옛 캐시를 지운다.

## 텔레그램 원격 조종 (`scripts/telegram-bot.mjs`, `npm run bot`)

휴대폰에서 **로컬 PC 의** Claude Code 에 명령을 내린다. 앱에 들어가는 기능이 아니라 개발 도구다
(Vercel 배포와 무관하고, 봇을 안 띄우면 저장소에 아무 영향이 없다).

- **롱폴링(`getUpdates`)을 쓰는 이유가 전부다.** 로컬 PC 가 텔레그램 서버에 물어보는 구조라
  공인 IP·포트포워딩·HTTPS 인증서가 하나도 필요 없다. 공유기 NAT 뒤에서 그대로 돈다.
  웹훅으로 바꾸면 그 셋이 전부 필요해진다 — **바꾸지 말 것.**
- **claude.ai/code(휴대폰 브라우저)와 용도가 다르다.** 그쪽은 클라우드 컨테이너라 로컬 파일·git·
  Vercel CLI 를 못 만지고 프록시가 외부 호스트를 막는다(도면 수집이 로컬 전용인 것과 같은 이유).
  "로컬 PC 에서 실행"이 목적일 때만 이 봇을 쓴다.
- 설정은 `.env.local`(gitignore 대상). 항목은 `.env.local.example` 에 있다.
  `chat_id` 를 모르면 `npm run bot -- --setup` — 봇에 말을 걸면 터미널에 찍힌다.

### 이것은 "휴대폰에서 로컬 PC 에 명령을 던지는 통로"다

봇 토큰이 유출되면 남이 이 PC 에서 명령을 실행한다. 방어가 셋 겹인데 **무게가 서로 다르다.**

| 겹 | 무엇 | 성격 |
|---|---|---|
| 1 | `chat_id` 화이트리스트 | **보안 경계.** 목록에 없으면 답조차 하지 않는다 |
| 2 | `--allowedTools` / `--disallowedTools` | **보안 경계.** Claude 가 쓸 수 있는 도구 자체를 좁힌다 |
| 3 | 위험 요청 `/confirm` | 어림짐작. 실수 방지용 — **끌 수 있다**(`TELEGRAM_CONFIRM=off`) |

- **3번을 보안으로 착각하지 말 것.** 사용자가 보낸 글자를 정규식으로 보는 것이라 쉽게 우회된다
  ("그거 올려줘"). 목적은 휴대폰에서 오타 한 번에 배포되는 사고를 막는 것뿐이다.
- **그래서 3번만 끄는 것은 경계를 허무는 일이 아니다.** 매장에서 폰으로 일할 때 실제로 가장
  크게 걸리는 것이 이 되묻기다 — `삭제`·`merge`·`배포` 같은 흔한 말이 든 평범한 요청까지
  전부 `/confirm` 을 기다린다(2026-08-10 사용자 지적: *"이래서 안 되고 저래서 안 되고"*).
  **기본은 켜짐**이어야 한다 — 설정을 안 건드린 사람이 자기도 모르게 되묻기 없이 배포하면 안 된다.
  모르는 값(`offf` 같은 오타)은 켜진 쪽으로 넘어간다.
- **"오프라인에서 하던 업무를 그대로"** 하려면 둘을 함께 켠다 —
  `TELEGRAM_TOOL_PRESET=full` (도구를 전부 연다) + `TELEGRAM_CONFIRM=off` (되묻지 않는다).
  둘 중 하나만 바꾸면 반쪽만 열린다: 프리셋만 full 이면 되묻기에 걸리고, 확인만 끄면
  `git push` 자체가 도구 제한에 막힌다.
- **화이트리스트에 없는 사람에게는 답하지 않는다.** "권한이 없습니다"라고 답하면 봇이 존재한다는
  사실을 확인시켜 준다. 대신 터미널에 `chat_id` 를 찍어 본인이면 등록할 수 있게 한다.
- **설정이 없으면 아예 뜨지 않는다**(fail closed). 화이트리스트가 비어 있는데 뜨면 봇 사용자명을
  아는 누구나 이 PC 에 명령할 수 있다. `--setup` 만 예외다(화이트리스트를 만들기 전 단계라서).
- 기본(`safe`)에서 `Bash` 는 **접두 지정으로만** 연다(`Bash(git status:*)`). `Bash` 를 통째로
  허용 목록에 넣는 순간 나머지 제한이 전부 무의미해진다 — `test-telegram.mjs` 가 검사한다.

### 로컬 세션과 겹치지 않는다 — 작업 자물쇠 · 대기열 (2026-08-13)

*"봇이랑 로컬이랑 오더가 중복되지 않도록 … 지금 작업중인 것 다음 작업으로"*(사용자).
PC 앞에서 작업하는 중에 폰으로 지시를 넣으면 **claude 프로세스가 둘이 같은 작업 트리를
고쳐 서로 덮어썼다.** 봇의 `state.busy` 는 봇 메모리 안에만 있어 로컬을 볼 수가 없다.

- **`.claude/agent.lock` 한 장으로 조율한다**(`scripts/agent-lock.mjs`, gitignore 대상).
  로컬은 훅이 잡고 놓고(`.claude/settings.json` 의 UserPromptSubmit·Stop·SessionEnd),
  봇은 `dispatch` 전에 잡는다. 잡는 것은 `writeFileSync(…, {flag:'wx'})` 한 번 —
  O_EXCL 이라 동시에 만들려 하면 커널이 하나만 통과시킨다(읽고-확인-쓰기는 경합이 남는다).
- **겹치면 버리지 않고 줄을 세운다.** 예전에는 "작업이 돌고 있습니다" 한 줄을 보내고
  그 지시를 흘려버렸다 — 폰에서 던져 놓고 나중에 보러 오는 이 봇의 쓰임새와 정면으로
  어긋난다. 지금은 `state.queue` 에 넣고 2초마다·작업이 끝날 때마다 소진한다(`drain`).
  `/queue`(보기 · `clear` 비우기 · `force` 로컬 자물쇠 풀기).
- **`dispatch` 를 await 하지 않는다.** 예전 폴링 루프는 작업이 도는 15분 동안
  `getUpdates` 를 아예 안 불렀다 — 그래서는 대기 안내도 못 보내고 로컬 자물쇠가
  풀리는 것도 못 본다. 대신 `state.busy` 를 **동기적으로** 세워 재진입을 막는다.
- 자리를 못 잡으면 그 작업을 **줄 맨 앞(`unshift`)** 으로 되돌린다. `push` 하면
  나중에 보낸 것이 먼저 돈다.

**생사를 재는 법이 주인마다 다르다 — 여기서 한 번 틀렸다.**
- 훅은 **명령 한 줄 돌리고 즉시 끝나는 프로세스**라 그 pid 를 적으면 **잡은 순간 죽은
  자물쇠**가 된다(실제로 그렇게 만들었다가 `status` 가 곧장 '없음' 을 뱉어 잡았다).
  훅은 턴의 시작·끝에만 불리므로 심장박동도 못 찍는다.
- 그래서 로컬은 **대화 기록 파일(`transcript_path`)이 만져진 시각**을 심장박동으로 쓴다.
  Claude Code 가 메시지·도구 결과를 그때그때 이어 쓰므로 일하는 동안 반드시 움직인다.
  15분 조용하면 썩은 것으로 본다(`IDLE_MS`).
- 봇은 스스로 도는 프로세스라 pid + 10초 간격 `beat` 를 쓴다.
- **재부팅을 먼저 본다**(`bootStamp` = `now − os.uptime()`). PC 를 끄면 훅이 못 돌아
  파일만 남는데, 켠 뒤에는 그 pid 를 다른 프로그램이 물려받아 "살아 있다"로 보인다.
- 시계를 가짜로 앞당기는 검사에서는 `boot` 도 그 시점 기준으로 찍어야 한다 —
  안 그러면 그 값만 뒤처져 전부 '재부팅'으로 판정된다.

**훅이 빠지면 조용히 반쪽이 된다** — 봇은 로컬을 못 보고 예전처럼 동시에 달리는데
화면에는 아무 표시도 안 난다. 그래서 봇이 뜰 때 `.claude/settings.*` 를 확인해
크게 경고한다(`hooksInstalled`). 훅 명령은 **exec 형식(`command:"node"` + `args:[…]`)**
으로 둔다 — 셸을 거치지 않아 한글이 든 경로·따옴표가 다시 해석되지 않는다.

**봇이 띄운 세션은 '로컬'이 아니다**(2026-08-15 실측). 봇은 `claude -p` 를 **이 저장소를
cwd 로** 띄우므로 **자식 세션도 `.claude/settings.json` 의 자물쇠 훅을 그대로 돈다.**
그래서 자식의 UserPromptSubmit 이 `acquire local` 을 부르고 자기를 띄운 봇의 자물쇠를
보고 "충돌"이라고 신고했다 — **폰에서 보낸 모든 명령이 `[작업 자물쇠] 텔레그램 봇 작업
중 …` 을 맥락에 달고 시작했다.** 파일이 엉키지는 않지만(`release` 가 주인을 대조한다)
모델이 그 문장을 *"봇이 돌고 있으니 기다리자"* 로 읽으면 **명령이 아무것도 안 하고 끝난다.**
- 봇이 spawn 할 때 넣는 **환경변수 표식**(`SELF_ENV` = `AGENT_LOCK_SESSION=bot`)으로
  알아본다. 환경변수는 손자 프로세스까지 물려지므로 pid 계보를 뒤질 필요가 없다
  (윈도우는 계보 추적이 비싸고 pid 재사용까지 겹친다).
- **acquire·release 를 둘 다 건너뛴다.** release 만 남기면 봇 자물쇠가 썩은 것으로
  판정되는 찰나에 자식의 Stop 훅이 그것을 지운다.
- `test-telegram.mjs` 가 **CLI 를 진짜로 돌려** 검사한다. 함수만 부르면 `cli()` 의
  분기를 건너뛰어 아무것도 못 지킨다. 표식을 넘기는 쪽(spawn env)이 빠지면 그 분기가
  영영 안 타므로 그것도 함께 본다.

**썩었어도 남의 자물쇠는 지우지 않는다**(2026-08-15). `release` 가 `read()` 가 `null` 이면
주인을 안 보고 파일을 지우고 있었다. `read()` 는 **"파일이 없다"와 "있는데 썩었다"를
둘 다 `null` 로 뭉개기** 때문이다. 봇은 10초마다 박동을 찍는데 판정선이 60초라,
**절전에서 깨어난 직후** `Date.now()` 가 뛰어 살아 있는 봇이 다음 박동까지 최대 10초간
'썩음'으로 읽힌다 — 그 창에서 로컬 Stop 훅이 돌면 멀쩡한 봇 자물쇠가 지워지고 둘이
동시에 달린다. 판정 없이 원본을 읽는 `readRaw()` 로 두 상태를 갈라 주인을 대조한다.
- **복구는 그대로 된다** — 진짜로 죽어 남은 자물쇠는 `acquire` 가 스스로 걷어낸다
  (EEXIST → 썩었으면 unlink 후 재시도). 지우는 길이 하나 더 있었을 뿐이다.
  **안전장치를 뺄 때는 "그 길이 유일한가"를 먼저 볼 것.**
- 주인을 알 수 없는 **깨진 파일은 걷어낸다** — 안 그러면 아무도 못 지우는 파일이 된다.

**고친 뒤 봇을 반드시 재시작할 것.** 돌고 있는 봇은 옛 코드를 메모리에 들고 있다.
그리고 **봇이 띄운 세션 안에서 그냥 재시작할 수는 없다** — 그 세션의 답을 폰으로
보내 주는 것이 바로 그 봇이라, 죽이는 순간 **답이 전달되지 않아 성패조차 모르게 된다.**
`npm run bot` 을 그냥 한 번 더 치는 것도 재시작이 아니다 — 봇이 **둘**이 되고 텔레그램은
토큰당 `getUpdates` 소비자를 하나만 허용해 둘째가 409 로 튕긴다(옛 봇은 그대로 남는다).

PC 앞이면 `Ctrl+C` → `npm run bot`. **폰에서 해야 하면 분리된 감시 프로세스**를 쓴다
(`.scratch/restart-bot.ps1`, 커밋되지 않으므로 경로를 여기 적어 둔다):
- **고정 시간으로 기다리지 말 것.** `dispatch()` 는 `try` 안에서 `await send()` 하고
  뒤따르는 `finally` 에서 자물쇠를 놓는다 — **자물쇠가 풀리는 순간이 곧 "답이 도착했다"** 다.
  그 조건을 기다린다(jsdom 검사에서 `ready()` 를 쓰는 것과 같은 이유).
- 옛 봇을 죽인 뒤 **이름으로 한 번 더 훑어 낙오된 봇을 쓸어낸다** — 둘이 떠 있으면 409 다.
- 새 봇은 `node scripts/telegram-bot.mjs` 로 **직접** 띄운다(npm → cmd 껍데기 둘이 준다).
  띄운 뒤 10초 살아 있는지 확인하고, 곧바로 죽으면 한 번 더 시도한다 —
  설정이 없으면 뜨지 않는(fail closed) 봇이라 실패는 1초 안에 드러난다.
- **PowerShell 스크립트는 ASCII 로만 쓴다.** Windows PowerShell 5.1 은 BOM 없는 `.ps1`
  을 ANSI 로 읽어 한글이 깨진다. 저장소 경로에 한글이 있으므로 **경로를 적지 말고**
  `$PSScriptRoot` 에서 끌어낼 것.
- 결과는 `.scratch/bot-restart.log`, 새 봇 출력은 `.scratch/bot.log`.

### 권한 프리셋 — `TELEGRAM_TOOL_PRESET`

| | `safe`(기본) | `full` |
|---|---|---|
| 할 수 있는 것 | 읽기·편집·테스트 | **PC 앞에 앉은 것과 동일** — push·배포·임의 명령 |
| 보안 경계 | chat_id + 도구 제한 | **chat_id 하나뿐** |

**원격에서 배포까지 하려면 `full` 이 필요하다.** main 에 push 하면 Vercel 이 자동 배포하므로
휴대폰에서 배포가 된다는 뜻이고, 동시에 토큰이 유출되면 남이 프로덕션에 배포할 수 있다는 뜻이다.

**`full` 의 허용 목록을 비우지 말 것 — 정반대로 동작한다.** 헤드리스에는 물어볼 상대가 없어
"제한을 안 거는 것"과 "전부 허용하는 것"의 결과가 반대다. 네 가지를 직접 돌려 확인했다:

| 설정 | `git push` |
|---|---|
| `--allowedTools` 없음 | ❌ "This command requires approval" |
| `--allowedTools "*"` | ❌ 와일드카드를 받지 않는다 |
| `--permission-mode dontAsk` | ❌ `git status` 는 되지만 push 는 거부 |
| `--permission-mode bypassPermissions` | ❌ root/sudo 환경에서 CLI 가 거절 |
| **`--allowedTools "Bash,Read,…"`** | ✅ **된다** |

그래서 `FULL_TOOLS` 에 **도구 이름을 하나씩 적는다.** "full 인데 목록이 왜 있지" 하고 비우면
아무것도 못 하게 된다. `test-telegram.mjs` 가 목록이 비는 것과 `*` 를 둘 다 막는다.

- **접두 지정으로 "main 만 빼고 push 허용"을 만들 수 없다.** 매칭이 **토큰 단위**라
  `Bash(git push origin claude/:*)` 는 안 맞고(조각), 전체 브랜치명을 적어야 맞는다(실측).
  브랜치별로 적는 수밖에 없으므로 사실상 push 는 **열거나 닫거나** 둘 중 하나다.
- `full` 로 뜨면 시작할 때 경고 상자를 띄우고, 휴대폰에서 `/status` 로도 현재 프리셋이 보인다 —
  full 인 줄 모르고 쓰는 상황을 만들지 말 것.
- 이 저장소는 **public** 이라 토큰이 커밋되면 즉시 공개된다. 테스트가 `.gitignore` 등재 여부와
  소스·`.example` 안의 토큰 패턴(`숫자:영숫자35자`)을 함께 검사한다.

### 구현에서 조용히 깨지는 것들

- **프롬프트는 argv 가 아니라 stdin 으로 넘긴다.** `--allowedTools` 가 가변인자라 뒤 인자를
  삼키고, argv 길이 제한·따옴표 이스케이프 문제도 함께 붙는다. `claude -p` 는 stdin 을 프롬프트로
  받으므로 셋을 한꺼번에 피한다(실측 확인).
- **`--output-format stream-json` 에는 `--verbose` 가 반드시 함께 가야 한다.** 없으면 CLI 가 거부한다.
- **대화를 잇는 것은 `--resume <session_id>`.** 세션 id 는 `system/init` 과 `result` 이벤트에
  둘 다 실려 온다. 이것을 안 들고 있으면 매번 새 대화가 되어 "아까 그거"가 통하지 않는다.
- **메시지는 4,096자 제한이고, 넘으면 통째로 거부된다**(잘려 나가는 게 아니다). `splitMessage` 가
  3,800자로 쪼갠다. 자를 자리를 못 찾아도 반드시 끝나야 한다 — 무한 루프를 테스트가 검사한다.
- **`parse_mode` 를 쓰지 않는다.** 코드·특수문자가 섞이면 Telegram 이 파싱에 실패해 메시지가
  통째로 안 간다. 서식보다 "도착하는 것"이 중요하다.
- **봇을 껐다 켤 때 밀려 있던 옛 메시지를 흘려보낸다**(`offset:-1`). 안 그러면 몇 시간 전 명령이
  한꺼번에 실행된다.
- **폴링이 실패해도 죽지 않는다**(지수 백오프, 최대 60초). 매장에서 다시 켜 줄 사람이 없다.
- 작업은 **한 번에 하나만** 돈다. 같은 작업 트리에서 `claude` 를 둘 돌리면 파일이 서로 엉킨다.

### 윈도우에서 `claude` 를 못 띄우던 문제 (2026-08-10)

**`spawn('claude')` 가 윈도우에서 ENOENT 로 죽는다.** PATH 의 `claude` 는 실행파일이 아니라
`claude.cmd` 셸 스크립트이고, Node 는 확장자 없는 이름으로 `.cmd` 를 찾지 않는다. 증상이
고약하다 — **봇은 멀쩡히 뜨고 메시지도 받는데 답만 못 한다.** 봇 문제로 보여 토큰·화이트리스트를
계속 뒤지게 된다(실제로 그랬다).

- **`.cmd` 를 직접 가리켜도 안 된다.** Node 는 보안 조치 이후 `.cmd`/`.bat` 를 `shell:true`
  없이 spawn 하지 못한다. 그렇다고 `shell:true` 로 열면 `Bash(git status:*)` 같은 인자를
  cmd.exe 가 다시 해석해 **도구 제한이 조용히 어긋난다** — 이 봇에서 제일 위험한 종류의 고장이다.
- npm 전역 설치본에는 **네이티브 `claude.exe`** 가 함께 들어 있다
  (`<npm전역>/node_modules/@anthropic-ai/claude-code/bin/claude.exe`). `resolveClaudeBin()` 이
  이것을 찾아 쓴다 — 셸을 거치지 않아 인자가 그대로 전달된다.
- 찾는 순서: `CLAUDE_BIN` → PATH 의 `claude.exe` → npm 전역의 `claude.exe` → `claude`.
  **리눅스·맥은 첫 줄에서 바로 `claude` 로 빠진다** — 그쪽은 확장자 없는 실행파일이 정상이다.
- 그래도 못 찾으면 `describeSpawnError()` 가 **무엇을 고칠지** 알려 준다(ENOENT → `CLAUDE_BIN`,
  EINVAL → `.cmd` 말고 `.exe`). 토큰 오류를 사람 말로 바꾼 것과 같은 이유다 — 스택만 보면
  세팅을 여기서 포기한다.
- 테스트는 경로를 **`path.join`·`path.delimiter` 로 짓는다.** 문자열로 박으면 리눅스 CI 에서
  구분자가 달라 깨진다.

## 운영 종료된 모듈

- **패키지 플래너**(2026-08-03 종료) — `/planner` 라우트·`package-planner.html`·`test-planner.mjs`를
  삭제했다. 종료 시점에 이미 허브 카드·사이드바 어디에도 링크가 없어 통합검색이 유일한 진입로였다.
  `lib/logEvent.ts`의 `LogModule` 유니온에는 `'planner'`를 **남겨 둔다** — 구글 시트에 과거 로그가
  남아 있어 타입에서 빼면 집계 코드가 타입 에러를 낸다. `'compareInstant'`(타사비교로 통합)도 같은 이유다.
  - **대시보드 목록에서는 감춘다**(`MODULE_META` 의 `retired:true` → `LIVE_MODULES`, 2026-08-11 사용자 요청).
    라벨 자체는 지우지 않는다 — 최근 이벤트 목록·CSV 에서 이름이 필요하고, 지우면 그 자리에
    모듈 키(`planner`)가 그대로 노출된다. **화면에서 빼는 것과 데이터에서 지우는 것은 다른 일이다.**
  - 감춘 만큼 목록 합계와 "기록된 이벤트" 총계가 어긋나 보이므로 **건수만 한 줄로 밝힌다.**
    **모듈 이름은 적지 않는다** — 가려 달라고 한 것을 각주로 되살리면 뜻이 없다.
  - 막대 길이 기준(`maxVal`)도 **보이는 모듈 중** 최대치로 잡을 것. 감춘 모듈까지 넣으면
    화면에 없는 값이 기준이 돼 막대가 이유 없이 짧아진다.

## AX 현황 대시보드 (`/admin`) — 관리자용

허브 메인·사이드바 표기는 **"AX 현황 대시보드[관리자용]"**로 통일한다(3곳: `app/page.tsx` 카드+섹션 제목,
`components/Navigation.tsx` `ADMIN_LINK`, `app/admin/page.tsx` 헤더).

- **인증 상태는 `sessionStorage` + 만료시각(2시간)으로 둔다.** 예전에는 `localStorage`에 `'1'`만 저장해
  한 번 통과한 기기가 브라우저를 껐다 켜도 영구히 열려 있었다(비밀번호를 건 의미가 없었다).
  레거시 키 `ax_admin_unlocked`는 마운트 시 제거해 이미 열려 있던 기기를 다시 잠근다.
  비밀번호 해시는 `app/api/admin-auth/route.ts`(서버)에만 두고 검증 결과만 돌려준다 — 클라이언트로 옮기지 말 것.
- **비밀번호는 환경변수로 준다 — `ADMIN_PW`(평문) 또는 `ADMIN_PW_HASH`(SHA-256 64자).**
  둘 다 있으면 **평문이 이긴다**. 둘 다 서버 전용이라 `NEXT_PUBLIC_` 접두사를 붙이면 안 된다 —
  붙이는 순간 클라이언트 번들에 실려 검증이 통째로 무의미해진다(테스트가 막는다).
  이 저장소는 public이라 소스에 해시를 두면 누구나 받아 오프라인 대입이 가능하므로,
  미설정 시 폴백 해시로 동작하되 프로덕션에서 경고를 남긴다.
  - **평문(`ADMIN_PW`)이 기본 권장이다**(2026-08-11 사용자 선택). 해시 방식은 값이 노출돼도
    비밀번호가 바로 드러나지 않는 장점이 있지만, **사람이 붙여넣다 틀리는 사고가 훨씬 잦다.**
    실제로 해시를 등록했는데 로그인이 안 돼 한참을 헤맸다 — 화면에는 "비밀번호가 다릅니다"
    한 줄만 떠서 오타인지 미적용인지 배포 문제인지 구분할 수가 없었다.
  - **검증 로직은 `lib/adminAuth.ts` 에 순수 함수로 있다.** 라우트에 박아 두면 테스트가 안 된다 —
    위 사고의 진짜 원인이 그것이었다. `test-admin.mjs` 가 평문·해시·공백·따옴표·대문자·우선순위
    10가지 조합을 검사한다.
  - **붙여넣다 섞이는 것을 걷어낸다** — 앞뒤 공백, 감싸는 따옴표, 해시의 대문자.
    `timingSafeEqual` 은 **길이부터** 보므로 끝에 줄바꿈 하나만 붙어도 즉시 불일치다.
    비밀번호가 공백으로 시작·끝나는 경우는 지원하지 않는다(붙여넣기 사고가 훨씬 잦다).
  - **경고문에 비밀번호·해시를 싣지 않는다.** 로그를 보는 사람이 곧 관리자가 된다.
    대신 "어느 변수를 쓰는지 · 해시 길이가 64자가 맞는지"만 적어 원인을 짚을 수 있게 한다.
- **환경변수는 기존 배포에 소급 적용되지 않는다.** 값을 등록·수정한 뒤 **반드시 재배포**해야
  반영된다. Vercel 대시보드의 Redeploy 버튼이면 되고, 커밋을 새로 올릴 필요가 없다
  (2026-08-11 이걸 확인 안 하고 빈 커밋을 올려 배포를 한 번 낭비했다 — 재배포 요청을 받으면
  **최근 배포 목록부터 확인할 것**).
- **로그인이 안 될 때 원인을 가르는 법**: 런타임 로그에 `[admin-auth] … 미설정` 경고가
  **찍히면** 환경변수가 안 들어간 것이고, **안 찍히는데도 안 되면** 값이 틀린 것이다
  (함수가 콜드스타트했는지는 `POST /api/admin-auth` 의 `cache=MISS` 로 확인한다).
- 인증 API에 **10분 / 10회 시도 제한**(IP 기준)과 `timingSafeEqual` 상수시간 비교를 넣었다. 초과 시
  429를 돌려주고 화면에 "10분 뒤 다시 시도"로 표시된다. 서버리스라 인스턴스별 카운트지만 단일 기기
  무차별 대입은 실질적으로 막힌다.
- **허브 메인화면 페이지뷰는 집계에서 제외한다.** 허브는 모든 모듈의 진입점이라 링크를 타고 들어오거나
  카드를 누르기만 해도 조회수가 쌓여 "실제로 도구를 썼다"는 신호가 아니다.
  ① `app/page.tsx`가 아예 로그를 남기지 않고 ② 이미 쌓인 로그는 `excludeHubViews()`로 걸러낸다.
  단 `hub`/`search`(통합검색)·`hub`/`feedback`(건의)은 의도적 행동이라 **남긴다** — 통째로 지우지 말 것.
  화면과 CSV 내보내기 모두 필터된 로그를 쓰므로 집계와 내보내기가 어긋나지 않는다.
- 관리자 대시보드 자체의 조회도 로그로 남기지 않는다(예전엔 열 때마다 hub 페이지뷰가 1건씩 늘었다).
- **`LogModule` 에 모듈을 추가하면 `MODULE_META` 에도 반드시 넣을 것.** 안 넣으면 사용 현황에
  **한 줄도 안 잡혀 "안 쓰는 모듈"로 읽힌다.** 실제로 AS 관련 정보·배치 시뮬레이터·컨시어지
  포스터 셋이 이 상태로 방치돼 있었다(2026-08-11 발견). `test-admin.mjs` 가 두 목록을 대조한다.
- `scripts/test-admin.mjs`가 위 네 가지를 소스 수준에서 회귀 검사한다.

### 로그 파이프라인 (2026-08-11 전면 개정 — 매장 500곳 대비)

프로덕션 실데이터 1,623건을 직접 열어 보고 고쳤다. **추측이 아니라 받은 원문을 보고 판단할 것.**

**들어온 값은 들어오는 자리에서 맞춘다**(`normalizeLogs`). 시트가 보내는 것이 두 군데 틀려 있었다:
- **`date` 가 `'Wed Jul 29 2026 00:00:00 GMT+0900 (Korean Standard Time)'`** 로 온다(시트 칸이
  Date 라서). `aggregateByDay` 는 `'YYYY-MM-DD'` 로 찾으므로 **일별 추이 막대가 전부 0** 이었다.
  이제 **`ts`(숫자)에서 날짜를 다시 만든다** — 이미 쌓인 옛 줄까지 함께 고쳐진다.
- **시트 헤더 행이 이벤트로 섞여 온다**(`{ts:null, module:'module'}`). `ts` 가 숫자가 아닌 행을 버린다.
- Apps Script 를 고쳐도 되지만 그쪽은 배포가 따로 돌고 **이미 쌓인 줄은 그대로**다. 받는 쪽에서
  고치는 것이 옛 데이터까지 살린다.

**이벤트는 모아서 보낸다**(`outbox`). 예전에는 1건이 GAS 실행 1회였다:
- Apps Script 무료 계정 한도가 **하루 실행시간 90분 · 동시 실행 30개**다. 실측 비율로 500매장을
  환산하면 하루 5~6천 건이고 `appendRow` 가 건당 1.5초면 **145분 — 한도의 1.6배**다.
- 20건씩 묶어 `/api/logs` 로 보낸다. **GAS 실행 횟수가 1/20** 이 되고, Apps Script 쪽도
  `appendRow` 반복 대신 `setValues` 한 번이라 **시간도** 준다.
- **`mode:'no-cors'` 를 버리고 서버 라우트를 거치는 이유는 성공 여부를 알기 위해서다.**
  예전에는 응답을 읽을 수 없어 한도를 넘겨 실패해도 아무도 몰랐고(`.catch(() => {})` 가
  네트워크 오류까지 삼켰다) 그래서 재시도도 없었다. 매장 전파가 끊긴 동안의 사용이 통째로 빠졌다.
- 실패분은 대기함에 남아 다음에 다시 간다. 화면을 떠날 때는 `keepalive` 로 마저 보낸다.
- **배치 id 는 내용에서 결정적으로 만든다**(`첫ts-끝ts-건수`). 서버는 저장했는데 응답이
  유실되면 같은 배치를 다시 보내게 되는데, id 가 같아야 Apps Script 가 알아보고 버린다
  (`CacheService`, 6시간). 새 이벤트는 뒤에 붙고 보낼 때 앞에서 잘라 가므로 재시도해도 id 가 같다.
- 성공한 배치를 지울 때 **길이로 자르지 말 것** — 보내는 사이에 들어온 이벤트를 지우게 된다.
  보낸 것과 같은 키를 가진 것만 걸러낸다.

**`doGet` 은 기간을 받는다**(`?days=60`). 대시보드는 14일치만 그리는데 시트 전체를 읽고 있었다 —
실측 왕복 **3.9초**였고 서버 라우트 타임아웃이 8초라 매장이 늘면 대시보드가 통째로 빈다.

**`docs/apps-script/Code.gs` 를 고쳤으면 Google 에 다시 배포해야 한다**(배포 관리 → 새 버전).
저장소만 고쳐서는 아무 일도 일어나지 않는다. 새 `doPost` 는 **한 건짜리도 계속 받으므로**
배포 시점이 어긋나도 로그가 끊기지는 않는다.

### jsdom 테스트는 고정 시간으로 기다리지 말 것

`test-finder` 가 `npm test` 안에서만 `window.parseQuery is not a function` 으로 두 번 죽었다.
원인은 `await wait(200)` 이라는 **고정 대기**다 — 스위트를 연달아 돌리면 부하 때문에 큰 인라인
스크립트(finder-app.html 은 제품 578종)가 200ms 안에 못 끝난다. **단독 실행에서는 늘 통과해**
원인을 찾기 어렵다.

지금은 `ready(조건)` 으로 **조건을 기다린다**(finder·install·compare). 두 가지를 주의할 것:
- **조건은 "스크립트가 돌았는가"여야 한다.** `.cat-drop-row` 같은 DOM 요소는 정적 HTML 에
  이미 있어 조건이 즉시 참이 되고, 그러면 고정 대기와 다를 바가 없다(실제로 그렇게 한 번 깨졌다).
  `typeof window.selectCat === 'function'` 처럼 **스크립트가 만든 것**을 본다.
- 새 jsdom 스위트를 만들 때도 같은 방식으로 할 것.

## 허브 통합검색 (`/search`)

허브 메인 상단 검색창에서 **제품명·모델코드·카테고리·기능**을 한 번에 찾아 해당 모듈로 보낸다.

- 각 모듈 데이터는 `public/*.html` 인라인 스크립트 안에 있어 React 페이지가 직접 읽을 수 없다.
  그래서 `scripts/build-search-index.mjs`가 정적으로 추출해 **`public/search-index.json`**(약 430건)을
  만들고, `/search` 페이지는 그 JSON만 fetch해서 검색한다.
- **인덱스는 두 파일로 나뉜다**(2026-08-03). `search-index.json`은 검색에 필요한 필드만 담은 경량본
  (411KB)이고, 펼쳐볼 때 쓰는 상세 스펙은 `search-detail.json`(454KB)에 따로 나가 **사용자가 처음
  "상세 ▼"를 누를 때만** 받는다. 두 파일은 `i` 필드로 연결되므로 **배열 순서를 바꾸지 말 것** —
  어긋나면 엉뚱한 제품의 스펙이 뜬다(`test-search.mjs`가 정합성·`d` 플래그를 검사한다).
  `kw`는 토큰 중복을 제거해 저장한다(검색은 공백 분리 토큰의 `includes` 판정이라 결과가 동일하고
  90KB가 줄었다). 상세 필드가 경량본에 다시 새어 들어가면 테스트가 실패한다.
- **모듈 데이터(제품·카테고리)를 수정하면 `npm run build:index`를 다시 돌리고 커밋해야 한다.**
  `scripts/test-search.mjs`가 "커밋된 인덱스 == 지금 재생성한 인덱스"를 검사하므로 빠뜨리면 테스트가 실패한다.
- **검색 결과를 누르면 그 항목이 열려야 한다 — 앱 첫 화면이 열리면 고장이다.**
  딥링크: 제품 상세검색 `?q=` · 설치환경 `?cat=` · 타사비교 `?cat=` ·
  **AI구독 케어 `?cat=`(제품 key) · AS `?q=`(앱 항목 제목) · 배치 시뮬레이터 `?pick=카테고리|사이즈`**.
  - **뒤 셋은 2026-08-12 까지 조용히 깨져 있었다 — 194건이다.** 색인은 `?cat=` 을 붙여
    보내는데 care 앱이 읽지를 않았고, AS 108건·배치 70건은 **맨 `/as`·`/place`** 였다.
    그래서 *"수원 물류센터"* 를 찾아 눌러도 AS기간 첫 화면이 열려, 상담사가 앱 안에서
    같은 말을 다시 쳐야 했다(사용자 지적: *"선택된 곳으로 자동 이동이 되어야 하는데
    이동이 안 됩니다"*). **찾아 준 것과 다른 화면이 뜨면 찾아 준 뜻이 없다.**
  - **AS 는 새 화면 전환 코드를 만들지 않는다.** 앱이 이미 항목마다 자기 자리로 데려가는
    `go()`(탭 전환 · 묶음 펼침 · 목록 필터)를 들고 있어 그것을 부른다 — 사람이 눌렀을 때와
    결과가 같아야 한다. 그래서 `?q=` 는 **화면 제목(`냉장고 보증기간`)이 아니라 앱 항목
    제목(`냉장고`)** 이다. 한 글자만 달라도 예외 없이 검색 결과만 뜨므로
    `test-search.mjs ④-c` 가 원문 데이터와 전수 대조한다.
  - **배치 시뮬레이터는 시트를 억지로 먼저 열지 않는다.** 축척이 없으면 가전을 올릴 수
    없다는 규칙이 먼저다 — 요청을 `deepPick` 으로 들고 있다가 **시트가 열릴 때** 그 줄을
    체크한다(`applyDeepPick`). 도면을 불러오는 순서가 바뀌어도 찾아 준 것이 사라지지 않는다.
  - `test-search.mjs ④` 의 모듈 목록이 **셋뿐이라 이걸 놓쳤다.** 딥링크를 내보내는 모듈은
    전부 거기 있어야 하고, `④-b` 가 "자료 항목이 맨 주소로 가지 않는가"를 함께 지킨다.
  **중요**: 이 쿼리는 Next 라우트에 붙는 것이라 그냥 두면 iframe 안 정적 앱까지 전달되지 않는다.
  `components/IframeModule.tsx`가 마운트 후 `ref.current.src`에 현재 쿼리스트링을 합쳐 넣어 전달한다
  (hydration은 속성 불일치를 DOM에 반영하지 않으므로 useState 초기화나 src prop만으로는 동작하지 않음).
- **'모델파인더'는 '통합검색'(`/search`)이 되었다**(2026-08-11 사용자 요청). 같은 이름이
  둘이면 어느 쪽이 무엇인지 알 수 없으므로 **진입을 하나로 합쳤다.** 예산·치수 필터·AI
  추천이 있는 `/finder` 는 **'제품 상세검색'** 으로 남아 있고, 통합검색의 제품 상세에서
  "제품 상세검색에서 열기"로 이어진다.
- **통합검색을 '제품 상담 도구' 그룹에 넣지 말 것**(2026-08-11 사용자 지적: *"이미 상단에
  검색창이 있고 하단에 통합검색 바로 할 수 있는 공간이 마련되어 있습니다. 너무 중복입니다"*).
  진입은 **허브 상단 검색창 + 하단 탭** 둘이면 된다. 넓은 화면은 하단 탭이 없으므로
  **사이드바 최상단(허브 옆)** 에 둔다 — 도구가 아니라 진입점이라 그룹 밖이 맞는 자리다.

### 용어 세분화 · 다조건 매칭 (`lib/searchTerms.ts`)

*"3가지 질문을 하면 3가지 모두 인식된 것 검출"*(2026-08-11 사용자)이 이 절의 요구사항이다.

- **조건은 AND, 조건 안의 표기는 OR.** "무풍 에어컨 1등급"이면 셋 다 맞는 것만 남고(142→101→15),
  각 조건은 동의어 중 아무 표기로든 걸리면 충족이다.
- **동의어 표는 '같은 뜻의 다른 표기'만 넣는다.** '에어컨 → 무풍'처럼 상위·하위를 넣으면
  "무풍 에어컨"이 모든 에어컨에 걸려 **조건을 겹쳐 좁히는 일이 통째로 무의미해진다.**
- **한 글자 표기는 넣지 못하게 던진다.** 판정이 `kw.includes(표기)` 라 어디에나 걸린다 —
  '노트북 → 북' 을 넣었더니 '북'이 든 항목이 전부 노트북으로 잡혔다.
- **단위를 뗀 맨숫자로 확장하지 않는다.** `20kg → 20` 을 넣었더니 20이 든 값이면 무엇이든 걸려
  **567건**이 나왔다(제대로는 9건). `65인치 ↔ 65형` 처럼 **표기가 갈리는 짝만** 넣는다.
- **색인과 질의가 같은 정규화를 거쳐야 한다**(`normalize`). 한쪽만 쉼표를 지우면
  `912×1,853` 로 색인되고 `1853` 으로 찾아 영영 안 걸린다. 그래서 규칙을 한 파일에 두고
  `build-search-index.mjs` 가 그것을 가져다 쓴다(그래서 이 스크립트는 `--experimental-strip-types` 로 돈다).
- **자연어로 물어도 걸려야 한다**(2026-08-11 사용자 요청). 사람은 *"냉장고 컴프레서 몇 년이야"*
  처럼 묻는데 AND 라 '몇' 하나가 0건이면 문장 전체가 0건이 된다. 두 겹으로 푼다:
  ① 묻는 말·맺는 말은 앞머리/꼬리로 걸러내고(`STOPWORD_HEAD`·`STOPWORD_TAIL`)
  ② **실제로 0건인 조건은 빼고 찾는다** — 활용형을 목록으로 다 못 잡으므로 데이터가 스스로
  판정하게 한다. 조용히 빼지 않는다: 조건 칩이 그 말을 빨갛게 보여주고 아래 한 줄이 밝힌다.
- **조사 떼기(`PARTICLES`)에 `야·요·죠`를 넣었다** — "물류센터야"가 안 걸렸다. 떼어낸 형태를
  **더할 뿐** 원형을 버리지 않으므로 걸리던 것이 안 걸리는 일은 없다.
- **토큰 앞뒤의 문장부호를 뗀다.** *"냉장고?"* 가 **0건**이었다 — 자연어로 물으라고 만든
  검색에서 물음표 하나로 0건이 되면 안 된다. **가운데 기호는 건드리지 않는다**
  (`1577-3922`·`2026.03.19`·`912×1,853` 이 조각나면 그것들로 못 찾는다).
- **한 글자는 조건으로 쓰지 않는다.** 판정이 `kw.includes` 라 어디에나 걸린다 —
  *"왜 이거 안 되나요 진짜 좀 알려주세요"* 가 '안' 하나 때문에 **145건**으로 흩어졌고
  'a' 하나로 678건이 나왔다. 동의어 표에서 한 글자를 막아 둔 것과 같은 이유다.
- **제품 상세검색(`m:'finder'`) 묶음에는 제품만 넣는다**(2026-08-11 사용자 요청:
  *"말 그대로 가전제품(CE+MX+B2B)만 검색"*). 예전에는 카테고리 줄도 넣어 "냉장고"를 치면
  제품 108종 사이에 **"냉장고"라는 분류 줄**이 끼어 있었다. 분류로 찾는 일은 설치환경·
  타사비교 묶음이 한다. `test-search.mjs` 가 이 묶음에 `t!=='product'` 가 섞이면 실패시킨다.
- **0건일 때 무엇이 걸렸는지 말한다.** "없습니다" 한 줄만 띄우면 상담사는 세 조건 중 무엇이
  문제인지 몰라 통째로 다시 친다. 조건별 건수를 칩으로 띄우고, 다 따로는 있는데 함께가 없으면
  **"'데이코' 빼고 다시 찾기 (11건)"** 를 한 번에 눌러 갈 수 있게 한다.
- **뜻 없는 용언 활용형은 목록으로 뺀다**(`STOPWORDS` 의 놓을·놓는·넣을·쓸·하는·있는…).
  이것들은 조사 떼기로도 안 걸러지고 **하필 어딘가에 한 건쯤 걸려** 0건 자동 완화에도 안 잡힌다 —
  그래서 문장 하나를 통째로 죽인다. *"안방에 놓을 에어드레서 크기"* 가 실제로 그랬다
  ('놓을' 이 1건에 걸려 AND 가 깨졌다).
- **영문/한글 표기가 갈린 짝을 찾아 넣을 것.** AS 원문은 `DD MOTOR`, 타사비교는 `DD모터` 라
  *"드럼세탁기 dd모터 몇 년 보증인가요"* 가 0건이었다(각각 1건·3건인데 교집합이 없었다).
- **화면 말과 데이터 말이 다르면 동의어로 잇는다** — `크기 ↔ 치수·규격·사이즈`.
  사람은 "크기가 어떻게 되죠"라고 묻는데 색인에는 '치수·규격'으로 적혀 있었다.

### 화면에 보이는 제목은 반드시 검색어에 들어간다 (2026-08-11)

절마다 `kw` 를 손으로 지은 탓에 **AS 묶음 107건만 제목이 빠져 있었다** — 화면에는
`김치냉장고 보증기간` 이라고 적혀 있는데 그 말을 그대로 쳐도 **0건**이었다(`로열블루`도 0건).
**화면에 적힌 말로 못 찾는 것은 검색이 아니다.**

- 절마다 고치면 다음 모듈에서 또 빠지므로 **`add()` 한 곳에서** 제목을 붙인다.
  `test-search.mjs ⑦` 이 전 항목에 대해 **제목의 낱말이 kw 에 있는지**를 검사한다
  (kw 는 토큰 단위로 중복 제거되므로 통짜 문구가 아니라 낱말로 봐야 한다).
- 같은 이유로 멤버십 `ROYAL`(로열블루)을 색인에 넣었다 — 화면에 있는데 색인에 없었다.

### 방 이름으로도 찾을 수 있어야 한다

상담에서 나오는 말은 *"안방에 놓을 에어드레서"* 이지 *"에어드레서 규격"* 이 아니다.
동의어표에 `안방 ↔ 침실1` 을 넣어 두고도 **색인에 두 표기가 한 건도 없어** 그 질의가 0건이었다.

- 방↔가전 대응은 배치 시뮬레이터의 **`ROOM_PLAN` 을 읽어 쓴다**(가전 선택 시트의 방 드롭다운
  기본값이 나오는 그 표다). 따로 적으면 한쪽만 고쳤을 때 화면과 검색이 어긋난다.
- 침실은 **안방·침실1~3·주침실**을 함께 넣는다(도면 표기가 '침실1'인 곳이 절반이다).

### 색인은 전 모듈을 담는다

예전에는 모델파인더·설치환경·타사비교·AI구독케어만 담았다(430건 → 지금 905건).
**모듈을 새로 만들고 색인 생성기에 넣지 않으면 통합검색에서 통째로 사라지는데 화면에는 아무
표시도 안 난다** — `test-search.mjs ⑥` 이 모듈별 최소 건수를 지킨다.

- 미니앱 안의 객체 리터럴은 **정규식으로 긁지 말고 평가해서 읽는다**(`literal()`).
  정규식은 원문이 조금만 바뀌어도 조용히 빈 값이 되고, 그러면 그 모듈이 검색에서 사라진다.
  못 찾으면 **던진다.**
- 설치환경은 `data-kw` 만이 아니라 **본문(공간·설비·체크리스트·주의)까지** 담는다 —
  "이격거리 50mm"·"천장고"·"전용 콘센트"가 상담에서 실제로 묻는 말인데 하나도 안 걸렸다.
- 타사비교는 카테고리만이 아니라 **삼성 대표모델과 셀링포인트**를, AS는 **품목별 보증·물류센터
  21곳·이전설치 20행·싱크장 리폼**을, 배치 시뮬레이터는 **사이즈별 대표 규격**을 담는다.
  `size-reps.json` 의 `hidden` 항목은 넣지 않는다(목록·추천·대안이 같은 목록을 봐야 한다).

- 모델파인더 자체 검색은 예산·치수 필터 등 고급 기능이 있어 그대로 둔다. 허브 통합검색은 "빠른 진입점",
  파인더는 "전문 검색"으로 역할을 나눈다.
- **제품 결과는 클릭하면 그 자리에서 상세 스펙이 펼쳐진다**(카탈로그 PDF를 뒤지지 않고 확인하기 위함).
  인덱스 항목에 `spec`(fx 전체)·`on`/`off`·`price`를 함께 담아 `/search`가 표로 렌더한다. 그래서
  **제품의 `fx` 항목이 곧 상세보기 내용**이다 — 카탈로그에서 스펙을 추가로 확보하면 fx에 넣을 것
  (모니터는 이 작업으로 7 → 15항목까지 채웠다). 인덱스 크기는 약 255KB.

## 검사는 **바뀔 수 있는 것을 고정값으로 붙들면 안 된다** (2026-08-16)

하루에 **같은 종류로 네 번** 걸렸다. 전부 "검사가 앱을 못 따라가 조용히 무력해진" 경우다.

| 검사 | 붙들고 있던 것 | 어떻게 드러났나 |
|---|---|---|
| `test-finder` | *"나머지 예산 칸은 비어 있다"* 는 **전제** | 자동 분배를 넣자 합계가 250 → 540 이 되어 실패 |
| `test-finder` | 카테고리 카드 수 **8 고정** | 품목 감지를 좁히자 2개가 되어 실패 |
| `test-plans` | 도면 경로 `plans/c39/84.jpg` (**단지 id**) | 단지를 5곳 추가하니 id 가 밀려 `page.evaluate: undefined` 로 **스위트가 죽었다** |
| `test-prod` | 버튼 **문구** `'이 공간 확정'` | 흐름이 바뀌어 그 단계가 사라졌는데 **6회 전부 15초 타임아웃** |

- **고정해도 되는 것** — 원문에서 확인한 값(골든), 물리 법칙에 가까운 불변식
  (프리미엄 ≥ 스탠다드 ≥ 가성비), 안 바뀌는 식별자(`#d-ok` 같은 id).
- **고정하면 안 되는 것** — 화면 문구, 자리 순번(단지 id), 개수, "지금 이러하다"는 전제.
  **데이터에서 유도할 것** — 카드 수는 `고른 품목 수`에서, 합계는 `칸의 합`에서.
- **늘 실패하는 검사는 아무것도 지키지 못한다.** `test-prod` 는 `npm test` 에 없어서
  6회 전부 실패하는 채로 아무도 못 봤다. 그 사이 정말 고장이 났어도 못 봤을 것이다.
- **못 찾았을 때 화면 상태를 함께 남길 것.** `Timeout` 한 줄만 남으면 다음 사람이 원인
  찾기를 처음부터 다시 한다. 실제로 이번에 **문구 탓이라 짐작해 고쳤다가 또 실패**했고,
  그때서야 화면을 찍어 진짜 원인(그 단계가 사라졌다)을 알았다.

## 테스트 & 검증 워크플로우

7개 모듈 전부에 `scripts/test-*.mjs` 회귀 테스트가 있다 (jsdom으로 `public/*.html`을 `runScripts:'dangerously'`로 로드해 인라인 스크립트의 전역 함수를 직접 호출·검증하는 동일 패턴, 매 세션 재작성할 필요 없음). 변경 후 항상 관련 스크립트 + 전체를 실행:

```bash
node scripts/test-install.mjs   # 설치환경가이드: 21개 카테고리 전체 렌더링, 이미지 개수/카드노출/링크 유효성, 키워드 검색
node scripts/test-finder.mjs    # 모델파인더: 50개 카테고리 전수 검색(350종), AI추천/브랜드뷰 흐름, 패키지모드
node scripts/test-care.mjs      # AI Care: 16개 제품 전수, 12/36개월 플랜전환, overview/timeline 모드
node scripts/test-compare.mjs   # 타사비교: 15개 카테고리 × 브랜드 × 모델 413개 조합, escHtml/history XSS 회귀
node scripts/test-levelup.mjs   # 레벨업테스트: 25문항 구성, 채점(CE/MX/에세이), 이름·사번·에세이 XSS 회귀
node --experimental-strip-types scripts/test-admin.mjs   # AX 대시보드: lib/logEvent.ts 집계·CSV 내보내기 회귀
node scripts/test-consistency.mjs   # 크로스파일 모델코드 일관성: 골든 모델코드 4파일 존재·최상위 SKU 동일성 회귀
node scripts/test-search.mjs    # 통합검색: 인덱스 최신성(재생성 대조), 대표 검색어 모듈 매칭, 딥링크 처리 존재
node scripts/test-sizereps.mjs  # 사이즈별 대표모델: 재생성 대조, 치수 범위, 축 뒤바뀜, 모델코드 실존, 골든 치수
node scripts/test-as.mjs        # AS 관련 정보: 골든값 7품목, 근거 없는 값 미생성, 출처 링크, 물류센터 21곳, 이전설치 20행·개인정보 미포함, 상단 검색
node scripts/test-place.mjs     # 배치 시뮬레이터: 기하(회전·SAT 겹침·벽 스냅 법선·방 안팎), 이격거리 원문 대조, 자동 배치
node scripts/test-telegram.mjs  # 텔레그램 봇: fail-closed 시작, 도구 제한, 위험요청 확인, 4096자 분할, 토큰 유출 경로
node scripts/test-plans.mjs     # (브라우저) 도면 코퍼스 8벌 전수 인식 — 누른 자리별 방 넓이(㎡) 대조
node scripts/test-real.mjs      # (브라우저) 실제 분양 도면 30장 — 품질 지표·개구부 짝비교
node scripts/test-e2e.mjs       # (브라우저) 프로덕션 빌드를 띄워 렌더·인증 게이트·지연 로드 확인
node scripts/build-search-index.mjs   # (테스트 아님) 통합검색 인덱스 재생성 — 모듈 데이터 수정 후 필수
node scripts/build-size-reps.mjs     # (테스트 아님) 사이즈별 대표모델 재생성 — 모델파인더 DB 수정 후 필수
npx tsc --noEmit                # 타입체크
```
(`npm test`로 위 전체를 한 번에 실행 가능. **단 `npm test` 에는 `test:e2e` 가 들어 있지 않다** —
브라우저·빌드가 필요해 CI 별도 잡이기 때문이다. 그래서 UI 흐름을 바꾸고 `npm test` 만 돌리면
E2E 가 깨진 줄 모른다. **실제로 축척 흐름을 바꾼 뒤 E2E 가 깨진 채로 두 번 배포됐다.**
화면 흐름을 건드렸으면 `npm run test:all`(= `npm test && npm run test:e2e`)을 쓸 것.)

새 카테고리·제품을 추가하거나 이미지 개수가 바뀌면 각 스크립트의 기대값(예: `test-install.mjs`의 `expectedImageCounts`)을 반드시 함께 갱신할 것 — 안 하면 테스트가 실패한다. `compare-app.html`/`test-app.html`은 XSS 회귀 가드가 포함돼 있으므로 이스케이프 로직(`escHtml`)을 건드릴 때 특히 주의.

AX 현황 대시보드(`app/admin/page.tsx`)는 정적 HTML이 아닌 React 클라이언트 컴포넌트라 다른 모듈과 같은 jsdom-전체페이지 패턴은 쓸 수 없다. 대신 실제 로직이 몰려 있는 `lib/logEvent.ts`(집계·CSV 내보내기)를 Node의 `--experimental-strip-types`로 직접 임포트해 순수 함수 단위로 검증한다(`scripts/test-admin.mjs`).

### 브라우저 E2E (`scripts/test-e2e.mjs`) — `npm run test:e2e`

위 10개 스위트는 전부 **데이터와 순수 함수**만 본다. React 페이지(`/`, `/admin`, `/search`)의 실제
렌더링·인증 게이트·네트워크 요청은 아무도 검사하지 않아, 실제로 두 번 사고가 났다 —
①관리자 인증이 `localStorage`에 영구 저장돼 비밀번호가 무력화된 채 배포됐고
②건조기 셀링포인트에 사실이 아닌 문구가 남은 채 배포됐다. 둘 다 사람이 브라우저로 열어보고 나서야
발견했다. 그 확인을 CI가 대신한다.

- **프로덕션 빌드**를 띄워서 검사한다(개발 서버가 아니라 실제 배포 형태).
  **소스가 `.next` 보다 새로우면 다시 빌드한다** — 예전에는 `.next` 가 있기만 하면 그대로 띄웠는데,
  그러면 화면을 고친 뒤 E2E 를 돌려도 **옛 화면을 검사하고 ALL PASS 가 뜬다**(2026-08-11 실제로 그랬다).
  CI 는 매번 새 체크아웃이라 안 걸리고 **로컬에서만 조용히 낡는다.**
- 검사 항목: 허브 렌더 + 허브 페이지뷰 미기록 / 7개 모듈 iframe 로드 / 관리자 인증 게이트(레거시 키가
  있어도 잠김·오답 시 잠김 유지·localStorage 미저장) / 통합검색 지연 로드(진입 시 경량본만, 펼칠 때 상세) /
  `/planner` 404 / 전 페이지 콘솔 오류·4xx 응답 없음.
- 콘솔의 "Failed to load resource"는 **URL을 담고 있지 않아** 버리고, `response` 리스너로 실제 URL과 함께
  4xx/5xx를 따로 수집한다. 일부러 404를 요청하는 구간(`/planner`)에서는 수집을 잠시 끈다.
- CI에서는 브라우저 설치·빌드가 필요해 **별도 잡(`e2e`)**으로 돌린다. 로컬에서는
  `PLAYWRIGHT_CHROMIUM=/opt/pw-browsers/chromium npm run test:e2e`처럼 실행 파일을 지정할 수 있다.
- `playwright`가 설치돼 있지 않으면 실패가 아니라 **SKIP**으로 빠진다(데이터 테스트만 돌리는 환경 배려).

같은 제품의 모델코드가 test-app.html/finder-app.html/compare-app.html에 각각 독립적으로 박혀 있어
한 파일만 고치고 나머지를 놓치는 사고가 실제로 여러 번 있었다(냉장고 RF→RM, 세탁기 WD25→WD90,
김치냉장고 RQ→RK 등). `scripts/test-consistency.mjs`가 이를 감지하는 안전망이다.

- **PART A**: 골든 모델코드가 대상 파일 전부에 존재하는지 (문자열 존재 확인)
- **PART B**: 타사비교의 P등급(최상위) 모델코드가 **모델파인더 DB에도 있는지**. 없으면 상담 중
  고객이 되물었을 때 파인더에서 스펙·가격을 확인할 수 없다. 이 검사를 넣자마자 에어컨
  `AF90H19D27SRT`(→`SRS` 오타)와 식기세척기 코드 불일치를 실제로 잡아냈다.
  - **그런데 `SRT` 는 오타가 아니었다**(2026-08-16 정정). samsung.com 에 제품 지면이
    실재한다 — `air-conditioners/package-af90h19d27srt-d2c/AF90H19D27SRT/`,
    *"Bespoke AI 무풍콤보 갤러리 프로 **62.6/18.7㎡**"*. 뒷자리만 다른 코드는 색상
    변형일 수 있으니 통일하지 말고 함께 실으라는 규칙(식기세척기 `U01`/`USW` 사례)이
    여기에도 그대로 해당한다. 지금 타사비교는 `SRS`·`SRT` 를 둘 다 들고 있고 그게 맞다.
- 어느 쪽이 맞는지 확정하지 못한 코드는 `UNRESOLVED` 맵에 **이유와 함께** 넣어 TODO로 출력한다.
  추정으로 한쪽에 맞추면 틀린 모델코드를 고객에게 안내하게 되므로 불일치를 드러낸 채로 둔다.
- **뒷자리만 다른 코드는 "틀린 것"이 아니라 색상 변형일 수 있다 — 통일하지 말고 함께 실을 것.**
  식기세척기 `DW90F79P1U01S`(코타 화이트) ↔ `DW90F79P1USWS`(새틴 화이트)를 오래 불일치로 달아
  두었는데, samsung.com 에 **두 제품페이지가 모두 살아 있었다**(2026-08-10 확인). 한쪽으로
  통일했으면 실존하는 제품을 지우는 셈이다. 접미가 색상인 패턴(`U01`·`USW`·`UAP` 등)을 먼저 의심하고,
  모델파인더 엔트리의 `fx` 모델번호 행과 `kw` 에 **두 코드를 함께** 넣는다 — 상담사가 어느 색상
  코드로 물어도 파인더에서 걸려야 한다(PART B 가 보는 것도 결국 "그 코드로 찾아지는가"다).

커밋 전 실수로 생성되는 `tsconfig.tsbuildinfo`, `package-lock.json`은 `.gitignore`에 등록되어 있으니 git에 올라가지 않는지 확인할 것 (과거 여러 번 실수로 커밋되었다가 별도 정리 커밋이 필요했음).

## 배포 후 검증

push 후 Vercel 대시보드/CLI로 최신 배포의 커밋 해시가 로컬 HEAD와 일치하고 `READY` 상태인지 확인. 가능하면 실제 배포 URL(`salescopilot-store.vercel.app/install`)을 브라우저로 열어 변경한 카테고리를 선택해 이미지가 깨지지 않고 로드되는지 스크린샷으로 육안 확인한다.

## 사용자 선호 (항상 준수)

- 정확도 최우선, 전문가 보고서 수준, 교차검증·사실확인 필수
- AI가 작성했다는 티(AI 작성 문구, "🤖 Generated with Claude" 류 서명 등)는 결과물/커밋 메시지에 남기지 않는다
- 간결하고 직접적인 커뮤니케이션 선호 — 불필요한 설명 최소화
- **디자인은 심플 이즈 베스트**(2026-08-10). 우선순위는 **시인성 · 심플함 · 직관성** 셋이다.
  - **삼성 블루가 메인**이고 전반적으로 그 톤을 유지한다. **타사비교만 부각색**을 쓴다 —
    경쟁 제품을 나란히 놓고 우열을 가리는 앱이라 시선을 끄는 것이 기능이다.
    한때 아홉 앱을 전부 파랑으로 눌렀다가 "타사비교는 부각돼야 한다"는 지적을 받았다.
  - **더하는 것보다 빼는 것을 먼저 본다.** 정보를 촘촘히 채우면 상담 중에 못 읽는다 —
    매장에서 화면을 보는 시간은 몇 초다.
  - **같은 품목이면 앱이 달라도 같은 아이콘**을 쓴다(타사비교 ↔ AS기간 ↔ 설치환경).
    상담사가 앱을 옮겨 다녀도 같은 그림이 같은 품목을 가리켜야 눈이 헤매지 않는다.
    헷갈리는 짝은 일부러 가른다 — 냉장고 🧊 ↔ 냉동고 🥶 · 정수기 🚰 ↔ 제습기 💧 ·
    모니터 🖥️ ↔ 데스크탑 🖱️.
  - **머리(헤더)는 아홉 앱이 같아야 한다** — 남색 바 + 아이콘 배지 + 제목 + 부제.
    앱을 넘겨 볼 때 머리가 같아야 한 제품으로 읽힌다.
- **작업 하나가 끝날 때마다 보고한다**(2026-08-10 요청). 여러 건을 몰아서 한 번에 알리지
  말 것 — 중간에 방향을 바꿀 기회가 사라진다.
- **남은 작업과 그 순서를 항상 들고 있는다.** 사용자가 알고 싶어 하는 것은 "지금 무엇을
  하고 있나"가 아니라 **"무엇이 남았고 어디까지 왔나"**다. 작업을 시작할 때 목록을 세우고,
  하나 끝날 때마다 **끝난 것 / 남은 것 / 다음 것**을 함께 적는다. 새로 발견한 할 일도
  그 자리에서 목록에 넣어 알린다(조용히 늘리지 말 것).
