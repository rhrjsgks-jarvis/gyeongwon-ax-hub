---
name: url-quiz-generator
description: 자사 제품 URL 1개 + 경쟁사 URL 1~4개를 입력하면 직원 역량 평가용 인터랙티브 HTML 퀴즈를 자동으로 생성하는 스킬. "URL로 퀴즈 만들어줘", "이 페이지로 비교 시험 만들어줘", "자사 타사 URL 주면 문제 만들어줄 수 있어?", "제품 페이지 URL 넣으면 퀴즈 생성 가능해?", "경쟁사 비교 퀴즈 만들어줘", "직원 교육용 퀴즈 URL로 만들고 싶어", "경쟁사 3개랑 비교 문제 만들어줘" 등의 요청에 반드시 사용하세요. URL과 퀴즈/시험/문제 키워드가 함께 나오면 무조건 이 스킬을 쓰세요.
---

# URL 기반 비교 퀴즈 자동 생성

자사 URL + 경쟁사 URL(들)을 분석해서 완성형 HTML 퀴즈를 만듭니다.
경쟁사가 1개든, 2개든, 3개든 동일하게 작동합니다.

---

## 0단계: 오더 받으면 먼저 이것부터 (프로액티브 제안)

사용자가 URL과 퀴즈 요청을 주면, **바로 만들기 전에** 아래를 확인·제안합니다.
이 단계의 목적은 "일단 만들어봤는데 이게 아니네요" 상황을 방지하는 것입니다.

### 확인할 것들

**URL 역할 정리** (모호하면 확인)
```
✅ 제가 이렇게 이해했습니다:
- 자사: [URL]  ← [제품명 추정]
- 경쟁사 1: [URL]  ← [제품명 추정]
- 경쟁사 2: [URL]  ← [제품명 추정] (있으면)
```

**설정값 제안** (아직 받지 않은 항목에 대해 추천값과 함께 제시)
```
📋 설정 제안 (다르게 원하시면 말씀해주세요):
- 문제은행: 40문항 생성 → 25문항 랜덤 출제 [변경 가능]
- 제한 시간: 30분 [변경 가능]
- 회사명: [URL에서 추정한 이름] [확인 필요]
```

**추가 기능 제안** (간단히 yes/no로 답할 수 있게)
```
🔧 추가할까요?
- Google Sheets 자동 기록 — Apps Script URL이 있으면 연동 가능합니다
- Netlify 배포 — 만든 후 영구 URL로 배포 도와드릴게요
```

사용자가 "그냥 기본으로 해줘" 또는 "바로 만들어줘"라고 하면 제안한 기본값으로 즉시 진행합니다.
이 제안 단계를 길게 늘이지 말고, **메시지 하나로 간결하게** 보여주세요.

---

## 1단계: 정보 수집

### 필수
- 자사 URL × 1개
- 경쟁사 URL × 1~4개 (몇 개든 가능)

### 선택 (기본값으로 진행 가능)
| 항목 | 기본값 |
|------|--------|
| 회사명 | URL에서 추정 |
| 퀴즈 제목 | "[제품명] 제품 전문가 챌린지" |
| 문제은행 크기 | 40문항 |
| 출제 문항 수 | 25문항 |
| 제한 시간 | 30분 |
| Google Sheets URL | 없음 (결과 화면만 표시) |

---

## 2단계: 페이지 분석

`mcp__workspace__web_fetch`로 각 URL 내용을 가져와 분석합니다.

**각 페이지에서 추출할 것:**
- 제품명, 모델명, 시리즈명
- 핵심 기술 방식
- 주요 스펙 (해상도, 밝기, 크기 옵션 등)
- 차별화 특장점 및 마케팅 포인트
- 가격대 (있으면)
- 타겟 고객 및 활용 환경

**그리고 반드시 하나 더 — 「용어 대조표」를 만든다.**
비교 문항은 **이 표에서만** 나온다(3단계 참조). 양쪽 원문을 나란히 놓고
**같은 것을 서로 다르게 부르는 항목**만 뽑는다.

```
[용어 대조표]  ← 같은 이름을 쓰는 항목은 적지 않는다
항목        | 자사 표기            | 경쟁사 표기
냉장고 모터 | 디지털 인버터 모터   | 리니어 인버터 모터
...
```

같은 이름을 쓰는 항목(예: 양쪽 다 `인버터 컴프레서`)은 **표에 넣지 않는다** —
그런 항목으로는 변별이 되지 않아 문제를 낼 이유가 없다.

**분석 결과를 구조화해서 정리:**
```
[자사 제품]
- 이름: ...
- 핵심 기술: ...
- 강점: ...

[경쟁사 1 제품]
- 이름: ...
- 핵심 기술: ...
- 특징: ...

[경쟁사 2 제품] (있으면)
...
```

페이지 접근 실패 시: 해당 제품의 일반 지식으로 대체하고, 사용자에게 어떤 정보로 대체했는지 알립니다.

---

## 3단계: 문항 생성

경쟁사 수에 따라 문항 구성 비율을 자동 조정합니다.

### 경쟁사 1개 (URL 2개)
| 유형 | 비율 | 설명 |
|------|------|------|
| 자사 특장점 | 35% | 자사만의 강점 |
| 경쟁사 이해 | 20% | 경쟁사 특징 정확히 파악 |
| 자사 vs 경쟁사 비교 | 35% | 차이점 명확히 구분 |
| 실전 시나리오 | 10% | 고객 대응, 상황별 추천 |

### 경쟁사 2개 (URL 3개)
| 유형 | 비율 |
|------|------|
| 자사 특장점 | 35% |
| 경쟁사 A 이해 | 10% |
| 경쟁사 B 이해 | 10% |
| 자사 vs 경쟁사들 비교 | 35% |
| 실전 시나리오 | 10% |

### 경쟁사 3개 이상 (URL 4개+)
| 유형 | 비율 |
|------|------|
| 자사 특장점 | 35% |
| 각 경쟁사 이해 | (30% ÷ 경쟁사 수) |
| 종합 비교 (3개 이상 동시 비교) | 25% |
| 실전 시나리오 | 10% |

> 경쟁사가 많을수록 "어떤 상황에서 어느 제품이 최적인가" 유형의 시나리오 문항이 더 효과적입니다.
> 경쟁사 3개 이상이면 시나리오 유형을 10→15%로 늘리는 것을 권장합니다.

> **위 비율은 "무엇을 묻는가"의 배분이고, 아래 난이도는 "얼마나 어렵게 묻는가"의 배분이다.
> 둘은 따로 맞춘다** — 비교 문항이 35%라고 해서 그것들이 다 어려운 것은 아니다.

### 난이도 — 이 시험은 "붙는 시험"이 아니다

**목적이 둘이다.** 훈련시키는 것과, **"내 지식이 이것밖에 안 됐구나"를 겪게 하는 것.**
쉽게 내면 둘 다 실패한다 — 다 맞은 사람은 배운 것이 없고, 자기 수준도 모른 채 나간다.

- **난이도 배분: 상 50% · 중 35% · 하 15%.** '하'는 몸 푸는 용도지 점수 주려고 넣는 것이 아니다.
- **목표는 평균 60점대다.** 만점이 여럿 나오면 그 회차는 실패한 것이다 — 문항을 다시 조인다.
- **'상' 문항의 정의**: 카탈로그·제품 지면을 **실제로 열어 본 사람만** 답할 수 있는 것.
  기억으로 풀리면 '상'이 아니다.
- **아래는 즉시 버린다**
  - 정답만 길고 나머지 보기가 터무니없는 문항 (읽지 않고도 찍힌다)
  - 브랜드 슬로건·마케팅 문구 암기 ("○○의 슬로건은?")
  - 상식으로 풀리는 문항 ("냉장고는 무엇을 하는 기기인가")
  - 보기 4개 중 2개가 명백히 같은 말인 문항

### 모델명을 반드시 적는다 — 항의를 막는 규칙

**모델을 특정하지 않은 기능 문항은 반드시 항의가 들어온다.** 세대·라인업마다 답이 갈리기 때문에
출제자가 생각한 답과 응시자가 아는 답이 **둘 다 맞는** 상황이 생긴다.

- **특정 모델의 기능·스펙을 묻는 문항은 문항 본문에 모델코드를 적는다.**
  - ✅ `무풍 갤러리 프로(AF90H19D27SRS)의 냉방면적은?`
  - ❌ `무풍 에어컨의 냉방면적은?`
- 제품명과 모델코드를 **병기**한다. 코드만 적으면 무슨 제품인지 몰라 문제가 안 읽힌다.
- **`요즘`·`최신`·`일반적으로`·`대부분` 으로 시작하는 문항은 쓰지 않는다.**
- **값에 조건이 붙으면 조건도 문항에 넣는다.**
  - ✅ `2015년 1월 이후 제조분 기준, 인버터 컴프레서 무상보증 기간은?`
  - ❌ `인버터 컴프레서 무상보증 기간은?` (조건이 빠지면 답이 둘이 된다)

### 보기의 용어는 공식 표기 그대로

기능의 **유무를 판단시키는 문항**에서 용어가 흔들리면 그 문항은 시비가 붙는다.

- 보기에 쓰는 기능명은 **제품 지면·카탈로그의 정식 표기 그대로** 옮긴다.
  `UV 살균 LED` · `AI 맞춤세탁` · `무풍 냉방` — **줄이거나 풀어 쓰지 않는다.**
- "있다/없다"를 묻는다면 **그 표기가 원문에 실제로 있는지 확인하고 낸다.**
  원문에서 확인하지 못한 기능은 출제하지 않는다 — 없는 것을 "없음"이라고 단정할 수 없다.
- 해설에 **근거를 적는다**: 모델코드 + 어디서 확인한 값인지(제품 지면 / 카탈로그 / 지원 페이지).

### 자사 vs 경쟁사 비교 문항 — 여기가 이 시험의 핵심이다

**① 문항에 양쪽 모델을 정확히 적는다.**
- ✅ `Bespoke AI 4도어(RM70F91B1AP)와 LG 디오스 오브제컬렉션(M874GBB252)을 비교할 때…`
- ❌ `삼성 냉장고와 LG 냉장고를 비교할 때…`
- **보기에도 마찬가지다.** 보기가 "삼성", "LG" 로만 적혀 있으면 어느 모델 이야기인지 알 수 없다.

**② 양사가 같은 용어를 쓰는 항목은 출제하지 않는다.**
같은 이름을 양쪽이 다 쓰면 변별이 안 되고, 맞혀도 아는 것이 늘지 않는다.
(예: 양사 모두 `인버터 컴프레서`라고 부르는 항목 — 이건 문제가 되지 않는다)

**③ 출제 대상은 "용어가 다르거나 헷갈리는 것"이다.** 여기서 응시자가 흔들려야 한다.

| 항목 | 삼성 | LG |
|---|---|---|
| 냉장고 모터 | **디지털 인버터 모터** | **리니어(인버터 리니어) 모터** |
| 세탁기 모터 | **DD Motor**(직결) | **인버터 DD 모터** |
| TV 패널·화질 | **Micro RGB · Neo QLED** | **OLED evo** |
| 의류관리 | **에어드레서** | **스타일러** |
| 로봇청소기 흡입 표기 | **W(흡입력)** — Pa 를 적지 않는다 | **Pa(진공도)** 로 적는 모델이 있다 |

> **이 표는 예시다. 매번 두 URL 원문을 대조해 "용어가 갈리는 항목"을 직접 뽑아 표로 만들고,
> 그 표에서만 비교 문항을 낸다.** 표에 없는 항목으로 비교 문항을 만들지 않는다.

**④ 오답은 네 가지 방식으로만 만든다.** 아무 말이나 넣으면 찍힌다.

1. **브랜드 교차** — 상대 브랜드의 **정식 용어**를 우리 보기에 넣는다. 가장 강력한 함정이다.
   (삼성 냉장고 문항의 오답으로 `리니어 인버터 모터`)
2. **세대 교차** — 이전 세대의 값·모델코드를 넣는다. 한 세대 어긋난 코드는 실제로 자주 헷갈린다.
3. **조건 누락** — 사실은 맞지만 조건이 빠진 진술.
   (`에어컨 무상보증 2년` → 냉방 전용일 때만 맞고 냉난방 겸용은 1년)
4. **근사값** — 정답에서 5~15% 비켜난 숫자. 20% 넘게 벌리면 눈으로 걸러진다.

> **오답도 사실 확인을 거친다.** 없는 기술명·없는 모델코드를 지어내면 안 된다 —
> 해설에서 "왜 틀렸는지"를 근거로 설명할 수 없게 되고, 그때 항의가 들어온다.
> 오답은 **"실제로 그렇게 알고 있는 사람이 있는 오해"** 여야 한다.

### 출제 금지 목록

- 원문에서 확인하지 못한 값 (추정·일반 지식으로 채운 문항)
- 양사 용어가 **같은** 항목의 비교 문항
- 모델을 특정하지 않은 기능·스펙 문항
- 마케팅 문구·슬로건 암기
- 조건이 붙는 값인데 조건을 뺀 문항

### 문항 JSON 형식

```javascript
{
  q: "질문 내용 — 모델코드를 반드시 포함한다",
  opts: ["선택지 A", "선택지 B", "선택지 C", "선택지 D"],
  ans: 0,               // 정답 인덱스 (0~3)
  exp: "해설 — 왜 맞는지 + 오답이 왜 그럴듯한지 + 실무 팁",
  type: "own",          // "own"/"competitor_a"/"competitor_b"/"compare"/"scenario"
  diff: "상",           // "상"/"중"/"하"  ← 상 50% · 중 35% · 하 15%
  models: ["AF90H19D27SRS"],   // 이 문항이 다루는 모델코드 (비교 문항은 양쪽 모두)
  src: "samsung.com 제품 지면"  // 값을 확인한 출처
}
```

**생성 후 스스로 점검한다 — 하나라도 걸리면 그 문항을 고친다.**

- [ ] 난이도 '상'이 절반 이상인가
- [ ] 기능·스펙 문항에 모델코드가 적혀 있는가
- [ ] 비교 문항에 **양쪽** 모델코드가 적혀 있는가
- [ ] 비교 문항이 **용어가 갈리는 항목**에서만 나왔는가
- [ ] 보기의 기능명이 원문 표기 그대로인가
- [ ] 오답 네 종류를 골고루 썼는가 (한 가지만 반복하면 패턴이 읽힌다)
- [ ] 모든 값에 `src` 가 있는가

---

## 4단계: HTML 퀴즈 파일 생성

완성된 **단일 HTML 파일** (CSS·JS 모두 인라인, 외부 의존성 없음).

### 필수 기능

**인트로 화면 (전체화면 오버레이)**
- 회사명 + 웹사이트 (있으면)
- 퀴즈 제목 + 설명 (N문항 · 제한시간 M분)
- 비교 대상 제품들 나열 (자사 vs 경쟁사명들)
- 사번 8자리 숫자 입력 + 유효성 검사
- "시험 시작하기" 버튼 / Enter 키

**타이머**
- 사번 입력 후 시작
- 헤더에 MM:SS 상시 표시
- 10분 이하 → 주황색
- 5분 이하 → 빨간색 + 깜빡임
- 0이 되면 자동 채점·제출

**문항 표시**
- 전체 문항 한 페이지 스크롤 (페이지네이션 없음)
- 상단: "답변: N / 전체: M" 진행 현황
- 선택지 클릭 시 즉시 하이라이트
- 제출 버튼은 전체 답변 완료 시에만 활성화

**결과 화면**
- 사번, 응시 일시, 소요 시간
- 점수 (정답수 / 전체문항, 백분율)
- 등급 메시지 (90↑: 완벽, 75↑: 우수, 60↑: 양호, 미만: 더 노력)
- 틀린 문항의 정답 + 해설 표시
- "다시 풀기" 버튼

**랜덤화**
- 매 로드마다 Fisher-Yates 셔플 → 앞 N개 선별

**Google Sheets 연동** (SCRIPT_URL 있을 때만)
```javascript
const SCRIPT_URL = ''; // 없으면 빈 문자열
// showResult() 내에서:
if (SCRIPT_URL) {
  fetch(SCRIPT_URL + '?' + new URLSearchParams({
    empId, score: pct+'점', correct, total, datetime, elapsed
  }), { method: 'GET', mode: 'no-cors' }).catch(() => {});
}
```

### 핵심 JS 구조
```javascript
const EXAM_DURATION = 30;   // 분
const EXAM_COUNT    = 25;   // 출제 수
const SCRIPT_URL    = '';   // Google Sheets URL

let empId = '', chosen = {}, submitted = false;
let timerSeconds = 0, timerInterval = null, filtered = [];

const ALL_QUESTIONS = [ /* 생성된 문항들 */ ];

function shuffle(arr) {
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
}

// 초기화
shuffle(ALL_QUESTIONS);
filtered = ALL_QUESTIONS.slice(0, EXAM_COUNT);
render();
```

### 디자인
- 깔끔하고 전문적 (화이트 배경, 그레이 톤)
- 자사 브랜드 컬러를 포인트로 활용 (URL에서 파악 가능하면)
- 모바일 친화적 (max-width: 700px)
- 정답: 초록, 오답: 빨간으로 채점 후 표시

---

## 5단계: 파일 저장 및 전달

파일명: `[회사명]_[제품명약칭]_퀴즈.html`

파일을 저장하고 링크로 제공합니다.

**완료 후 자동으로 추가 제안:**
```
✅ 퀴즈 파일이 완성되었습니다!

혹시 이런 것도 필요하신가요?
💾 Netlify 배포 — 영구 URL로 직원들에게 공유 가능
📊 Google Sheets 연동 — 응시 기록 자동 저장 (아직 안 하셨다면)
✏️ 문항 수정 — 특정 문항을 바꾸거나 추가하고 싶으시면 말씀해주세요
```

---

## 빠른 참고

**사번 검증:**
```javascript
if (!/^\d{8}$/.test(val)) { /* 에러 표시 */ return; }
```

**타이머 색상:**
```javascript
if (timerSeconds <= 300) el.style.color = '#DC2626';
else if (timerSeconds <= 600) el.style.color = '#D97706';
```

**소요 시간:**
```javascript
const elapsed = EXAM_DURATION * 60 - timerSeconds;
const min = Math.floor(elapsed / 60), sec = elapsed % 60;
```
