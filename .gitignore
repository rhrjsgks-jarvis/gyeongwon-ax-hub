node_modules/
.next/
out/
.env.local
.env
*.log
.DS_Store
tsconfig.tsbuildinfo
package-lock.json
next-env.d.ts
.scratch/

# 파일을 건네받는 임시 함(README 만 있고 내용물은 작업이 끝나면 저장소 안으로 옮긴다).
업로드/

# 실제 분양 도면 이미지는 커밋하지 않는다 (public repo · 저작권).
# 측정값만 담긴 index.json은 커밋한다 — scripts/fixtures/plans-real/README.md 참고.
scripts/fixtures/plans-real/*.png
scripts/fixtures/plans-real/*.jpg
scripts/fixtures/plans-real/*.jpeg
scripts/fixtures/plans-real/*.webp
scripts/fixtures/plans-real/*.gif

# 작업 자물쇠 — 실행 중에만 존재하는 상태 파일 (scripts/agent-lock.mjs)
.claude/agent.lock

# 밤샘 작업 지시문 — 저장소 루트에 두고 쓰는 개인 메모다. **이 저장소는 public 이라**
# `git add -A` 에 딸려 들어가면 그대로 공개된다. 저장소에 남기기로 정하면 -f 로 넣을 것.
밤샘작업지시문.md
