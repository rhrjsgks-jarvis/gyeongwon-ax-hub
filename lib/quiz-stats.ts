/* 자동 생성 — 손으로 고치지 말 것. `npm run build:examtool` 이 다시 쓴다.
   화면에 적는 개수를 데이터에서 세어 넣기 위한 파일이다. */
export const QUIZ_BANK_TOTAL = 1295          /* 시험지·웹시험이 뽑는 전체 은행 */
export const QUIZ_APP_TOTAL = 1295           /* 레벨업 챌린지 앱이 읽는 QB 전체 */
