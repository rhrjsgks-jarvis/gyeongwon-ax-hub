/*
 * 개발중인 서비스 목록 — **여기 한 곳에서만 적는다.**
 *
 * 허브 메인(`app/page.tsx`)의 「개발중」 그룹과 사이드바 최하단 「개발중인 서비스」
 * (`app/dev/page.tsx`)가 같은 것을 보여준다. 두 곳에 각각 적으면 반드시 어긋나고,
 * 어긋난 쪽을 본 상담사가 없는 도구를 찾게 된다 — 이 저장소가 허브 카드 개수·앱 버전·
 * 비교표 값에서 반복해서 데인 종류다.
 */
export type DevModule = {
  href: string
  icon: string
  title: string
  desc: string
  color: string
  bg: string
  updated: string
  status: 'dev'
}

export const DEV_MODULES: DevModule[] = [
  {
    /*
     * 2026-08-30 사장님 요청으로 신설 — *"당사제품끼리 비교할수있는 방법이 있으면
     * 좋겠습니다. 최대 4개까지 … 동일품목군에서끼리만 … 위치는 개발 중 안으로"*.
     *
     * **타사비교와 다른 도구다.** 그쪽은 우열을 등급(S~E)으로 말하지만 이쪽은
     * 우리 제품끼리라 등급을 매기지 않는다 — 상담사가 "어느 쪽이 이 고객에게 맞나"를
     * 고르는 자리이지 이기고 지는 자리가 아니다.
     *
     * 자료는 제품 상세검색과 **같은 파일**을 쓴다(finder-core.json + finder-extra.json).
     * 따로 만들면 값이 갈라지고, 갈라진 쪽을 본 상담사가 틀린 사양을 읽는다.
     */
    href: '/own-compare',
    icon: 'compare',
    title: '당사제품 비교',
    desc: '같은 품목군 안에서 삼성 제품 최대 4개를 나란히 — 값이 갈리는 항목만 추려 보여준다',
    color: '#1428A0',
    bg: '#EEF2FF',
    updated: '2026.08',
    status: 'dev',
  },
  {
    /*
     * 2026-08-17 사용자 요청으로 「제품 상담 도구」에서 내렸다 — 도면 인식·3D 를 아직
     * 다듬는 중이다. 사이드바·하단탭에서도 함께 뺐다.
     *
     * **비밀번호는 걸지 않는다**(2026-08-17 사용자 재요청: *"비밀번호 없이 확인해볼 수
     * 있게 해주세요"*). 한때 `/place` 를 `DevGate` 로 감쌌는데, 자물쇠를 풀면 그 화면이
     * 지고 있던 경고까지 함께 사라지므로 **경고 띠만 도구 위에 남겼다**(`app/place/page.tsx`).
     * 잠가야 했던 이유는 "반쯤 만든 화면을 고객에게 보여주는 것"이지 접근 자체가 아니다.
     */
    href: '/place',
    icon: 'place',
    title: '가전 배치 시뮬레이터',
    desc: '구매할 가전을 고르면 도면에 맞춰 배치를 추천 — 이격거리·간섭·방 이탈까지 판정(카탈로그 실측 71개 사이즈)',
    color: '#1428A0',
    bg: '#EEF2FF',
    updated: '2026.08',
    status: 'dev',
  },
  {
    /*
     * 이 저장소가 아니라 **별도 배포**다. `next.config.js` 의 rewrites 가 요청을 넘긴다.
     * 끝에 슬래시를 붙이지 않는다 — 308 이 무한히 오간다(그 커밋 메시지 참조).
     *
     * **접속 코드는 뺐다**(2026-08-17 사용자 결정: *"허브 비번만으로 확인할 수 있게,
     * 접속 코드는 로컬에서만"*). 이 허브 쪽 잠금(`DevGate`)이 이미 문을 지키므로 두
     * 겹은 상담 중에 걸리적거리기만 한다.
     * **다만 실제로 끄는 것은 저쪽 앱의 일이다** — 이 저장소에는 그 코드를 검사하는
     * 자리가 없다(`SALES_APP_PASSCODE` 는 telecom-plan-app 의 환경변수다).
     * 그 배포에서 값을 비우거나 검사를 끄지 않으면 화면은 계속 코드를 묻는다.
     */
    // 허브 껍데기 안에서 연다 — /dev/telecom 은 이 저장소의 페이지이고,
    // 그 안의 iframe 이 /dev/telecom/index.html(저쪽 배포)을 띄운다. 하단 탭바가
    // 사라지지 않게 하려는 것이다(2026-08-30 사장님 지시).
    //
    // rewrite 와 부딪히지 않는다: rewrites() 가 배열이면 **파일 라우트를 먼저**
    // 보므로 정확히 '/dev/telecom' 은 이 페이지가, 그 아래 경로는 rewrite 가 받는다.
    href: '/dev/telecom',
    icon: 'phone',
    title: '통신 요금제 상담 도구',
    desc: '3사 요금제 349건·부가서비스 542건·결합 27건·단말 출고가·제휴카드 40장 — 월납부금액과 할부기간 총액까지 계산',
    color: '#1428A0',
    bg: '#EEF2FF',
    updated: '2026.08',
    status: 'dev',
  },
]
