/*
 * 경원 AX 허브 서비스워커
 *
 * 목적은 하나다 — 매장에서 전파가 약하거나 끊겨도 이미 한 번 본 화면은 열리게 하는 것.
 * 정적 미니앱(public/*.html)이 1MB에 가까워 매번 다시 받으면 지하·엘리베이터에서 체감이 나쁘다.
 *
 * 전략
 *  - 네비게이션(HTML 문서 요청): 네트워크 우선 → 실패 시 캐시. Next 페이지가 배포 직후에도
 *    항상 최신이어야 하므로 캐시를 먼저 쓰지 않는다.
 *  - 정적 미니앱·검색 인덱스: stale-while-revalidate. 캐시가 있으면 즉시 보여주고 뒤에서 갱신한다.
 *    데이터가 하루이틀 늦는 것보다 "안 열리는" 쪽이 영업 현장에서 훨씬 치명적이다.
 *  - 그 외(_next 정적 자산 등): 캐시 우선. 파일명에 해시가 들어 있어 내용이 바뀌면 URL이 바뀐다.
 *
 * 배포할 때마다 CACHE_VERSION을 올려 옛 캐시를 정리한다.
 */
/*
 * v2 (2026-08-09) — `size-reps.json` 의 사이즈 이름이 통째로 바뀌었다(냉장고 용량 → 도어
 * 구성, 에어컨 냉방면적 → 설치 형태). SWR 은 **캐시가 있으면 그걸 먼저 보여주므로**
 * 배포 직후 한 번은 옛 목록이 뜬다 — 사용자가 "아직도 그대로"라고 보게 된다.
 * 데이터 표기를 바꿀 때는 버전을 함께 올릴 것.
 */
// v3 (2026-08-10) — 앱 이름과 아이콘이 바뀌었다(AX 허브 → 세일즈 코파일럿).
// manifest·아이콘이 캐시에 남아 있으면 옛 이름으로 계속 설치된다.
/*
 * v8 (2026-08-12) — **미니앱 동작을 고쳤으면 반드시 여기를 올릴 것.**
 *
 * 공유 버튼이 "지금 화면이 아닌 다른 화면을 공유한다"는 지적을 받아 코드를 고쳐 배포했는데
 * **이미 쓰던 기기에서는 그대로였다.** 껍데기(Next 페이지)는 네트워크 우선이라 새것이
 * 오는데 미니앱은 SWR 이라 **캐시에 있던 옛 파일**이 먼저 나오기 때문이다 —
 * 새 헤더 + 옛 미니앱이 섞여, 고쳐 둔 `openId` 정리가 그 기기에서만 적용되지 않았다.
 * (재현 확인: 옛 finder-app.html 을 물려 주면 화면에 없는 제품이 그대로 공유된다.)
 *
 * 데이터 표기뿐 아니라 **미니앱의 동작을 바꿨을 때도** 같은 일이 난다. 위 v2 주석이
 * 데이터만 이야기하고 있어 이번에 놓쳤다 — 판단 기준은 "public/*-app.html 이 바뀌었는가"다.
 */
/*
 * v13 (2026-08-12) — 통합검색 딥링크를 care·as·place 세 미니앱이 읽게 했다.
 * 셋 다 `*-app.html` 이 바뀌었으므로(SWR 대상) 여기를 올리지 않으면 이미 쓰던 기기에서
 * **캐시에 있던 옛 미니앱이 먼저 나와** 여전히 첫 화면만 열린다 — v8 을 올리게 만든 것과
 * 똑같은 사고다.
 */
/*
 * v15 (2026-08-12) — 카테고리 고르기를 심벌 타일로 통일하고(설치환경·타사비교),
 * 제품 상세검색의 이름·제품 수를 고치고, 도면 전체 인식을 고쳤다.
 * install·compare·finder·place 네 미니앱이 바뀌었다.
 *
 * **번호가 한 번 겹쳤던 것을 여기서 푼다.** 두 갈래가 나란히 작업하며 각자 v12 → v13 으로
 * 올렸다(한쪽은 통합검색 딥링크, 한쪽은 도면 방 이름 붙이기). 합치면서 같은 v13 을 그대로
 * 두면, 먼저 배포된 v13 을 이미 받은 기기는 **캐시 키가 같아 옛 미니앱을 지우지 않는다** —
 * 버전을 올린 뜻이 통째로 사라진다. 그래서 겹친 v13 을 건너뛰고 올린다.
 *
 * (v14 를 찍은 뒤에도 미니앱을 더 고쳐 아래 `test-consistency` 의 캐시 버전 대조가
 *  걸렸다. 그 검사가 보는 것은 "마지막으로 버전이 바뀐 커밋 **이후에** 미니앱이
 *  바뀌었는가"이므로, 덩어리를 다 만든 **맨 마지막에** 한 번 올리는 것이 맞다.)
 */
/*
 * v17 (2026-08-12) — 이번 덩어리에서 미니앱 다섯이 바뀌었다.
 *  · install·compare·care — 한 개 고르기를 **같은 심벌 타일**로 통일(AS 기준)
 *  · finder — 이름을 '제품 상세검색'으로, 제품 수를 데이터에서 세어 넣음
 *  · place — 도면 전체 인식 · 설치 높이(벽걸이 TV·사운드바·에어컨·시스템에어컨·인덕션)
 *
 * **버전은 덩어리를 다 만든 맨 마지막에 한 번 올린다.** 중간에 찍어 두면 그 뒤에 고친
 * 미니앱이 옛 캐시로 남고, 아래 test-consistency 의 대조가 바로 그것을 잡는다.
 */
/*
 * v57 (2026-08-15) — 타사비교(compare-app.html) 가 바뀌었다.
 * 스마트폰에 AP·RAM·저장공간을 넣고, 값을 모르는 항목("미공개"·"비대상")에
 * 단위·우열 배지를 붙이지 않게 고쳤다. 미니앱은 SWR 이라 여기를 안 올리면
 * 이미 쓰던 기기가 옛 파일을 계속 쓴다.
 */
/* v66 (2026-08-15) — 도면을 올리면 집 전체를 먼저 잡는다(place-app.html). */
/*
 * v71 (2026-08-16) — 타사비교(compare-app.html)에서 근거 없는 소음 값 9칸을 지우고
 * 정수기 음성 제어를 채웠다. **v70 을 찍은 뒤에 compare 를 또 고쳐 대조가 걸렸다** —
 * 위 v17 주석이 적어 둔 그대로, 버전은 덩어리를 다 만든 **맨 마지막에** 한 번 올려야 한다.
 */
/*
 * v72 (2026-08-16) — 제품 상세검색(finder-app.html)의 AI 추천 엔진을 넷 고쳤다.
 * 문장 예산 자동 분배 · 품목 명시 시 용도 규칙 끄기 · 같은 가격 같은 티어 ·
 * 후보 종수 밝히기. 버튼은 계속 hidden 이지만 미니앱 파일이 바뀌었으므로 SWR 규칙상
 * 여기를 올리지 않으면 이미 쓰던 기기가 옛 파일을 계속 쓴다.
 */
/*
 * v73 (2026-08-16) — 타사비교(compare-app.html)의 전기요금 환산에 기후환경요금·부가세·
 * 전력산업기반기금을 넣었다. 화면은 여전히 hidden 이지만 미니앱 파일이 바뀌었으므로
 * SWR 규칙상 여기를 올려야 이미 쓰던 기기가 새 계산을 받는다.
 */
/*
 * v74 (2026-08-16) — place-app.html 은 주석만 바뀌었지만(도면 단지 수 139 → 144),
 * 이 검사는 파일이 바뀌었는지만 보므로 함께 올린다. 기능 변화는 없다.
 */
/*
 * v75 (2026-08-16) — 로봇청소기 소음을 바로잡았다(compare-app.html). 삼성 55 → 63(원문),
 * 근거 없는 타사 11칸 제거. 상담 화면의 수치가 바뀌므로 옛 캐시가 남으면 안 된다.
 */
/*
 * v76 (2026-08-16) — 로봇청소기 배터리도 바로잡았다(compare-app.html).
 * 삼성 4종 220 통일(사양표가 4종 동일) · LG 로보킹 180 → 110 · 근거 없는 타사 7칸 제거.
 */
/*
 * v77 (2026-08-16) — AI 추천의 티어 역전과 예산 초과 처리를 고쳤다(finder-app.html).
 * 버튼은 계속 hidden 이지만 미니앱 파일이 바뀌었다.
 */
/*
 * v78 (2026-08-16) — AI 추천이 청소기 형태(로봇/무선/유선)를 지키고, 김치냉장고가
 * 냉장고를 딸고 오지 않게 고쳤다(finder-app.html). 버튼은 계속 hidden.
 */
/*
 * v79 (2026-08-16) — AI 추천이 에어컨 설치 형태와 인덕션 화구 수를 지키고,
 * 예산 초과 시 가장 싼 것을 보여준다(finder-app.html). 버튼은 계속 hidden.
 */
/*
 * v95 (2026-08-18) — finder-extra.json 에서 **법정 표시 12종 460줄**을 걷어냈다.
 * 사양이 아니라 전자상거래 고지(제조국·A/S 책임자 전화번호·품질보증기준…)라 kw 에
 * 섞여 검색을 흐린다. 이 파일도 SWR 이라 여기를 안 올리면 이미 쓰던 기기가 옛 것을 쓴다.
 */
/*
 * v96 (2026-08-18) — 제품 상세검색 히어로 문구가 삼성닷컴 수집분이 붙기 전 숫자
 * (586종)를 그대로 말하고 있었다. syncCounts 가 없는 id(heroSub2)를 찾던 탓이다.
 * 상단바는 2,012종인데 바로 아래 문장이 586종이라 같은 화면이 두 숫자를 말했다.
 */
/*
 * v97 (2026-08-18) — place-app.html 은 주석만 바뀌었다(리빙 4종이 2D·3D 양쪽에
 * 이미 있는데 "만들지 않는다"로 적혀 있었다). 이 검사는 파일이 바뀌었는지만 보므로
 * 함께 올린다. 기능 변화는 없다.
 */
/*
 * v98 (2026-08-18) — 벽 인식의 조각 제거 문턱을 낮췄다(place-app.html). AI Hub 정답
 * 30장으로 재니 지우던 조각이 쓰레기가 아니라 진짜 벽 토막이었다 — 윤곽선 도면에서
 * 지킴 52 → 64%, 정밀도는 오히려 57 → 58%. 인식 결과가 바뀌므로 옛 캐시가 남으면 안 된다.
 */
/*
 * v99 (2026-08-18) — 자동 배치가 3D 와 같은 도어 판정을 쓴다(place-app.html).
 * 예전에는 배치는 사각형 어림으로 보고 3D 는 문짝 스윕으로 봐서, 자동 배치 60대 중
 * 6대가 3D 에서 빨갛게 떴다. 배치 결과가 바뀌므로 옛 캐시가 남으면 안 된다.
 */
/*
 * v100 (2026-08-18) — 3D 에서 가전이 벽에 묻히던 것을 고쳤다(place-app.html).
 * 벽 #F3F5F8 에 가전 본체가 #FAFBFC 라 가전이 벽보다 오히려 밝았다 — 실제 도면으로
 * 세워 보니 거실의 냉장고·식기세척기·공기청정기가 안 보이고 검은 TV 만 보였다.
 */
/*
 * v101 (2026-08-18) — 벽 검출이 나란한 두 선 사이를 메운다(place-app.html).
 * 속이 빈 벽에서 몸통을 통째로 놓치던 원리 결함을 고쳤다 — 정답 90장에서 재현율
 * 24.4 → 45.1%, 정밀도 63.9 → 68.1%. 인식 결과가 바뀌므로 옛 캐시가 남으면 안 된다.
 */
/*
 * v102 (2026-08-18) — 3D 벽 두께를 도면에서 재서 쓴다(place-app.html). 60mm 가정은
 * 정답 23,861개 중앙값(약 200mm)의 3분의 1이라 벽이 종이 판으로 보였다.
 */
/*
 * v103 (2026-08-18) — 개구부 개폐 궤적을 2D·3D 에 그리고, 그 자리를 막는 가전에
 * "문 개폐에 지장" 주의를 붙인다(place-app.html). 예전에는 개구부 선에 몸체가 닿을
 * 때만 걸려서, 문 앞 1m 에 냉장고를 세워도 아무 말이 없었다.
 */
/*
 * v104 (2026-08-18) — 가전 3D 자산이 들어올 자리를 열었다(GLTFLoader 자체 호스팅 ·
 * public/models/ 등록부 · 없으면 지금 상자로 폴백). 그리고 제품 색을 삼성닷컴
 * 수집분으로 19개 더 채웠다(77 → 96) — 3D 가 그 색으로 선다.
 */
/*
 * v111 (2026-08-19) — 시험지 출력기를 앱에서 떼어 자립형 파일로 옮겼다.
 * `exam-print-app.html` 이 public 에서 빠지고(→ scripts/exam-print-template.html),
 * `test-app.html` 의 QB 에서 갤럭시북 S 문항을 뺐다(삼성스토어 미운영 모델).
 * 둘 다 SWR 대상이라 여기를 안 올리면 이미 쓰던 기기가 옛 파일을 계속 쓴다.
 */
/*
 * v112 (2026-08-19) — 단종 모델 인덕션 NZ63DB553CFT 문항 6건을 은행에서 뺐다
 * (test-app.html 의 QB). SWR 대상이라 여기를 안 올리면 이미 쓰던 기기가
 * 단종 모델을 계속 출제한다.
 */
/*
 * v113 (2026-08-19) — 제품 상세검색이 NZ63DB503CFT 를 "스마트 컨트롤 미지원"으로
 * 보여주고 있었다(사장님 확인: 지원). 상담사가 고객에게 그대로 읽던 값이라
 * 옛 캐시가 남으면 안 된다. finder-app.html 은 SWR 대상이다.
 */
/*
 * v114 (2026-08-19) — 인덕션 "최고급 모델번호" 문항을 되살렸다(test-app.html 의 QB).
 * 단종 553 을 뺄 때 함께 사라졌는데 정답은 현행 최고급 857 이라 잃을 이유가 없었다.
 * 오답 보기만 현행 503 으로 갈았다.
 */
/*
 * v126 (2026-08-20) — 새 모듈 「설치비용 · 사전준비」(install-cost-app.html + install-cost.json).
 * 신규 설치 추가비(삼성전자로지텍) 131행 · 이전/재설치/철거(삼성케어플러스) 304행 ·
 * 멀티탭 안전 기준. 둘 다 SWR 대상이라 여기를 올리지 않으면 이미 쓰던 기기가
 * **새 지면을 아예 못 받는다**(자료 파일이 없어 빈 화면이 된다).
 *
 * 같은 덩어리에 place-app.html 도 들어간다 — 도구막대 '축척 맞추기'가 길이 입력 막대를
 * 닫지 않아 **치수선 두 점을 눌러도 아무 일이 안 일어나던 것**을 고쳤다(E2E 가 잡았다).
 */
/*
 * v127 (2026-08-20) — 실재하지 않는 모델코드 `RQ32C7B84AP` 를 은행에서 걷어냈다
 * (test-app.html 의 QB). 지원 지면이 "제품 모델이 존재하지 않습니다"로 답하고 현행
 * 카탈로그에 RQ32 계열이 0종이다 — 단종품과 달리 **애초에 없는 코드**다.
 * 그것을 주어로 삼은 문항 1개를 지우고, 오답 보기로만 든 2문항은 실재 코드로 갈았다.
 * SWR 대상이라 여기를 안 올리면 이미 쓰던 기기가 없는 모델을 계속 출제한다.
 *
 * 같은 덩어리로 test-app.html 에 둘 더 —
 *  · **큐커 오븐 · 전자레인지 모델 문항 10건 삭제**(사장님 지시). 삼성닷컴
 *    `microwave-ovens`·`ovens` 목록이 제품을 한 종도 주지 않는다(인덕션 지면은 16종).
 *    **전자레인지 이격거리 문항은 남긴다** — 모델이 아니라 설치 기준이라 그대로 쓰인다.
 *  · **영문 기능명 48문항에 한글 병기**(사장님 지시). 규격·고유명 12종은 일부러 두었다.
 */
/*
 * v128 (2026-08-20) — 설치비용 앱을 사장님 지적대로 손봤다(install-cost-app.html + json).
 *  · 절을 **기본으로 펼친다** — 눌러야 열리면 무엇이 있는지 알 수가 없다
 *  · 검색 결과가 본문을 덮지 않고 검색창 아래로 내려오고, 누르면 **그 줄로** 데려간다
 *  · 표를 버리고 **「이름 → 금액」 목록**으로 세웠다(전기레인지·정수기 표가 읽히는 이유)
 *  · **설치 전 준비 지면의 그림 6장을 옮겨 왔다** — 설치 불가 조건·필요 공간·타공 기준이
 *    그림에만 있어 글로는 아무것도 남지 않았다. 그림 속 글도 함께 옮겨 적어 검색에 건다.
 * 자료 파일과 그림이 새로 생겼으므로 여기를 올리지 않으면 이미 쓰던 기기가 못 받는다.
 */
/*
 * v129 (2026-08-20) — 설치비용 앱의 **비고·주석을 제자리에 그린다**(사장님 지적:
 * "식기세척기처럼 다른 항목도 주석이나 비고 내용이 있을 경우 표기").
 * 재 보니 케어플러스 쪽이 **한 칸씩 밀려** 있었다 — `앵글 → 규격 110,000원 /
 * 금액 "설치비 + 자재비 기준…"`. 행마다 칸 수가 달라(병합으로 상위 이름이 빠진 행,
 * 비고가 빈 행) 머리글 자리로 자른 탓이다. 지금은 뒤에서 자른다.
 * 비고 0 → 52건 · 화면에 99줄. 「이동 거리」 표는 두 쌍이 나란한 구조라 쪼갰다(25 → 50행).
 * 표 밖 각주(`※ 신제품 설치 시 무상`)도 담고, `무상` 과 `해당 없음` 을 다른 색으로 가른다.
 */
/*
 * v130 (2026-08-20) — 설치비용 앱 화면을 다듬었다(사장님 요청: "시각적으로 보기 좋게").
 *  · **넓은 화면에서 절을 두 단으로 흘린다** — 에어컨은 절이 16개라 한 단이면 PC 에서
 *    3,667px 이고 오른쪽 절반이 비었다. 실측 3,667 → **2,063px**.
 *  · 폰에서도 **금액을 오른쪽에** 세운다(420px 아래에서 밑으로 내리던 것을 되돌렸다) —
 *    "이름 → 금액" 대비가 살아난다. 390px 넘침 0.
 *  · 절 제목에 삼성 블루 짧은 바 · 건수를 제목 옆 배지로 · 행 hover · 칩 오른쪽 페이드.
 *  · 준비 탭 그림 폭을 520px 로 묶고 항목을 두 단으로 — PC 8,009 → **3,621px**.
 *  · 원문 오타 「스텐 · 金㎡」의 뜻을 각주로 밝힌다(고치지는 않는다).
 *
 * **버그도 하나 잡았다** — `listOf` 끝의 `</div>` 가 하나 더 붙어 `<details>` 를 일찍
 * 닫고 있었다. 절 16개가 전부 형제로 튀어나와 두 단 배치가 먹지 않았다.
 */
/*
 * v131 (2026-08-20) — 품목마다 화면 짜임이 달랐다(사장님 지적: 식기세척기만 1열).
 * 에어컨·전기레인지는 절이 여럿이라 카드가 두 단으로 흘렀는데, **식기세척기는 절이
 * 하나(47행)라 흘릴 카드가 없어** 오른쪽이 통째로 비었다. 지금은 **큰 절이 두 단을
 * 가로지르고 그 안의 줄을 나눈다**(값이 하나뿐이고 14줄 이상일 때).
 * 값이 여럿인 표(이전설치·재설치·철거)는 값 상자 셋이 나란해야 해서 나누지 않는다.
 * 실측 — 식기세척기 PC 4,300 → 2,226px · 이전설치 탭 2,922 → 2,410px.
 */
/*
 * v132 (2026-08-20) — **점별 사용 로그**(사장님 요청). 첫 접속에 지점을 고르고 들어가고,
 * 그 지점이 모든 이벤트에 실린다.
 *
 * 미니앱 셋(install·as·care)과 공용 `share-kit.js` 가 바뀌었다 — 미니앱은 iframe 안이라
 * 로그를 직접 못 보내므로 `AX_LOG` 가 부모에게 알린다. **SWR 대상이라 여기를 안 올리면
 * 이미 쓰던 기기가 옛 미니앱을 계속 써서 클릭 로그가 통째로 안 쌓인다.**
 *
 * 중복은 세션당 한 번으로 막는다 — AI구독 케어는 한 제품 안에서 12/36개월·회차·타입을
 * 오가므로 그 전환을 다 세면 제품 하나가 수십 건이 된다.
 */
/*
 * v133 (2026-08-20) — 지점 두 가지를 더했다(사장님 요청).
 *  · **테스트점 Z000 「테스트점 (로그 미기록)」** — 관리자가 점검할 때 그 조작이 매장
 *    통계에 섞이면 실제 사용량과 구분할 방법이 없어진다. 기기에도 시트에도 안 쌓는다
 *    (로컬만 막으면 대기함에 남아 다음 전송 때 올라간다). 헤더에 **노란 칩**으로
 *    "로그 미기록"을 띄운다 — 되돌리는 것을 잊으면 그 매장 통계가 통째로 빈다.
 *  · **영업팀 활성 플래그(Y/N)** — 전국 지점을 다 넣으면 목록이 수백 곳이 되는데 지금은
 *    경원영업팀만 쓴다. 화면에는 Y 만 보인다. 다른 팀이 쓰기 시작하면 Y 로 바꾸면 된다.
 * 미니앱은 안 바뀌었지만 화면(Next)이 바뀌었고 버전 표시를 맞춘다.
 */
/*
 * v134 (2026-08-20) — **접속할 때마다 지점을 묻는다**(사장님 요청).
 * 저장 자리를 localStorage → **sessionStorage** 로 옮겼다. 앱을 닫으면 지워지고 다음에
 * 열 때 다시 고른다(같은 세션에서 화면을 옮길 때는 안 묻는다 — 그것까지 물으면 상담이
 * 막힌다). 익명 세션 id 와 수명이 같아져 "한 세션 = 한 매장의 상담 한 판"이 된다.
 *
 * 그리고 **지점을 고르는 것만으로는 로그를 남기지 않는다** — 예전에는 거기서
 * hub/tab_switch 가 하나 쌓여 **앱을 열었다 닫기만 해도 사용 기록이 생겼다.**
 * 허브 메인 페이지뷰를 집계에서 뺀 것과 같은 이유다(진입은 사용이 아니다).
 */
/*
 * v135 (2026-08-20) — 지점 고르기를 **필수**로 바꿨다(사장님: *"이제는 어느 지점에서
 * 활용하는지 기록이 중요합니다"*). '나중에 고르기'를 없앴고 바깥을 눌러도 안 닫힌다 —
 * 건너뛸 길이 남아 있으면 그 세션 사용이 통째로 '(미지정)'으로 빠지는데,
 * **지점을 모르는 기록은 점별 집계에서 쓸모가 없다.**
 * 이미 고른 뒤 헤더에서 다시 열었을 때만 닫을 수 있다(바꾸려다 만 경우다).
 *
 * 곁들여 — 입력칸에 **예시 지점을 적지 않는다**(한 매장 이름이 기본값처럼 읽혀 통계가
 * 그 곳으로 쏠린다), **테스트점은 목록에 안 내민다**(무심코 고르면 그 매장 사용량이
 * 통째로 사라진다 — 코드나 이름을 쳐야 나온다).
 */
/*
 * v136 (2026-08-20) — 지점 입력칸에서 **지점명을 미리 보여주지 않고 자동완성도 끈다**
 * (사장님 지시). 목록을 펼쳐 두면 상담사가 읽어 내려가다 눈에 걸린 것을 누른다 —
 * 자기 매장이 아닌데 고르면 그 매장 통계가 통째로 엉뚱한 곳에 잡힌다. 쳐야 나온다.
 * 브라우저 자동완성이 지난번 입력을 띄우면 그 원칙이 그 자리에서 무너지므로 함께 껐다
 * (맞춤법 교정·첫 글자 대문자도 — 점코드가 `Zn01` 로 바뀌면 못 찾는다).
 */
/*
 * v137 (2026-08-20) — **점코드·점명이 통행증이다**(사장님 결정: *"점코드 또는 점명으로만
 * 접속해도 됩니다"*). 별도 비밀번호를 두지 않고 지점 식별자 자체로 들어온다.
 *
 * 그래서 목록을 통째로 없앴다 — 화면에 매장 이름이 하나라도 적혀 있으면 아무나 그것으로
 * 들어올 수 있어 뜻이 없다. **정확히 일치할 때만** 통과하고(부분 입력 `수` 로는 못 들어온다),
 * 틀렸을 때 **왜 틀렸는지는 알려 주지 않는다**(갈라 주면 하나씩 넣어 보며 찾아낼 수 있다).
 * 사람이 치는 방식만 받아 준다 — 점코드 대소문자, 지점명 띄어쓰기.
 */
/*
 * v139 (2026-08-20) — **들어오기 전에는 앱을 아예 그리지 않는다**(사장님 지시:
 * *"하얀 배경에 점을 입력하는 창만. 아예 어떤 어플인지 모르게"*).
 * 지점 화면이 앱 위에 뜨는 모달이 아니라 **앱을 감싸는 문**이 됐다 — 덮기만 하면 뒤
 * 화면이 DOM 에 남아 글자가 읽히고(E2E 가 실제로 잡았다) 자바스크립트가 돌기 전에
 * 허브가 번쩍 보인다. 기본값도 「잠김」이라 서버가 보낸 HTML 에 앱이 안 들어간다.
 * 문에는 입력칸 하나와 「확인」뿐이다.
 *
 * 곁들여 — 지점명 `스타필드 수원` → `스타필드수원`(사장님 지시, 나머지가 전부 붙여
 * 쓴 표기라 여기만 띄어 있었다). 입력은 띄어쓰기를 무시하므로 어느 쪽으로 쳐도 들어간다.
 */
/*
 * v140 (2026-08-20) — 문 화면에 **앱 이름과 안내 문구를 되살렸다**(사장님 지적).
 * 처음에 "어떤 어플인지 모르게"를 이름까지 지우는 것으로 읽었는데, 감춰야 했던 것은
 * **허브 내용**(메뉴·카드·자료)이지 정체가 아니었다 — 이름이 없으면 상담사가
 * "잘못 들어왔나" 하고, 입력칸만 덩그러니 있으면 왜 넣는지 모른 채 마주친다.
 * 삼성 워드마크 + 세일즈 코파일럿 + "어느 매장인가요?" 까지 나오고, 허브는 여전히 안 그린다.
 */
/*
 * v141 (2026-08-21) — 시험 문제은행(test-app.html 의 QB)에서 **사실이 틀린 문항을 걷어냈다**.
 * 사장님이 12건을 지적했고 전부 삼성닷컴 원문으로 확인했다 — 없는 모델(갤럭시 북6 프로 360),
 * 품목이 틀린 기능(에어워시는 건조기·세탁기가 아니라 **에어드레서**), 국내 표기가 아닌 용어
 * (오토 클린 → **자동건조** · Self Clean+ → **무세제 통세척+** · 냉장고 플렉스존 → **멀티 팬트리**),
 * 근거 없는 수치(에어컨 30분 · 냉장고 −23~7℃), 정답이 뒤집힌 문항(빅 아이스메이커 위스키볼).
 * **704 → 697문항.** SWR 대상이라 여기를 안 올리면 이미 쓰던 기기가 틀린 문제를 계속 출제한다.
 */
/*
 * v142 (2026-08-21) — 부정형 38건 · 최상급 43건을 전수로 훑어 결함 4건을 더 고쳤다.
 * 부정형은 **오답 3개가 전부 사실**이어야 성립하는데, 걸린 것은 모두 "정답이 둘"이었다 —
 * 게이밍 허브(한국에 없는 Amazon Luna 를 오답으로 둠 · 같은 문항이 둘) · 실외기(가이드에
 * 없는 베란다형/옥상형 구분) · 청소기(없는 제품명 '스팀 제트') · S26 밝기(소스 주석이
 * 스스로 "확정 못해 잠정 적용"이라 적어 둔 값). **697 → 696문항.**
 */
/*
 * v143 (2026-08-21) — 노트북 **모델코드 자리에 애칭이 들어 있던 것**을 바로잡았다
 * (매장 요청: "모델명이 풀네임이 아니다"). 화면은 그 필드를 「모델코드」로 띄우는데
 * 노트북 7종만 "Book6 Ultra (16인치)" 같은 별명이었다 — 다른 카테고리는 전부 모델코드다.
 *
 * 훑다가 **4종이 실재하지 않는 것**도 나왔다: 이름은 북6인데 코드가 북2 세대
 * (NP750Q·NP730Q·NP960Q)다. 북6에는 360 트림도 15.6인치도 없고 '북5 엣지'도 없다.
 * usp 5개는 7종이 전부 같은 문구였고 스냅드래곤 제품에까지 "Intel Core Ultra" 라고
 * 적혀 있었다. 남은 3종은 기본 모델코드(NT960UJH·NT960XJG·NT940XJG)로 바꾸고
 * 제품명을 한글 정식명으로 옮겼다. **586 → 582종.**
 */
/*
 * v144 (2026-08-21) — 노트북을 **접미까지 붙은 풀 모델코드**로 통일했다(사장님 지시).
 *
 * 카탈로그 3종은 기본 코드까지밖에 없고(NT960UJH), 그 코드 하나가 3~5개 SKU 를
 * 아우른다. 접미를 붙이려면 SKU 를 하나 골라야 하는데 **짝을 지을 수 없었다** —
 * 카탈로그가 북6 프로를 Ultra 7 258V 라고 적는데 삼성닷컴 북6 프로에는 그 CPU 가
 * 없다(Ultra 5 · Ultra X7 뿐). 짐작으로 고르면 나머지 SKU 에게 거짓말이 된다.
 *
 * 삼성닷컴 수집분 39종이 같은 제품을 접미까지 붙은 코드로 이미 싣고 있어
 * 카탈로그 3종을 뺐다. **잃은 것은 가격 3건뿐**이고, 그 가격도 어느 SKU 의 값인지
 * 알 수 없어 그대로 두는 편이 더 위험했다(390만원이 Ultra 9/2TB 값인지
 * Ultra 7/1TB 값인지 화면이 말해 주지 못한다). **582 → 579종.**
 *
 * (덧) 인라인 DB 는 **전파가 끊긴 매장의 폴백**이라 노트북이 인라인 0종이 되면
 * 그 상황에서 노트북만 통째로 사라진다(test-finder 의 카테고리 전수 검색이 잡았다).
 * 그래서 삼성닷컴 SKU 3종을 인라인으로 옮기고 수집분에서는 뺐다(중복 방지).
 *
 * 같은 김에 **"카탈로그 동일모델" 배지가 카탈로그에 없는 제품에 붙던 것**도 고쳤다 —
 * 로더가 수집분 1,426종 전부에 cat_ok=true 를 줬는데, 그 제품들은 카탈로그에 없어서
 * 수집한 것이라 정확히 반대다. 상담사가 화면을 그대로 읽으면 틀린 말이 된다.
 */
/*
 * v145 (2026-08-21) — **매장에서 치는 제품군 애칭을 검색이 받아 준다**(사장님 지시).
 * 표시는 풀 모델코드로 하되, 상담사는 북6 · 탭S11 처럼 제품군을 먼저 부르고 그 안에서
 * 세부 모델을 고른다. 그런데 **붙여 쓰면 0건**이었다 — 탭S11 · 갤럭시북6 · Z플립7 · book6.
 *
 * 0건인 조건은 "없는 말"로 보고 조건에서 빠지므로 **정확도까지** 무너졌다 —
 * 탭S11 울트라 가 울트라 하나만 남아 S26 Ultra 를 내놓았다(지금 3건).
 * 표를 새로 만들지 않았다 — 공백을 지운 쪽끼리 견주면 부분일치로 걸린다.
 * 영문 애칭만 음차한다(book→북 · tab→탭 · watch→워치 …).
 *
 * 함께 고친 둘 — SYN 의 울트라→스마트폰 상위 개념 섞기를 뺐고(S26 울트라 12 → 7건),
 * 제품군 이름에 있는 말에 가산점을 줬다(북6 울트라 로 찾으면 울트라가 위로).
 */
/*
 * v146 (2026-08-21) — **복수 조건이 실제로 교집합으로 좁히게 했다**(사장님 지적:
 * "북6 울트라 로 검색하면 북6 모델이 다 노출된다. 교집합만 보여주는 게 맞다").
 *
 * 원인이 셋이었다 — ① 부분일치가 다른 낱말 속을 때린다(프로 ⊂ 프로세서 492종 ·
 * ultra ⊂ core™ ultra · **fe ⊂ feet·fedora·chamfer** 라 S25 FE 검색이 이미 망가져
 * 있었다) ② 조사 제거가 프로 → 프 로 만들어 조건에서 통째로 빠졌다 ③ SYN 이 형태를
 * 가리키는 말에 카테고리를 달아(4도어→냉장고) 그 카테고리 전체가 조건을 통과했다.
 *
 * 실측 26짝: 안 좁혀지던 것 4 → **0**. 북6 울트라 20 → 4건 · 탭 S11 울트라 172 → 5건.
 */
/*
 * v147 (2026-08-21) — 제품 상세검색(/finder) 이름을 **모델파인더(제품 상세검색)** 으로 바꿨다
 * (사장님 지시 — 발표 자료와 맞춘다). 현장이 여전히 '모델파인더'로 부르고 경영진
 * 보고 자료도 그 말을 쓰기 때문이고, 괄호로 지금 이름을 함께 남겨 화면을 보던
 * 상담사가 헷갈리지 않게 한다.
 *
 * **화면에 뜨는 일곱 곳을 한꺼번에 바꿨다** — 허브 카드 · 사이드바 · 라우트 래퍼 ·
 * 통합검색의 "…에서 열기" · 앱 <title> · 앱 헤더 · 공유 카드 이름.
 * 한 곳만 고치면 화면마다 다른 말을 한다(CLAUDE.md 용어 절이 만들어진 이유다).
 * 길이도 재 봤다 — 허브 카드 2줄 · 앱 헤더 116px · 전 해상도 가로 넘침 0.
 */
/*
 * v148 (2026-08-21) — 이름을 **두 줄로 나눴다**(사장님 지적: "모델파인더 내려쓰기
 * 제품상세검색. UI가 이쁘지 않습니다").
 *
 * 「모델파인더(제품 상세검색)」 은 괄호가 한 줄에 붙어 지저분하고, 허브 카드에서
 * 2줄로 접히며 뒷줄에 「제품 상세검색)」 만 남았다. 지금은 **이름이 「모델파인더」** 하나이고
 * 옛 이름은 **카드 제목 아래 작은 줄**(ModuleCard.sub)로 내렸다.
 * 사이드바·앱 헤더처럼 한 줄뿐인 자리는 모델파인더만 쓴다 — 옛 이름은 카드 부제와
 * 검색어에 남으므로 '제품 상세검색'으로 찾던 사람도 그대로 찾는다.
 */
const CACHE_VERSION = 'axhub-v211';
const RUNTIME = `${CACHE_VERSION}-runtime`;

// stale-while-revalidate 대상 — 모듈 미니앱과 검색 인덱스
// `as`·`poster` 가 빠져 있었다(2026-08-11). 미니앱은 전부 같은 규칙을 타야 매장에서
// 전파가 끊겨도 한 번 본 화면이 열린다 — 새 미니앱을 만들면 여기에 함께 넣을 것.
/*
 * **공용 스크립트도 여기 들어가야 한다**(2026-08-13 추가). 예전에는 미니앱 HTML 과 json 만
 * 잡고 있었는데, 인라인이던 코드를 `share-kit.js`·`prod-symbols.js`·`back-kit.js` 로 뽑아낸
 * 뒤로 그것들이 **아무 규칙에도 안 걸려 그냥 네트워크**였다. 전파가 끊기면 미니앱 HTML 은
 * 캐시에서 뜨는데 심벌·공유·뒤로가기가 통째로 죽는다 — 캐시가 반쪽만 듣는 상태였다.
 * 파일 이름에 버전이 없으므로 캐시 우선(vendor)이 아니라 stale-while-revalidate 로 둔다.
 */
/*
 * **공용 벽선 라이브러리도 여기 들어간다**(2026-08-17). `plan-library.json` 은 도면
 * 이미지 없이 방 경계(mm 좌표)만 담아, 전파가 끊겨도 그것만 있으면 **배치를 그대로
 * 할 수 있다** — 도면을 못 받아도 3D 가 서고 가전을 놓을 수 있는 유일한 자료다.
 * 그런데 아무 규칙에도 안 걸려 그냥 네트워크였다(`share-kit.js` 때와 같은 종류).
 *
 * 도면 색인·이미지(`plan-index.json`·`/plans/`)는 아직 규칙이 없다 — 그쪽은 수십 MB 라
 * 따로 판단할 일이다.
 */
/*
 * **삼성닷컴 수집분도 여기 들어간다**(2026-08-17). `finder-extra.json` 은 제품 상세검색이
 * 시작할 때 받아 붙이는 자료다 — 인라인에 넣으면 그 지면이 2.65MB 가 되어 매장 폰에서
 * 파싱만 3초가 걸린다. 캐시에 없으면 **전파가 끊긴 매장에서 새 제품이 통째로 사라진다.**
 */
const SWR = /\/(finder|compare|install|install-cost|care|quiz|test|place|as|poster)-app\.html$|\/(search-(index|detail)|size-reps|plan-library|finder-extra|install-cost|svc-centers)\.json$|\/(share-kit|prod-symbols|back-kit)\.js$/;

self.addEventListener('install', (e) => {
  // 미리 받아두지 않는다. 첫 방문에 1MB를 강제로 받게 하면 오히려 느려진다.
  self.skipWaiting();
});

self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys()
      .then((keys) => Promise.all(
        keys.filter((k) => !k.startsWith(CACHE_VERSION)).map((k) => caches.delete(k))
      ))
      .then(() => self.clients.claim())
  );
});

function swr(req) {
  return caches.open(RUNTIME).then((cache) =>
    cache.match(req).then((cached) => {
      const network = fetch(req)
        .then((res) => {
          if (res && res.ok) cache.put(req, res.clone());
          return res;
        })
        .catch(() => cached);
      return cached || network;
    })
  );
}

function cacheFirst(req) {
  return caches.open(RUNTIME).then((cache) =>
    cache.match(req).then((cached) =>
      cached || fetch(req).then((res) => {
        if (res && res.ok) cache.put(req, res.clone());
        return res;
      })
    )
  );
}

function networkFirst(req) {
  return caches.open(RUNTIME).then((cache) =>
    fetch(req)
      .then((res) => {
        if (res && res.ok) cache.put(req, res.clone());
        return res;
      })
      .catch(() => cache.match(req))
  );
}

self.addEventListener('fetch', (e) => {
  const req = e.request;
  if (req.method !== 'GET') return;

  const url = new URL(req.url);
  if (url.origin !== self.location.origin) return;   // 외부 도메인은 건드리지 않는다
  if (url.pathname.startsWith('/api/')) return;      // 로그·인증 API는 항상 네트워크

  if (req.mode === 'navigate') { e.respondWith(networkFirst(req)); return; }
  if (SWR.test(url.pathname))  { e.respondWith(swr(req)); return; }
  if (url.pathname.startsWith('/_next/static/')) { e.respondWith(cacheFirst(req)); return; }
  /*
   * 라이브러리(three.js 등)는 캐시 우선. 파일명에 버전이 박혀 있어 내용이 바뀌면
   * 이름이 바뀌므로 굳을 걱정이 없고, 671KB 를 매번 받으면 매장 전파에서 3D 가 안 열린다.
   * **CDN 을 쓰지 않고 여기 두는 이유가 이것이다** — 오프라인에서도 3D 가 떠야 한다.
   */
  if (url.pathname.startsWith('/vendor/')) { e.respondWith(cacheFirst(req)); return; }
  /*
   * **가전 3D 자산도 캐시 우선**(2026-08-18). `/models/` 는 파일 이름이 바뀌지 않는 한
   * 내용도 그대로라 캐시 우선이 맞다(vendor 와 같다). 매장 전파가 약해도 열려야 하고,
   * 모델 파일은 크므로 매번 받으면 3D 가 그만큼 늦어진다.
   * **등록부(manifest.json)만 SWR** — 자산을 새로 넣으면 그날 반영돼야 한다.
   */
  if (url.pathname === '/models/manifest.json') { e.respondWith(swr(req)); return; }
  if (url.pathname.startsWith('/models/')) { e.respondWith(cacheFirst(req)); return; }
  /*
   * 설치환경 규격도도 캐시 우선. 파일 이름이 삼성닷컴 원본 경로에서 오고 그 경로에
   * 개정번호(`_v3`·`_v5`)가 박혀 있어 내용이 바뀌면 이름이 바뀐다 — 굳을 걱정이 없다.
   * **매장에서 전파가 끊겨도 규격도가 떠야 한다** — 설치 상담에서 정작 필요한 그림이다.
   */
  if (url.pathname.startsWith('/install-img/')) { e.respondWith(cacheFirst(req)); return; }
  /* 설치 전 준비 안내 그림 — 설치 불가 조건·필요 공간·타공 기준이 그림에 있어 매장에서
     전파가 끊겨도 열려야 한다. 파일 내용이 바뀌면 이름을 바꿔 올릴 것(캐시 우선이다). */
  if (url.pathname.startsWith('/install-cost-img/')) { e.respondWith(cacheFirst(req)); return; }
  /*
   * 단지 목록(경기 12개 시 2,969곳)도 캐시를 먼저 보여주고 뒤에서 갱신한다.
   * 상담 시작이 *"어느 단지 사세요"* 라 이게 안 뜨면 그 자리에서 막힌다 —
   * 목록이 하루 늦는 것보다 안 열리는 쪽이 훨씬 치명적이다(미니앱과 같은 판단).
   */
  if (url.pathname.startsWith('/apt/')) { e.respondWith(swr(req)); return; }
});
